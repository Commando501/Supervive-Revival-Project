SESSION 72 — HYBRID match-entry shim: FOUNDATION BUILT + validated in the DS client; possession blocked on "needs a Loki controller"
====================================================================================================================================
Date: 2026-07-11 (autonomous). User asked to START building the full hybrid match-entry shim: use the S55 game-thread
native-call primitive, injected into the DS-networked client (which the DS route S70 puts in the LIVE tutorial world
with a valid GameState), to drive the local player into a controllable hero. RESULT: the shim FOUNDATION works — the
primitive fires in the DS-networked client, the census resolves every target, and possession functions are callable —
but the possession path is blocked by the SAME "client needs a Loki PlayerController" wall, now confirmed from inside
the client's own gameplay code.

WHAT WAS BUILT
--------------
tools/sigbypass-mod/ds_hybrid.cpp — a clean, DS-context shim (no force-open, no CustomLogin — the client is already in
LVL_Tutorial via the network). Reuses the proven machinery from tutorial_launch.cpp: ProcessInternal hook @base+0x13454A0,
FFrame-template capture, CallNative primitive (call a UFunction thunk @+0xE0 with a hand-built frame), GUObjectArray
walk, FName/class resolution, off-thread object finding. Two modes:
  MODE_CENSUS (Phase 1): read-only census of the DS client.
  MODE_POSSESS_DP (Phase 2): call ClientRestart(PC, DefaultPawn) client-side via the primitive.
Build: clang++ -shared -O2 ds_hybrid.cpp -o ds_hybrid.dll -lkernel32. Inject (into the LIVE client):
tools/inject/inject.exe mmap <PID> ds_hybrid.dll. Marker: docs/ds-hybrid-marker.txt.

PHASE 1 CENSUS — SUCCEEDED (injected into the live DS client, primitive fired, hitsGT=1):
  * Local PlayerController = STOCK "PlayerController" (confirmed; the stub's PC class).
  * Resolved on it: Possess (thunk RVA 0x3702740), ClientRestart (thunk RVA base+0x3C5F990, childProps valid),
    ServerRestartPlayer, ClientSetHUD. AcknowledgePossession / SetPawn / OnPossess are NOT UFunctions (C++ only).
  * LokiGameState instance found; the stub's replicated DefaultPawn found; a BP_HERO_*_C class found (need a better
    filter — the first match was a projectile, "BP_HERO_Assault_AssaultRifle_Projectile_V2_C"); ProgressionManager
    (proven world-context object) found. NO live Loki PC actor (only stock), NO live hero pawn.
  (Thunk addresses are code = base-relative/stable across runs; object addresses change per run — re-find live.)

PHASE 2 POSSESS — CALLABLE but INEFFECTIVE:
  ClientRestart(PC, DefaultPawn) was called via the primitive and RETURNED cleanly (no crash, client stayed alive).
  BUT no possession resulted — no ServerAcknowledgePossession/ServerMove back to the stub, no client-side view/pawn
  change. TWO reasons, both fundamental:
  1. ClientRestart's callable thunk (Func@+0xE0) is the EXEC wrapper, which routes through the RPC/ProcessEvent path
     (a no-op for native fns in this build, S54) rather than running ClientRestart_Implementation (the actual
     client-side possession is a plain C++ function, NOT a resolvable UFunction). So the exec call didn't possess.
  2. ★ DECISIVE: after the inject the client logged "LogBlueprintUserMessages: Warning: Attempting to get null
     controller. Use TryGetLocalLokiController instead." — SUPERVIVE's gameplay code queries TryGetLocalLokiController
     and gets NULL because the local PC is the STOCK networked APlayerController. So even a WORKING possession on the
     stock PC would not engage SUPERVIVE's hero control — the gameplay layer requires a LOKI PlayerController.

CONCLUSION — the wall, confirmed from every angle
-------------------------------------------------
The hybrid FOUNDATION is real and reusable (primitive + census + callable functions in the DS client). But a
controllable hero needs the client to have a LOKI PlayerController (ALokiBaseController / BP_LokiPlayerController), not
the stock networked one — the exact wall the force-open route (S63), the missions-DS route (PC_MainMenu_C), and the
S71 pawn route all hit. The full hybrid would have to: spawn a BP_LokiPlayerController locally, make it the local
player's ACTIVE controller (displacing the stock networked PC that the server keeps authoritative), assign a hero,
spawn a BP_HERO pawn, possess via the C++ possession path (need _Implementation / direct state), and trigger the
drop-in — i.e. reimplement SUPERVIVE's entire client match-entry while the networked session fights it. That is a
deep, uncertain, multi-session effort (a moonshot), not a near-term unlock.

NEXT-STEP LEADS (if pushing the hybrid further)
-----------------------------------------------
  * Get client-side possession to actually run: find ClientRestart_Implementation's address (disasm the exec thunk's
    call target) and call it directly, OR write the possession state directly (AController.Pawn offset + PawnClientRestart).
    But (2) above means this only matters once a LOKI PC is the active controller.
  * The crux: give the local player a live BP_LokiPlayerController. Spawn one via the primitive (SpawnActor) + swap the
    ULocalPlayer's PlayerController to it. Very invasive; the server's replicated stock PC will contend.
  * Better hero-class filter: match "BP_HERO_<Name>_C" (no extra path segments) — the projectile classes share the prefix.

PHASE 3 — client-side hero SPAWN (the decisive viability test): CRASHES; and the spawn-via-primitive is itself unsolved
-----------------------------------------------------------------------------------------------------------------------
ds_hybrid.cpp MODE_SPAWN_HERO: find a real BP_HERO pawn class (filter by LokiCharacter ancestry — got BP_HERO_Assault_C,
excluding projectiles), then GameplayStatics BeginDeferredActorSpawnFromClass(World=ProgressionManager, HeroClass,
hardcoded FTransform@(0,0,500), coll=AlwaysSpawn) + FinishSpawningActor + Possess(stockPC). Injected LIVE into the DS
client (in the tutorial world). RESULT across 3 runs: the resolve is PERFECT (real hero class, valid ProgressionManager
world-ctx, all thunks, param offsets confirmed correct — ParamOffset even found Finish ret@0x78 vs my 0x70 default), but
the FIRST native call — BeginDeferredActorSpawnFromClass — CRASHES the client every time: marker stops at ">>> Begin..."
with NO "<<< deferred=", client dead, and NO [VEH] line = a __fastfail (stack-cookie), which VEH can't catch.
ISOLATION TEST (spawn a STOCK ADefaultPawn class via the SAME path instead of a hero): ALSO crashes identically. So it
is NOT the hero's BP/GAS construction — the BeginDeferredActorSpawnFromClass CALL THROUGH THE PRIMITIVE is itself broken
(a struct-FTransform-param ABI issue: `const FTransform&` param passing via the S56 Code=NULL/PropertyChainForCompiledIn
recipe). This is the EXACT wall S68 hit across a full session ("param-name mismatch → crash"). Client-side actor spawn
via the native-call primitive is an unsolved hard sub-problem (2 sessions).

★★★ CONCLUSION — the client-side hybrid is a DEAD END for reasonable effort. ★★★
Four independent hard walls, each confirmed live this session, block a controllable hero via injection/shim:
  1. SPAWN: client-side actor spawn via the native-call primitive crashes (__fastfail) for even a stock pawn — the
     GameplayStatics struct-param ABI is unsolved (S68 + S72, ~2 sessions).
  2. POSSESS: ClientRestart via the primitive doesn't possess — the callable thunk is the exec wrapper that routes
     through ProcessEvent (no-op for native, S54), not the C++ _Implementation.
  3. CONTROLLER: even a working possession on the STOCK networked PC won't drive a hero — SUPERVIVE's gameplay queries
     TryGetLocalLokiController and gets NULL (confirmed in the client's own log); it needs a LOKI PlayerController.
  4. DROP-IN: the tutorial hero drop-in is SERVER-AUTHORITATIVE + GAMEMODE-DRIVEN (Comp_GameMode_DropPlane_Tutorial);
     neither the DS route (stub GameMode) nor the force-open route (round stuck at EGP_BeginInit) runs the real
     BP_LokiGameMode_Tutorial. The stub can't run it (BP content) and can't provide a Loki PC (S41 RPC-sig wall).
Any ONE is a hard multi-session wall; TOGETHER they make the shim/injection path a dead end. The ONLY remaining
theoretical route to a controllable hero is running the REAL BP_LokiGameMode_Tutorial server-side — which needs the
game's Blueprint content loaded into the stub (an IoStore/mod-pak content overlay, repeatedly found NON-TRIVIAL and
outside the shim approach). That is a fundamentally different, large effort, not a continuation of the hybrid.

WHAT WAS ACHIEVED (the real, banked milestone)
----------------------------------------------
The DS route delivers the client into the LIVE tutorial world with a valid replicated LokiGameState (S70) — the
loading-screen wall that blocked this for many sessions is GONE, and it's a stable spectator view of the live tutorial
world. That is the reasonable-effort ceiling. A controllable hero is beyond it without the content-overlay route.

STATE AT HANDOFF
----------------
tools/sigbypass-mod/ds_hybrid.cpp committed (census + primitive proven in the DS client; possess + spawn-hero modes).
The primitive + census are reusable for any future client-side driving. Stub + ags left running; the game crashes on
each spawn-mode inject (expected). ags address 127.0.0.1:7777. If ever revisiting: the ONE unsolved sub-problem worth
cracking is the GameplayStatics struct-param ABI (BeginDeferredActorSpawnFromClass via the primitive) — but note walls
2-4 remain even if it's solved.
