**CONTROL FIRST — 7/7 PASS, non-degenerate, discriminating both ways** [M]

| control | expected impl | source | record impl | result |
|---|---|---|---|---|
| `ALokiGameMode::SpawnPlayer` | `0xf7eb50` | FK-1 disasm | `0xf7eb50` | PASS/EMPTY |
| `ALokiPlayerState::AuthSetSpawnTeamLeader` | `0xf7ec20` | FK-1 | `0xf7ec20` | PASS/EMPTY |
| `ALokiTeamState_TeamOnly::SetDropLeader` | `0xf7ec20` | FK-1 | `0xf7ec20` | PASS/EMPTY |
| `ALokiDropPlane::OverridePlaneLocations` | `0xf7ec20` | FK-1 | `0xf7ec20` | PASS/EMPTY |
| `ALokiRoundGameMode::GoToPhase` | `0x5601020` | S124 disasm | `0x5601020` | PASS/REAL |
| `ALokiGameState::BP_AuthSetCurrentPhase` | `0x567a160` | S124 | `0x567a160` | PASS/REAL |
| `ALokiRoundGameMode::OnNewPhase` | `0x330c56c` | S124 | `0x330c56c` | PASS/VTABLE-FWD |

4 known-EMPTY + 3 known-REAL **C++** functions — not the degenerate "AS names have no record" control. Seven further undesigned agreements [M]: `AddPlayerToDropPlane 0x55cbb60`; `AddTeamDropEvent 0x557eae0` prologue `48 8b c4 48 89 58 10`; `AuthSetDropComplete 0x2e09510` = `c6 81 d0 00 00 00 01 c3`; `GetDropPod 0x3078470`; `CanExit 0x525c240`; `BroadcastEventRouterReady 0x56dd340`; `SetPilotPlayerState 0x55e59e0`. Eighth control on the vtable route, reproduced in `s129`+`tuthero`: `0x8a22520` +0x4c0=`0x56face0`, +0x4c8=`0xf7ec20`, +0x4d0=`0x56f26a0`, +0x4d8=`0x56df250`, +0x4e0=`0xf7ec20`, +0x4e8=`0x56fae90`.

**FULL TABLE: `G:\git\Supervive Revival Project\scratchpad\s131\lane4-record-sweep.md`** (112 rows, §6A = the 100, §6B = 12 extras). Raw: `scratchpad\s131\sweep_full.tsv`. Tools: `scratchpad\s131\tools\{rectab.py,lane4_sweep.py,gen_md.py}`, re-runnable from repo root.

**STEP 1 — key extraction: the count is a problem and I did not force it.** [M] The per-key §2.5 table **is not on disk**: a repo-wide grep for `COVBLOCKED` returns **exactly one file** (unit: files) — the doc itself. The doc states the count (100) and split, names ~25 keys in prose, and **never enumerates the 100 nor lists the "8 drop classes."** [M] **Three** different 8-class sets sum to exactly 100 under the UHT oracle — reconstruction is under-determined. Adopted set C (`ALokiDropPlane` 25, `ULokiPlayerDropPlaneComponent` 34, `ULokiRideableComponent` 18, `ALokiTeamState_TeamOnly` 7, `ALokiDropPodBase` 6, `ULokiGameModeDropPlaneComponent` 4, `ULokiDropPhaseLibrary` 4, `ULokiDropPhaseDebuggingTool` 2 = 100), corroborated by a signal not used to select it: it has exactly **14** non-`Native` keys and the doc independently reports BlueprintImplementableEvent = 14, with a **zero-off-diagonal** 86/14 cross-tab.

**STEP 2 — `recs.py` fitness: partially fit, replaced.** It reproduces gold values (7/7 above) but is **name-only with no class attribution**, searches the whole image rather than `.data`, and returns multiple hits for names shared across classes — unusable for a per-`(class,func)` sweep. Layout re-derived empirically: **9 qwords, stride `0x48`, record start = `name_ptr − 8`**; `+0x08` name, `+0x10` exec thunk, `+0x18` impl; `+0x28..+0x40` mutable runtime fields. [M] **16,277 records in 1,551 contiguous runs** across `.data` `0x99c7000..0xa0b7000`; runs are **per-UClass, alphabetically sorted** — that is what enables attribution (name-set overlap vs `uht_funcflags_tuthero.csv`; `AddPlayerToPlane` run scores 21/21 vs `ALokiDropPlane`, 1/25 vs nearest rival). 15,720/16,277 attributed. Fold control over the whole table: `0xf7ec20`×371, `0xf7eb60`×76, `0xf7eb50`×40, `0xb9e1f0`×15 are top-1/2/4; other high-multiplicity impls are `48 8b 01 ff a0 <disp>` vtable forwarders, not folds. Two near-folds exist (`0xfc57d0`×15, `0xfc6cf0`×13) and **no key here uses either**.

**STEP 5 — THE HEADLINE.** [M] **All 11 keys with a dark exec-thunk page yield their impl address; 10 of 11 get a definite verdict.** 11 matches the doc's COVBLOCKED-THUNK = 11 exactly. Page `0x5456000` is confirmed dark in all 3 images — the page-boundary finding stands — and the record table reads past it:

- `ULokiRideableComponent::AuthPlayerEnterWorldNew` → **EMPTY** `0xf7ec20` — **a NEW empty stub; drop-class empties go 13 → 14.**
- `AuthPlayerEnterWorld 0x55cce70`, `AuthPlayerEnterWorldAttachedToRidable 0x55cd510`, `AuthPlayerPreSpawnOnAddToPlane 0x55cd800`, `AuthPlayerDetachPlayerFromRidable 0x55cccb0`, `ContainsPlayer 0x55d0270`, `GetLandingTeleportLocation 0x55d89f0` → **REAL** (disassembled: `0x55cce70` = `test rdx,rdx / je / push rbp / lea rbp,[rsp-0x170] / sub rsp,0x270`). Confirms CLAUDE.md's S130 addresses by an independent route and **retires §2.5's "not-looked-at"** on the player-entry family.
- Also resolved: `ALokiTeamState_TeamOnly::{GetByTeamIndex 0x56e6740, GetFuzzyPlayerLocationComponentByTeamIndex 0x56e7b90}`, `ULokiGameModeDropPlaneComponent::SetDropPlane 0x55e55e0` → REAL.
- **Unresolved (4, `IMPL-PAGE-DARK`)**: `FindValidDropLocationInRadius 0x5605b90`, `SelectDropPodDestination 0x56fa590`, `TryLaunchDropPod 0x56ff1d0`, `GetDropLeader 0x3259330`. Address known, **not** a fold, body unreadable ⇒ COVERAGE-BLOCKED, not ABSENT.

**Reconciliation** (set C, 100 keys → `REAL 63 + REAL/RPC-SEND-STUB 7 + EMPTY 12 + IMPL-PAGE-DARK 4 + NO-RECORD 14`): doc BPIE 14→NO-RECORD 14 exact; EMPTY-STUB 11→EMPTY 11 same keys; EMPTY-VIA-VTABLE 2→blind spot; COVBLOCKED-THUNK 11→9 REAL+1 EMPTY+1 dark; REAL 51+INLINED 4+CONST 2 = 57→REAL 57. **[I] Residual 2 keys**: the arithmetic needs 2 of the doc's 5 COVBLOCKED-IMPL to now read REAL and I cannot exhibit them. **Not** a coverage effect — [M] no set-C key is dark in `merged2`+`tuthero` but lit in `s129`. Most likely set C differs from the doc's set by ~2 keys. Do not read the reconciliation as proof the sets are identical.

**THE INSTRUMENT'S OWN BLIND SPOT [M]:** **7 of 112 keys carry `Net`.** For an RPC the record's impl is the **UHT send stub** (the four `ULokiPlayerDropPlaneComponent` ones cluster at `0x542b2b0..0x542b600`, one TU), **not** the `_Implementation` — so it reads REAL where the `_Implementation` is EMPTY (`ServerPassDropLeader`, `ServerSetDropPodDestination`). §2.5's vtable route is correct and I reproduced it. **Never grade a `Net` key from this table alone.** The 3 `Multicast*` keys on `ULokiRideableComponent` are unresolved by both routes here (its vtable was not located) — uninterpretable, not REAL.