## VERDICT

**No data poke can satisfy the getter — and the getter never needed satisfying.** `0xF7EB50` is `33 c0 c3` (`xor eax,eax; ret`), byte-identical in **4/4 images** (`merged4`, `merged3`, `s131-rideable-live`, `tutorial-hero`). Three bytes, **zero memory operands** — it does not read its argument, a cache, or a global, so no write anywhere in the address space can change its return [M]. That forecloses the entire poke-the-getter family, permanently.

But the pointer it should have returned is **dead**. Register-liveness over the guard's fallthrough (`0x55CD590..0x55CD5CB`, every instruction, `capstone regs_access`): **zero reads of RAX**; the first RAX read is the return of a *different* call. **The round game mode is a precondition, not a data dependency** [M]. And the wall's entire persistent output is **one TArray append**, whose consumer is a **REAL, unstripped function**.

---

## 1. `[r14+0xC0]` — IDENTIFIED [M]

`r14 = rcx = this` (`ULokiRideableComponent`), set at `0x55CD543 mov r14,rcx`.

```
0x55CD55B  mov rax,[r14+0xC0]      ; UActorComponent::WorldPrivate
0x55CD562  test rax,rax
0x55CD565  jne +8
0x55CD567  mov rcx,r14
0x55CD56A  call 0x35AFC40          ; UActorComponent::GetWorld_Uncached()
```

`0x35AFC40` is a **structural byte-for-byte match** for stock `GetWorld_Uncached`: `rcx=[this+0xB8]` (Owner) → validity → `AActor::GetWorld` → else `rbx=[this+0x28]` (**OuterPrivate**) → `IsA(UWorld)` → else null. `Outer@0x28` is consistent with this build's already-measured non-stock UObject layout (`ObjectFlags@0x0C`, `InternalIndex@0x10`, `Class@0x18`, `Name@0x20`), and `+0x0C` bit-30 is re-confirmed by this function's own `IsValid` at `0x55CD535`.

⇒ **`UActorComponent::OwnerPrivate @ +0xB8`, `UActorComponent::WorldPrivate @ +0xC0`** [M] — adjacent, in stock declaration order. `+0xB8` independently re-confirmed 3× (`GetRidePosition 0x55DAB5F`, `AuthPlayerDetachPlayerFromRidable 0x55CCCE5`, `GetWorld_Uncached 0x35AFC49`).

**It is a cached world pointer, not a cached game mode.** The stripped call is *downstream* of it and takes the world as its only argument.

## 2. WHAT `0xF7EB50` REPLACED

Signature recovered from the call site: `<T>* f(UWorld*)`, result `IsChildOf(ALokiRoundGameMode)`-tested.

- `0x5453580` = **`ALokiRoundGameMode::StaticClass()`** [M] — `GetPrivateStaticClassBody` with `.rdata` name `LokiRoundGameMode` + package `/Script/Loki`, caching to `.data 0xA02A8C8`.
- `0x55C7DD0` = **`UStruct::IsChildOf(ALokiRoundGameMode)`** [M] — textbook `FStructBaseChain::IsChildOfUsingStructArray` (`Parent.Num@+0x40`, `StructBaseChainArray@+0x38`, `[rdi+0x18]` = the object's `ClassPrivate`). Only **3 call sites image-wide** (uncapped): `0x55CD583`, `0x55CD856`, `0x55CE175`.

**The smoking gun is a sibling that was stripped the same way but did not fold** [M]:
```
ULokiBlueprintLibrary::GetLokiGameMode   impl 0x5630970
  sub rsp,0x28 ; call 0x56F0330 (get world) ; xor eax,eax ; add rsp,0x28 ; ret
```
The world fetch survived, the return was zeroed — so it is **not** byte-identical to `33 c0 c3` and escaped ICF. Its twin `GetLokiGameState` (`0x5630980`) is fully REAL. ⇒ **[I, strong] the callee at `0x55CD572` was a `Get…RoundGameMode(UWorld*)` accessor reduced to `return nullptr` by the same `WITH_SERVER_CODE` strip**, which then folded onto `0xF7EB50`.

## 3. IS THERE ANOTHER ROUTE TO A ROUND GAME MODE? — YES, THE OBJECT EXISTS [M]

`UGameplayStatics::GetGameMode` (impl `0x37D7BF0`, thunk `0x3804740`) is **REAL** and returns `[World+0x250]`. Positive control in the same pass: `GetGameState` (`0x37D7D00`) returns `[World+0x258]`, reproducing CLAUDE.md's measured `UWorld+0x258 = GameState`.

⇒ **`UWorld::AuthorityGameMode @ UWorld+0x250`** [M], one pointer before GameState, stock declaration order. S124 read the live `BP_LokiGameMode_Tutorial_C` at `0x1B3857EA4C0` and dispatched `ALokiRoundGameMode::GoToPhase` on it. **The object is present and reachable; only the accessor was deleted.** That is moot for *this* wall (dead value) — it matters only if a future call site actually consumes one.

## 4. WHAT THE FUNCTION WOULD DO — FULL TRANSCRIPTION [M]

Extent `0x55CD510..0x55CD7FA` = **746 bytes**, 5 chained `.pdata` rows (`pdata_union.csv`) — **fully covered, 0 zero-pages in merged4**, so nothing below is COVERAGE-BLOCKED.

The source file and lines came out of the log records:
`C:\TheoryCraft\build-staging\Loki\Source\Loki\DropPhase\LokiRideableComponent.cpp` — **line 299** = `"…failed to get the round game mode"` (Error), **line 327** = `"AuthPlayerEnterWorldAttachedToRidable: Attempting to attach ourselves to a drop pod whithout a pre-spawned hero."` (Error; the typo is the game's).

```
AuthPlayerEnterWorldAttachedToRidable(this, ALokiPlayerState* PS, const FVector* Loc)
  if (!PS) return;                                       // SILENT
  if (!IsValid(PS)) return;                              // SILENT
  if (this->HasEverContainedPlayer(PS)) return;          // SILENT   0x55DCAA0
  UWorld* W = WorldPrivate ?: GetWorld_Uncached();
  GM = <0xF7EB50>(W);          if (!GM)                  -> LOG(:299), return   <-- THE WALL
  if (!GM->IsA(ALokiRoundGameMode))                      -> LOG(:299), return
  // ---------- GM IS NEVER READ AGAIN [M] ----------
  zUp = Loc->Z + 7500.0                                  // .rdata 0x8B1D4C8 = 7500.0
  Chr = PS->GetLokiCharacter()   if (!Chr)               -> LOG(:327), return
  if (!Chr->IsA(ALokiHeroCharacter))                     -> LOG(:327), return   // 0x54F8DC0
  ... rotation from Chr->[+0x1B0]->[+0x200/+0x210], helpers 0x20B9EA0 / 0x10A50C0
  ULokiGameplayStatics::LokiTeleportActor(Chr, ...,  MaxAdjustZ, 0.5f, ...)      // 0x56680F0
  SetActorEnableCollision(Chr, true)                                            // 0x339A550
  ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep(Chr, &Loc)                 // 0x55C1B20
  SetActorEnableCollision(Chr, false)
  Chr->[+0x1C10] = GetServerTime(Chr)                                           // 0x37D9D40
  this->PlayersAttached.Add(PS)                          // [+0x130]/[+0x138]/[+0x13C]
```

Every one of those is a **REAL reflected UFunction with a live exec thunk** — `LokiTeleportActor 0x537B570`, `SetActorEnableCollision 0x33AC820`, `GetLokiCharacter 0x54373A0`, `GetServerTime 0x3805EF0`.

## 5. THE COMPONENT LAYOUT — 4 INDEPENDENT CONFIRMATIONS [M]

| offset | member | how |
|---|---|---|
| `+0x0E0` | `OnPlayersInsideCountChanged` | `OnRep_PlayersInsideCount 0x55E0FC6` |
| `+0x118` | `bCanExit` (bool) | `CanExit 0x525C240 movzx eax,byte[rcx+0x118]` |
| `+0x11C` | `PlayersInsideCount` (i32) | `OnRep_PlayersInsideCount 0x55E0FC0` |
| **`+0x120`** | **`TArray<ALokiPlayerState*> PlayersInside`** | `ContainsPlayer 0x55D0270` |
| **`+0x130`** | **`TArray<ALokiPlayerState*> PlayersAttached`** | the wall's tail `0x55CD738/767` |
| `+0x140` | `TSet<…> PlayersThatExited` (HashSize `+0x148`) | `HasEverContainedPlayer`'s hash path `0x55DCAE0` |

Corroborated by (a) `binds_members.csv` declaration order props 5→9 packing with **zero gaps**, and (b) ★ **the Angelscript bytecode's own member-offset immediates**: `LokiDropPod.as` `ADDSi 288` = `.PlayersInside` and `ADDSi 304` = `.PlayersAttached` — 0x120 and 0x130, from an artifact entirely independent of the disassembly.

⇒ `HasEverContainedPlayer` = `PlayersInside ∪ PlayersThatExited`.

## 6. ★★★★★ THE ACTUAL ANSWER — THE CONSUMER OF THE ONLY PERSISTENT WRITE IS REAL

`ULokiRideableComponent::AuthPlayerDetachPlayerFromRidable` — **impl `0x55CCCB0`, 440 B, 15 direct calls, ZERO `0xF7EB50`, no round-game-mode reference of any kind** [M]:

```
if (!PS) return; if (!IsValid(PS)) return;
if (!LandingActor) LandingActor = OwnerPrivate;     // [+0xB8]
iterate PlayersAttached ([this+0x130]) ; if EMPTY -> return   <-- ITS ONLY REAL GATE
  GetLokiCharacter · IsA(ALokiHeroCharacter) · SetActorEnableCollision
  SetPredropHidden(…)                    <-- un-hides the hero
  GetLokiCharacterMovement · [MoveComp vtbl+0x3E0]
  GetLandingTeleportLocation(…)          <-- REAL, 963 B, 0 folds
  0x339A7A0 (place)  ·  MulticastOnPlayerEnteredWorld(PS)
```

**The entire dismount is implemented and unstripped. Its sole precondition is the one TArray append the wall dies before performing.**

## 7. PLAIN VERDICT ON `UFunction.Func` SWAP (lever 3a) — **DEAD**

- Exec thunk `0x5456380` has **0 direct callers** (uncapped full-`.text` rel32 scan).
- Impl `0x55CD510` has **3**: `0x545643D` (the thunk's own tail), `0x59701E0` (in fn `0x5966CB0`), `0x597F2BD` (in fn `0x597DC60`) — the latter two inside the **Angelscript AOT range `0x59128B0–0x5A7F070`**, i.e. `LokiDropPod.as::SpawnCrewPod` and `LokiDropShip.as::SpawnDropPodForTeam` **calling the impl by rel32**.

⇒ **The game's own callers bypass `UFunction.Func` entirely.** A Func swap intercepts nothing the game does, and in any case a Func swap replaces a whole function — it cannot remove an internal `call 0xF7EB50`, which is a `.text` rel32. **Removing that call requires a standing `.text` write, which is foreclosed** (measured 10/10 vs 3/36 lethal). I am not recommending it.

## 8. RANKED LEVERS

**#1 — Poke `PlayersAttached` (+0x130), then call `AuthPlayerDetachPlayerFromRidable`. Risk class: DATA (0/22).**
Mirror the wall's own tail exactly on the live component: `old=[c+0x138]; [c+0x138]=old+1; if (old+1 > [c+0x13C]) call 0xF988D0(rcx=c+0x130, edx=old); [[c+0x130] + old*8] = PS`. `0xF988D0` is the game's own `ResizeGrow` — the same call the real function makes, so the allocator is the game's.
Then S55-direct-thunk `AuthPlayerDetachPlayerFromRidable` (**thunk `0x5456100`**, args `(ALokiPlayerState*, AActor* LandingActor)`; pass `nullptr` and it defaults to the pod).
**Receipt:** RPM readback `[c+0x138] == 1` and `[[c+0x130]] == PS` before; after, the hero's `AActor` location moves and `SetPredropHidden` un-hides it. *No* log receipt — the function is silent (0 log strings in its extent, verified).

**#2 — Reproduce the wall's transient effects directly. Risk class: NONE (reflected calls, 0/22).**
All four are REAL thunks: `LokiTeleportActor 0x537B570` (13 params, static, world-context; signature in `binds_members.csv:42693`) · `SetActorEnableCollision 0x33AC820` · `SpawnAndMoveLokiCharacter_MoveStep` impl `0x55C1B20` · `GetServerTime 0x3805EF0` → `Chr+0x1C10`. Combine with #1 for the complete success path.
**Receipt:** `LogLokiRideable` is live (`.data 0xA035E80`, Verbosity 5 / CompileTime 7) and `ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep` logs `"Null Character was given to move"` (`LokiCharacter.cpp:5484`) on a null — a free argument-validity canary.

**#3 — Also poke `PlayersInside` (+0x120) + `PlayersInsideCount` (+0x11C). Risk class: DATA.** Only if a consumer needs it. ⚠ **Ordering trap: this makes `HasEverContainedPlayer` true, which turns the wall itself into a SILENT no-op** — do it *after* any run that uses the wall's own error line as a receipt.

**#4 — `AuthPlayerEnterWorld` (thunk `0x54561D0`). Risk class: NONE, but LOW VALUE.** It is *worse*, not better: **3** `0xF7EB50` calls (`0x55CCF22`, `0x55CD405`, `0x55CD4C7`) — it does not bail on the null, it stashes it at `[rsp+0x50]` and passes it as `this` to two further stripped methods. Its one salvageable act is the terminal `MulticastOnPlayerEnteredWorld` (`0x54537C0`, REAL), which #1 reaches anyway. Its first gate requires PS ∈ `PlayersInside`, and `AuthAddPlayer` — the API that would put it there — is itself `FOLD ret0`.

**#5 — `.text` patch of `0x55CD572`. FORECLOSED.** One byte to `E8`→`90`-family or a rel32 rewrite would work mechanically. It is a standing module-image write: **10/10 armed windows died vs 3/36 without, Fisher p = 8e-8**. Best-idea-that-is-foreclosed, listed for completeness only.

## 9. ONE AWAY, OR FIRST OF N? — **BOTH, AND THE N IS NAMED**

For the *dismount*: **one away.** #1+#2 need no stripped code.

But the same strip is dense here, and one instance is immediately downstream:

⚠ **`Loki::LokiIsClient` impl = `0x00B9E1F0` = `mov al,1; ret` — hardcoded TRUE. `Loki::LokiIsServer` impl = `0x00F7EB60` = `xor al,al; ret` — hardcoded FALSE** [M, from the record table; the addresses are folded so they name the *behaviour*, but a real NetMode test cannot be a 3-byte constant]. ⇒ **`ALokiDropPod::KickPlayersFromPod()` — the pod's own automatic exit driver, which is exactly what iterates `PlayersInside`/`PlayersAttached` and calls the detach — returns immediately, unconditionally.** We must drive the detach ourselves; it will never self-drive. Blast radius: **102 `LokiIsClient` / 100 `LokiIsServer` occurrences across 78 AS files** (unit: occurrences), 22 in the bare `if (LokiIsClient) {` early-return form.
★ Positive control that AS still executes: S131 measured `SpawnDropPodForTeam` returning true and spawning pods, and that function carries no `LokiIs*` guard.

**Stripped-method census, drop family** (record table, `merged4`, 16,277 records): `ALokiDropPlane` 5/21 · `ULokiRideableComponent` 4/18 (`AuthAddPlayer`, `AuthRemovePlayer`, `AuthPlayerEnterWorldNew`, `AuthSetCanJump`) · `ALokiDropPod` 2/26 (`AuthSetCurrentRideable`, `OnPlayerExitedDropPod`) · `ALokiDropPodBase` 0/5 · `ULokiGameModeDropPlaneComponent` 0/3. **11 of 73 = 15 %** against the image-wide empty-impl base rate of **1.2 % (78/6,669)** — informative, not ambient.

## 10. FREE CORRECTIONS TO THE RECORD

- **`ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable` "ALWAYS fails" is right; "its dead tail has zero external rel32 entries" is not the useful frame** — the tail is 470 bytes of fully-decrypted, entirely REAL code, and I transcribed all of it.
- **`GetLandingTeleportLocation` is REAL (`0x55D89F0`, 963 B, 0 folds)** — FK-22 §2.5 lists it COVERAGE-BLOCKED. That is now resolved, exactly as lane-4 predicted the record table would do.
- `AuthPlayerPreSpawnOnAddToPlane` (`0x55CD800`, 496 B) is REAL and carries **1** `0xF7EB50` (`0x55CD842`) — same wall, third instance.
- ⚠ **I committed the hand-arithmetic defect this brief warns about** (read `call 0x7ff6b4453580` as RVA `0x4453580` instead of `0x5453580`, off by 0x1000000, and the wrong address disassembled into *plausible* instructions rather than blanking). Everything above is machine-resolved by `scratchpad/s131/dd.py`, which prints the RVA next to every branch and rip-relative operand.

**Tools written, all offline/read-only:** `scratchpad/s131/dd.py` (RVA-annotating disassembler — recommend adopting it over raw `fkdis.py d`), `lane_e_targets.py`, `liveness.py`, `revname.py`, `byname.py`, `classrun.py`, `classfolds.py`, `callers.py` (uncapped rel32 caller scan + pdata owner attribution), `foldcount.py`, `calls_in.py`, `logrec.py`, `logsin.py`, `foldbytes.py`, `rs.py`.