## LANE C — RESULT

**VERDICT (headline): the wall is ONE CALL, not the first of N — and `AuthPlayerEnterWorld` is strictly WORSE, not better. [M]**
Downstream of the stripped getter there are **11 call sites / 10 distinct targets and ZERO stripped folds**. And the round‑game‑mode pointer is **never used** on the success path — it is loaded into `rax`, tested for null, `IsA<>`‑checked, and then dead. The gate is *vestigial validation*, not a data dependency.

Extents from `tools/strxref/index/pdata_union.csv`, chained contiguous rows unioned: `AuthPlayerEnterWorldAttachedToRidable` = **`0x55CD510..0x55CD7FA` (746 B, 5 rows)**; `AuthPlayerEnterWorld` = **`0x55CCE70..0x55CD506` (1686 B, 7 rows)**; `AuthPlayerPreSpawnOnAddToPlane` = **`0x55CD800..0x55CD9F0`**; `AuthPlayerDetachPlayerFromRidable` = **`0x55CCCB0..0x55CCE68`**. Image = `dumps/merged4.dump.exe`, `.text` **16,683 / 30,281 pages decrypted = 55.09 %** (unit: 4 KB pages). `.data` record table read from the single‑state `dumps/s131-rideable-live` (**16,253 records**), never from a merged image.

**[M] Signature, from the Angelscript binding (`tools/asdump/out/modules/GameMode/DropPhase/LokiDropPod.as.txt:4029`):**
`void ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable(ALokiPlayerState@ PlayerState, const FVector& Location)` ⇒ `rcx`=component, `rdx`=PlayerState, `r8`=&Location. Matches the disassembly exactly (`rbx=r8`, `[rbx+0x10]` read as a `double` = `Location.Z`).

---

## 1. THE SUCCESS PATH, TRANSCRIBED

Walked with `scratchpad/s131/tools/cflow.py path 0x55CD510 --nt 0x55CD513 --nt 0x55CD548 --nt 0x55CD555 --nt 0x55CD57A --nt 0x55CD58A --nt 0x55CD5D1 --nt 0x55CD5E1 --stop 0x55CD77B` (every bail forced not‑taken; all VA→RVA arithmetic done by machine).

```
; ---- preconditions (upstream of the wall) ----
0x55CD54E  call 0x55DCAA0        ; HasEverContainedPlayer(PS)  -> jne bail (SILENT)
0x55CD55B  mov  rax,[r14+0xC0]   ; UActorComponent cached world
0x55CD56A  call 0x35AFC40        ;   else UActorComponent::GetWorld_Uncached
0x55CD572  call 0x00F7EB50       ; <<< THE WALL: xor eax,eax; ret >>>
0x55CD57A  je   0x55CD7B2        ; ALWAYS TAKEN today  -> the logging bail

; ---- SUCCESS PATH BEGINS ----
0x55CD580  mov  rcx,rax
0x55CD583  call 0x55C7DD0        ; IsA<ALokiRoundGameMode>(gm)   -> je 0x55CD7B2 (SAME log)
;   *** rax (the game mode) is clobbered here and NEVER READ AGAIN [M] ***

0x55CD590  movsd xmm0,[rbx+0x10] ; Location.Z
0x55CD598  addsd xmm0,[.rdata 0x8B1D4C8]   ; += 7500.0
0x55CD5A0  movsd xmm1,[rbx+0x10] ; raw Location.Z
0x55CD5B5  movups xmm6,[rbx]     ; Location.X,Y
           [rbp+0x20..0x38] = FVector{X,Y,Z}          (raw landing point)
           [rbp+0x00..0x18] = FVector{X,Y,Z+7500.0}   (pod altitude)

0x55CD5C6  call 0x56BE0D0        ; GetLokiCharacter(PS)  = PS+0x430 then IsA
                                 ;   null -> 0x55CD794 "...whithout a pre-spawned hero."
0x55CD5DA  call 0x54F8DC0        ; IsA<ALokiHeroCharacter>(hero)  -> same log
0x55CD5E7  mov  rcx,[rsi+0x1b0]  ; hero->RootComponent ; null -> default rot (.data 0x99C87B8 = 0,0,0)
0x55CD614  call 0x20B9EA0        ; scene-component cached-transform fetch (RootComp+0x1E8)
0x55CD654  call 0x10A50C0        ; rotation/quat conversion  -> [rbp-0x20..-0x08] = FRotator

0x55CD703  call 0x56680F0        ; ULokiBlueprintLibrary::LokiTeleportActor
                                 ;   rcx=hero, rdx=&{X,Y,Z+7500}, r8=&FRotator,
                                 ;   xmm3=0.0f, [rsp+0x28]=0.5f (.rdata 0x76A10E0), 9 stack args
0x55CD70D  mov  dl,1 ; call 0x339A550   ; SetActorEnableCollision(hero, true)
0x55CD719  lea  rdx,[rbp+0x20] ; call 0x55C1B20
                                 ; ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep(hero,&Loc)
                                 ;   -> IsFinite x3 -> 0x339A7A0 K2_SetActorLocation(bSweep=1,bTeleport=0)
0x55CD723  xor  edx,edx ; call 0x339A550 ; SetActorEnableCollision(hero, false)
0x55CD72B  call 0x37D9D40        ; GetServerTime(hero) -> float -> [hero+0x1C10]
0x55CD738  ...  [r14+0x138]/[r14+0x13c]/[r14+0x130]
0x55CD75B  call 0xF988D0         ; TArray grow (only if Num+1 > Max)
0x55CD767  mov  [rax+rbx*8], rdi ; PlayersAttached.Add(PlayerState)
0x55CD76B..0x55CD793  epilogue, ret
```

Names are **[M]**, each from its own artifact: `0x55C7DD0` → `GetPrivateStaticClass 0x5453580` → literal `"LokiRoundGameMode"` at `.rdata 0x8A4F332`; `0x54F8DC0` → `0x5395720` → `"LokiHeroCharacter"` at `.rdata 0x899A832`; `0x55C1B20` from its own error literal `"ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep Null Character was given to move"` (`.rdata 0x8B19060`, UTF‑16); `0x56680F0` / `0x56BE0D0` / `0x339A550` / `0x37D9D40` / `0x55DCAA0` from the `.data` record table. Member offsets confirmed against the AS property list: **`+0x120/+0x128` = `PlayersInside`**, **`+0x130/+0x138/+0x13c` = `PlayersAttached`**, **`ALokiPlayerState+0x430` = the character**, **`AActor+0x1b0` = RootComponent**.

---

## 2. EVERY CALL ON THE PATH, GRADED

Coverage checked across **30 images on disk** (7 named + every `dumps/*/SUPERVIVE-Win64-Shipping.dump.exe`).

| site | target | name | grade |
|---|---|---|---|
| `0x55CD54E` | `0x55DCAA0` | `HasEverContainedPlayer` | **REAL** (12/30 present) |
| `0x55CD56A` | `0x35AFC40` | `UActorComponent::GetWorld` uncached | **REAL** 30/30 |
| **`0x55CD572`** | **`0x00F7EB50`** | — | **STRIPPED FOLD `33 c0 c3`** ← the wall |
| `0x55CD583` | `0x55C7DD0` | `IsA<ALokiRoundGameMode>` | **REAL** 29/30 |
| `0x55CD5C6` | `0x56BE0D0` | `GetLokiCharacter` | **REAL** 12/30 |
| `0x55CD5DA` | `0x54F8DC0` | `IsA<ALokiHeroCharacter>` | **REAL** 30/30 |
| `0x55CD614` | `0x20B9EA0` | scene-transform fetch | **REAL** 30/30 |
| `0x55CD654` | `0x10A50C0` | rotation conversion | **REAL** 30/30 |
| `0x55CD703` | `0x56680F0` | `LokiTeleportActor` | **REAL by record table — body COVERAGE‑BLOCKED 0/30** |
| `0x55CD70D`,`0x723` | `0x339A550` | `SetActorEnableCollision` | **REAL** 30/30 |
| `0x55CD719` | `0x55C1B20` | `SpawnAndMoveLokiCharacter_MoveStep` | **REAL** 12/30 |
| `0x55CD72B` | `0x37D9D40` | `GetServerTime` | **REAL** 30/30 |
| `0x55CD75B` | `0xF988D0` | `TArray::ResizeGrow` | **REAL** 30/30 |

One level deeper, `foldscan.py` over each callee's chained `.pdata` extent: `GetLokiCharacter` 0 folds · `SetActorEnableCollision` (484 B) 0 · `GetServerTime` 0 · `TArray` grow 0 · `0x20B9EA0` 0 · `0x10A50C0` 0 · `0x339A7A0` (429 B, the real move) 0. `SpawnAndMoveLokiCharacter_MoveStep` shows 2 × `0xF7EC20` — **both are `UE_LOG`‑emit stubs in error branches** (`0x55C1B6D` inside the *null character* branch, `0x55C1CD9` inside the *NaN location* branch), each in the `0xFAC920` build‑FString → `0xF7EC20` emit → `0xFF9310` free triad. **Neither is on the success path and neither is functionality.**

**`LokiTeleportActor` grade, with its negative control:** record `.data 0x9BE64F0` = `{name→"LokiTeleportActor", thunk 0x537B570, impl 0x56680F0}`. `0x56680F0` is not any of the four fold constants, and its **fold multiplicity is 1** against `0xF7EC20` = **371** and `0xF7EB50` = **40** (unit: records in the 16,253‑record table). Its 8 neighbours in the same 154‑record `ULokiBlueprintLibrary` run have monotone distinct impls. ★ **The control that makes this a measurement rather than a default: 7 of those same 154 records DO grade EMPTY** (`IsEditorPreviewActor`, `RespawnTeamInRandomRadiusAsync`, and all five `ServerDrawDebug*`) — the method discriminates inside this exact run.
⚠ **Its BODY is COVERAGE‑BLOCKED, not clean.** Pages `0x5666000` and `0x5668000` are all‑zero in **30/30** images (its exec thunk page `0x537B000` too), so whether `LokiTeleportActor` internally calls a stripped stub **is not established**. The `.pdata` gap `0x5668027..0x5669770` is a harvest artifact of the same never‑executed region, not evidence of a leaf function.

---

## 3. STRIPPED‑STUB COUNT BETWEEN THE GETTER AND A RIDER ATTACHING

**ZERO.** [M, with the one coverage caveat above]

The only stripped code anywhere in reach of this function is:
1. `0x55CD572` → `0xF7EB50` — the getter itself (upstream of the branch; it *is* the wall).
2. `0x55CD7E4` → `0xF7EC20` — the `UE_LOG` emit inside the bail block we currently take.

**What `0xF7EB50` replaced:** nothing recoverable. `0xF7EB50` is literally `33 c0 c3`, so the stripped accessor's *entire compiled body* is `return nullptr` — ICF‑folded together with 39 other zero‑returning UFunction impls including **`ALokiGameMode::SpawnPlayer`** (FK‑1). There is no dead tail, no guarded branch, nothing to restore.

★★ **Independent corroboration that this is a targeted `WITH_SERVER_CODE`‑class strip of *game‑mode access specifically*, with its own same‑TU positive control [M]:**
```
0x5630970  ULokiBlueprintLibrary::GetLokiGameMode   (own .pdata row, 16 B)
   sub rsp,0x28 ; call 0x56F0330 (LokiGetWorld) ; xor eax,eax ; add rsp,0x28 ; ret   <- returns null, always
0x5630980  ULokiBlueprintLibrary::GetLokiGameState  (next .pdata row, 63 B)
   sub rsp,0x28 ; call 0x56F0330 ; test rax,rax ; jz ret ; rbx=[rax+0x258] ; IsA(0x55740F0) ; ret rbx  <- WORKS
```
Same file, same helper, adjacent records: the **GameMode** getter is nulled, the **GameState** getter is intact. Names from the record table (`byimpl[0x5630970]=['GetLokiGameMode']`, `byimpl[0x5630980]=['GetLokiGameState']`).

**Is there another route to a round game mode object on this client? YES — the object was never hidden. [M]**
`UGameplayStatics::GetGameMode` impl **`0x37D7BF0`** is REAL and BlueprintCallable (thunk `0x3804740`); its body is `GetWorldFromContextObject(...)` then **`return *(void**)(World + 0x250)`** ⇒ **`UWorld+0x250 = AuthorityGameMode`**, an offset read out of an unrelated third function. S124 already measured the live `BP_LokiGameMode_Tutorial_C` in a staged world.
⚠ **But that route cannot be plumbed into `0x55CD510`.** The value is produced by a direct `rel32 call` and consumed in registers — **it is never stored to memory**, so there is **no data poke**. And `0xF7EB50` has **80 direct rel32 call sites in merged4's decrypted `.text`** (a floor at 55.09 % coverage; the "27,217" in `CLAUDE.md` is a *pointer‑occurrence* count, a different unit) — it cannot be repointed.

⇒ Only two levers exist, and the second is free:
* **(a)** a 5‑byte detour at `0x55CD572`. This is a module‑image write — the project's measured worst hazard class (standing `.text` 10/10 armed windows died vs 3/36; transient ×3 = 4/12 at a 320 s hold). Exposure here would be call‑duration, not 600 s, but it is the same write class. ★ And because the returned pointer is **dead after the two gates**, the detour does not even need to produce the real game mode — anything whose class chain contains `ALokiRoundGameMode` passes, and NOP‑ing the two `je`s is equally sufficient.
* **(b)** **don't call the function — do its 9 steps.** All of them are directly callable today with **zero `.text` writes**, and four are registered UFunctions with live exec thunks: `GetLokiCharacter` (thunk `0x54373A0`, impl `0x56BE0D0`) · `LokiTeleportActor` (thunk `0x537B570`, impl `0x56680F0`) · `SetActorEnableCollision` (thunk `0x33AC820`, impl `0x339A550`) · `GetServerTime` (thunk `0x3805EF0`, impl `0x37D9D40`); `SpawnAndMoveLokiCharacter_MoveStep` (`0x55C1B20`) is a plain native static. **Precondition [M]: `HasEverContainedPlayer(PS)` must be false** — i.e. the PlayerState is in neither `PlayersInside` (`+0x120`, linear scan) nor `PlayersThatExited` (the `TSet` reached by the Murmur finalizer `imul ecx,eax,0x85EBCA6B` at `0x55DCAE0`).

---

## 4. THE SIBLINGS — AND THE ANSWER TO "IS `AuthPlayerEnterWorld` A BETTER TARGET?"

**NO. It is the worst of the four. [M]**

`AuthPlayerEnterWorld` (`0x55CCE70`) has the **same** getter dependency — `0x55CCF22 call 0xF7EB50`, reached by the identical `[this+0xC0]` / `0x35AFC40` world fetch — but **no gate**: the null is stored to `[rsp+0x50]`/`rbx` and carried forward. It is carried forward to **two more stripped calls**:

```
0x55CD405  mov rcx,[rsp+0x50] ; mov rdx,r12(PlayerState) ; lea r8,[rbp+0x40](FTransform)
           xor r9d,r9d ; [rsp+0x20]=0 ; call 0x00F7EB50      <<< STRIPPED >>>
0x55CD4C7  mov rcx,rbx        ; same shape, other arm        <<< STRIPPED >>>
```
That is **`ALokiGameMode::SpawnPlayer`** — FK‑1's dead stub, whose registered impl `byimpl[0xF7EB50]` literally lists `'SpawnPlayer'`. **The entire payload of `AuthPlayerEnterWorld` is the stripped function.** Everything before it (a 32‑iteration golden‑angle ring search — `0x8B1D4BC` = 0.0981748 rad, `0x7794C80` = 150.0 uu — calling `IsValidPositionOnNavmesh 0x5666A00` / `0x46CB9F0` with the `"PlaceOnGroundTrace"` tag, falling through to `"Could not find a valid 3D terrain location…"`) exists only to build the `FTransform` argument it hands to a `ret 0`. **⇒ 3 stripped calls; patching its getter would gain literally nothing.**
It also has a second, independent blocker: it **bails at `0x55CCED7` unless the PlayerState is already in `PlayersInside` (`+0x120`)**, the exact array `AuthPlayerEnterWorldAttachedToRidable` requires to *not* contain it. And the AS call site is `LokiDropPod.as:1558`, inside **`KickPlayersFromPod`** — it is the *eject* path, not an entry path.

| function | impl | round‑GM getter | gate on it? | GM used after gate? | stripped stubs on success path |
|---|---|---|---|---|---|
| **`AuthPlayerEnterWorldAttachedToRidable`** | `0x55CD510` | yes `0x55CD572` | yes ×2 → same log | **NO — dead** | **0** |
| `AuthPlayerEnterWorld` | `0x55CCE70` | yes `0x55CCF22` | **no** | yes, as `this` for SpawnPlayer | **2** (`0x55CD405`, `0x55CD4C7` = `SpawnPlayer`) |
| `AuthPlayerPreSpawnOnAddToPlane` | `0x55CD800` | yes `0x55CD842` | yes ×2 → own log | **yes** — `call [[gm]+0xA48]` at `0x55CD8E9` | **1** (`0x55CD91C`, `0xF7EC20`, arg = character) |
| `AuthPlayerDetachPlayerFromRidable` | `0x55CCCB0` | **none** | — | — | **2** (`0x55CCD5B`, `0x55CCE4E`, both take the character) |
| `AuthPlayerEnterWorldNew` | `0x00F7EC20` | — | — | — | it *is* the stub |

Also from the same record run, all REAL: `ContainsPlayer 0x55D0270` · `HasEverContainedPlayer 0x55DCAA0` · `GetLandingTeleportLocation 0x55D89F0` · `GetRidePosition 0x55DAB50` · `MulticastOnPlayerEnteredWorld 0x54537C0`; EMPTY: `AuthAddPlayer`, `AuthRemovePlayer`, `AuthSetCanJump`, `AuthPlayerEnterWorldNew`.
★ Worth flagging as the **next** wall: `AuthPlayerDetachPlayerFromRidable` — the "put the rider on the ground when the pod lands" function — is otherwise fully intact (`GetLandingTeleportLocation` → `0x339A7A0 K2_SetActorLocation` → `MulticastOnPlayerEnteredWorld`), but carries 2 stripped character calls of its own. Two independent things confirm the `0x55CD510` route is the designed one: it is called from **`ALokiDropShip::SpawnDropPodForTeam`** (`LokiDropShip.as:161`, `v42.AuthPlayerEnterWorldAttachedToRidable(v38, LandingLocation)`, compiled at `0x597F2BD`) and from `LokiDropPod::SpawnCrewPod` (`:3942`, compiled at `0x59701E0`) — i.e. it sits directly on the function S130 just made work.

---

**Files/tools written this lane (all offline, read‑only):** `scratchpad/s131/tools/cflow.py` (control‑flow‑following disassembler with forced branch polarity, rip‑rel + UTF‑16 string annotation, fold tagging), `scratchpad/s131/tools/grade.py` (call‑target grading + 30‑image coverage), `scratchpad/s131/tools/foldscan.py` (chained‑`.pdata` extent → per‑callee fold census), `scratchpad/s131/tools/recidx.py` + `recidx_ride.pkl` (reverse index impl/thunk → name, 16,253 records).
⚠ **Instrument note for the record:** the first ASCII scan for `"round game mode"` returned **zero hits and was uninterpretable, not negative** — every behavioural literal in this image is UTF‑16LE; the wide scan found exactly **2** occurrences, in `AuthPlayerPreSpawnOnAddToPlane` and `AuthPlayerEnterWorldAttachedToRidable` and nowhere else. And I reproduced the recorded hand‑arithmetic defect once (`0x7ff6b4453580 − base` read as `0x4453580` instead of `0x5453580`, dropping a carry) — it produced a plausible‑looking mid‑function disassembly before the machine recomputation caught it.