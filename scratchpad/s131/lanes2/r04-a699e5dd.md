## LANE A — RESULT

### 0. Headline

**[M] The fold at `0x55CD572` ignores its argument.** `0x0F7EB50` = `33 c0 c3` (`xor eax,eax; ret`), read from `dumps/merged4.dump.exe` at RVA `0x0F7EB50`. ⇒ **no data poke anywhere can make this call return non-zero.** The wall is not "one poke away" in the data sense.

**[M] But the value it should have returned is already computed, IsA-checked and cached by *unstripped* code on the very same object** — see §5. That is the lane's most consequential find.

---

### 1. Full transcribed body — `ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable`

Extent **`0x55CD510 – 0x55CD7FA` = 746 bytes** [M] — union of 5 contiguous `pdata_union.csv` rows (`…510-…5A5`, `…5A5-…77B`, `…77B-…794`, `…794-…7B2`, `…7B2-…7FA`). The single-row figure is 149 bytes; do not cite it as the size.

Signature [M] from `tools/asdump/out/binds_members.csv:44932`:
`void AuthPlayerEnterWorldAttachedToRidable(ALokiPlayerState PlayerState, const FVector& SpawnLocation)`
⇒ `rcx=this(component)`, `rdx=PlayerState`, `r8=&SpawnLocation`. Exec thunk `0x5456380` → impl `0x55CD510` (record-table, fold multiplicity 1).

```
;--- GUARDS (all four bail SILENTLY) ---------------------------------------
0x55CD510  test rdx,rdx ; je 0x55CD793           -> bare ret            [SILENT]
0x55CD535  mov eax,[rdx+0xC]; shr 30; not; test 1
0x55CD548  je 0x55CD77B                          -> epilogue            [SILENT]  (IsValid(PS))
0x55CD54E  call 0x55DCAA0                        = HasEverContainedPlayer(this,PS)   [M, record]
0x55CD553  test al,al ; jne 0x55CD77B            -> epilogue            [SILENT]

;--- THE WALL --------------------------------------------------------------
0x55CD55B  mov rax,[r14+0xC0]                    ; r14=this; +0xC0 = UActorComponent::WorldPrivate
0x55CD562  test rax,rax ; jne 0x55CD56F
0x55CD567  mov rcx,r14 ; call 0x35AFC40          ; GetWorld_Uncached(component) -> UWorld*
0x55CD56F  mov rcx,rax                           ; <== ARG at 0x55CD572 IS A UWorld*   [M]
0x55CD572  call 0x0F7EB50                        ; <== STRIPPED FOLD, returns 0
0x55CD577  test rax,rax
0x55CD57A  je 0x55CD7B2                          ; ALWAYS TAKEN -> LOGGING BAIL (cpp:299)

;=== SUCCESS PATH BEGINS HERE (never reached in this build) ================
0x55CD580  mov rcx,rax ; call 0x55C7DD0          ; IsA<ALokiRoundGameMode>(GM)         [M, §2]
0x55CD588  test al,al ; je 0x55CD7B2             ; same bail
0x55CD590  xmm0 = SpawnLocation.Z + 7500.0       ; const @.rdata 0x8B1D4C8 = 7500.0    [M]
           [rbp-0x28] = that;  [rbp+0x20..0x30] = a copy of SpawnLocation (X,Y,Z)
0x55CD5C6  mov rcx,rdi ; call 0x56BE0D0          = GetLokiCharacter(PlayerState)       [M, record]
0x55CD5CE  test rax,rax ; je 0x55CD794           -> BAIL cpp:327 "…without a pre-spawned hero"
0x55CD5DA  call 0x54F8DC0                        = IsA<ALokiHeroCharacter>(chr)        [M, §2]
0x55CD5E1  test al,al ; je 0x55CD794             -> same bail
0x55CD5E7  mov rcx,[rsi+0x1b0]                   ; hero sub-object (mesh/capsule-ish)
   if null -> 0x55CD684: use FVector::ZeroVector (.data 0x99C87B8 = {0,0,0})           [M]
   else    : read [rcx+0x200],[rcx+0x210]; call 0x20B9EA0(rcx+0x1e8);
             cmpneqpd both halves; if changed, write back + call 0x10A50C0 (transform compose)
0x55CD6A0..0x55CD6FF  build an 8+-arg frame: byte[rsp+0x58]=1, byte[rsp+0x60]=0,
             three null qwords, three zero bytes, float 0.5 (.rdata 0x76A10E0), float 0.0,
             the location copy, and the Z+7500 value
0x55CD703  call 0x56680F0                        = LokiTeleportActor                   [M, record]
0x55CD70D  mov dl,1 ; call 0x339A550             = SetActorEnableCollision(hero,true)  [M, record]
0x55CD719  lea rdx,[rbp+0x20]; call 0x55C1B20    = ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep
                                                   [M, from its OWN error string @0x8B19060]
0x55CD723  xor edx,edx ; call 0x339A550          = SetActorEnableCollision(hero,false)
0x55CD72B  call 0x37D9D40                        = GetServerTime                       [M, record]
0x55CD730  movss [rsi+0x1c10], xmm0              ; stamp the hero
0x55CD738  ; TArray::Add  -- Data +0x130 / Num +0x138 / Max +0x13C  (grow via 0x0F988D0)
0x55CD767  mov [Data + idx*8], rdi               ; <== FINAL EFFECT: PlayersAttached.Add(PlayerState)
;=== SUCCESS PATH ENDS =====================================================

0x55CD794  ; BAIL A, LokiRideableComponent.cpp:327, Error(2), cat @.data 0xA036AC0
           ; "AuthPlayerEnterWorldAttachedToRidable: Attempting to attach ourselves to a drop pod
           ;  whithout a pre-spawned hero."   (record @.rdata 0x8B1CFF0)
0x55CD7B2  ; BAIL B, LokiRideableComponent.cpp:299, Error(2), cat @.data 0xA035E80
           ; "ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable failed to get the round
           ;  game mode"                    (record @.rdata 0x8B1CF08, msg @0x8B1CF30)
0x55CD7CE  lea rdx,[msg]; lea rcx,[rsp+0x70]; call 0x0FAC920   ; FString(msg)
0x55CD7E4  call 0x0F7EC20                        ; SECOND stripped fold, void f(FString&)
0x55CD7F3  call 0x0FF9310                        ; free
```

**The array is `PlayersAttached` [M].** `ContainsPlayer` (impl `0x55D0270`) is a linear scan of `Data@+0x120 / Num@+0x128`; `HasEverContainedPlayer` (impl `0x55DCAA0`) scans the same array then a TSet at `+0x148`. Declared order in `binds_members.csv` is `PlayersInside`(prop 7) → `PlayersAttached`(prop 8) → `PlayersThatExited`(prop 9), so `+0x120`=PlayersInside, **`+0x130`=PlayersAttached**, `+0x140`=the TSet.

---

### 2. Identification of `0x55C7DD0` (needed to name the fold's return type) — **[M]**

`0x55C7DD0` is `bool IsA<ALokiRoundGameMode>(UObject*)`:
- `0x55C7DE2 call 0x5453580`, and `0x5453580` builds/returns the UClass from the literals `W"LokiRoundGameMode"` (`.rdata 0x8A4F332`) + `W"/Script/Loki"` (`.rdata 0x88152C0`), cached in `.data 0xA02A8C8` ⇒ **`ALokiRoundGameMode::StaticClass()`**.
- The body is UE5's `FStructBaseChain::IsChildOfUsingStructArray`: `rdi = obj->ClassPrivate` (`+0x18`, this build's classOff), `idx = Parent[+0x40]` (`NumStructBasesInChainMinusOne`), `&Parent+0x38` (`StructBaseChainArray`), test `idx <= rdi[+0x40] && rdi[+0x38][idx] == &Parent+0x38`.
- Same template instantiated for `ALokiHeroCharacter` at `0x54F8DC0` (its StaticClass getter `0x5395720` carries `W"LokiHeroCharacter"`).

⇒ the fold's return is **`Cast<ALokiRoundGameMode>`-ed**, so its static type is a **base** of `ALokiRoundGameMode` (an `IsA` on an already-exact type would be dead code).

---

### 3. Naming the folded callee

**Method: from the call site, not the address.** `0x0F7EB50` is non-identifying — **27,217 stored qword pointers** (`.rdata` 23,150 / `.data` 4,067, uncapped; matches the figure already in `CLAUDE.md`) and **40 registered UFunction impls**.

**[M] It is NOT a reflected UFunction.** Full `.data` record-table sweep of `merged4` (16,277 records; positive control = the target's own record `{AuthPlayerEnterWorldAttachedToRidable, thunk 0x5456380, impl 0x55CD510}` resolved correctly): **40 records (unit: records)** carry `impl == 0x0F7EB50`, and **none** is a game-mode accessor. Full list: `CheatGetAllClientActorsByClassName, CollapseNodesToFunction, CopyMetaData, CreateNewGeometryCacheGroomBindingAsset(+WithPath), CreateNewGroomBindingAsset(+WithPath), CreateReferenceGraph, FindMetaDataByClass, FindOrAddMetaDataByClass, GetAbilitySystemComponent, GetAsyncPhysicsDataToConsume, GetAsyncPhysicsDataToWrite, GetCreepScore, GetCurrentKillStreak, GetCycleDepth, GetDeaths, GetGameplayCueClass, GetHeroCharacter, GetInteractionRig, GetKills, GetKnocked, GetKnocks, GetLocalLokiPlayerCheatsBP, GetMaxKillStreak, GetParameterValueInt, GetPendingGraphAssets, GetPlayerCheats, GetPlayerController, GetPlayerState, GetRevives, GetRevivesPlusResurrects, GetStaticMeshDescription, GetSystem, GetTreeNodeNum, GetVertexOffsetUsage, MergeMeshesToProperty, RemoveInventoryEffect, **SpawnPlayer**, UpdateTargetEnemy`. (`SpawnPlayer` here re-confirms FK-1.)

**[I, strong] It is a Loki free/static game-mode accessor whose entire shipping body is `return nullptr`.** Evidence:
1. [M] arg = `Component->GetWorld()`; return null-checked then `Cast<ALokiRoundGameMode>`.
2. [M] the author's own words for the failing step: *"failed to get the round game mode"* — and this string exists **exactly twice image-wide**, both in `LokiRideableComponent.cpp` (`:299` here, `:257` in `AuthPlayerPreSpawnOnAddToPlane`), i.e. the *caller* logs, the accessor fails quietly.
3. ★ **[M] The named sibling is gutted the same way.** `GetLokiGameMode` (thunk `0x52E4D90`, impl **`0x5630970`**) is `sub rsp,0x28; call 0x56F0330; xor eax,eax; ret` — it calls the world helper and **returns null unconditionally**. `0x56F0330` is identified as **`LokiGetWorldWithError`** [M] from its own log record at `.rdata 0x8B33C98` → `LokiUtilities.cpp:443` → `W"LokiGetWorldWithError %hs failed to get the world from a valid context object (%s)"`, `lea` site `.text 0x56F0409`.
   Its immediate neighbour **`GetLokiGameState` (`0x5630980`) is INTACT**: `LokiGetWorldWithError(); rbx = World->GameState (+0x258); IsA(...); return`. **Same TU, same idiom, game-state kept, game-mode emptied.**
4. [M] `UGameplayStatics::GetGameMode` (thunk `0x3804740`, impl **`0x37D7BF0`**) is intact and is `World->[+0x250]` ⇒ **`UWorld::AuthorityGameMode` @ `UWorld+0x250`** (sibling of the already-[M] `GameState @ +0x258`).

⇒ A `GetLokiGameMode`-family overload taking a world/world-context: the version that still calls `LokiGetWorldWithError` survives with a visible `xor eax,eax`; a version handed the world already has nothing left but `return nullptr` and therefore **ICF-folds into `0x0F7EB50`**. Grade **[I]** — ICF erased the body and no name record points here; do not write it up as [M].

**[M] regardless of its name, its compiled behaviour is `return 0`, argument unread.**

---

### 4. Same-shaped call sites (counts)

| measurement | merged4 | control |
|---|---|---|
| direct `E8` calls to `0x0F7EB50` in `.text`, **uncapped** | **80** (unit: call sites) | merged2 80, tutorial-hero 77 |
| of those, preceded by `call 0x35AFC40 (GetWorld); mov rcx,rax` | **9** | — |
| of those 9, result immediately null-tested | **6** (`0x55CD572, 0x55CD842, 0x58CD3FB, 0x58CD759, 0x5902A39, 0x5907019`) | — |
| of those 9, result stored+used without an immediate null test | 1 (`0x55CCF22`, inside `AuthPlayerEnterWorld`) | — |
| of those 9, result **discarded** | 2 (`0x55E5623`, `0x58DBDB9`) | — |
| of those 9, followed by `IsA<ALokiRoundGameMode>` (`0x55C7DD0`) | **2** — `0x55CD572` and `0x55CD842`, **both in `LokiRideableComponent.cpp`**, both bailing with the same message (`:299` / `:257`) | — |
| of those 9, calling `[vtable+0x818]` on the result for a float, with a constant fallback | 3 (`0x58CD3FB`, `0x58CD759`, `0x5902A39`) | — |

Denominator context: `call 0x35AFC40` occurs **946** times; **153** are `GetWorld(); mov rcx,rax; call Y`; the fold is `Y` in **9**.

⇒ **This is not a one-off, and it is not one accessor either.** It is ≥9 distinct world-taking accessors that ICF collapsed onto the same empty body. The two that matter to FK-22 are the two rideable entry points — and they are the *same* stripped accessor as each other with near-certainty (same TU, same cast, same message).

**[M] Callers of `0x55C7DD0` (`IsA<ALokiRoundGameMode>`) — uncapped: exactly 3.** Two are the rideable bails; the third is §5.

---

### 5. ★ THE OTHER ROUTE — it exists, it is unstripped, and it is on the same object

`0x55CE140` (`ULokiRideableComponent` vtable override, `.rdata` slot `0x895A290`, **zero direct callers**, page decrypted in all 6 images) does **exactly what the fold was supposed to do**, inline and intact:

```
0x55CE14D  call 0x359E4A0                     ; Super::<lifecycle>
0x55CE152  mov rax,[rbx+0xC0]                 ; this->WorldPrivate
0x55CE15C  jne .have ; mov rcx,rbx; call 0x35AFC40   ; GetWorld_Uncached
0x55CE166  mov rdi,[rax+0x250]                ; <== World->AuthorityGameMode      NOT STRIPPED
0x55CE170  test rdi,rdi ; je .null
0x55CE175  call 0x55C7DD0                     ; IsA<ALokiRoundGameMode>           SAME helper
0x55CE17C  je .null
0x55CE17E  mov [rbx+0xE0], rdi                ; <== ULokiRideableComponent cached RoundGameMode
   .null:  mov qword [rbx+0xE0], 0
```

**[M]** ⇒ the shipping client *does* resolve an `ALokiRoundGameMode` from `World->AuthorityGameMode`, with the identical type check, and stores it at **`Component+0xE0`**. Consequences:

- **A free, zero-cost live readback exists**: read `[RideableComponent + 0xE0]` on the staged tutorial world. Non-null ⇒ the value the fold would have returned is present and the *only* thing between us and the success path is the fold. Null ⇒ `World->AuthorityGameMode` is not an `ALokiRoundGameMode` on this route and the problem is upstream. **Neither branch is currently known; this is one read-only RPM read and it should be taken before anything else.**
- **[M] Nothing in the rideable TU reads `[this+0xE0]`** — a scan of `0x55CC800–0x55E0000` finds only 2 `mov r64,[X+0xE0]` and both are `[rbp+…]` stack, not `this`. The cache is written and never consumed: its consumers are precisely the stripped code.
- ⚠ The exact vtable slot index is **UNRESOLVED** — adjacent vtables are contiguous in `.rdata` and my boundary walk overruns (it reported 3142 slots, which is nonsense). Its neighbours point into `0x35Bxxxx` (engine `UActorComponent` bodies), so [I, strong] it is a `UActorComponent` lifecycle override (`BeginPlay`/`InitializeComponent`), not [M].

---

### 6. What the rest of the function would do — and whether it is the first of N

Every callee on the success path is a **real, non-folded** function, except one:

| callee | identity | grade |
|---|---|---|
| `0x55C7DD0` | `IsA<ALokiRoundGameMode>` | REAL, decrypted, [M] |
| `0x56BE0D0` | `GetLokiCharacter` | REAL, decrypted, [M] |
| `0x54F8DC0` | `IsA<ALokiHeroCharacter>` | REAL, decrypted, [M] |
| `0x20B9EA0`, `0x10A50C0` | transform helpers, unnamed | REAL, decrypted, [M] |
| **`0x56680F0`** | **`LokiTeleportActor`** | ⚠ **COVERAGE-BLOCKED — its page is all-zero in `merged4`.** Not a known fold, but its body is unread. Do **not** call it REAL. |
| `0x339A550` | `SetActorEnableCollision` | REAL, decrypted, [M] |
| `0x55C1B20` | `ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep` | REAL, decrypted, [M] |
| `0x37D9D40` | `GetServerTime` | REAL, decrypted, [M] |
| `0x0F988D0` | `TArray` grow | REAL, decrypted, [M] |

**Family grade, `merged4` record table** (relevant to "first of N"):

```
AuthPlayerEnterWorldAttachedToRidable  0x5456380 -> 0x55CD510  REAL   (this wall)
AuthPlayerPreSpawnOnAddToPlane         0x5456540 -> 0x55CD800  REAL   (same wall, cpp:257)
AuthPlayerEnterWorld                   0x54561D0 -> 0x55CCE70  REAL   (calls the fold 3x; NO null-bail on the first)
AuthPlayerDetachPlayerFromRidable      0x5456100 -> 0x55CCCB0  REAL
ContainsPlayer / HasEverContainedPlayer / CanExit / GetRidePosition /
GetLandingTeleportLocation / MulticastOnPlayer*                 REAL
AuthPlayerEnterWorldNew                0x5456460 -> 0x0F7EC20  EMPTY
AuthAddPlayer / AuthRemovePlayer / AuthSetCanJump               EMPTY
AuthBeginGlideDiveFromDropPod          0x530BFD0 -> 0x0F7EC20  EMPTY
AddPlayerToPlane / OverridePlaneLocations / AuthSetSpawnTeamLeader /
SetDropLeader                                                   EMPTY
SpawnPlayer                            0x534C070 -> 0x0F7EB50  EMPTY
AddPlayerToDropPlane                   0x5350630 -> 0x55CBB60  REAL
```

⇒ **Verdict: this specific function is ONE wall, not a chain** — its own success path bottoms out in real code and ends in a plain `TArray::Add`. But it is *not* one poke, and the pod→hero handoff *after* it (`AuthBeginGlideDiveFromDropPod`) is still an EMPTY fold, so FK-22's ceiling has not moved. `GetLandingTeleportLocation` is now graded **REAL** (`0x55D89F0`) — correcting `fk22`'s "COVERAGE-BLOCKED" for that entry.

**Three ways past `0x55CD572`, ranked:**
1. **Reproduce the success path from a shim** (zero `.text` writes): `LokiTeleportActor` → `SetActorEnableCollision(true)` → `SpawnAndMoveLokiCharacter_MoveStep` → `SetActorEnableCollision(false)` → stamp `[hero+0x1c10]` → `PlayersAttached.Add(PS)` at `Component+0x130`. All heap/data + S55 direct thunks. Blocked only by `LokiTeleportActor`'s unread body (one `dumpimage` from a state that ran it, or one page read).
2. **Redirect the 5-byte call at `0x55CD572`** to a stub returning `[Component+0xE0]` or `World->[+0x250]` — a **`.text` write**, and it must stand across the call. On this project's own ladder (standing `.text` 10/10 vs no-module-write 3/36) that is the worst write class; only viable transiently.
3. ⛔ **Never patch `0x0F7EB50` itself** — 80 direct call sites + 27,217 stored pointers + 40 UFunction impls, including `SpawnPlayer`.

---

### 7. COVERAGE-BLOCKED vs ESTABLISHED

**Established — all 8 load-bearing addresses are decrypted in 6/6 images** (`merged4`, `merged3`, `merged2`, `tutorial-hero`, `s131-rideable-live`, `s131-droppod-live`): `0x55CD510`, `0x55CD572`, `0x55CD7B2`, `0x0F7EB50`, `0x55C7DD0`, `0x5453580`, `0x37D7BF0`, `0x5630970`. **merged4 was not required for this lane** — the whole function was already readable in `tutorial-hero`.

**Coverage bound:** merged4 `.text` = **16,683 / 30,281 pages = 55.09 %** (merged3 16,681; merged2 16,638; tutorial-hero 16,112 = 53.21 %). **44.91 % is undecrypted.**
- The **80** call-site count is therefore a **FLOOR**, not a count (tutorial-hero reads 77 over the same `.text`).
- **`LokiTeleportActor` `0x56680F0` is COVERAGE-BLOCKED, not EMPTY.**
- The claim "nothing reads `[Component+0xE0]`" is bounded to the decrypted portion of the `0x55CC800–0x55E0000` window.
- The `.rdata` string evidence (log records, class-name literals) is **not** coverage-bounded — `.rdata` is identical across every image on disk (FK-18).

**Instrument caveats honoured:** `findptr`'s 200-row cap was bypassed (all pointer/call counts recomputed uncapped); every rip-relative and rel32 target in this report was machine-computed, none by hand — an early hand read of `call 0x7ff6b46be0d0` as RVA `0x46BE0D0` (correct: `0x56BE0D0`) produced a plausible-looking wrong disassembly and was caught only by re-deriving with the machine.

**Artifacts:** `G:\git\Supervive Revival Project\scratchpad\s131\laneA_resolve.py` (annotated disassembler: resolves every call target and rip-relative to RVA + string + record name), `laneA_callsites.py`, `laneA_sites_detail.py`, `laneA_siblings.py`, `laneA_worldsucc.py`, `laneA_rectab.py`, `laneA_family.py`, `laneA_xref2.py` (numpy disp32 xref), `laneA_cov.py`.