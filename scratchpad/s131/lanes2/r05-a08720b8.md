OFFLINE, read-only. All addresses are RVAs against `dumps/merged4.dump.exe` (ImageBase `0x7FF6AF000000`, `.text` 16,683/30,281 pages = **55.09 %** decrypted — every "N sites" below is a **lower bound over that 55 %**, never a total). Every rel32/rip-rel target was machine-resolved (`scratchpad/s132/dis2.py`, new — prints RVA-normalised operands); no hand arithmetic.

# HEADLINE — the answer inverts the question

**[M] In `AuthPlayerEnterWorldAttachedToRidable` the round-game-mode pointer is a PURE GUARD. It is never used again.** Verbatim bytes `0x055CD577`–`0x055CD590` (25 B, contiguous, no spill):

```
055CD577 4885c0        test rax,rax
055CD57A 0f8432020000  je  0x055CD7B2        ; log bail
055CD580 488bc8        mov rcx,rax
055CD583 e848a8ffff    call 0x055C7DD0       ; IsA<ALokiRoundGameMode>
055CD588 84c0          test al,al
055CD58A 0f8422020000  je  0x055CD7B2
055CD590 <body starts>                        ; rax now holds a bool; the pointer is GONE
```
No `mov [rsp+x],rax`, no second register copy. Contrast the sibling `AuthPlayerPreSpawnOnAddToPlane`, which does `mov rsi,rax` at `0x055CD847` and later `call qword [rsi_vtable+0xA48]` — **that one genuinely needs the object; ours does not.**

⇒ **This wall is not "one poke away", it is ZERO pokes away: entering the impl at `0x055CD590` skips it with no module-image write at all.** Lane B's answer (below) is a real, positive result but is *not required* for this function.

---

# §1 — What `0xF7EB50` replaced [I, strong — with a same-module surviving twin]

**The twin is compiled in and I found it.** `0x055CE140`, **2,302 bytes after** the second stripped call site, in the same DropPhase module:

```
055CE140  (this = ULokiGameModeDropPlaneComponent)
055CE14D  call 0x0359E4A0                ; Super BeginPlay
055CE152  mov  rax,[rbx+0xC0]            ; cached UWorld  (else 055CE161 call 0x035AFC40 = GetWorld)
055CE166  mov  rdi,[rax+0x250]           ; World->AuthorityGameMode      <<<<<<
055CE16D  test rdi,rdi ; je skip
055CE172  mov  rcx,rdi ; call 0x055C7DD0 ; IsA<ALokiRoundGameMode>  -- THE SAME FUNCTION the wall calls
055CE17C  je   skip
055CE17E  mov  [rbx+0xE0],rdi            ; this->LokiGameMode = RGM
```
The wall is `mov rcx,World; call FOLD; test rax; je; mov rcx,rax; call 0x055C7DD0`. **The IsA callee is byte-identical (`0x055C7DD0`) in both**, so the stripped callee is a one-line `UWorld* → ALokiRoundGameMode*` accessor whose surviving inlined form is `[World+0x250]`. Whether it did anything *more* is unknowable — grade the body **[I]**, the argument/return role **[M]** from the call site.

Supporting [M]:
* `0x055C7DD0` = `IsA(ALokiRoundGameMode::StaticClass())` — `FClassBaseChain` fast-tree test (`obj+0x18` ClassPrivate, `cls+0x38` chain array, `cls+0x40` NumBasesMinusOne) against `0x05453580`, whose UHT name argument is the wide literal at `.rdata 0x08A4F330 = "ALokiRoundGameMode"` (`lea rdx → 0x08A4F332`, the `A`-skipped form UHT uses), SizeOf `0x7D8`.
* Both bail blocks decoded from their UE5 static-log records: `.rdata 0x08B1CF08` → `"ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable failed to get the round game mode"`, file `C:\TheoryCraft\build-staging\Loki\Source\Loki\DropPhase\LokiRideableComponent.cpp`, **line 0x12B = 299**, verbosity 2 (Error).
* Fold multiplicity, **uncapped, unit = rel32 sites in merged4 `.text`**: `0xF7EB50` **83** (80 call / 3 jmp), `0xF7EC20` 5184, `0xF7EB60` 227, `0xB9E1F0` 101. ⚠ These are **not** comparable to CLAUDE.md's `0xF7EB50 = 27,217`, which is `findptr`'s **qword-pointer** count across all sections. Two different units for the same address; quote which one you mean.
* Stripped-getter call sites inside `0x55CB000..0x55DD000` (7): `AuthPlayerEnterWorld` ×3 (`0x055CCF22`, `0x055CD405`, `0x055CD4C7`), `AuthPlayerEnterWorldAttachedToRidable` ×1 (`0x055CD572`), `AuthPlayerPreSpawnOnAddToPlane` ×1 (`0x055CD842`), plus 2 in a pdata-less function (`0x055D9173`, `0x055D9413`).

---

# §2 — ACCESSOR TABLE: is any of them REAL?

**★ YES — one is, and it is `BlueprintCallable|Static|Pure` with no `BlueprintAuthorityOnly`.**

| accessor | owner | exec thunk | impl | grade | evidence |
|---|---|---|---|---|---|
| **`GetGameMode`** | `UGameplayStatics` | **`0x3804740`** | **`0x37D7BF0`** (also inlined into the thunk at `0x038047C3`) | **REAL — returns `[World+0x250]`** | thunk disasm ends `mov rax,[rax+0x250]; mov [rdi],rax` |
| `GetGameState` | `UGameplayStatics` | `0x38047F0` | `0x37D7D00` | REAL — returns `[World+0x258]` | **positive control**: thunk RVA and offset both reproduce values CLAUDE.md already holds [M] |
| `GetLokiGameMode` | `ULokiBlueprintLibrary` | `0x52E4D90` | **`0x5630970`** | **NEUTERED** — real body, `call 0x56F0330; xor eax,eax; ret` ⇒ **always null** | `execGetLokiGameMode` tail at `0x052E4DFD call 0x05630970`, result stored to the return slot |
| `FindLokiGameModeComponent` | `ULokiBlueprintLibrary` | `0x52E2910` | **`0x562B730`** | **NEUTERED** — `call 0x56F0330; xor eax,eax; mov byte[out],1; ret` | same method |
| *(unnamed helper)* | `LokiRideableComponent.cpp` | — | **`0xF7EB50`** | **FOLD `xor eax,eax; ret`** | the wall, ×5 sites in the module |
| `ALokiRoundGameMode::GoToPhase` | `ALokiRoundGameMode` | `0x5457200` | `0x5601020` (1619 B, 4 chained `.pdata` rows, seen in 76 dumps) | REAL | record `0x09C1F298`; S124 flew it |
| `ULokiGameModeDropPlaneComponent::LokiGameMode` | *field, not fn* | — | `+0x0E0` | **REAL cached `ALokiRoundGameMode*`** | §3 |

⚠ Note the shape: **the `ULokiBlueprintLibrary` GAME-MODE accessors are neutered while their GAME-STATE siblings 16 bytes later (`0x5630980`, `0x562B750`) are real** and read `[World+0x258]`. That is a deliberate, class-wide client strip of the *helpers*, **not** of the data path — `[World+0x250]` is still read by live compiled code (§1).

Coverage note: within the decrypted 55 %, `ALokiRoundGameMode::StaticClass()` (`0x5453580`) has exactly **3** rel32 references — its own NoRegister thunk chain (`0x52D3630` → `0x5455EB0`), its registration site (`0x545392D`), and `0x055C7DE2` (the IsA helper). Its cached `UClass*` slot `.data 0x0A02A8C8` is read from **nowhere but `StaticClass()` itself**. So `Cast<ALokiRoundGameMode>` appears in one place image-wide-as-decrypted. **COVERAGE-BLOCKED for the dark 45 %, not ABSENT.**

---

# §3 — CACHED-FIELD ANALYSIS

**[M] `UWorld::AuthorityGameMode = UWorld + 0x250`** — derived from `UGameplayStatics::GetGameMode`, with `UWorld::GameState = +0x258` re-derived from its sibling in the same pass as the calibration control (matches the project's existing [M]).

**[M] `ULokiGameModeDropPlaneComponent` caches it.** UHT `FObjectPropertyParams` decoded at `.rdata 0x08955950…0x08955A10`; the decoder was calibrated on two known-offset controls in the same run — `APlayerController::CheatManager` read `0x520` and `CheatClass` read `0x528`, both matching CLAUDE.md [M]:

| .rdata record | name | offset | class getter |
|---|---|---|---|
| `0x08955950` | `OnPlaneDropLocationChanged` | `+0x0D0` | (delegate) |
| **`0x08955990`** | **`LokiGameMode`** | **`+0x0E0`** | `0x5455EB0` → `0x52D3630` → `ALokiRoundGameMode::StaticClass` |
| `0x089559D0` | `LokiDropPlane` | `+0x0E8` | `0x53359A0` |
| `0x08955A10` | `PlayerStatePlaneComponentClass` | `+0x0F0` | class-property |

**The writer runs on component begin-play and has no authority guard.** `0x055CE140` has **0 rel32 callers** and exactly **1** qword reference (`.rdata 0x0895A290`) ⇒ it is reached only through a vtable. Its first act is `call 0x0359E4A0(this)`; `0x0359E4A0` occupies **vtable index 119 in 47 of 170 vtables that hold it** (modal), and CLAUDE.md independently records slot 119 / disp `0x3B8` = `BeginPlay`. ⇒ **[M]** `0x055CE140` is a virtual override that calls `UActorComponent::BeginPlay` first; **[I, strong]** it *is* `ULokiGameModeDropPlaneComponent::BeginPlay`. Its whole body (~0x60 B, fully decrypted) contains **zero** role / NetMode / HasAuthority reads.

**[M] Consumer with no null check:** `ULokiGameModeDropPlaneComponent::AddPlayerToDropPlane` (thunk `0x5350630`, impl `0x55CBB60`) does `mov rcx,[rsi+0xE0]; mov rcx,[rcx+0x780]` at `0x055CBBD5` — it *assumes* the cache is populated.

⇒ **Pre-registered, falsifiable prediction for S132: `[Comp_GameMode_DropPlane_Tutorial + 0xE0]` is a non-null `ALokiRoundGameMode*` in a staged `LVL_Tutorial` world** (S124 measured that component live at `0x1B3771413C0`). One read-only RPM read settles it and costs nothing.

---

# §4 — IF THE GETTER DID RETURN ONE, WHAT WOULD THE REST DO?

`ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable(ALokiPlayerState*, const FVector& SpawnLocation)` — signature [M] from `binds_members.csv:44932` and from the exec thunk (`0x5456380` → `call 0x055CD510` at `0x0545643D`; `rcx`=component, `rdx`=PS, `r8`=&FVector). `.pdata` extent `0x055CD510–0x055CD7FA` = **746 B, 5 chained rows, seen in 20 dumps**.

Gates, in order:
1. `rdx != 0` → silent `ret`
2. `!(PS->ObjectFlags@+0x0C & bit30)` → silent
3. **`HasEverContainedPlayer(comp, PS)` (`0x055DCAA0`, REAL) == false** → else silent. Reads `PlayersInside` then falls back to a hash set on `PlayersThatExited`; returns `mov al,1` at `0x055DCB57` on hit.
4. **the round-game-mode gate** (the wall) → logs line 299
5. **`GetLokiCharacter(PS)` = `Cast<ALokiHeroCharacter>(PS->PawnPrivate)`** (`0x056BE0D0` REAL; reads `PS+0x430`, **`APlayerState::PawnPrivate` [M]** from a UHT record with Offset `0x430`; `0x054F8DC0` = `IsA(ALokiHeroCharacter)`, class name literal at `.rdata 0x0899A830`) → **null ⇒ bail-B, `LokiRideableComponent.cpp:327`, Error: `"AuthPlayerEnterWorldAttachedToRidable: Attempting to attach ourselves to a drop pod whithout a pre-spawned hero."`** (the game's typo)

Then the actual work — **every callee graded**:

| callee | grade | identity |
|---|---|---|
| `0x056BE0D0` | REAL (4/4 images) | `GetLokiCharacter` (record) |
| `0x054F8DC0` | REAL | `IsA<ALokiHeroCharacter>` |
| `0x020B9EA0` | REAL, 149 B | scene-component transform read (`[hero+0x1B0]+0x1E8`) |
| `0x010A50C0` | REAL, 621 B | transform compose |
| **`0x056680F0`** | **`.text` page ALL-ZERO in 27/27 dump images** — but **REAL by the record-table instrument**: `.data` triple `{"LokiTeleportActor", thunk 0x537B570, impl 0x056680F0}`, impl ≠ any fold | `LokiTeleportActor` |
| `0x0339A550` | REAL, 484 B | `SetActorEnableCollision` (record) — called `true` then `false` around the move |
| `0x055C1B20` | REAL, 495 B | unnamed (no record) |
| `0x037D9D40` | REAL, 65 B | `GetServerTime` (record) → stored to `hero+0x1C10` |
| `0x00F988D0` | REAL | `TArray` reallocate |

Terminal write: **appends the PlayerState to the component's `TArray` at `+0x130`**, `{Data@0x130, Num@0x138, Max@0x13C}`, stride 8 (`mov [rax+rbx*8], rdi`).

Also: `SpawnLocation.Z + 7500.0` is computed into a local (`double 7500.0` at `.rdata 0x08B1D4C8`).

**[M] `ULokiRideableComponent` array names** (UHT records `.rdata 0x08A500B0 / 0x08A50130 / 0x08A501B0`, same block as `ALokiRoundGameMode`'s literal at `0x08A4F330`):
`PlayersInside` **+0x120** · **`PlayersAttached` +0x130** · `PlayersThatExited` **+0x140** (Set). The names independently corroborate the offsets: `ContainsPlayer` (`0x055D0270`) reads only `+0x120`; `HasEverContainedPlayer` reads `+0x120` then the set at `+0x140`.

⇒ **Verdict: not "the first of N".** One more hard precondition (a live `ALokiHeroCharacter` on `PS->PawnPrivate`), then nine callees of which **eight are REAL in-image and the ninth is a registered, non-folded UFunction impl whose page has simply never executed.** No further folds, no further authority gates.

Bonus, correcting the record: **`ULokiRideableComponent::AuthPlayerEnterWorld` (thunk `0x54561D0`, impl `0x055CCE70`) is REAL, not empty** — only `AuthPlayerEnterWorldNew` is a fold. But it is **worse**, not better: it calls the stripped getter three times and **stores the null without a null check** (`0x055CCF2F mov [rsp+0x50],rax`, `0x055CCF37 mov rbx,rax`), and it *requires* the PS to already be in `PlayersInside` — which only `AuthAddPlayer` writes, and that is `FOLD ret0`. Do not use it as a substitute. Full `ULokiRideableComponent` block grade (records `0x09C1E4D8`–`0x09C1E958`): EMPTY = `AuthAddPlayer`, `AuthPlayerEnterWorldNew`, `AuthRemovePlayer`, `AuthSetCanJump`; REAL = everything else including `GetLandingTeleportLocation` (`0x055D89F0`) and `GetRidePosition` (`0x055DAB50`), both of which S130 listed as COVERAGE-BLOCKED.

---

# §5 — THE ONE EXPERIMENT FOR S132

**MID-FUNCTION TRAMPOLINE — zero `.text` writes, zero PI hooks.** Replicate the game's own prologue in an allocated stub and jump to `0x055CD590`, past the guard.

Stub, called as `stub(component, PlayerState, &FVector Landing)` — bytes 0..0x1E are the game's own prologue from `0x055CD519`, only reordered so `rbx/rdi/r14` are loaded after the frame (exactly as the original does at `0x055CD538/53E/543`):

```
48895c2410 48897c2418 4c89742420        ; mov [rsp+10],rbx / [rsp+18],rdi / [rsp+20],r14
55 488d6c24a0 4881ec60010000            ; push rbp / lea rbp,[rsp-0x60] / sub rsp,0x160
498bd8 488bfa 4c8bf1                    ; mov rbx,r8 / mov rdi,rdx / mov r14,rcx
48b8 <BASE+0x055CD590 as imm64> ffe0    ; mov rax,imm64 / jmp rax
```
(49 bytes; imm64 at stub offset 39.) The epilogue's `lea r11,[rsp+0x160]; mov rsp,r11; pop rbp; ret` returns to *our* caller correctly. It writes the caller's 4 home slots (`+0x08…+0x20`), so the shim must call it with the standard 32-byte shadow space — nothing exotic.

**The shim must itself perform the three guards it skipped** (all cheap, all read-only): `PS != null`; `!(PS->ObjectFlags@+0x0C & 0x40000000)`; and call `0x055DCAA0(comp, PS)` first, aborting if it returns 1.

**Arguments — take them from the game's own call site, don't invent them.** `LokiDropShip.as:163` (Angelscript, pseudo-source; disassembly appendix is ground truth) does `ULokiRideableComponent::Get(pod).AuthPlayerEnterWorldAttachedToRidable(GetTeamDropLeader(TeamIndex), LandingLocation)`. S131 already resolved exactly this: `comp = 0x2BDCFA4FA00`, `Landing = (-3206.4, 5070.5, 100.0)` from the pod's own `CurrPodDestination` (ASLR-dependent; re-derive).

### RECEIPT — and the S131 harness would have MISSED it
⚠⚠ **`ContainsPlayer` IS THE WRONG RECEIPT AND WOULD HAVE PRODUCED A FALSE NEGATIVE.** [M] it reads `PlayersInside` (`+0x120`); the wall's only write is to `PlayersAttached` (`+0x130`). S131's `RESULT-rideable-s131-live.txt` uses `ContainsPlayer` as its before/after (`R0c`/`R2`) — **on a successful call it would still have read `after=0`** and the run would have been written up as "no rider attached". Fix this before flying.

1. **Primary [deterministic, unreachable by any other decrypted path]:** read-only RPM on the component. `Num = *(int32*)(comp+0x138)` must go **N → N+1**, and `*(void**)( *(void**)(comp+0x130) + 8*N ) == PlayerState`.
2. **Negative control in the same read:** `*(int32*)(comp+0x128)` (`PlayersInside.Num`) must **not** change.
3. **Secondary:** `*(float*)(hero+0x1C10)` becomes `GetServerTime()` (was presumably 0).
4. **Self-identifying failure, with a verified zero baseline:** if `PS->PawnPrivate` is not an `ALokiHeroCharacter`, the function logs `LokiRideableComponent.cpp:327` — `"…whithout a pre-spawned hero."` **[M] that string occurs 0 times across both S131 logs (1.48 MB combined), while `LogLokiRideable` fires exactly 2/2 as the round-game-mode bail** — so the category's verbosity demonstrably passes Error (both records are verbosity 2), and its appearance is a clean, never-before-seen discriminator rather than an uninterpretable silence.
5. **Void condition:** if `Num` does not move AND neither log line appears, the trampoline itself did not run — re-check the stub, do not interpret anything else.

### Free pre-checks first (read-only RPM, no injection, no launch cost)
`PS->PawnPrivate @ +0x430` non-null and `IsA<ALokiHeroCharacter>` — **this is the single most likely reason the run fails**, and S131 never checked it for either of its two candidates; `comp+0x138` baseline; `[Comp_GameMode_DropPlane_Tutorial + 0xE0]` (settles §3's prediction); `[World+0x250]`.

**Fallback if the trampoline is rejected:** rewrite only the rel32 at `0x055CD573` so the `call` targets a stub returning `[World+0x250]`. Same instruction length, 4 bytes, transient — but it *is* a `.text` write (project-measured 4/12 at a 320 s hold vs 0/22 for none). Strictly worse; named only for completeness.

---

# §6 — Instrument notes for the record

* **`.pdata` is coverage-limited exactly like `.text`.** `pdata_union.csv` has hard gaps `0x5668027–0x5669770` and `0x558208D–0x55861A0` — precisely the undecrypted pages. A `NONE` there is **not** evidence of absence; `0x56680F0` reads NONE and is a real, named UFunction impl.
* **`0x56680F0` and `0x55820E0` are ZERO in all 27 dump images on disk** (checked exhaustively). The `.data` record table graded the first anyway — **that instrument is worth more than S130 claimed and should be run over the remaining COVERAGE-BLOCKED keys; it costs nothing.**
* **`fkdis.py findptr` counts and rel32 counts are different units for the same fold** and differ by ~300×. Always say which.
* `merged4` = `merged3` + the post-wall live image, contributing **7,570 bytes in 2 pages**; the wall's bail block was already decrypted in `merged2`, so the new pages did not carry this result — the analysis reproduces on `merged2`/`tuthero`/`s129` for every address except the two dark ones.
* New tools, all read-only: `scratchpad/s132/xr.py` (numpy rel32/rip-rel/qword xrefs, uncapped), `scratchpad/s132/dis2.py` (RVA-normalising disassembler), `scratchpad/s132/grade.py` (REAL/FOLD/PAGE-DARK across 4 images), `scratchpad/s132/recs_merged4.pkl` (16,277 `.data` name/thunk/impl records).