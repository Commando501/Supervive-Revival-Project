## VERDICT

**3/3 core claims CONFIRMED. 6 secondary claims REFUTED or corrected. 5 remain [I]/ungrounded.** The report's operational conclusion survives intact: no data poke can satisfy the getter, the game-mode pointer is dead after the two guards, and the detach is reachable by a `PlayersAttached` poke. What fails is framing and secondary counts — including one claim the session's *own* sibling lane got right and the final report dropped.

Everything below re-derived with my own loader/disassembler (`G:\git\Supervive Revival Project\scratchpad\s131rev\av.py`, `rec.py`, `callers.py`), independent of `fkdis.py`/`dd.py`/`rectab.py`.

---

## RE-DERIVATION 1 — "`0xF7EB50` is `33 c0 c3`, zero memory operands ⇒ no poke can satisfy the getter" → **CONFIRMED [M]**

`0xF7EB50` = `33 c0 c3` = `xor eax,eax; ret`. Verified in **7 images**, not 4 (merged4/3/2, s131-rideable-live, s131-droppod-live, tutorial-hero, s129-poolgate), all four bytes identical and the containing page non-zero in every one. Zero memory operands ⇒ no write anywhere changes its return. **The poke-the-getter family is foreclosed.**
Call site re-read: `0x055CD572 e8 d9 15 9b fb` → target `0x00F7EB50` (rel32 machine-resolved, not hand-computed); `0x055CD577 test rax,rax; 0x055CD57A je 0x055CD7B2`.

Fold controls re-read in the same pass: `0xF7EC20 = c2 00 00`, `0xF7EB60 = 32 c0 c3`, `0xB9E1F0 = b0 01 c3`.

## RE-DERIVATION 2 — "the round game mode is a precondition, not a data dependency" → **CONFIRMED [M], with one nuance the report understates**

Full 746-byte disassembly, `0x055CD510..0x055CD7FA`, **0 zero-pages in merged4**. The returned pointer is consumed exactly twice, both as guards:
- `0x055CD577 test rax,rax` (null → bail)
- `0x055CD580 mov rcx,rax` → `0x055CD583 call 0x055C7DD0` (`IsChildOf`; fail → same bail)

`0x055CD583`'s call clobbers RAX with its own `bool` return; no store of RAX to memory occurs anywhere between `0x055CD572` and `0x055CD583`. From `0x055CD588` to the epilogue the pointer is unreachable. Next RAX reads are `0x055CD5CB mov rsi,rax` (return of `GetLokiCharacter 0x056BE0D0`) and `0x055CD61D mov rbx,rax`. ⇒ if the getter returned *any* object passing `IsChildOf(ALokiRoundGameMode)`, the body would run and never look at it again.

`0x055C7DD0` **is** the `IsChildOf` — it calls `0x05453580`, then walks `[rbx+0x18]` (ClassPrivate, this build's layout) against `[rax+0x38]`/`[rax+0x40]` (`FStructBaseChain` array/count). **3 rel32 sites image-wide, uncapped**: `0x055CD583`, `0x055CD856`, `0x055CE175`.
`0x05453580` **is** `ALokiRoundGameMode::StaticClass` **[M, not structural]**: it `lea`s the UTF-16 literal `LokiRoundGameMode` at `.rdata 0x08A4F332` and `/Script/Loki` at `.rdata 0x088152C0`, caching to `.data 0x0A02A8C8`.

**Bail identity settled from the artifact, both messages:**
`.rdata 0x08B1CF08` = {fmt→`0x08B1CF30`, file→`0x08B1CDD0`, **line 299, verbosity 2 (Error)**} → `"ULokiRideableComponent::AuthPlayerEnterWorldAttachedToRidable failed to get the round game mode"`, file `C:\TheoryCraft\build-staging\Loki\Source\Loki\DropPhase\LokiRideableComponent.cpp`.
`.rdata 0x08B1CFF0` = {**line 327, verbosity 2**} → `"AuthPlayerEnterWorldAttachedToRidable: Attempting to attach ourselves to a drop pod whithout a pre-spawned hero."` Same file. Both line numbers CONFIRMED.

## RE-DERIVATION 3 — "`AuthPlayerDetachPlayerFromRidable` is real, has no round-game-mode reference, and its gate is the one thing the wall dies before doing" → **CONFIRMED [M] with two corrections**

Record table (own scanner, name→`.data` qword→{+8 thunk, +0x10 impl}): `AuthPlayerDetachPlayerFromRidable` rec `.data 0x09C1E520`, **thunk `0x05456100`, impl `0x055CCCB0`** — matches the report. Extent per `pdata_union.csv` = `0x055CCCB0..0x055CCE68` = **440 bytes**, 11 chained rows, **0 zero-pages**, **15 direct calls + 1 indirect** (`call [r8+0x3E0]`). **Zero `0xF7EB50`, zero references to `0x05453580` or `0x055C7DD0`** — CONFIRMED.

**Correction 3a — the gate is `Find(PS)`, not "non-empty".** `0x055CCCEC` loads Data `[rbp+0x130]`, `0x055CCCF3` Num `[rbp+0x138]`; `0x055CCD01 je` handles empty; then `0x055CCD0F mov rbx,[rsp+0x88]` **re-reads PS from its home slot** (stored at `0x055CCCB9` before the pushes) and `0x055CCD17..0x055CCD23` is a linear scan — **not found also returns** (`jmp 0x055CCE53`). Independently corroborated in Angelscript: `tools/asdump/out/modules/GameMode/DropPhase/LokiDropPod.as.txt:1540` `if (v12.PlayersAttached.Contains(v32))`. This *sharpens* lever #1 rather than breaking it: you must append the exact `ALokiPlayerState*` you then pass.

**Correction 3b — "the entire dismount is … unstripped" is REFUTED.** It carries **2 calls to `0x00F7EC20` (`ret 0`)**: `0x055CCD5B` (arg `rcx = rdi` = the hero character, immediately after the `IsA(ALokiHeroCharacter)` gate) and `0x055CCE4E` (`dl=3, r8d=0`). The first is a stripped method **on the character** with no FString marshalling, i.e. not the benign "stripped diagnostic reporter" shape seen at `0x055CD7E4`. The report's own sibling lane already said so — `G:\git\Supervive Revival Project\scratchpad\s131\lanes2\r03-a1cd22f9.md:142`: *"carries 2 stripped character calls of its own"* — and the final report dropped it. "ZERO `0xF7EB50`" is true and is a narrower statement than the headline it supports.

---

## OTHER CLAIMS I CONFIRMED

| claim | result | how |
|---|---|---|
| wall thunk `0x5456380` / impl `0x055CD510` | ✅ [M] | rec `.data 0x09C1E5B0` |
| extent 746 B / 5 chained rows / fully covered | ✅ [M] | `pdata_union.csv` rows `510-5A5, 5A5-77B, 77B-794, 794-7B2, 7B2-7FA`; 0 zero-pages |
| 3 rel32 callers of the impl, 2 in AS AOT | ✅ [M] | uncapped scan: `0x0545643D` (own thunk row `0x5456380..0x5456452`), `0x059701E0`, `0x597F2BD` |
| `UWorld::AuthorityGameMode @ +0x250` | ✅ [M] | `GetGameMode` rec thunk `0x03804740` impl `0x037D7BF0` → `mov rax,[rax+0x250]`; control `GetGameState` thunk `0x038047F0` impl `0x037D7D00` → `+0x258`, reproducing CLAUDE.md |
| smoking gun `GetLokiGameMode` | ✅ [M] | rec thunk `0x052E4D90` impl **`0x05630970`** = `sub rsp,0x28; call 0x056F0330; xor eax,eax; add rsp,0x28; ret`. Twin `GetLokiGameState` impl `0x05630980` same call then `[World+0x258]` + cast — **REAL**. Two siblings, one nulled. |
| `+0xB8` Owner / `+0xC0` World, `0x35AFC40` = the uncached getter | ✅ shape [I, strong] | `0x035AFC49 [rcx+0xB8]`, `RF_ClassDefaultObject` (bit 4) test, `[rbx+0x28]` Outer + `IsA` — textbook `GetWorld_Uncached`. No record ⇒ name is structural, see R12. |
| component layout | ✅ [M] | `CanExit` impl `0x0525C240` = `movzx eax,byte[rcx+0x118]; ret` · `OnRep_PlayersInsideCount` impl `0x055E0FC0` = `mov edx,[rcx+0x11C]` · `ContainsPlayer` impl `0x055D0270` = Data `+0x120`, Num `+0x128` · wall tail Data `+0x130`/Num `+0x138`/Max `+0x13C` · `HasEverContainedPlayer` impl `0x055DCAA0` searches `+0x120` then hashes against `[r10+0x148]`. Fully self-consistent (16-byte TArray stride). |
| AS bytecode offsets | ✅ [M] | `LokiDropPod.as.txt:1619 ADDSi 288 ; .PlayersInside` / `:1642 ADDSi 304 ; .PlayersAttached` = 0x120 / 0x130 — an artifact independent of the disassembly |
| `KickPlayersFromPod` returns immediately | ✅ [M] | **bytecode appendix, not pseudo-source**: `0000 PshGPtr __WorldContext; 000C CALLSYS LokiIsClient; 0018 JLowZ ->L0028; 0020 JMP ->L0328`. `LokiIsClient` rec thunk `0x052E64A0` impl `0x00B9E1F0` (`mov al,1; ret`); `LokiIsServer` thunk `0x052E7150` impl `0x00F7EB60` (`xor al,al; ret`). |
| `GetLandingTeleportLocation` REAL, 963 B, 0 folds | ✅ [M] | rec impl `0x055D89F0`; chain `0x055D89F0..0x055D8DB3` = 963 B, 6 rows, 0 fold calls, 0 zero-pages. FK-22 §2.5's COVERAGE-BLOCKED entry is genuinely resolved. |
| `AuthPlayerEnterWorld` is worse: 3× `0xF7EB50` | ✅ [M] | rec impl **`0x055CCE70`** (thunk `0x054561D0`), extent `..0x055CD506` = 1686 B; folds at exactly `0x055CCF22`, `0x055CD405`, `0x055CD4C7` |
| 4 rideable stubs are folds | ✅ [M] | `AuthAddPlayer`/`AuthRemovePlayer` thunk `0x02C2CE30`, `AuthPlayerEnterWorldNew` thunk `0x05456460`, `AuthSetCanJump` thunk `0x05296F30` — all impl `0x00F7EC20`. Also `AuthSetCurrentRideable`, `OnPlayerExitedDropPod` = `0x00F7EC20`. |
| MoveStep canary exists | ✅ [M] | `.rdata 0x08B19038` = {line **5484**, verb 2} `"ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep Null Character was given to move"`, `LokiCharacter.cpp` |
| constants | ✅ [M] | `.rdata 0x08B1D4C8` = **7500.0** (double); `.rdata 0x076A10E0` = **0.5f** |
| `0xF988D0` is a real grow | ✅ [I, strong] | 149 B, 1 row, reads `[rcx+8]`/`[rcx+0xC]` vs `edx` — `TArray` Num/Max shape. No record ⇒ name structural. |
| lever-#3 ordering trap | ✅ [M] | poking `PlayersInside` makes `HasEverContainedPlayer` (`0x055DCAA0`) true at `0x055CD54E`, and `0x055CD555 jne` silently bails. Correct and worth keeping. |

---

## REFUTED / CORRECTED

**R1 — "the wall's entire persistent output is one TArray append" — REFUTED.** It also does `0x055CD730 movss dword ptr [rsi+0x1C10], xmm0` (result of `GetServerTime`, rec impl `0x037D9D40`), moves the character twice (`0x055CD703` → `LokiTeleportActor` impl `0x056680F0`; `0x055CD719` → `SpawnAndMoveLokiCharacter_MoveStep 0x055C1B20`) and toggles collision twice (`0x055CD70D` `dl=1`, `0x055CD723` `dl=0`, impl `0x0339A550`). The report's own §4 transcribes all of these and then calls them "transient" in lever #2 — internally inconsistent. Actor position is not transient.

**R2 — AS call-site → function attribution is UNGROUNDED.** "`0x59701E0` (in fn `0x5966CB0`)" and "`0x597F2BD` (in fn `0x597DC60`)": **no row in `pdata_union.csv` contains either address.** Nearest rows are `0x5966CB0..0x59676D9` (ends **0x8B07 below** `0x59701E0`) and `0x597DC60..0x597E642` (ends **0xC7B below** `0x597F2BD`). The AS AOT range `0x059128B0..0x05A7F070` (1,492,928 bytes) is only **40.8 %** pdata-covered, so nearest-preceding-row attribution cannot work there — and it reproduces the report's two answers exactly, which is how I identified the method. **The conclusion survives on a different artifact**: `tools/asdump/out/modules/GameMode/DropPhase/LokiDropShip.as.txt:161` (`SpawnDropPodForTeam`, line 123) and `.../LokiDropPod.as.txt:3942` (`SpawnCrewPod`) both call `AuthPlayerEnterWorldAttachedToRidable`. Note also the repo's own `docs/fk1-angelscript-settled.md:217` puts `SpawnDropPodForTeam` at `0x0597E730`, outside the report's cited `0x597DC60` extent — a conflict the report inherited without noticing.

**R3 — "102/100 occurrences across 78 AS files" — the denominator is not reproducible and the numerator double-counts.** `tools/asdump/out/` contains **four copies of the same corpus** (`out/`, `out/a/`, `out/b/`, `out/modules/`). Per tree: **24 files**, 102 `Loki::LokiIsClient` + 100 `Loki::LokiIsServer` occurrences (matches the report's occurrence figures). Across all four: **96 files**, 358 + 350 occurrences. **78 matches neither.** And each call appears twice per file (pseudo-source line + appendix comment), so **actual source-form call sites = 51 client / 50 server**; bare `if (Loki::LokiIsClient(__WorldContext)) {` = **22** ✓ (client) / 36 (server). Unit was quoted — good — but "occurrences" was then used as a blast-radius proxy where call sites is the meaningful number, over-stating it ~2×.

**R4 — "Exec thunk `0x5456380` has 0 direct callers" is COVERAGE-BLOCKED, not ABSENT.** I reproduce 0, but merged4's `.text` is **16,683 / 30,281 pages non-zero = 55.09 %** (unit: 4 KB pages). It is also structurally uninformative — exec thunks are dispatched through `UFunction.Func`, never rel32 — so a 0 there was never evidence. The load-bearing half (3 sites on the impl) reproduces exactly.

**R5 — "All four are REAL thunks" (lever #2) is wrong for one of the four.** `SpawnAndMoveLokiCharacter_MoveStep` has **no record and no exec thunk**: 0 ASCII hits, 3 UTF-16 hits, all `.rdata` log literals (`0x08B19080`, `0x08B19150`, `0x08B19220`). It is a raw native address — a different dispatch and risk class from `LokiTeleportActor 0x537B570` / `SetActorEnableCollision 0x33AC820` / `GetServerTime 0x3805EF0`. It also contains 2 `0x00F7EC20` calls of its own (`0x055C1B6D`, `0x055C1CD9`).

**R6 — the stripped census "11/73 = 15 % vs 1.2 % base rate" is a LOWER BOUND and not like-for-like.** The record-table grader flags `impl ∈ {4 known folds}`; it is structurally blind to a non-folded `return nullptr` stub — and the report's own `GetLokiGameMode` (`0x05630970`) is exactly that shape. Separately, the 1.2 % (78/6,669) base rate is FK-1's empty-impl census, a different instrument, so the ratio compares two measurements that were not made the same way.

---

## STILL [I] OR UNGROUNDED — and what settles each

1. **`LokiTeleportActor`'s body has never been seen.** Its impl `0x056680F0` sits on an **all-zero page (`0x5668000`) in merged4** — COVERAGE-BLOCKED. Graded REAL only by fold-exclusion (see R6), and its "13 params, static, world-context" comes from `binds_members.csv`, not from code. **Settles by:** re-`dumpimage` after any run that executes a teleport, or accept the record-table grade explicitly as fold-exclusion.
2. **"`0xF7EB50` replaced a `Get…RoundGameMode(UWorld*)` reduced by `WITH_SERVER_CODE`"** — [I, strong]. The `GetLokiGameMode`/`GetLokiGameState` twin is genuinely good evidence, but no artifact names the deleted callee. **Settles by:** the UHT/`binds_members` oracle, or a PDB-less name from a sibling build. Low value — the operational answer does not change.
3. **`LokiIsClient` true / `LokiIsServer` false ≠ "strip".** On a client-only build these are *correct*, and the compiler folding them is expected. The report's framing ("the same strip is dense here") conflates a stripped server implementation with a constant-folded predicate. The consequence for `KickPlayersFromPod` is identical either way, so nothing operational rides on it — but do not carry the framing forward. Also unverified: that the AOT-compiled body dispatches to the registered impl rather than inlining its own NetMode test.
4. **`AuthorityGameMode` is populated on this client** — [M, but from S124/S130, not from this session's artifacts]. Nothing here re-measures it. Moot for the wall (dead value); load-bearing only if a future call site consumes one.
5. **Lever #2's completeness.** "Combine with #1 for the complete success path" omits `0x055CD5E7..0x055CD682` entirely — `[Chr+0x1B0]` → `[+0x200]`/`[+0x210]`, `add rcx,0x1E8`, helpers `0x020B9EA0` / `0x010A50C0`, conditional write-back of `[rbx]`/`[rbx+0x10]`/`[rbx+0x20]`/`[rbx+0x30]`, else a hardcoded default from `.data 0x099C87B8`. That block computes the transform actually fed to `LokiTeleportActor`. #1+#2 gives you the append plus a teleport to a *hand-supplied* transform, not the game's. **Settles by:** transcribing that block, or by lever #6 below.

---

## ONE LEVER THE REPORT DID NOT LIST

**Mid-function entry at `0x055CD590`** — zero `.text` writes, zero data pokes, no fold involvement: enter the wall *past* the guard block with a hand-built frame. Feasible because the frame is fully visible: prologue stores `rbx`/`rdi`/`r14` at `[rsp+0x10/0x18/0x20]` **before** `push rbp; lea rbp,[rsp-0x60]; sub rsp,0x160`, and the epilogue at `0x055CD77B` restores them via `r11 = rsp+0x160`. Required entry state: `rdi = PS`, `rbx = &FVector Location`, `r14 = component`, `rbp/rsp` per the prologue. It runs the game's own transform math, which lever #2 does not reproduce. Riskier than #1+#2 (hardcodes an interior RVA, no ABI contract) — but it is strictly the higher-fidelity option and belongs on the ranked list, not absent from it.

## NET

The report is substantially sound and its ranking of levers is right. Its defects are: one internally-contradicted headline (R1), one "unstripped" claim its own team had already corrected (Correction 3b), a nearest-preceding-`pdata` attribution presented as fact (R2), a duplicated-corpus file count fused to a per-tree occurrence count (R3), COVERAGE-BLOCKED stated as ABSENT once (R4), and three `[I]`-grade structural identifications printed as `[M]` (R12/`0x35AFC40`, `0x55C7DD0`, `0xF988D0`). None of these moves the answer to the question asked: **the wall is one poke away for the dismount, the named N is `KickPlayersFromPod` never self-driving, and the getter itself is permanently unsatisfiable.**