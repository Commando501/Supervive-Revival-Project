**3 re-derived CONFIRMED · 2 sub-claims REFUTED (neither overturns the headline) · 4 material omissions · 8 items still [I].** All re-derivation done with my own tools (`scratchpad/s132rev/rev.py`, `xref.py`, `logrec.py` — independent PE parser + capstone, no use of `scratchpad/s132/*`). Image `dumps/merged4.dump.exe`, ImageBase `0x7FF6AF000000` confirmed; file-offset == RVA confirmed from the section table.

# RE-DERIVED — the three load-bearing claims

### 1. HEADLINE "the round-game-mode pointer is a pure guard" — **CONFIRMED [M]**
Whole function disassembled `0x055CD510`–`0x055CD7FF` (not just the 25-byte window). Verbatim:
```
055CD572  e8d9159bfb   call 0x00F7EB50
055CD577  4885c0       test rax,rax
055CD57A  0f8432020000 je   0x055CD7B2
055CD580  488bc8       mov  rcx,rax
055CD583  e848a8ffff   call 0x055C7DD0
055CD588  84c0         test al,al
055CD58A  0f8422020000 je   0x055CD7B2
055CD590  f20f104310   movsd xmm0,[rbx+0x10]     <- pointer gone
```
`rax` is consumed by `test` and by `mov rcx,rax`; no store, no second copy. **Machine-checked, not eyeballed:** a full rel32 sweep of `0x00F7EB50` gives **83 sites (80 call / 3 jmp)** in merged4 `.text`, of which **7** lie in `0x55CB000..0x55DD000` — `0x55CCF22, 0x55CD405, 0x55CD4C7, 0x55CD572, 0x55CD842, 0x55D9173, 0x55D9413` — exactly the report's list, and exactly **one** inside the wall. The report's contrast control also holds: `AuthPlayerPreSpawnOnAddToPlane` does `0x055CD847 mov rsi,rax` and `0x055CD8DD mov r9,[rsi]` / `0x055CD8E9 call qword [r9+0xa48]`.
★ **The control the report should have run and didn't, which I ran and it passes:** the success path `0x055CD590`–`0x055CD767` contains **zero** calls to any of the four folds. That is what rules out "the later use of `GM` was itself stripped, leaving an orphan guard over a gutted body".

### 2. §1 the surviving twin / `UWorld+0x250` — **CONFIRMED [M], and I can strengthen it**
`0x055CE140` byte-for-byte as reported: `0x055CE152 mov rax,[rbx+0xC0]` · `0x055CE161 call 0x035AFC40` · `0x055CE166 mov rdi,[rax+0x250]` · `0x055CE172 call 0x055C7DD0` (**the same** IsA callee as the wall) · `0x055CE17E mov [rbx+0xe0],rdi`. Distance from `0x055CD842` = `0x8FE` = 2302 ✓. 0 rel32 callers; exactly 1 qword ref, `.rdata 0x0895A290` ✓.
★ **Stronger than the report's derivation:** `UWorld::AuthorityGameMode = +0x250` is [M] **from the property name itself**, not from `GetGameMode`'s body — UHT record `.rdata 0x0825D920`, name string `0x0807FAE0` = `"AuthorityGameMode"`, **Offset 0x0250**. (The other `AuthorityGameMode` record, `0x0807F560`, has Offset `0x03C0` — that is `AGameStateBase`'s, a decoy the report never had to face.) `GetGameMode` record independently confirmed: `.data 0x09A94EE0` = `{"GetGameMode", thunk 0x03804740, impl 0x037D7BF0}`, and `0x037D7C0E mov rax,[rax+0x250]` vs the sibling `0x037D7D1E mov rax,[rax+0x258]`.

### 3. §5 "`ContainsPlayer` is the wrong receipt" — **CONFIRMED [M]. This is the report's most valuable finding.**
UHT property records, decoded with my own offsets (`+0x30` ArrayDim / `+0x32` Offset), validated by reproducing the report's independent set:
`.rdata 0x08A500B0` **PlayersInside Offset 0x0120** (genflags 0x16 Array) · `0x08A50130` **PlayersAttached 0x0130** (0x16) · `0x08A501B0` **PlayersThatExited 0x0140** (genflags **0x18**, Set) ✓.
The terminal write is `0x055CD738 movsxd rbx,[r14+0x138]` … `0x055CD767 mov [rax+rbx*8],rdi` — i.e. **+0x130**. `ContainsPlayer` (record `.data 0x09C1E768`, thunk `0x05456700`, impl `0x055D0270`) is `mov rax,[rcx+0x120]; movsxd rcx,[rcx+0x128]` — **+0x120 only**. And S131 really did use it: `scratchpad/s131/evidence/RESULT-rideable-s131-live.txt:27,53,62` (`R0c ContainsPlayer(before)` / `R2 ContainsPlayer(after)` / `before=0 … after=0`). ⇒ **a successful call would have read `after=0` and been written up as "no rider attached."**

# REFUTED

### R1 — the §5 receipt-4 justification. **REFUTED [M].** The two bails log to DIFFERENT categories.
```
055CD794 (line 327, hero-null)   cmp byte [0x0A036AC0],2 … lea rcx,[0x0A036AC0]
055CD7B2 (line 299, game mode)   cmp byte [0x0A035E80],2 … lea rcx,[0x0A035E80]
```
Different `FLogCategoryBase` globals, and different **FName indices** at `+0x04`: `0x0001A4A0` vs `0x0001A4E5`, identical in `rideable`/`droppod`/`s129`/`tuthero`. `0x0A035E80` = **LogLokiRideable** [M — its record `0x08B1CF08` format string is verbatim the live line at `Loki-s131-rideable-confirmed.log:3991`]. `0x0A036AC0` = **`LogLokiGameMode`** [M — sibling record `0x08B308E0` `'Client is ready to play.'` verb=4, emitted at `0x056CA0D2` with the same `rcx`, and the live log prints `LogLokiGameMode: Display: Client is ready to play.`]. So "LogLokiRideable fires 2/2 ⇒ **the** category passes Error" is a **missing positive control** on the category actually used.
**The prediction survives on other evidence**, which is what should be recorded: both objects read `Verbosity = 0x05` at `+0x00` in all four live images (≥2 = Error), and `LogLokiGameMode` emits 11 lines in the same log. ⇒ bail-B **will** print — but as
`LogLokiGameMode: Error: AuthPlayerEnterWorldAttachedToRidable: Attempting to attach ourselves to a drop pod whithout a pre-spawned hero.`
**Anyone who greps `LogLokiRideable` for receipt 4, as the report's framing invites, gets a false negative.**

### R2 — §4 "`GetLokiCharacter(PS)` = `Cast<ALokiHeroCharacter>(PS->PawnPrivate)`". **REFUTED [M].**
`0x056BE0D6 mov rbx,[rcx+0x430]` then `0x056BE0E5 call 0x054F8C40` → `0x052F01E0` whose UHT name literal is **`"LokiCharacter"`**, SizeOf `0x1950`. The `ALokiHeroCharacter` test is a **separate** call in the wall: `0x055CD5DA call 0x054F8DC0` → `0x05395720`, name **`"LokiHeroCharacter"`**, SizeOf `0x2060`. Two-stage narrowing (`ALokiCharacter` then `ALokiHeroCharacter`), not one. Net precondition on `PawnPrivate` is unchanged, but the identity was attached to the wrong callee — the same class of error as the thunk-vs-impl session. (`PawnPrivate @ +0x430` is otherwise **CONFIRMED [M]**: UHT record `.rdata 0x081B4F30`, Offset `0x0430`, unique name occurrence.)

# MATERIAL OMISSIONS

- **M1 — the wall impl has THREE rel32 callers, not one.** `0x0545643D` (the exec thunk) **plus `0x059701E0` and `0x0597F2BD`**, both inside the Angelscript StaticJIT body range. Both set `rcx`=component, `rdx`=PlayerState, `r8`=&FVector — a third and fourth independent confirmation of the signature the report took from `binds_members.csv:44932` (line verified verbatim) and the exec thunk.
- **M2 — there are TWO AS call sites and the report cited the wrong one for the value it recommends.** `tools/asdump/out/a/GameMode.DropPhase.LokiDropPod.as.txt:3710` passes `this.CurrPodDestination`; `LokiDropShip.as:163` passes `LandingLocation`. S131 used `CurrPodDestination` ⇒ the DropPod site is the model. Corroborated in machine code: caller `0x059701E0` builds `r8 = [rsp+0x4a8] + 0x478`, i.e. a field at **+0x478** of the pod object — a free, unclaimed offset lead.
- **M3 — the game-mode bail block is not only a log.** After the `UE_LOG` it does `0x055CD7CE lea rdx,[0x08B1CF30]` (the format string) → `0x055CD7DA call 0x00FAC920` → `0x055CD7E4 **call 0x00F7EC20**` (fold `ret 0`) → free. A **second stripped consumer** sits on the bail path.
- **M4 — gate 3 does not gate re-entry.** `HasEverContainedPlayer` (`0x055DCAA0`) tests `PlayersInside` (+0x120) then the Set at `+0x140/0x148/0x174/0x178/0x180/0x188`. It never reads `PlayersAttached`, so repeated trampoline calls append **duplicates** to `+0x130`.

# THE RECIPE — verified correct

Stub assembled and disassembled by machine: **49 bytes, imm64 at offset 39** ✓. First 28 bytes are **byte-identical** to the game's own `0x055CD519`–`0x055CD534` prologue (`48895c241048897c24184c8974242055488d6c24a04881ec60010000`); the three reg loads are byte-identical to `0x055CD538/53E/543`. Frame arithmetic machine-checked: the stub's `[S+0x10]/[S+0x18]/[S+0x20]` are exactly what the epilogue reads at `[r11+0x18]/[+0x20]/[+0x28]` with `r11 = rsp+0x160`, and `mov rsp,r11; pop rbp; ret` returns `rsp` to entry. `rsi`/`xmm6` are saved at `0x055CD5A5`/`0x055CD5AD` — **after** the entry point — and restored at `0x055CD76B` on both reachable exits ✓. `rax` is dead at `0x055CD590`, so `mov rax,imm64` is safe (the report never said so). `.pdata` row `[0x55CD510,0x55CD5A5]` covers the entry point, so a stack walk from it sees the frame the stub established.

Also confirmed exactly: merged4 `.text` **16,683/30,281 pages = 55.09 %**, delta vs merged3 = **2 pages** (`0x5456000`, `0x551B000`) / **7,570 differing bytes** / **0 regressions**; `0x056680F0` and `0x055820E0` zero in **27/27** `.dump.exe` images; `.pdata` holes are precisely `0x5668027–0x5669770` and `0x558208D–0x55861A0`; wall `.pdata` chain = 5 rows `0x55CD510→0x55CD7FA` = **746 B**, `seen_in_dumps 20`; `ALokiRoundGameMode::StaticClass 0x05453580` has exactly **3** rel32 refs (`0x0545392D` call, `0x055C7DE2` call, `0x052D3630` jmp), name literal `0x08A4F332`, SizeOf `0x7D8`, cached slot `0x0A02A8C8` read from **3** rip-rel sites all inside `StaticClass` itself; the `ULokiRideableComponent` record block grades exactly as reported (EMPTY = `AuthAddPlayer`/`AuthPlayerEnterWorldNew`/`AuthRemovePlayer`/`AuthSetCanJump`; `AuthPlayerEnterWorld 0x055CCE70` REAL and storing the null unchecked at `0x055CCF2F`/`0x055CCF37`); `.rdata 0x08955990` `LokiGameMode` Offset `0x00E0` and `0x055CBBD5 mov rcx,[rsi+0xe0]; mov rcx,[rcx+0x780]` unchecked; `0x05630970` and `0x0562B730` neutered, siblings `0x05630980`/`0x0562B750` reading `[World+0x258]`.

# STILL UNGROUNDED — and what settles each

1. **`0x0359E4A0` = BeginPlay, "vtable index 119 in 47 of 170 vtables (modal)"** — **UNVERIFIABLE by me.** A backward scan from `0x0895A290` for a vtable head over-walks (adjacent vtables are contiguous; it lands at index 1024). The report graded it `[I, strong]` and that grade is right. Immaterial: the §3 prediction holds for any lifecycle virtual. *Settles:* read `[comp+0xE0]` live.
2. **`0x056680F0` `LokiTeleportActor` = REAL** — **[I, strong], not [M]** as §4's table implies. Page dark in 27/27; no `.pdata` row. I added the missing quantification: its impl appears **exactly 1×** among **16,308** record triples, against the folds at **371 / 76 / 40 / 15** — so it is not a *shared* fold. It still cannot exclude a singleton stripped stub, because the fold set (4 addresses) has never been shown complete. *Settles:* `dumpimage` after a run that executes it, or one RPM read of 16 bytes.
3. **The `.data`-record instrument's own validity for dark pages** — it grades by "impl ∉ {4 known folds}". *Settles:* census impl-address multiplicity image-wide and treat every impl with multiplicity ≥ ~10 as a candidate fold; that is free and would harden every COVERAGE-BLOCKED re-grade S130/S131 want to do.
4. **Names for `GetLokiGameMode` / `FindLokiGameModeComponent`** (thunks `0x52E4D90`/`0x52E2910`) — bodies verified neutered, name↔impl binding not re-derived. *Settles:* one record-table lookup.
5. **"GetGameMode is `BlueprintCallable|Static|Pure`, no `BlueprintAuthorityOnly`"** — not re-derived. Moot for the trampoline.
6. **"AuthorityGameMode is populated and `IsA<ALokiRoundGameMode>` would pass on the tutorial route"** — the report never states this, yet it is the premise that makes its own `.text`-rel32 fallback work at all. It rests on S124's live `BP_LokiGameMode_Tutorial_C`. **[I].** *Settles:* read `[World+0x250]` and its class name, read-only RPM — and it belongs in the pre-check list.
7. **Pre-check list is incomplete.** Add: `HasEverContainedPlayer(comp, PS) == 0` (gate 3 is the one guard the shim must reproduce and the only one that can silently no-op the call) and `comp+0x128` as the negative-control baseline.
8. **Semantics beyond this function.** "Not the first of N" is a claim about *this* function only; `AuthPlayerEnterWorld`'s unchecked null store at `0x055CCF2F` and the 4 folded `Auth*` members show the surrounding class is partly gutted. Nothing here shows a rider that has been appended to `PlayersAttached` actually rides.