## VERDICT: 2 of 3 re-derived claims CONFIRMED, 1 REFUTED. Plus 3 secondary refutations. The headline (§0) survives; §5 — the report's own "most consequential find" — is on the wrong class.

---

## CLAIM 1 — "the fold ignores its argument; no data poke can fix it" — **CONFIRMED**

Re-derived with my own PE reader + capstone (`scratchpad/rev/img.py`, `xdis.py`), not `fkdis.py`.

- `0x55CD572` bytes `e8 d9 15 9b fb`; machine-computed target `0x7FF6AFF7EB50` − ImageBase `0x7FF6AF000000` = **RVA `0xF7EB50`**.
- `0xF7EB50` = `33 c0 c3` = `xor eax,eax; ret`, **byte-identical in 6/6 images** (merged4/3/2, tuthero, s131-rideable-live, s131-droppod-live). Next byte `0x F7EB53` = `48 83 ec 20` — a different function starts there, so the fold is exactly 3 bytes.
- Argument is a `UWorld*`: `[r14+0xC0]`, else `call 0x35AFC40`, which I disassembled — it is the generic `UObject::GetWorld` (walks `[this+0xB8]`, falls back to `[this+0x28]` validated by `0x302B930`). `mov rcx,rax` at `0x55CD56F`. **[M]**
- Guard chain, extent, and both bails re-read byte-for-byte. Extent = **5 chained `pdata_union.csv` rows** `0x55CD510→5A5→77B→794→7B2→7FA` = **`0x2EA` = 746 bytes** (149+470+25+30+72). The 149-byte single-row figure is correctly flagged as not-the-size.
- Log record `.rdata 0x8B1CF08` decodes to `{msg 0x8B1CF30, file …\DropPhase\LokiRideableComponent.cpp, line 299, verbosity 2}`. Siblings: `0x8B1CFF0` line **327** ("whithout" typo present), `0x8B1CE30` line **257**. `"failed to get the round game mode"` occurs **exactly 2× in `.rdata`** (`0x8B1CEBE`, `0x8B1CFAC`), 0× ASCII. **All CONFIRMED.**

⚠ One nuance the report blurred: the message that actually printed live is emitted at **`0x55CD7C9`** (`call 0x106B650`, gated on `cmp byte [0xA035E80],2`). The `call 0xF7EC20` at `0x55CD7E4` is a *second, separate* stripped call taking the FString. The report's transcript lists both but never says which one fired.

---

## CLAIM 2 — "§5: `0x55CE140` is a `ULokiRideableComponent` vtable override caching the RoundGameMode at **Component+0xE0**; free readback = `[RideableComponent+0xE0]`" — **REFUTED**

The *code* at `0x55CE140` is exactly as transcribed (I re-read all 102 bytes: `[rbx+0xC0]` / `0x35AFC40` → `mov rdi,[rax+0x250]` → `call 0x55C7DD0` → `mov [rbx+0xE0],rdi`, else `mov qword [rbx+0xE0],0`). **The class attribution is wrong.**

Machine-derived chain, every step:

| step | evidence |
|---|---|
| pointers to `0x55CE140` | uncapped numpy scan: **1**, at `.rdata 0x895A290` (0 in `.data`, 0 in `.text`); rel32 callers **0** |
| enclosing vtable | nearest `lea`'d vtable base below = **`0x8959ED8`**; next `lea`'d base above = `0x895A398` |
| slot index | `(0x895A290 − 0x8959ED8)/8` = `0x3B8/8` = **119** (computed, not by hand) |
| who stores `0x8959ED8` | ctor `0x55CA060` (`mov [rbx],rax`) and deleting-dtor `0x534E870`, whose `operator delete` size is **`0x168`** |
| ctor→class | `0x55CA060` reached from the single `0F 85` rel32 at `0x534DDB6` (thunk head `0x534DDB0`); that thunk is `lea`'d exactly once, at `.text 0x534CDB7`, inside the `GetPrivateStaticClassBody` call at `0x534CD60..0x534CDED` |
| **the name** | that call's `rdx` = `.rdata 0x89558C2` = **`"LokiGameModeDropPlaneComponent"`**, `InSize = 0x168` — matches the dtor's delete size |

And the control, by the identical method:

| | `ULokiRideableComponent` |
|---|---|
| registration | `0x5452740`, Name `.rdata 0x8A4FE6A` = `"LokiRideableComponent"`, **`InSize = 0x1E0`** |
| `InClassConstructor` | `0x5452930` → `0F 85` → real ctor **`0x55CA480`** |
| vtable it stores | **`0x8A52020`** (secondary `0x8A524F8`) — **not** `0x8959ED8` |
| **its own slot 119** | `[0x8A52020+0x3B8]` = **`0x359E4A0`** = the engine base impl (which `0x55CE140` calls first at `0x55CE14D` as `Super::`) |

★ **Two-sided control:** slots 115–124 of the two vtables are **identical in 9 of 10 positions** (`0xF7EB60, 0x35B8160, 0x35B1CE0, 0x35B7F30, ·, 0x35AA140, 0x35C4A80, 0x35C4370, 0x20AD820, 0x35BC510`) and differ **only at slot 119**. That is exactly the shape of "DropPlane overrides it, Rideable does not."

**Correct value:** `0x55CE140` = `ULokiGameModeDropPlaneComponent` vtable **slot 119** override. The cached, IsA-validated `ALokiRoundGameMode*` lives at **`DropPlaneComponent + 0xE0`**.

⚠⚠ **This would have produced a false negative if flown.** `ULokiRideableComponent`'s own ctor **also zeroes `+0xE0`** (`0x55CA4CD mov [rbx+0xE0], rax`, rax=0) — the two classes share base layout — so `[RideableComponent+0xE0]` would read **0**, and the report's own decision rule says *"Null ⇒ `World->AuthorityGameMode` is not an `ALokiRoundGameMode` on this route and the problem is upstream."* A guaranteed-zero read, recorded as a property of the game. The pointer to use is **`Comp_GameMode_DropPlane_Tutorial`** — which CLAUDE.md already has a live address for from S124 (`0x1B3771413C0`, ASLR-dependent, re-derive).

---

## CLAIM 3 — "`GetLokiGameMode` is gutted while its same-TU sibling `GetLokiGameState` is intact" — **CONFIRMED**; its supporting identification is **REFUTED**

**Confirmed [M]**, from the record table (my own 16,277-record scanner, independently reproducing the report's count exactly) plus disassembly:
- `GetLokiGameMode` thunk `0x52E4D90` → impl **`0x5630970`** = `sub rsp,0x28; call 0x56F0330; xor eax,eax; add rsp,0x28; ret` — calls the world helper, **discards it, returns null unconditionally**.
- `GetLokiGameState` thunk `0x52E4E20` → impl **`0x5630980`** (adjacent) — same `call 0x56F0330`, then `mov rbx,[rax+0x258]`, `IsA` via `0x55740F0`, returns. **Intact.**
- `UGameplayStatics::GetGameMode` thunk `0x3804740` → impl **`0x37D7BF0`** — independent function, `GEngine@0x9FB9A70` → `0x3EDBE70` → `mov rax,[rax+0x250]`. ⇒ **`UWorld+0x250 = AuthorityGameMode`**, `+0x258 = GameState`. **CONFIRMED.**

**REFUTED sub-claim — this is the thunk-vs-impl failure class the prompt names.** The report wrote: *"`0x56F0330` is identified as **`LokiGetWorldWithError`** [M] from its own log record at `.rdata 0x8B33C98` → `LokiUtilities.cpp:443` … `lea` site `.text 0x56F0409`."*

- `0x56F0330` is a **4-instruction leaf** ending at `0x56F0341`: `test rcx,rcx; jne; xor eax,eax; ret / mov rax,[rcx]; jmp [rax+0x188]` — a null-guarded virtual dispatch. **It logs nothing.**
- `pdata_union.csv` has **no row starting at `0x56F0330`** (leaf, no unwind). The next row is `0x56F0350`. `0x56F0409` lies in the row **`0x56F03DF – 0x56F048A`** — a *different function*, ~0xAF bytes past the end of the leaf.
- The record `0x8B33C98` does decode to `{"LokiGetWorldWithError %hs failed to get the world from a valid context object (%s)", …\LokiUtilities.cpp, line 443, verbosity 2}` — but it belongs to **`0x56F03DF`**, not `0x56F0330`.

**Correct value:** `0x56F0330` is an anonymous null-guarded `UObject::GetWorld()` dispatcher; the `LokiGetWorldWithError` body is at **`0x56F03DF`**. The structural argument (gutted vs intact sibling) is untouched — it rests on the bytes at `0x5630970`/`0x5630980`, which I read directly.

---

## FURTHER REFUTATIONS

**(a) "[M] Nothing reads `[this+0xE0]` … the cache is written and never consumed: its consumers are precisely the stripped code." — REFUTED.**
`ULokiGameModeDropPlaneComponent::AddPlayerToDropPlane` (record: thunk `0x5350630` → impl **`0x55CBB60`**, mult 1, extent `0x55CBB60–0x55CBC21`) does at **`0x55CBBD5`**: `mov rcx,[rsi+0xE0]` then `mov rcx,[rcx+0x780]` — **reads the cache and dereferences it with no null guard** (`rsi = rcx = this`, set at `0x55CBB67`). Corroborating the class ID: `0x55CBB6D` reads `[this+0xE8]`, then `[that+0x3C8]` → passes it to `HasEverContainedPlayer` — and `binds_members.csv` declares `ALokiDropPlane::RideableComponent` (property 3). ⇒ `DropPlaneComp+0xE8 = ALokiDropPlane*` [I, strong].
★ The report's scan window was `0x55CC800–0x55E0000`, which **excludes `0x55CBBD5`**. The window was drawn around "the rideable TU" — i.e. the blind spot was manufactured by the wrong class attribution in Claim 2. Instrument-artifact pattern, and the negative had no positive control.
★ Operational consequence: **any shim that calls `AddPlayerToDropPlane` on a component whose `+0xE0` is null null-derefs.** That is a new hazard the report did not surface.

**(b) "`AuthPlayerEnterWorld` … calls the fold 3x" — REFUTED. It is 1.** Extent `0x55CCE70–0x55CD07D` (3 chained rows, 525 B). Uncapped rel32 `E8` counts inside it: `0xF7EB50` → **1** (`0x55CCF22`) · `0xF7EC20` → 0 · `0xF7EB60` → 0 · `0xB9E1F0` → 0. The "no null-bail" half is confirmed: the result is stored to `[rsp+0x50]`/`rbx` and used at `0x55CCF3A` with no test.

**(c) "The array is `PlayersAttached` [M]" — grade inflated to [M]; it is [I].** The *offsets* are [M] (`Data +0x130`, `Num +0x138`, `Max +0x13C`, grow `0xF988D0`, store `mov [rax+rbx*8], rdi`; and `ContainsPlayer 0x55D0270` / `HasEverContainedPlayer 0x55DCAA0` both scan `+0x120`/`+0x128`). The **name** binding rests on `binds_members.csv` declaration order (7 `PlayersInside`, 8 `PlayersAttached`, 9 `PlayersThatExited`) plus 0x10-stride arithmetic. No offset oracle was consulted. Report as [I].

**(d) Two counts are criterion-dependent, not wrong, but the predicate was never stated.** "6 of the 9 immediately null-tested" — under a strict *next-3-bytes* `48 85 c0` test I get **3**; the other three (`0x55CD842`, `0x58CD759`, `0x5907019`) interpose a `mov rXX,rax`, which I verified by disassembly, so the report's 6 is defensible. "153 are `GetWorld(); mov rcx,rax; call Y`" — I count **171** sites followed by `mov rcx,rax` out of **946** `call 0x35AFC40` (946 CONFIRMED exactly); 153 is presumably 171 minus non-`call` continuations. State the predicate with the number.

**(e) "merged4 was not required for this lane" — over-broad.** True for the function body (all 15 load-bearing addresses are decrypted in 6/6 images, verified page-by-page). But `.text` page **`0x5456000`** — which holds the exec thunks the report printed (`0x5456380`, `0x5456C80`) — is **ZERO in tuthero** and non-zero in merged4/rideable. No claim breaks, because those RVAs came from `.data`, not from disassembling the page. Worth saying so, since it is exactly why fk22 graded `GetLandingTeleportLocation` COVERAGE-BLOCKED and why the record-table method beats it here.

---

## CONFIRMED EXACTLY (independent re-derivation, my own tooling)

| claim | my value |
|---|---|
| `.data` records scanned, merged4 | **16,277 records** — identical to report |
| records with `impl == 0xF7EB50` | **40 records**, list **set-identical** incl. `SpawnPlayer` (thunk `0x534C070`) |
| stored qword pointers to `0xF7EB50` | **27,217** = 23,150 `.rdata` + 4,067 `.data` (uncapped) |
| direct `E8` call sites to `0xF7EB50` | merged4 **80**, merged3 80, merged2 80, tuthero **77**, rideable 72, droppod 72 |
| GetWorld-fed fold sites | **9**, set-identical to the report's list |
| …followed by `IsA<ALokiRoundGameMode>` | **2**: `0x55CD572`, `0x55CD842` (`AuthPlayerPreSpawnOnAddToPlane`, impl `0x55CD800`, cpp:257) |
| callers of `0x55C7DD0` | **exactly 3**: `0x55CD583`, `0x55CD856`, `0x55CE175` |
| `0x55C7DD0` identity | `IsA<ALokiRoundGameMode>` — getter `0x5453580` builds from `W"LokiRoundGameMode"` `.rdata 0x8A4F332` + `W"/Script/Loki"` `0x88152C0`, cache `.data 0xA02A8C8`; body is `IsChildOfUsingStructArray` on `[obj+0x18]` |
| `0x54F8DC0` identity | `IsA<ALokiHeroCharacter>` — getter `0x5395720`, `W"LokiHeroCharacter"` `.rdata 0x899A832` |
| `0x55C1B20` identity | own string `.rdata 0x8B19060` = `"ALokiCharacter::SpawnAndMoveLokiCharacter_MoveStep Null Character was given to move"` |
| `0x8B1D4C8` | **7500.0** (IEEE754 double) |
| ZeroVector `.data 0x99C87B8` | 24 zero bytes |
| `LokiTeleportActor 0x56680F0` | **all-zero page in 6/6 images** ⇒ COVERAGE-BLOCKED, correctly labelled, correctly excluded from "REAL" |
| `GetLandingTeleportLocation` | impl **`0x55D89F0`**, mult 1, page decrypted 6/6 ⇒ REAL. fk22's COVERAGE-BLOCKED grade came from the *thunk* page `0x5456000`; the correction is sound |
| family EMPTY/REAL table | all 16 entries reproduced. ⚠ report never printed `0xF7EC20`'s multiplicity: it is **371 records**. Thunk folds: `0x2C2CE30` **23-way**, `0x5254180` **92 records** (CLAUDE.md says 91 — re-derive) |
| merged4 `.text` coverage | **16,683 / 30,281 pages = 55.09 %** (merged3 16,681; merged2 16,638 = 54.95 %; tuthero 16,112 = 53.21 %) |
| signature | `binds_members.csv`: `void AuthPlayerEnterWorldAttachedToRidable(ALokiPlayerState PlayerState, const FVector& SpawnLocation)` |

---

## STILL UNGROUNDED, AND WHAT WOULD SETTLE EACH

1. **[I] The folded callee's name** ("a Loki free/static game-mode accessor whose body is `return nullptr`"). Correctly graded [I] and **unsettleable by construction** — ICF erased the body, no name record points at `0xF7EB50` for a game-mode accessor. Do not upgrade. Behaviour is [M] and that is all that is needed.
2. **[I] `+0x130` = `PlayersAttached`.** Settle with the UHT `FPropertyParams` oracle in `dumps/tutorial-hero/…dump.exe` (the same instrument S130 used for `AActor+0x6C`), not declaration order.
3. **[I] `DropPlaneComp+0xE8` = `ALokiDropPlane*`** (mine). Same oracle, or one live RPM read of the object's class name.
4. **UNRESOLVED: what virtual slot 119 *is*** on `UActorComponent` (base impl `0x359E4A0`). This governs **when** `+0xE0` is populated and therefore whether the readback is even possible before the drop phase. Settle offline: grade `0x359E4A0` against `UActorComponent`'s known lifecycle overrides, or find the engine call site.
5. **UNTESTED, and now the single cheapest decisive measurement:** read **`[Comp_GameMode_DropPlane_Tutorial + 0xE0]`** on a staged tutorial world — one read-only RPM, no injection. Non-null ⇒ `World->AuthorityGameMode` **is** an `ALokiRoundGameMode` on the client route, and the value the fold refuses to return is obtainable as a plain two-deref data read (`World+0x250`), not a poke. Null ⇒ the problem is upstream of the fold entirely. **Add a positive control:** read `[+0xE8]` in the same pass — if it is also null the component simply never ran slot 119, and `+0xE0`'s null is uninterpretable rather than negative.
6. **`LokiTeleportActor 0x56680F0` body** — genuinely unread in 6/6 images. Blocks report-option 1 (reproduce the success path from a shim). Free fix: `dumpimage` from a state that has run it, or a live page read.

---

## BOTTOM LINE ON THE ACTUAL QUESTION

The report's answer — **one wall, but not one poke** — survives verification, and the supporting §6 table is sound (every success-path callee REAL and decrypted except `LokiTeleportActor`, which is correctly marked COVERAGE-BLOCKED rather than absent; the pod→hero handoff `AuthBeginGlideDiveFromDropPod` is still `impl 0xF7EC20`, so FK-22's ceiling is unmoved).

What changes is the **route past it**. §5's "other route" is real, unstripped, and reads `World->AuthorityGameMode` with the same `IsA` — but it lives on the **drop-plane** component, and that is strictly better news than the report claimed: the same class's `AddPlayerToDropPlane` (REAL) *consumes* `+0xE0` unguarded, so the cache is a live, load-bearing field on the exact object the drop route already needs, not dead storage. The proposed readback must be re-aimed, and any shim driving `AddPlayerToDropPlane` must populate `+0xE0` first or it faults.