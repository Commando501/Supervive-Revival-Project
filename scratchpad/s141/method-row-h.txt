| **★★★★★ S141-h** | **`python -c "..."` inside the Bash tool COMMAND-SUBSTITUTES every backtick in the payload** — and this repo's prose is dense with `` `code spans` `` | I used it to patch two settled docs. Bash silently executed each backtick span as a shell command and substituted the (empty) output, so `` `Velocity.Z` is NOT zeroed `` was written to disk as **" is NOT zeroed"** | **the script printed its success message both times**, and the corruption was invisible in the tool result — visible only on re-reading the file. It hit `docs/s140-t2-armj-THE-BOT-WALKS.md` and `CLAUDE.md`, i.e. exactly the two documents a successor reads first | ⇒ ★★ **never pass repo prose through a double-quoted bash string.** Write the replacement text with the Write tool to a file, then have python read it (`open(...).read()`); or use the Edit tool. ⇒ ★★ **and ALWAYS re-read the patched region after a scripted doc edit** — a "patched" message is not evidence the bytes are right. Recovered by `git checkout` for CLAUDE.md and by Edit for the other |
