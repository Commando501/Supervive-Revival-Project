| **★★★★★ S141-a** | **a byte-pattern displacement scanner with two off-by-ones and a premature `break`** — back-scan started at 3 instead of 1, extent test used `<=` instead of `<`, and it stopped at the first alignment that decoded | it reported a clean-looking **"2 writes, 0 reads"** for `CMC+0x1678` | that would have been recorded as a finding about the game. **It was caught ONLY because the scanner was given two ALREADY-KNOWN sites as positive controls IN THE SAME RUN, and one came back MISSED** | ⇒ ★★ **a displacement scanner must be controlled on sites you already know, in the same run**; and when a byte pattern has several valid instruction alignments, **collect them ALL and adjudicate afterwards — never `break` on the first** |
| **★★★★ S141-b** | **a marker line that NAMES A VALUE IT DOES NOT WRITE** — `[SNP] BOT sentinel Velocity = (2^-10, 0, 0)` was a hardcoded string while the arm wrote whatever `kShSentinel` held | under S141's ARM K it announced `(2^-10,0,0)` while writing `(0,0,-600)` | the RAW/dec dump two lines later carried the truth, which is the only reason the flight was interpretable at all | ⇒ ★ **read the RAW dump, never the announcement.** Fixed to print the actual vector. **A knob-driven value must never be described by a literal** |
| **★★★★★ S141-c** | **my own [I, strong] inference, refuted by my own follow-up BEFORE publication** — *"`+0x12B0` has two writers, S139 measured 1.0x not 2.0x, therefore the substep re-entry never happens and `PhysFalling` never reaches its tail"* | **[M] NEITHER `PhysFalling` ever calls `StartNewPhysics`** (vtable disp `0x720` absent from both call sets, no direct call to `0x3600990`/`0x055C2430`, no tail jump outside either body) | so the substep writer is unreachable there and a 1.0x advance is expected **either way** — the measurement did not discriminate anything | ⇒ ★★ **before publishing an inference from "only one of two writers fired", check that the other writer is REACHABLE at all.** And `+0x12B0` can never be a `PhysFalling` receipt |
| **★★★★★ S141-d** | **free reads added for the CURRENT hypothesis, not the LEADING one** | S141's arm gained `GravityScale`, `GravityDirection`, `MovementMode`, `byte +0x1001` and `+0x1678` — and **not `AnalogInputModifier`**, which is the single field the surviving candidate mechanism turns on | with one injection per sitting (FK-32), the missing read costs a whole flight | ⇒ ★★ **when you add free reads, add the ones the hypothesis you will believe AFTER the flight needs** — write the candidate list first, then instrument it |
| **★★★ S141-e** | **a control designed out of the experiment** | ARM K1 treated the player, which was the point — and thereby removed the within-run specificity control ARM G was built around | recorded in the source and the pre-registration and replaced by a **different-axis** design (bot Z, player Y), which held: zero cross-contamination | ⇒ ★ **a control you spend must be spent EXPLICITLY, with its replacement named in the same edit** — otherwise the next reader sees an arm that looks like it still has one |
| **★★★★ S141-f** | **a candidate receipt with SEVEN writers, nearly used as a one-writer receipt** | `ULokiCMC+0x1678` looked like a clean "`PhysFalling` ran" latch (`-1.0f` written at `0x055B9063`) | an image-wide scan found **seven** writers in the Loki CMC band, three of them writing `-1.0f` | caught before the arm was built; the field is now printed for information only | ⇒ ★ **before adopting any field as a receipt, census its writers** — one known writer is not the same as one writer |
| **★★★★★ S141-g** | **a whole adversarial-verification layer lost to infrastructure, in a session whose conclusions depended on it** | a 13-agent workflow returned 5 of 12: **every verifier and the adjudicator** died to API `529 Overloaded` / server errors, and two focused 4-agent retries returned 0 of 4 | the five lane analyses that landed were left **un-refuted**, and only converged where they happened to overlap (4-ways on two claims) | ⇒ ★★ **partial recovery is possible and worth doing: the dead verifier's own SCRIPTS were still on disk**, and re-running them was a genuine second instrument (it confirmed three claims and produced a new ordering fact). ⇒ ★★ **and mark every single-derivation claim as `[I]` in the write-up rather than letting the missing layer pass silently** |
