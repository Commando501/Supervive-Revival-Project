| **★★★★ S141-i** | **a record-table reader run WITHOUT its positive control, on a table whose layout I had assumed** | scanning `.data`+`.rdata` for a name pointer and reading `+8`/`+0x10` as `{thunk, impl}` | it printed a full nine-row table in which **several `+0x10` values decoded as ASCII** (`"Clie"`, `"AddT"`, `"K2_O"`, `"SetW"`) — they were the NEXT record's name pointer, because `.rdata` rows matching a name pointer belong to a **different table** | I had written a `CTRL` dict into the script and **never used it**. Validating against the repo's own recorded triple (`.data 0x09BC9AD0` = `{"GetRecentVelocity", 0x0530C7E0, 0x0530AC10}`) reproduced both and confirmed the layout — after which the `.data` rows were trustworthy and the `.rdata` rows were discardable | ⇒ ★★ **a control you write and do not RUN is not a control.** ⇒ ★ and **a name-pointer hit does not imply a record** — confirm the containing table before reading any field off it |
