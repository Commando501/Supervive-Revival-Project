| **★★★★★ S141-j** | **resolving a vtable displacement on the WRONG hierarchy's vtable** — I took the disps out of `ACharacter` and `UPrimitiveComponent` dispatch stubs and looked them up on the two **CMC** vtables | it produced a nine-row table of plausible RVAs, all grading "REAL", for `Jump`, `StopJumping`, `AddImpulse`, `AddRadialForce` … | **the giveaway was in my own output and I did not read it**: my "StopJumping" body was itself `mov rax,[rcx]; jmp [rax+0xa70]` — **a resolved impl that is another dispatch stub cannot be terminal.** Re-resolved on the CHARACTER vtables, `Jump` became `0x3536610` = `or dword [rcx+0x580],4 / mov dword [rcx+0x584],0 / ret`, unmistakably stock `ACharacter::Jump()` | ⇒ ★★ **the owner class decides the vtable; a displacement is meaningless without it.** ⇒ ★★ **and add a hard check: if a resolved target's first two instructions are `mov reg,[rcx]` + indirect `jmp`, you have resolved a STUB, not a body — re-resolve, do not report.** ⚠ This is the trap CLAUDE.md already records ("never sample a byte offset across unrelated vtables") and that **I had put in my own seed prompt for the lanes** |
| **★★★★★ S141-k** | **naming a function from the control flow I EXPECTED, rather than from its own bytes** — three times in one sitting, hunting `DoJump` | (1) `0x35395E0` called `ACharacter::CheckJumpInput` — it takes four args and is unidentified; (2) CMC disp `0x608` called `DoJump` — it is a two-term boolean returning `MovementMode==3 && UpdatedComponent!=null`; (3) CMC disp `0x728` called `DoJump` — it is a **forwarder to `0x608`** | each name was "confirmed" by the fact that the call sat where I expected `DoJump` to sit. The decisive negative — **`0x3520930`'s entire CFG contains ZERO writes to `+0xE8/+0xF0/+0xF8`** — was available at attempt one and I did not run it | ⇒ ★★★ **structural position is not identity.** For a function defined by what it WRITES, **start from the write and work back to the callers** — never from the caller and forward to the name. ⇒ ★ and when the same identification fails twice, the third attempt is not the fix: **stop and record NOT ESTABLISHED** (which is what finally happened) |
