**AGGREGATE:** 30 STRIPPED stubs on WALL E's critical path (out of 49 keyword hits; excludes AuthGrantGoldToTeam, TeleportEnemy, AuthClearLockedOutTeams, TryShowPrompt, IsPlatformSteamEnabled, stash-gem, item-timer, cosmetic-damage, respawn-beacon and pure stat-getter noise).

**TOP-10 HIGHEST-IMPACT**

1. **ALokiGameState::SetPlayerTeam  thunk=0x538AA70** — blocks: assigning a spawned bot to an opposing team so it registers as hostile. **in_fk1_register:** no (documented as blocker in CLAUDE.md but not in the 5-entry register). **icf_share:** unshared. **bypass_status:** GENUINELY_BLOCKING (fold `xor al;al;ret` = LokiIsServer FALSE).
2. **ALokiPlayerState::ServerSetHeroClass  thunk=0x5438720** — blocks: setting HeroClass on the bot's PlayerState during SpawnBot's post-controller branch. **in_fk1_register:** no. **icf_share:** unshared. **bypass_status:** GENUINELY_BLOCKING (SpawnBot bypass path currently pokes PlayerState directly).
3. **ALokiGameState::SetNumTeams  thunk=0x52FD8F0** — blocks: growing `TeamStates` so `GetTeamState` can return non-null (CLAUDE.md: "TeamStates can NEVER be non-empty on a client"). **in_fk1_register:** no. **icf_share:** other (0x52FD8F0 = void thunk shared with AddKillStat/AuthSetStashedTeamGems/AddAssistStat/AddSpikeKillsStat). **bypass_status:** CANDIDATE_DATA_POKE (populate TeamStates TArray directly, S131-style).
4. **ALokiMinionCharacter::AuthUpdateNearbyVisibleEnemies  thunk=0x5254180** — blocks: periodic enemy-perception refresh that feeds the minion's target selector. **in_fk1_register:** no. **icf_share:** 92-way (universal execFoo tail-jmp thunk). **bypass_status:** GENUINELY_BLOCKING.
5. **ALokiMinionCharacter::AuthUpdateEnemyList  thunk=0x5254180** — blocks: maintaining the minion's active enemy set. **in_fk1_register:** no. **icf_share:** 92-way. **bypass_status:** GENUINELY_BLOCKING.
6. **ALokiMinionCharacter::AuthAnyVisibleEnemyHeroCharactersInRange  thunk=0x5403AB0** — blocks: the hostility predicate that returns "is anyone shootable near me". **in_fk1_register:** no. **icf_share:** unshared. **bypass_status:** GENUINELY_BLOCKING (LokiIsServer FALSE fold).
7. **ALokiMinionCharacter::UpdateTargetEnemy  thunk=0x54071C0** — blocks: picking a specific enemy actor to attack. **in_fk1_register:** no. **icf_share:** unshared. **bypass_status:** GENUINELY_BLOCKING (nullptr fold).
8. **ALokiMinionCharacter::AddToEnemyList / RemoveFromEnemyList  thunk=0x2C2CE30** — blocks: mutating the enemy list at all (same thunk as FK-1 SetDropLeader). **in_fk1_register:** no (thunk is FK-1's #3 but these UFunctions are not). **icf_share:** 23-way. **bypass_status:** CANDIDATE_DATA_POKE (enemy list is a TArray field).
9. **ALokiCharacter::AuthCheatSetHealth  thunk=0x52FD620** — blocks: direct health mutation for self-damage calibration (S148/S153 unblock target). **in_fk1_register:** YES (5th entry, S152 Move 4). **icf_share:** 9-way. **bypass_status:** PROVEN_ALT_CALL_PATH (S153 thunkExact fix in main worktree, unflown).
10. **ALokiProjectile::AuthAddDamageMultiplier  thunk=0x52FD620** — blocks: server-side damage-scaling on outgoing projectiles. **in_fk1_register:** no. **icf_share:** 9-way (same body as AuthCheatSetHealth). **bypass_status:** GENUINELY_BLOCKING (projectiles already fire; multiplier is void).

Runners-up on critical path: **ALokiGameMode::EliminateTeam (0x5349CB0, YES=new)**, **ALokiTeamState::SetIsEliminated (0x5296F30)**, **ALokiTower::TickAttack (0x5254180)**, **AuthDamageDealtBySomeoneInTowerRange (0x5489D20)**, **CanTeamSeeMe (0x5436670)**, **CanTeamSeeMyTeam (0x52FD2D0)**, plus 3 ULokiTuningLibrary damage-numeric getters returning void (mis-typed as void_ret, breaks Loki-tuned damage scalars).

**NEW-INFO ASSESSMENT**

- **FK-1 register hits (5 total, 3 here):** AuthCheatSetHealth, AuthSetSpawnTeamLeader, SetDropLeader. Independent confirmation from a third instrument.
- **S152 batch hunt (95 hits):** covers the entire ALokiMinionCharacter enemy-list family (AuthUpdateEnemyList, AuthUpdateNearbyVisibleEnemies, AuthAnyVisibleEnemyHeroCharactersInRange), plus SetPlayerTeam, ServerSetHeroClass, SetNumTeams — so ~8 of the top-10 reconfirm S152.
- **Genuinely new in S153 vs prior sweeps:** flagged `new_vs_v1=YES` in the topic filter = **1 hit** (`EliminateTeam` at 0x5349CB0). The remaining ~21 topic hits (ProjectileAuthAddDamageMultiplier, TowerTickAttack, TowerAuthDamageDealtBySomeoneInTowerRange, TowerRemoveEnemyFromList, MinionSetMajorReward, MinionHandleAbilityActivated, TeamStateSetIsEliminated, HeroRecallTargetStateChanged, CanTeamSeeMe/MyTeam, tuning damage getters, kill/damage stat getters, AddToEnemyList, RemoveFromEnemyList, UpdateTargetEnemy, ResetHealth, AuthDealCosmeticDamage) reconfirm prior sweeps or are stat-cosmetic.

**BLOCKS-WHAT SUMMARY**

The 30-stub critical mass proves WALL E is a coherent server-authority strip, not scattered defects: bot **team identity** (SetPlayerTeam/SetNumTeams/ServerSetHeroClass), **hostility perception** (5 MinionCharacter enemy-list functions + tower + CanTeamSee), **damage scaling** (ProjectileAuthAddDamageMultiplier, tuning damage getters), and **win conditions** (EliminateTeam/SetIsEliminated) are all void folds. Achievable today via direct data pokes: TeamStates population, enemy-list TArray mutation, per-projectile damage constants, and the S153-fixed AuthCheatSetHealth path — a fully offline versus-AI bot fight remains buildable but requires substituting **every** authority path with poked state rather than adding one more shim.