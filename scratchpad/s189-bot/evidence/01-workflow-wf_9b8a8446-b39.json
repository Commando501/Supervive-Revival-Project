{
 "understand": {
  "spawnloc": {
   "findings": [
    {
     "claim": "SpawnAIFromClass with bNoCollisionFail=1 sets SpawnCollisionHandlingOverride = AlwaysSpawn(1) = spawn at the EXACT requested location with NO position adjustment. bNoCollisionFail=0 would give AdjustIfPossibleButDontSpawnIfColliding(3).",
     "grade": "M",
     "evidence": "Disasm of UAIBlueprintHelperLibrary::SpawnAIFromClass (RVA 0x4631C50, dumps/merged14.dump.exe): 0x4631D3F `movzx eax,[rsp+0x148]`(=bNoCollisionFail) -> 0x4631D4A `xor al,1` -> 0x4631D4C `add al,al` -> 0x4631D53 `inc al` -> 0x4631D55 `mov [rsp+0x70],al`(FActorSpawnParameters.SpawnCollisionHandlingOverride). bNoCollisionFail=1 => (!1)*2+1 = 1 = AlwaysSpawn. Enum ordering (Undefined0/AlwaysSpawn1/AdjustAlways2/AdjustDontSpawn3/DontSpawn4) matches shim comments tutorial_launch.cpp:4211,13649,15764. Shim passes bNoCollisionFail=1 (tutorial_launch.cpp:15764).",
     "implication": "A floor spawn is placed at EXACTLY the requested (x,y,z) with no snap; overlap with the floor does not fail the spawn and does not nudge it. Placement precision is fully under our control, so the requested Z is the actual spawn Z."
    },
    {
     "claim": "SpawnAIFromClass makes a NON-DEFERRED UWorld::SpawnActor (0x39C5280) then, if pawn->Controller(+0x400)==null, calls SpawnDefaultController (vtable [rax+0x8c0], slot 280). A non-deferred spawn runs the full construction chain (FinishSpawningActor -> PostInitializeComponents -> ACharacter::PostInitializeComponents -> CMC->SetDefaultMovementMode) AT SPAWN TIME, at the spawn transform.",
     "grade": "M",
     "evidence": "Disasm 0x4631DC5 `call 0x39c5280`(SpawnActor, not BeginDeferredActorSpawn); 0x4631DD2 `cmp [rax+0x400],rbx`(Controller==0); 0x4631DE1 `call [rax+0x8c0]`(SpawnDefaultController). BT param (r15) null-checked 0x4631DEA -> RunBehaviorTree block skipped, matching shim BT=null. Confirms tutorial_launch.cpp:15641-15644.",
     "implication": "MovementMode is decided at spawn, at the spawn location, by SetDefaultMovementMode -> FindFloor -- so the Z we pass IS the Z the floor-find sees. This is why the current air spawn (Z=13240) already reads a settled mode rather than MOVE_None."
    },
    {
     "claim": "SetDefaultMovementMode (RVA 0x35F9B30, CMC vtable slot 288, NOT Loki-overridden -- identical RVA on Loki and engine CMC vtables) is stock UE: it SetMovementMode(DefaultLandMovementMode @CMC+0x540), then reverts to MOVE_Falling(3) IFF (MovementMode==MOVE_Walking(1) AND CharacterOwner->BasedMovement.MovementBase(+0x470)==NULL), restoring the saved Velocity.Z.",
     "grade": "M",
     "evidence": "Disasm 0x35F9B78 `movzx eax,[rbx+0x540]`(DefaultLandMovementMode); 0x35F9B9F `movsd xmm6,[rbx+0xf8]`(SavedVelZ); 0x35F9BAB `call [rax+0x670]`(SetMovementMode); 0x35F9BB1 `cmp [rbx+0x231],1`; 0x35F9BC6 `cmp [rax+0x470],0`(MovementBase); 0x35F9BE3 `movsd [rbx+0xf8],xmm0`(restore VelZ); 0x35F9BEB `call [rax+0x670]`(SetMovementMode(3)). Slot resolve: LOKI vt 0x088F8570 and ENGINE vt 0x07FBED58 both slot288 -> 0x35F9B30.",
     "implication": "Whether a freshly spawned character ends up Walking(1) or Falling(3) turns entirely on whether FindFloor set a MovementBase. On the floor it will; in the air it won't. This is the decision point for the whole lane."
    },
    {
     "claim": "DefaultLandMovementMode == MOVE_Walking(1) on the BP_HERO_Ronin bot on this build. Deduced airtight: the revert-to-Falling(3) branch in SetDefaultMovementMode fires ONLY after SetMovementMode set MovementMode to 1 (`cmp [rbx+0x231],1; jne skip` at 0x35F9BB1). The current air-spawned bot is MEASURED at MovementMode=3, so DefaultLandMovementMode must be 1; if it were MOVE_Flying(5) the air bot would read 5.",
     "grade": "M",
     "evidence": "Air bot MovementMode +0x231 = 3 (MOVE_Falling) MEASURED: docs/s139-flight1-the-bot-is-not-special.md:59, docs/s138-flight9-movement-not-simulating.md:16, docs/s140-tier1-cfg.md:132. Combined with the SetDefaultMovementMode revert logic (revert to 3 requires prior set to 1).",
     "implication": "SetDefaultMovementMode targets MOVE_Walking, so a floor spawn triggers the Walking-mode floor-find at spawn. The only reason the air bot is Falling is FindFloor found nothing 13km up."
    },
    {
     "claim": "The Walking-mode floor setup (FindFloor + AdjustFloorHeight + SetBaseFromFloor) is INTACT engine code reachable on the Loki bot. Loki's OnMovementModeChanged (0x55B7BF0) tail-calls engine Super (0x35E9240); its Walking branch resolves CurrentFloor@CMC+0x438 and calls FindFloor(0x35E01D0), AdjustFloorHeight(0x35D2460), SetBaseFromFloor(0x35E7760) -- all inherited from the engine CMC vtable (not Loki-overridden) and NONE is a stripped fold.",
     "grade": "M",
     "evidence": "Loki OnMovementModeChanged 0x55B7C2C/0x55B7C33 `call 0x35E9240`(engine Super). Engine 0x35E92F5 `cmp al,1`(Walking); tail 0x35E9569: `lea rsi,[rdi+0x438]`(&CurrentFloor); 0x35E95CA `call [rax+0x9e0]`; 0x35E95D6 `call [rax+0x6e8]`. Slot resolve identical & real on both vtables: FindFloor(disp0x9e0)->0x35E01D0, AdjustFloorHeight(disp0x6e8)->0x35D2460, SetBaseFromFloor(disp0x6f0)->0x35E7760; folds 0x0F7EC20/EB50/EB60 (bytes c20000/33c0c3/32c0c3) do not match.",
     "implication": "On a floor spawn, FindFloor runs and (floor present) sets a base -> SetDefaultMovementMode does NOT revert -> the bot STAYS MOVE_Walking(1) at PostInitializeComponents, before it ever ticks. No fall, no hover fixed-point, no velocity kick needed."
    },
    {
     "claim": "The air-spawn is a live within-project CONTROL that validates every branch of this chain except the floor-found outcome. The bot at (600,0,13240) reverts to MOVE_Falling(3), which is exactly SetDefaultMovementMode's 'set Walking, no base, revert' path executing end-to-end. Only FindFloor's result differs between air and floor.",
     "grade": "M",
     "evidence": "docs/s139-flight1-the-bot-is-not-special.md:59 (bot MovementMode 3, identical to player); docs/s138-flight9-movement-not-simulating.md:16. Spawn loc = player RootComponent loc + KBSOFFSET(600) in X, Z=13240 from the player's sp-lifted Z (BsResolveAI tutorial_launch.cpp:15701-15703).",
     "implication": "The floor-spawn prediction is not a fresh untested path -- it is the SAME code with FindFloor succeeding instead of failing. The failing case is already measured, so confidence is high."
    },
    {
     "claim": "Neither BP_HERO_Ronin BeginPlay/PossessedBy nor the bot's controller re-teleports or re-lifts the pawn. The `sp` LIFT (+1800 in Z, GravityScale->0) is applied by the shim to the PLAYER spawn only, not to the SpawnAIFromClass bot.",
     "grade": "M",
     "evidence": "Air bot stays at its spawn Z=13240 across the whole census window (s138-flight9, s139-flight1, s140-t2-armj tables show bot at (600,0,13240), no teleport-to-floor). sp LIFT is player path: docs/s138-f2-marker-staged-sp.txt:24-25 ([LIFT] gravity OFF; hero lifted 11440->13240). BsResolveAI (tutorial_launch.cpp:15701-15703) computes bot loc as playerLoc+KBSOFFSET, no Z lift.",
     "implication": "A bot spawned at Z=90.15 REMAINS at ~90.15 (AdjustFloorHeight snaps to exact rest); nothing re-lifts it into the air. Floor placement is durable."
    },
    {
     "claim": "Recommended spawn point: a MEASURED-walkable tutorial-floor XY at the capsule rest Z=90.15 (capsule center; half-height 88.0504). Two proven points: (683,-716,90.15) where the F3 player LANDED and walked in Mode1, and (1356,-409,90.15) where the S140 bot LANDED and walked in Mode1. Spawn AT 90.15 or a few uu ABOVE; NEVER below (penetration risk).",
     "grade": "M",
     "evidence": "F3 player: t=1.184 LOC=(683,-716,90.15) Mode1 LANDED; t=2.889 VEL=(0,-500,0) Mode1 WALKING (docs/s189-mv-wasd-f3-THE_PLAYER_WALKED.md; scratchpad/s189-mv/evidence/f3-walking-Whold-motion.csv). S140 bot: +2s (1356.434,-409.393,90.150) Mode1 walking (docs/s140-t2-armj-THE-BOT-WALKS.md:45-47). Rest Z 90.15 = capsule center = RootComponent RelativeLocation = SpawnActor transform Z.",
     "implication": "Spawning the capsule center at exactly 90.15 places it in the rest configuration (bottom ~1.9uu above the floor, well within MAX_FLOOR_DIST 2.4) so FindFloor succeeds immediately. Above-rest is safer than below (AdjustFloorHeight snaps down); below-rest can embed the capsule and make FindFloor return an unwalkable/above result -> revert to Falling."
    },
    {
     "claim": "Discriminating receipt for 'spawned Walking' vs 'spawned Falling', by external RPM read of the bot's CMC (or a shim A2 marker): MovementMode byte @CMC+0x231 (1=Walking vs 3=Falling) is decisive; corroborate with Velocity @CMC+0xE8/+0xF8 and TimeSinceFallingStart @CMC+0x12b0 (0 if never entered Falling; advancing if Falling).",
     "grade": "M",
     "evidence": "MovementMode @+0x231 is the byte SetDefaultMovementMode and OnMovementModeChanged read/write throughout the disasm above. Velocity @+0xE8(.Z@+0xF8), TimeSinceFallingStart @+0x12b0 per docs/s140-tier1-cfg.md:353; ULokiCMC OnMovementModeChanged writes +0x12b0=0 when NOT entering Falling (0x55B7CCD).",
     "implication": "One read of CMC+0x231 immediately after the A1 stability census settles the whole question. A shim marker printing that byte in the A2 census is a zero-cost receipt."
    }
   ],
   "recommendation": "FLOOR SPAWN WILL YIELD MOVE_Walking IMMEDIATELY -- probability ~90%. Spawn the SpawnAIFromClass bot at a MEASURED walkable tutorial-floor point at the capsule rest Z, e.g. (683.0,-716.0,90.15) [F3 player's proven landing/walk point] or (1356.0,-409.0,90.15) [S140 bot's own proven landing/walk point]; offset X/Y a few hundred uu from where the player ends up so the two heroes are visibly separate but co-located. Spawn AT 90.15 or up to ~+20-30 above; NEVER below (penetration risk). Because SpawnAIFromClass uses AlwaysSpawn (bNoCollisionFail=1, no adjust), the capsule lands at exactly that Z; SetDefaultMovementMode targets DefaultLandMovementMode=MOVE_Walking, runs FindFloor (intact engine code, floor present) which sets a MovementBase, so it does NOT revert to Falling -- it stays MOVE_Walking(1) at PostInitializeComponents, and AdjustFloorHeight snaps it to exact rest. The AI wander (ALokiBotController::Tick, via KBSPSARMS ARM D 0x20 + ARM F DriveRecompute 0x80 + ARM G GAS 0x100) then drives ControlInputVector every ~2s and the bot walks from rest at exactly its seeded MoveSpeed cap (500 uu/s) -- the same Walking-from-rest behavior the F3 player already demonstrated [M], with NO velocity kick (no ARM J/H). MECHANISM CHANGE NEEDED (small, no new logic): override BsResolveAI's g_bsLoc from player-loc+KBSOFFSET(x)/Z=13240 to a fixed floor point -- add an absolute-XYZ knob (e.g. KBSABSLOC / KBSSPAWNZ) rather than deriving from the airborne player. This is the cleanest path to the session goal: it eliminates the fall, the hover fixed-point, and the velocity kick entirely, lets both heroes be Walking-on-the-floor in the same frame, and gives a bot peak speed bit-exact to its seeded MoveSpeed (the discriminator per R-S189-MV-WASD-i / R-S189-MV-a).",
   "open_questions": [
    "DefaultLandMovementMode==MOVE_Walking is DEDUCED from the air-bot=Falling(3) measurement, not directly read (CMC CDO +0x540 byte is unreliable in merged .data). A one-line RPM read of the spawned bot's CMC+0x540 (or CMC+0x231 post-spawn) would confirm directly.",
    "Exact MAX safe spawn-above-rest margin depends on the Loki CMC MaxStepHeight (stock default 45; not read on this build [I]). Keep any above-rest offset small (<~30uu) and prefer exact 90.15 so this never matters.",
    "Is the World Partition cell containing the chosen (x,y) guaranteed streamed-in when BsCallAI fires? Player and S140 bot demonstrably walked over (683..1356,-716..964) so collision is present in a staged world; but if the bot spawns before that cell streams, FindFloor fails and it is Falling. Spawn only after the world is settled (current A0->BsResolveAI->BsCallAI timing already runs post-stage).",
    "Not verified offline: whether an ALokiBotController (ARM D) OnPossess step or an early BP tick forces the bot's MovementMode away from Walking within the first frames. The plain-AIController S140 bot stayed Walking after landing, so unlikely, but OnPossess's GameplayEffect application was not audited for a SetMovementMode call.",
    "The exact Z snap during AdjustFloorHeight from a spawn a few uu above rest was not traced instruction-by-instruction; assumed stock snap-to-MIN_FLOOR_DIST behavior."
   ],
   "hazards": [
    "Do NOT spawn below the measured rest Z (e.g. <88): AlwaysSpawn does no adjustment, so the capsule can spawn embedded in the floor, and FindFloor from inside geometry can return an unwalkable/above-capsule result -> SetDefaultMovementMode reverts to MOVE_Falling and the bot sits in the hover fixed-point (the exact failure this lane exists to avoid).",
    "Do NOT reuse the airborne player Z (13240) or any player-loc-derived Z for the bot -- that is the current air spawn that produces MOVE_Falling. The g_bsLoc computation (tutorial_launch.cpp:15701-15703 and 17733) must become an absolute floor point, not just a KBSOFFSET tweak.",
    "Do NOT add a velocity kick (ARM J 0x800 / ARM H 0x200) for the floor bot -- unnecessary if it spawns Walking, and a kick in Walking mode is a different, confounding motion path than the AI-wander drive the goal wants. The whole point of the floor spawn is to make the kick redundant.",
    "Do NOT spawn on the Z=386 ledge the player falls onto (the -mv-play shipping arm's player lands there, not the floor); that ledge is MOVE_Falling until WASD slides it off. Use a floor point (Z=90.15).",
    "Confirm the bot (x,y) does not exactly coincide with where the player rests, or the two capsules visually stack (AlwaysSpawn still succeeds, but the goal wants both VISIBLY walking). Offset a few hundred uu.",
    "The MovementMode read must target the BOT's CMC (bot pawn -> CharacterMovement), not the player's -- both read 3 in the air today, so a wrong-object read would falsely confirm."
   ]
  },
  "botgas": {
   "findings": [
    {
     "claim": "(a) Immediately after SpawnAIFromClass + LokiBotController possession + ARM B (InitPlayerState) + ARM C (SetPlayerState) + ARM F (UpdateCharacterControllable), and BEFORE ARM G, the bot pawn's AbilitySystemComponentStorage +0xF00, AttributeSetStorage +0xF08 and AttributeSetHealthStorage +0xF10 all read 0.",
     "grade": "M",
     "evidence": "docs/s140-t2-marker-armj.txt:146-148 print `dst AbilitySystemComponentStorage @0xF00  0 -> 18C4FD6FF10`, `@0xF08  0 -> 18C4FD71710`, `@0xF10  0 -> 18C4FD71F20`; docs/s141-t3-marker-arml.txt:146-148 print the same `0 ->` triple on a different PID (256559DFF10/E1710/E1F20); docs/s139-flight4-gas-port-works.md \u00a71 shows a third process with `0 ->`. All three are the ARM-D 'B-treatment' pawn possessed by a LokiBotController (armj lines 84-121 confirm chain and PlayerState installed before GASX).",
     "implication": "The 'NULL per S139 F1' claim is reproduced on three PIDs on the fully-treated bot; ARM G's three pointer stores are the ONLY writer of these slots on the bot path, so any combined arm must include them (or an equivalent) for the bot."
    },
    {
     "claim": "(a, mechanism) Nothing on the bot path populates +0xF00 because ALokiBotController::OnPossess reads it through a two-instruction getter and SKIPS its entire GameplayEffect-apply + ability-grant block when it is NULL; ARM G writing +0xF00 later does NOT retroactively run that block.",
     "grade": "M",
     "evidence": "merged15 bytes: 0x055AC880 = `mov rax,[rcx+0xF00]; ret`. In OnPossess 0x5565470: `0x0556590B mov rcx,rsi / 0x0556590E call 0x55ac880 / 0x05565913 mov r12,rax / 0x05565916 test rax,rax / 0x05565919 je 0x5565cbd`; the GE loop at 0x05565932-0x05565A45 iterates Cfg+0x320 and calls `[r12 vt+0x6c8]` and 0x4467b90 on r12=ASC; 0x5565CBD is past the ability grants. Consistent with docs/s137-playerstate-and-lokibot-settled.md:421 and :534-538.",
     "implication": "An ARM-G bot never receives BotCheatEffects or the three OnPossess abilities; harmless for walking. Do NOT re-run OnPossess after ARM G \u2014 it would apply GEs to the shared CDO ASC process-wide."
    },
    {
     "claim": "(b) ARM G's CDO-subobject borrow resolves on this build at src @0x3E8 (AbilitySystemComponent, class LokiAbilitySystemComponent), @0x3F0 (AttributeSet, class LokiAttributeSet), @0x3F8 (AttributeSetHealth, class LokiAttributeSetHealth) on Default__LokiPlayerState_HeroAffiliated, and its by-name attribute offsets on that LokiAttributeSet are MoveSpeed +0xF0, MaxMoveSpeed +0x100, MaxAcceleration +0x120, GroundFriction +0x130, BrakingDecelerationWalking +0x140, Mass +0x170 \u2014 AGREEING exactly with the S189-MV table used on the player's spawnedSet0.",
     "grade": "M",
     "evidence": "ARM G resolves every offset by name (tutorial_launch.cpp:16525 `PropOffsetSuper(ClassOf(ha),kSrc[i])`, :16560 `PropOffsetSuper(ClassOf(src[1]),attrs[i].n)`); markers armj:143-155, arml:143-155 and s139-f4 \u00a71 print identical offsets across three PIDs. S189-MV hard-codes the same six offsets (tutorial_launch.cpp:22303-22310) and anchors them by name at Mana@0x210/MaxMana@0x220 (resolver :22225) plus the AttackSpeed@+0xE0=90.0f sibling control (docs/s189-mv-f1-MOVEMENT_UNLOCK.md SIBLING_READ_PRE/POST). The KWIREGAS carrier instance also resolves AbilitySystemComponent@0x3E8/AttributeSet@0x3F0/AttributeSetHealth@0x3F8 by name (docs/fk24-stage-s112c-ctl-18-4-probe-tutorial_launch_play.txt:36-40).",
     "implication": "Two independently-derived oracles (by-name on the CDO's set; hard-coded+anchored on the carrier's set) agree on one UClass (`LokiAttributeSet`), so a bot set seeded via ARM G and a player set seeded via S189-MV read the same layout. No offset work is needed to combine them."
    },
    {
     "claim": "(c) Writing into the CDO's AttributeSet subobject CANNOT touch the player's spawnedSet0: the player's set is an instance object created by K2_InitStats on a spawned carrier whose ASC was constructor-built, i.e. a different UObject from the CDO's default subobject; in one process the player's live ASC and the CDO's ASC are different addresses.",
     "grade": "M",
     "evidence": "tutorial_launch.cpp:12694 `SpawnActorCls(cls,\"LokiPlayerState_HeroAffiliated\")`, :12713-12727 `K2_InitStats(LokiAttributeSet/LokiAttributeSetHealth, null)`; markers docs/fk24-stage-s112c-ctl-18-4-*.txt:36-40 `carrier=0x2118D72B700 AbilitySystemComponent@0x3E8 = 0x21077B74D80 (constructor built it)`, `carrier.AttributeSet@0x3F0 = 0x211945C4980`. S189-MV's resolver walks THAT ASC's SpawnedAttributes (tutorial_launch.cpp:22198-22225; F1 marker `[S155] asc=0x1927A690100 carrier=0x19318C1FB70` and `spawnedSet0=0x19318915BC0`). arml:157 shows the player's live +0xF00 `25799C20100` vs the CDO ASC `256559DFF10` in the same process. ARM G writes only `src[1]` (the CDO's set) at :16563-16564.",
     "implication": "Bot ARM G and player mv-play seed two disjoint sets. The only way the player could see the CDO's values is if someone points the player's +0xF08 at the CDO set \u2014 which is exactly what ARM K1 does (arml:158). Keep KBSGASPLAYER=0."
    },
    {
     "claim": "(c, broader) No live object other than the CDO itself (and ARM-G-treated pawns) holds a pointer to the CDO's ASC/AttributeSet subobjects; the process-wide effect of ARM G is confined to pawns whose storage slots were explicitly set to those pointers.",
     "grade": "I",
     "evidence": "Per-instance subobjects are created by the class constructor on each new instance (marker 'constructor built it' at a fresh address on the spawned carrier, distinct from the CDO's), and S137's [M] rule (docs/s137-playerstate-and-lokibot-settled.md:172-184) says InitProperties copies no native-owned property from the CDO. Fresh BP_HERO_Ronin_C pawns read +0xF00/F08/F10 = 0 (armj/arml:146-148) so hero instances do not alias the CDO subobjects either. ds_hybrid.cpp:2381 notes '467 live LokiAttributeSets' exist, all distinct instances. NOT measured: whether the CDO ASC's own SpawnedAttributes contains the CDO set, or its OwnerActor/AvatarActor (no marker ever dumped the CDO ASC \u2014 grep of docs/*.txt finds only S81-PROMPT and the s139 pre-registration).",
     "implication": "ARM G is safe for the player's live GAS state as long as K1 is off. If two ARM-G bots are spawned in one world they SHARE one set (identical caps by construction) \u2014 acceptable, but it forecloses per-bot discriminator values."
    },
    {
     "claim": "(d) The CDO's ASC at bot+0xF00 is inert on the movement path: IsStunned returns false unless the ASC's tag container carries the Stunned tag; ConstrainInputAcceleration writes ZeroVector only when IsStunned is true; the HitStop DeltaTime kill requires feature-toggle 120 AND an IsA-passing ASC AND a matching tag. A default-constructed CDO ASC has an empty tag container.",
     "grade": "M",
     "evidence": "Bytes (merged15): IsStunned 0x055B2930 \u2014 `0x055B297D lea rcx,[rsi+0x7f0] / call [rax+0x10]` (= 0x055A9610 `mov rax,[rcx+0x710]; ret` \u2192 hero+0xF00) / `0x055B298F test rax,rax / je 0x55b29b8 (xor al,al)` / RF_Garbage bit-30 check / `0x055B29A1 mov rdx,[rbx+0x48]` (tag from a config CDO) / `0x055B29A5 lea rcx,[rax+0x148]` / `jmp [rax+0x18]` (HasMatchingGameplayTag). ConstrainInputAcceleration 0x055A75B0: `0x055A75E8 call 0x55b2930 / test al,al / je 0x55a760a` \u2192 ZeroVector store only on the true arm (0x055A75F1-0x055A7603). HitStop: `0x055B83B3 mov cl,0x78 / call 0x56e7c10 / test al / je skip` \u2192 interface \u2192 `0x055B83D9 call 0x54f8f40` (IsA) \u2192 `[rbx+0x148] vt+0x18` tag query \u2192 `0x055B83FA xorps xmm6,xmm6` only if true. EMPIRICAL [M]: with the CDO ASC at +0xF00 the bot fell, landed and walked 13,187 uu capped at exactly 500.0 (docs/s140-t2-armj-THE-BOT-WALKS.md:22-25,59-65; arml \u00a71 three samples on the cap) \u2014 impossible if dt were zeroed or input constrained to zero.",
     "implication": "The tag-emptiness of the CDO ASC is inferred [I,strong] from the bytes, but the walk itself is the direct measurement that none of these gates fire. Do not spend a flight re-testing the CDO-ASC-on-movement question."
    },
    {
     "claim": "(e) ARM K1 (KBSGASPLAYER=1) OVERWRITES the player's live KWIREGAS ASC at +0xF00 with the CDO ASC and points +0xF08/+0xF10 at the CDO's sets; the -mv-play route writes ONLY +0xF08 (= spawnedSet0 on the S155-bound carrier ASC) and never touches +0xF00 or +0xF10. K1 was never tested under input; mv-play was.",
     "grade": "M",
     "evidence": "arml:157-159 `PLR AbilitySystemComponentStorage @0xF00  25799C20100 -> 256559DFF10`, `@0xF08  0 -> 256559E1710`, `@0xF10  0 -> 256559E1F20`; the K1 player then had NO input driver (docs/s141-t3-arml-result.md:55-60 AnalogIM 0, Acceleration (0,0,0)) so its walk-under-WASD is UNTESTED. -mv-play: tutorial_launch.cpp:23068-23090 writes `*(hero+0xF08)=s189MvSet` only; awk over lines 22400-23200 finds no AttributeSetHealthStorage/0xF10 write; S189-MV F1 marker `WIREF08 heroF08Pre=0x0 heroF08Post=spawnedSet0 pointsAtSet=1`. Under that wiring F2b measured `ANALOGMOD=1.0000` at every W-hold sample with ACC=(0,-50000,0) (docs/s189-mv-wasd-f2-ANALOG_MOD_1_CLAMP_REFUTED.md), and F3 walked 2300 uu at bit-exact 500 uu/s in Walking mode (docs/s189-mv-wasd-f3-THE_PLAYER_WALKED.md:6,13-17).",
     "implication": "On the getter path both routes are equivalent (GetMaxSpeed reads hero+0xF08 \u2192 set+0xF0/+0x100 only; S189-MV F1 read-path [M]); the DIFFERENCE is the identity of +0xF00. K1 would orphan the carrier ASC that S155 bound (AvatarActor=hero) and S156/S189 seeded, so the hero's IAbilitySystemInterface would return an unbound CDO ASC while the carrier ASC still claims the hero. A +0xF08-only mask on K1 removes that hazard but still points the player at the SHARED CDO set, defeating the per-pawn bit-exact discriminator. Wiring ONLY +0xF08 to the player's OWN spawnedSet0 (= mv-play as built) is strictly safer and is the measured route."
    },
    {
     "claim": "The bot's ARM G values collide with the player's: KBSGASMOVESPEED defaults to 500.0f (= the player's seeded 0x43FA0000), and ARM G's MaxAcceleration=50000 is bit-identical to engine Super's stock CMC UPROPERTY, which R-S189-MV-a already flagged as an indistinguishability trap.",
     "grade": "M",
     "evidence": "tutorial_launch.cpp:16499-16501 `#define KBSGASMOVESPEED 500.0f`; :16552 `{\"MaxAcceleration\", 50000.0f}`; ds_hybrid.cpp:2405-2407 admits the values are copies of 'the CMC's own base props (read live)'. S189-MV deliberately seeds 45000 (0x4732C800) for that reason (tutorial_launch.cpp:22292-2298; docs/s189-mv-f1-MOVEMENT_UNLOCK.md 'R-S189-MV-a').",
     "implication": "If both pawns cap at 500.0 in one world, the session goal's 'bot peak bit-exact to ITS seeded MoveSpeed' cannot be attributed to the bot's set rather than the player's, and a CDO\u2192player leak would be invisible. The KBSGASMOVESPEED knob already exists, so a non-stock, non-500 bot value costs zero source edits."
    }
   ],
   "recommendation": "Wire the two pawns to two DISJOINT attribute sets and leave the player's +0xF00 alone: (1) PLAYER \u2014 use the -mv-play chain exactly as flown (S155 bind \u2192 S156 seed \u2192 S189-MV seed of the carrier's spawnedSet0 \u2192 KBFWIREF08 hero+0xF08=spawnedSet0 \u2192 KBFPOKEGRAVITY), with +0xF00 remaining the S155-bound carrier ASC and +0xF10 untouched; this is the only player wiring measured under WASD (F2b AnalogMod=1.0, F3 500.0 bit-exact). (2) BOT \u2014 use ARM G's CDO-subobject borrow unchanged in mechanism (3 storage stores + 6 attr writes into the CDO's LokiAttributeSet; flown 3\u00d7 and walked 2\u00d7), but build it with `-DKBSGASMOVESPEED=<non-stock, non-500 value>` (e.g. 437.0f = 0x43DA8000) so the bot's peak is bit-exact-attributable to the CDO set and any leak into the player's 500-capped set would show; consider also `-DKBSGASMOVESPEED`-style non-stock MaxAcceleration only if a Walking-arm MaxAccel discriminator is wanted (this changes a flown recipe \u2014 50000 walked). (3) Keep `KBSGASPLAYER=0` (default) \u2014 ARM K1 must not run in the combined DLL because it clobbers the S155-bound ASC at +0xF00 (arml:157) and was never tested under input. (4) Pre-register: player peak_vel_2d == 500.000 (0x43FA0000); bot peak_vel_2d == the bot knob value bit-exact; spawnedSet0.MoveSpeed@+0xF0 CurrentValue readback UNCHANGED after ARM G runs; player +0xF00 UNCHANGED (== carrier ASC) after ARM G; bot +0xF00 == CDO ASC and +0xF08 == CDO set post-ARM-G. (5) Order the arms so ARM G's FindObjExact GUObjectArray sweep (1.2\u20132.6 s game-thread) and the bot spawn happen after the player's WIREF08/POKEGRAVITY stations, and do not call OnPossess again on the bot after ARM G (it would apply GEs to the shared CDO ASC process-wide).",
   "open_questions": [
    "Which MaxAcceleration does the Walking arm read on the player? docs/s189-mv-f1 [M] says Walking reads seeded +0x120 (=45000), but F3 measured |Accel|=45768 in Walking and its doc attributes that to 50000 minus a friction term stock UE does not subtract from Acceleration. Untranscribed; a non-stock MaxAccel on BOTH pawns plus a Walking-mode Acceleration read would settle it.",
    "Does the CDO ASC's own SpawnedAttributes contain the CDO AttributeSet, and are its OwnerActor/AvatarActor NULL? Never dumped (no marker prints the CDO ASC's state). Matters only if any bot code path calls GetOwnerActor()/GetAvatarActor() on it; the wander path demonstrably does not need them.",
    "If two SpawnAIFromClass bots are ARM-G-treated in one world they share ONE CDO set (identical caps). Is that acceptable for the session goal, or does a second bot need its own set (which would be a new recipe: per-bot InitStats on a fresh ASC, untested)?",
    "The bot walked with the CDO's LokiAttributeSetHealth at +0xF10; the player walks with +0xF10 NULL. Whether any BOT-side reader (ALokiBotController::Tick / HandleLivingStateChanged) dereferences +0xF10 was not measured \u2014 so a 'mv-play-style' bot wiring that omits +0xF10 is UNTESTED; keep ARM G's three stores for the bot.",
    "ARM G bots never receive OnPossess's BotCheatEffects or the three granted abilities (GE block skipped on NULL ASC at possession time). Irrelevant to walking; relevant if a later session expects bot abilities."
   ],
   "hazards": [
    "KBSGASPLAYER=1 (ARM K1) in a combined DLL overwrites the S155-bound carrier ASC at hero+0xF00 with the unbound CDO ASC (arml:157) \u2014 orphaning the object the S156/S189 seeds and any AdjustHealth path use. It is off by default; verify the variant's -D list before building.",
    "KBSGASMOVESPEED=500.0f (default) equals the player's seeded 500 \u2014 both pawns capping at 500.0 defeats the bit-exact discriminator and hides any CDO\u2192player contamination. Set a non-500, non-stock bot value.",
    "ARM G's MaxAcceleration=50000 is bit-identical to engine Super's stock UPROPERTY (R-S189-MV-a trap) \u2014 a bot Walking-arm MaxAccel read cannot distinguish 'seed reached' from 'Super bypass'.",
    "ARM G writes are process-wide and never undone (source :16613); anything that later reads the CDO's AttributeSet (e.g. a real LokiPlayerState_HeroAffiliated spawn, which S80 says crashes anyway) would inherit 500/50000/8/2048/100.",
    "ARM G's FindObjExact walks GUObjectArray on the game thread (S130 measured ~2.0\u20132.3 s per pass); scheduling it before the player's WASD window could eat the input timing that already cost S189-MV-WASD ship-F1 its walk confirmation.",
    "Do not re-run ALokiBotController::OnPossess after ARM G \u2014 with +0xF00 now non-NULL the GE loop (0x5565932-0x5565A45) would ApplyGameplayEffect into the shared CDO ASC.",
    "This lane's disassembly is from dumps/merged15.dump.exe pages that read LIT (3707/3699/3578/3729/3960 non-zero bytes per page); OnPossess's GE-apply callee 0x4467B90 is still recorded DARK (S137) and was not read."
   ]
  },
  "dshybrid": {
   "findings": [
    {
     "claim": "The DS-route 'hero physically translated' measurement was MODE_MOVETEST (not MODE_PLAYABLE): per-hook-hit AddMovementInput(+X,1.0,bForce=true) with SetMovementMode(kMoveMode=1 Walking) RE-ASSERTED ON EVERY HOOK HIT, producing vel=(500,0,0) mode=1 at t+8 and a total translation of X 0\u219251.6 uu (Z 569.0\u2192576.9) over ~80 sparse ProcessInternal-hook hits.",
     "grade": "M",
     "evidence": "git show 6a227f9 body: '[MT] t+8 vel=(500.0,0.0,0.0) mode=1', '[MT] t+56 loc=(32.9,0.0,569.0)', '[MT] t+80 loc=(51.6,0.0,576.9)', 'Mode: MODE_MOVETEST(35)'. Source at that commit: kMoveMode = 1 (ds_hybrid.cpp:189@6a227f9) and the per-hit re-assert at :2455-2463@6a227f9 ('Re-assert the movement mode EVERY tick'). Hook cadence: MODE_MOVETEST loop installs the PI hook 120 ms / sleeps 60 ms (ds_hybrid.cpp:3467-3473 today).",
     "implication": "'The hero moved 51.6 uu' is a crawl, and the source itself says so (ds_hybrid.cpp:197-198: 'the sparse ProcessInternal hook only produced crawling motion'). The 'mode=1' readback is an instrument artifact \u2014 it was sampled in the same hook hit that had just re-asserted Walking. Do NOT carry 'DS SetMovementMode(Walking) held' as a measurement."
    },
    {
     "claim": "The DS route's exact write list (both MT and PL): (1) PC->Pawn=hero, hero->Controller=PC; (2) hero+0x1090 LivingState=1; (3) hero+0x1BE8 = 0 (clear pre-drop hide); (4) hero+0xF00/+0xF08/+0xF10 = Default__LokiPlayerState_HeroAffiliated CDO's AbilitySystemComponent/AttributeSet/AttributeSetHealth subobject pointers; (5) six attributes written at Base(+0x8) AND Current(+0xC) on that CDO AttributeSet: MoveSpeed=500, MaxMoveSpeed=500, MaxAcceleration=50000, GroundFriction=8, BrakingDecelerationWalking=2048, Mass=100; (6) reflected UFunction SetMovementMode(kMoveMode, 0) on the CMC; (7) per-frame AddMovementInput. There is NO Velocity write, NO Acceleration write, NO input-system binding \u2014 WASD is polled with GetAsyncKeyState and fed straight to AddMovementInput.",
     "grade": "M",
     "evidence": "ds_hybrid.cpp:2298-2306 (wire+LivingState+0x1BE8), :2396-2429 (MT storages + 6 attrs), :2620-2648 (PL same), :2650-2660 (PL SetMovementMode kMoveMode), :2693-2743 PlayableTick (GetAsyncKeyState W/S/A/D \u2192 dir \u2192 AddMovementInput vec/scale=1.0/bForce=1), :2502-2532 MT per-hit AddMovementInput. grep of the file for Velocity writes finds only the SpectatorPawnMovement puppet at :1325-1329 (a different mode).",
     "implication": "ARM G (tutorial_launch.cpp BsPsGasAttrs) is a faithful port of items (4)-(5). The tutorial route's player chain (S189-MV) differs in WHERE it writes the attributes (the player's own ASC.SpawnedAttributes[0], not the process-wide CDO subobject) \u2014 the DS/ARM G recipe writes a CDO default subobject shared by every consumer that borrows it."
    },
    {
     "claim": "'Stock engine chain' in the DS docs means AddMovementInput \u2192 ControlInputVector \u2192 CMC consume \u2192 Acceleration=(50000,0,0) \u2192 CalcVelocity \u2192 Velocity=(500,0,0) with no per-frame velocity write \u2014 and the 500 was bit-exact to the seeded MoveSpeed (an early instance of the R-S189-MV-WASD-i discriminator).",
     "grade": "M",
     "evidence": "6a227f9 body: 'The full chain now runs: AddMovementInput -> ControlInputVector -> CMC consumes -> Acceleration=(50000,0,0) -> CalcVelocity -> Velocity=(500,0,0)' and '[MT] t+8 vel=(500.0,0.0,0.0) mode=1 <- velocity == MoveSpeed exactly'; ds_hybrid.cpp:2546-2551 documents the ControlInputVector/LastControlInputVector split used to prove input reached the CMC; docs/next-session-prompt-s81.md:57-66 traces GetMaxSpeed 0x55ACB90 \u2192 [hero vt+0xC00] 0x558BD90 \u2192 0x55AC9F0 'mov rbx,[rcx+0xF08]; test; je \u2192 return 0'.",
     "implication": "The DS route never had a physics-step problem to solve \u2014 it only fed the input side and let CalcVelocity/PhysWalking produce velocity. The tutorial route's WALKING-mode results (S189 F3 player; S140 T2 bot) already exceed it by orders of magnitude (2,300 / 13,187 uu vs 51.6 uu)."
    },
    {
     "claim": "DS-route hero state: a networked NM_Client in the DS-stub-served LVL_Tutorial (pre-drop/BRALL state), hero hand-spawned via GameplayStatics at (0,0,600) (or pawn loc +150; S81 run: (0,0,650)), starting in MOVE_None; it fell and 'landed' at Z\u2248569 (spawn 600), then under per-hit Walking re-assert drifted UP 569.0\u2192576.9 while crawling 51.6 uu in X. A one-shot SetMovementMode(Walking) 'flipped straight to 3 (MOVE_Falling) and STUCK there even after the hero settled at a stable Z' \u2014 i.e. FindFloor did not find a WALKABLE floor under it (otherwise the landing itself would have auto-transitioned to Walking via SetPostLandedPhysics).",
     "grade": "I",
     "evidence": "Measured parts: ds_hybrid.cpp:2600-2617 spawn (sx,sy,sz default 0,0,600), :2347-2350 'a GameplayStatics-spawned hero has CMC MovementMode = 0 (MOVE_None)', :2510-2512 the flip-to-3 note; next-session-prompt-s81.md:65-66 'the hero falls+lands perfectly while never moving horizontally'; session-81-disconnect-characterized.txt:39-46 (S81 PL run: hero at (0,0,650), GetMaxSpeed=500, per-frame vtable hook slot 269, then ConnectionTimeout ~1-2 min later \u2014 NO location delta ever recorded under the per-frame driver). Inferred: the floor being non-walkable is derived from the stuck-at-3 observation plus the engine landing path (SetPostLandedPhysics site 0x035FB365 does SetMovementMode([rbx+0x542]=GroundMovementMode)).",
     "implication": "The DS hero was NEVER walking on the tutorial floor (rest Z 90.15). The only DS 'walk' evidence is a forced-Walking crawl on an unresolved/non-walkable surface at Z\u2248569-577. The tutorial route has strictly better ground truth for both pawns (player F3: 2,300 uu at Z=90.15 in Walking; bot S140 f3: 13,187 uu at Z=90.150 after auto 3\u21921 on landing)."
    },
    {
     "claim": "The DS route later ABANDONED Walking: kMoveMode was changed 1\u21925 (MOVE_Flying) in commit 10a7122 because the hand-spawned Character's CMC tick blocked the game thread ~20 s in a kernel wait (innermost waiter base+0x38463CB, a slot-34 virtual on [CMC+0xF0]) and tripped the 15 s netdriver timeout. Flying did NOT fix it; a per-frame SetMovementMode re-assert made it WORSE (OnMovementModeChanged \u2192 a physics wait each frame). This hazard has never reproduced on the standalone tutorial route.",
     "grade": "M",
     "evidence": "git show 10a7122 diff: '-static const unsigned kMoveMode = 1' / '+static const unsigned kMoveMode = 5 ... [S81: 5=Flying test ...]' and the PL fix 'DoPlayableSetup HARDCODED MOVE_Walking (g_pbuf[0]=1)'. session-81-disconnect-characterized.txt:139-160 ('Flying set-ONCE ... STILL dropped (Real 28.6s)', 'per-frame re-assert fires OnMovementModeChanged->a physics wait each frame = WORSE'). Positive controls that it does not transfer: S140 T2 f3 bot walked 13,187 uu over 10+ s and S189 F3 player walked 2,300 uu, both on a standalone client with no game-thread freeze; play-atlanding-walk (KFLYMODE=1) walked +1,246 uu at Z=90 (drop-sequence-status-s150.md:1093-1108, 'No Walking-mode init crash').",
     "implication": "Do not import the DS route's Flying default or its 'never re-assert per frame' rule as evidence about the tutorial route \u2014 they are properties of a networked-client + hand-spawned-actor configuration. KFLYMODE=5 in tutorial_launch's play (tutorial_launch.cpp:5729-5732, 13205-13207) inherits the same S81 rationale plus S94's cell-streaming crash (session-94-visible-movable-hero.md:33-37), and play-atlanding-walk already proved Walking works on real terrain."
    },
    {
     "claim": "Engine UCharacterMovementComponent::SetMovementMode (0x035FACD0, UCMC vtable slot 206 / disp 0x670) is stock: custom mode kept only if NewMode==7 (this build's MOVE_Custom), NavWalking(2) falls back to Walking(1) without navmesh, SAME-MODE EARLY-OUT (movzx esi,[rbx+0x231]; cmp esi,edi; jne), stores MovementMode@+0x231 / CustomMovementMode@+0x232, gates on HasValidData ([rax+0x6B8]), then calls virtual OnMovementModeChanged ([rax+0x678]) and sets byte +0x1C8=1. It contains NO floor check and NO revert to Falling.",
     "grade": "M",
     "evidence": "merged14 disassembly 0x035FACD0-0x035FADBD (this session, capstone): 0x035FACE8 cmp edx,7 / cmove; 0x035FACF6 cmp edx,2 ... 0x035FAD41 mov edi,1; 0x035FAD46 movzx esi,[rbx+0x231]; 0x035FAD4D cmp esi,edi / jne; 0x035FAD6F mov [rbx+0x231],dil; 0x035FAD79 mov [rbx+0x232],bpl; 0x035FAD80 call [rax+0x6b8]; 0x035FAD99 call [rax+0x678]; 0x035FAD9F mov byte [rbx+0x1c8],1. Vtable slot values read from .rdata 0x07FBED58 (UCMC) and 0x088F8570 (ULokiCMC) with ImageBase 0x7FF608F40000.",
     "implication": "A per-frame SetMovementMode(Walking) re-assert is a no-op whenever the mode is still Walking; it only does work (FindFloor sweep) on the frames where PhysWalking had flipped the mode back to Falling \u2014 which is exactly the DS MT loop's behaviour and why its readback always said 1."
    },
    {
     "claim": "Engine OnMovementModeChanged (0x035E9240, slot 207 / disp 0x678) on entering Walking: projects Velocity to remove the gravity-direction component (the UE5.4 form of Velocity.Z=0, using GravityDirection @+0x1D8/+0x1E8), sets bCrouchMaintainsBaseLocation (+0x54D bit 0x40) and GroundMovementMode(+0x542)=1, then (common tail, if IsMovingOnGround [rax+0x680]) FindFloor([rax+0x9E0]) + AdjustFloorHeight([rax+0x6E8]) + SetBase([rax+0x7F0]) from the floor result (walkable \u21d2 base = hit component, else SetBase(NULL)). On entering Falling: Velocity += GetImpartedMovementBaseVelocity ([rax+0x758]) and CharacterOwner->Falling() ([owner vt+0x9A0]). It ends with CharacterOwner->OnMovementModeChanged ([owner vt+0x9F0]). It contains ZERO calls to SetMovementMode ([+0x670]) \u2014 it does not revert an airborne Walking pawn by itself.",
     "grade": "M",
     "evidence": "merged14 disassembly 0x035E9240-0x035E9725 (this session): 0x035E92F5 cmp al,1 branch with the xorps/mulsd projection on [rdi+0xE8/0xF0/0xF8] against [rdi+0x1D8/0x1E8], 0x035E93A9 or [rdi+0x54d],0x40, 0x035E93C0 mov [rdi+0x542],al; 0x035E9415 cmp al,3 branch: call [rax+0x758], adds into +0xE8/+0xF0/+0xF8, call [rax+0x9a0]; tail 0x035E9586 call [rax+0x680] \u2192 0x035E95CA call [rax+0x9e0] \u2192 0x035E95D6 call [rax+0x6e8] \u2192 SetBase via r14=[rax+0x7f0]; else CurrentFloor.Clear (memset 0xF8 at rsi+0x10, FloorDist=-1) + SetBase(NULL); 0x035E9708 call [rax+0x9f0]. The .text scan for 'ff 9x 70 06 00 00' (call [reg+0x670]) returns 38 sites, none inside 0x035E9240-0x035E9725.",
     "implication": "Calling SetMovementMode(Walking) on an AIRBORNE pawn leaves it in Walking with CurrentFloor non-walkable and no base for the remainder of that frame; the revert happens on the NEXT PhysWalking tick, not inside the call."
    },
    {
     "claim": "The Walking\u2192Falling revert for an airborne pawn is in PhysWalking, reached two ways: (a) non-walkable floor \u2192 virtual CheckFall (slot 0x8E8 = 0x035D7C70, called at 0x035F03FB with bMustJump / bCheckedFall(+0x428) operands) \u2192 HandleWalkingOffLedge ([+0x6E0]) \u2192 if IsMovingOnGround ([+0x610]) \u2192 StartFalling ([+0x6D0]); (b) walkable floor but ShouldCatchAir ([+0x6D8]) \u2192 inlined CheckFall at 0x035F0450 \u2192 same StartFalling. StartFalling (0x03600890, slot 218 / disp 0x6D0) recomputes remainingTime, and if IsMovingOnGround() calls SetMovementMode(MOVE_Falling=3) at 0x0360095D (lea edx,[r9+3]; call [rax+0x670]) then tail-jumps StartNewPhysics ([rax+0x720]) so PhysFalling runs in the SAME frame. Neither PhysWalking nor StartFalling is Loki-overridden (slots 302 and 218 identical in both vtables); only IsMovingOnGround (slot 194) has a thin Loki wrapper (mode 7/custom 3 \u21d2 true, else jmp engine).",
     "grade": "M",
     "evidence": "StartNewPhysics jump table @0x03600BF8: case 1 \u2192 call [rax+0x970] (PhysWalking = slot 302 = 0x035EF960 in BOTH vtables). PhysWalking virtual-call list (this session): 0x035F03FB call [+0x8E8]\u21920x035D7C70, 0x035F0467 call [+0x6E0], 0x035F0474 call [+0x610], 0x035F04A6 call [+0x6D0]\u21920x03600890, 0x035F0597 call [+0x720]. CheckFall body 0x035D7C70-0x035D7D48: HasValidData \u2192 cmp byte [rsp+0x90],0 (bMustJump) / call [rax+0x950] (CanWalkOffLedges) \u2192 call [rax+0x6e0] \u2192 call [rax+0x610] \u2192 call [rax+0x6d0] \u2192 mov al,1. StartFalling 0x03600890-0x0360097E: 0x03600943 call [rax+0x610]; 0x03600959 lea edx,[r9+3]; 0x0360095D call [rax+0x670]; 0x0360097E jmp [rax+0x720]. Branch into 0x035F0450 is from 0x035F0080 after 0x035F0072 call [rax+0x6d8] (slot 219 \u2192 0x00F7EB60, the 'return false' fold = stock ShouldCatchAir) \u2014 so path (b) is dead by construction and path (a) is the live one. Loki IsMovingOnGround 0x055B1EC0: cmp [rcx+0x231],7 / cmp [rcx+0x232],3 \u2192 al=1 else jmp 0x035E6B20.",
     "implication": "SetMovementMode(Walking) on an airborne bot reverts to Falling within one movement tick and cannot be 'held' except by re-asserting every frame (the DS MT trick), which just alternates Walking/Falling and buys nothing over letting the pawn land. The correct lever is to get the pawn ONTO a walkable floor (spawn low or kick it off the ledge); the engine then transitions 3\u21921 by itself (SetPostLandedPhysics site 0x035FB365: movzx edx,[rbx+0x542]; call [rax+0x670]), which is what S140 f3 (bot) and S189 F3 (player) both measured live."
    },
    {
     "claim": "ULokiCharacterMovementComponent overrides SetMovementMode (slot 206 \u2192 0x055C0AC0) with a REFUSAL gate: if CharacterOwner (+0x198) is non-null, IsA(<class from lazy StaticClass 0x5395720>, via 0x12c7dd0) and CharacterOwner->HeroPredropHidden (+0x1BE8) != 0 and NewMode != MOVE_None(0), the Super (0x035FACD0) is SKIPPED entirely and the call returns having done nothing.",
     "grade": "M",
     "evidence": "merged14 disassembly 0x055C0AC0-0x055C0B4C (this session): 0x055C0ADA cmp qword [rcx+0x198],0 / je Super; 0x055C0AF0 call 0x5395720; 0x055C0AFF call 0x12c7dd0; test al / je Super; 0x055C0B14 cmp byte [rax+0x1be8],0 / je Super; 0x055C0B1D test edi,edi / jne 0x055C0B32 (epilogue, NO Super); 0x055C0B2D call 0x35facd0 (Super). +0x1BE8 = HeroPredropHidden [M]: tutorial_launch.cpp:14491-14497 (by-name PropOffsetSuper 'HeroPredropHidden' cross-checked against disasm 0x1BE8), docs/s132-dismount-settled.md:126 (SetPredropHidden 0x5599040 writes byte hero+0x1BE8). Class identity of the IsA check is [I,strong] (ALokiHeroCharacter \u2014 the class that owns HeroPredropHidden); its name string was not resolved here.",
     "implication": "This is why the DS route explicitly zeroes hero+0x1BE8 BEFORE SetMovementMode (ds_hybrid.cpp:2305, :2623). On the tutorial route the player's SetMovementMode(5)/(1) calls demonstrably took effect (play/S132 hover at 13,240; play-atlanding-walk at Z=90), so its byte is 0; a SpawnAIFromClass bot's HeroPredropHidden has never been read. A silent no-op here reads exactly like 'the bot ignores SetMovementMode' \u2014 read the byte (by name) before any reflected SetMovementMode call on the bot, or call SetPredropHidden(false) first (RM_MOUNT already does: tutorial_launch.cpp:15051-15057)."
    },
    {
     "claim": "The 'partial port fails' gotcha is real and inherited: wiring AttributeSetStorage makes ULokiCMC read every movement value from the attribute set, so a set with only MoveSpeed leaves MaxAcceleration=0 \u21d2 Acceleration=0 \u21d2 no motion (observed live on the DS route, 'Accel went 50000 -> 0 after step 2'). The tutorial route already honours it: ARM G and S189-MV both write all six attributes. S189-MV refined the mechanism: on the Falling arm GetMaxAcceleration delegates to the engine Super (CMC UPROPERTY 50000) and only the Walking arm reads AttrSet+0x120 \u2014 so the 50000 seed matters exactly when the pawn is on the ground.",
     "grade": "M",
     "evidence": "6a227f9 body item 3 ('a set with only MoveSpeed filled gives MaxAcceleration=0 ... (observed, and it is why Accel went 50000 -> 0 after step 2)'); ds_hybrid.cpp:2407-2411 comment; tutorial_launch.cpp BsPsGasAttrs (ARM G) writes the same six (ground state); docs/s189-mv-f1-MOVEMENT_UNLOCK.md via CLAUDE.md S189-MV block: GETTER_AFTER MaxAccel=47435000(50000) not 0x4732C800(45000) on mode=3 (the Falling-arm Super delegation).",
     "implication": "Any bot seed must write all six at Base(+0x8) and Current(+0xC). For the bit-exact discriminator, seed the BOT's MoveSpeed/MaxMoveSpeed to a value that is neither stock nor the player's 500 (R-S189-MV-a), so the bot's Walking peak proves it read ITS set, not the player's."
    },
    {
     "claim": "Other DS gotchas and which ones transfer: (i) 'spawning LokiPlayerState_HeroAffiliated crashes the client instantly' \u2014 transfers (both routes borrow the CDO subobjects instead); (ii) 'a GameplayStatics-spawned hero starts at MovementMode=0 (MOVE_None) so AddMovementInput is discarded' \u2014 does NOT apply to SpawnAIFromClass bots, which are measured at MOVE_Falling(3) from spawn; (iii) 'ARM G's CDO AttributeSet is a PROCESS-WIDE default subobject' \u2014 transfers: KBSGASPLAYER=1 (ARM K1) and the bot share ONE set, so their MoveSpeed can never be discriminated bit-exactly while both read it; (iv) 'never null PC->Player@+0x458' \u2014 irrelevant to a standalone client; (v) the 20 s CMC kernel wait and the per-frame re-assert 'physics wait' \u2014 DS-route specific, never seen on the tutorial route.",
     "grade": "M",
     "evidence": "(i) next-session-prompt-s81.md:72-76, ds_hybrid.cpp:2378-2382; (ii) ds_hybrid.cpp:2347-2350 vs docs/s138-flight9b-flymode-refuted.md:12 'A BASELINE mode=3 (Falling)' and CLAUDE.md S139 'MovementMode 3' on both pawns; (iii) ds_hybrid.cpp:2385-2389 'use the CDO's DEFAULT SUBOBJECTS' + ARM G KBSGASPLAYER description in the ground state (same three storage pointers onto the PLAYER hero); (iv) next-session-prompt-s81.md:114-118; (v) session-81-disconnect-characterized.txt:56-70,139-146 vs the multi-minute tutorial walks (s140-t2-armj-THE-BOT-WALKS.md:45-47; s189-mv-wasd-f3:34).",
     "implication": "For the joint-walk flight keep KBSGASPLAYER=0 (the player uses its own S189-MV-seeded spawnedSet0) so the two pawns read DIFFERENT attribute sets, and give them different MoveSpeed seeds. That is the only configuration in which 'bot peak speed == bot seed' and 'player peak == player seed' are simultaneously discriminating."
    },
    {
     "claim": "On the tutorial route the natural landing path is proven to transition Falling\u2192Walking for BOTH pawn kinds without any SetMovementMode call, and a reflected SetMovementMode(Walking) on a hero ALREADY standing on the floor is proven harmless and drivable.",
     "grade": "M",
     "evidence": "Bot: docs/s140-t2-armj-THE-BOT-WALKS.md:20-21,45-47 (fell at Vz -4000, Z=90.150 from +2 s, walked 13,187 uu; starting state MovementMode=MOVE_Falling :148). Player: docs/s189-mv-wasd-f3-THE_PLAYER_WALKED.md:34,58,94 (slid off the Z=386 ledge, reached Z=90.15, 'MovementMode transitioned to 1 (Walking)', final readback MovementMode=1). Forced Walking on terrain: drop-sequence-status-s150.md:1093-1108 (play-atlanding-walk KFLYMODE=1 \u2192 SetMovementMode(1) via tutorial_launch.cpp:13205-13207 \u2192 hero walked +1,246 uu with Z=90 the entire time, no init crash). Caveat R-S189-MV-WASD-h: a fall can stop on a non-walkable ledge (Z=386) and stay Falling (s189 F3:32,70).",
     "implication": "The bot does not need a SetMovementMode(Walking) lever; it needs to LAND on walkable floor. The cheapest known way (flown twice) is one horizontal Velocity kick (ARM H, KSHSENTX=600) from its air spawn; the untested-but-cheaper way is spawning it low over a known floor point. A reflected SetMovementMode(1) afterwards is a safe belt-and-braces step only once MovementMode reads 1 or the pawn is measured at Z=90.15."
    }
   ],
   "recommendation": "Do NOT port a SetMovementMode(Walking) lever from the DS route \u2014 the DS 'walk' was a 51.6 uu crawl under a per-hook-hit Walking re-assert on a non-walkable surface (Z 569\u2192577), later abandoned for Flying because of a DS-only kernel-wait drop; and from the bytes, SetMovementMode(Walking) on an airborne pawn reverts to Falling on the next PhysWalking tick (CheckFall 0x035D7C70 \u2192 StartFalling 0x03600890 \u2192 SetMovementMode(3) @0x0360095D), while the Loki override 0x055C0AC0 silently refuses any non-None mode when HeroPredropHidden(+0x1BE8) is set. What the DS route did contribute \u2014 the CDO-subobject borrow + full six-attribute block + AddMovementInput-only driving \u2014 is already in ARM G and S189-MV, and the tutorial route has already exceeded the DS evidence on both pawns via the NATURAL landing transition (bot S140 f3: 13,187 uu; player S189 F3: 2,300 uu). For the joint flight: (1) keep the proven '-mv-play' player chain unchanged (KBSGASPLAYER=0 so the player reads its own spawnedSet0); (2) stage the bot with ARM G + ARM F + ARM H exactly as 'sentinel-big' did (KBSPSARMS bits 0x100|0x80|0x200, KSHSENTX\u2248600 horizontal, KSHSENTZ=0) but set KBSGASMOVESPEED to a non-stock value distinct from the player's 500 (e.g. 450 \u2014 R-S189-MV-a) so the bot's Walking cap is bit-exact to ITS seed; (3) pre-register: bot MovementMode 3\u21921 on landing at Z=90.150, bot |V_xy| cap == KBSGASMOVESPEED bit-exact, player cap == 500 bit-exact, both durable over the 5 ShSampleLoop samples; (4) read HeroPredropHidden by name on the bot in the same census (free, one byte) so a future SetMovementMode refusal can never masquerade as a dead lever; (5) only if the bot stops on a ledge (R-S189-MV-WASD-h) add a second horizontal kick \u2014 never a Walking force. Zero new C++ is required beyond the existing knobs; if the operator wants one fewer injection, the second-cheapest change is to lower the bot spawn Z (a KBSOFFSET-style Z knob) toward a known floor point so gravity alone lands it \u2014 but that is unflown and interacts with the unexplained Vz=0 hover quirk, so the kick recipe should fly first.",
   "open_questions": [
    "What surface did the DS hero 'land' on at Z\u2248569 near the DS-world origin, and why did Z RISE 569.0\u2192576.9 during the Walking re-assert crawl (AdjustFloorHeight step-up? Loki ground-mantle?) \u2014 unrecoverable offline; the S80 MT marker is not tracked in git.",
    "Which class does the Loki SetMovementMode gate IsA-test at 0x055C0AF0 (lazy StaticClass 0x5395720)? Presumed ALokiHeroCharacter (owner of HeroPredropHidden); one strxref/name resolve settles it.",
    "What is HeroPredropHidden(+0x1BE8) on a SpawnAIFromClass bot pawn? Never read; a zero makes SetMovementMode usable, a one makes it a silent no-op. Read it by name in the next A0 census.",
    "Does forcing Walking on a Vz=0 hovering bot (Falling, no floor) break the hover by routing it through StartFalling\u2192StartNewPhysics\u2192PhysFalling in the same frame? The bytes show the path exists; whether PhysFalling then integrates gravity from Vz=0 is the same unexplained quirk S189 F1 saw (9.5 s hover) \u2014 [S], and not needed if the bot is kicked/landed instead.",
    "The 0x035EF8BA SetMovementMode(3) site (between PhysFlying 0x035EF1D0 and PhysWalking 0x035EF960) was not attributed to a function; irrelevant to the recommendation but worth naming if anyone relies on 'Flying has no auto-transition' beyond the live S81/S132 measurements.",
    "Bot peak-speed discriminator: ARM G writes the process-wide Default__LokiPlayerState_HeroAffiliated AttributeSet subobject; if any OTHER live character borrows that CDO set (none known), it would also read the bot's seed \u2014 confirm via the A0/A2 census that only the bot's +0xF08 points at it."
   ],
   "hazards": [
    "A reflected SetMovementMode(non-None) on any hero whose HeroPredropHidden(+0x1BE8) != 0 returns without calling the engine (Loki override 0x055C0AC0) \u2014 indistinguishable from a dead lever unless the byte is read first.",
    "SetMovementMode(Walking) while airborne is reverted by PhysWalking\u2192CheckFall\u2192StartFalling within one tick; a per-frame re-assert (the DS MT trick) alternates modes, fires FindFloor sweeps every frame, and the readback will lie 'mode=1' if sampled after the re-assert \u2014 an instrument artifact already committed once (6a227f9 'Mode now HOLDS at 1').",
    "KBSGASPLAYER=1 (ARM K1) makes the player and the bot read ONE shared CDO attribute set; with it on, 'peak speed == seed' cannot discriminate which pawn's seed was read. Keep it 0 for the joint flight.",
    "ARM G's attribute writes land on a process-wide CDO default subobject (not a per-instance set): any later consumer that borrows the same CDO subobjects inherits the bot's numbers; a bot MoveSpeed seed equal to the player's 500 or to any stock value forfeits R-S189-MV-a.",
    "The DS route's kMoveMode=5 / 'never re-assert' rules stem from a networked-client kernel-wait drop (session-81) that has never reproduced on the standalone tutorial route; importing them as facts about this route would foreclose Walking, which play-atlanding-walk, S140 f3 and S189 F3 all proved works.",
    "A bot landing on a non-walkable ledge (R-S189-MV-WASD-h; the player stopped at Z=386) stays MOVE_Falling and will not transition; pre-register the ledge outcome and its remedy (second horizontal kick) rather than forcing Walking.",
    "FK-32 budget: the bot arms and the player arm are separate manual-maps; the joint flight is at least gft\u2192fo\u2192sp\u2192player(-mv-play)\u2192bot(sentinel-family) = 5 injections, past the modal 4th-injection kill window (~300-350 s) \u2014 capture as you go and take the dumpimage early."
   ]
  },
  "aiwander": {
   "findings": [
    {
     "claim": "No flight contains a sample of the bot at exact rest in MOVE_Walking followed by re-growth; the closest measured re-growths are near-rest, not rest: S141 ARM L s0->s1 (Velocity.Y raw 0x0000000000000000 at +250 ms while Falling, |V_xy|=22 decaying; +750 ms Walking |V|=499.70 on the AI heading +Y within <=250 ms of landing) and s2->s3 (Walking |V|=93.2 wall-blocked at +2000 ms -> 500.0 on a new heading at +5000 ms; the intervening kick B was Z-only and MovementMode stayed 1). S140 +5 s |V|=377.3 is a mid-turn (Accel 42 deg off velocity), not rest.",
     "grade": "M",
     "evidence": "docs/s141-t3-marker-arml.txt:253-283 (s0/s1), :297-334 (s2/s3); docs/s140-t2-marker-armj.txt:275-295; capsule fall 1076->90 uu at -4000 uu/s bounds landing at ~+500 ms",
     "implication": "The 250/750/2000/5000/10000 ms sampler cannot see a rest->cap ramp: with MaxAcceleration 50000 the 0->500 step is one physics substep (10 ms). Re-growth under AI input in Walking mode is demonstrated from |V|=93 and from a fresh landing, but exact-rest-in-Walking on the bot is unmeasured."
    },
    {
     "claim": "The wander is continuous: ALokiBotController::Tick calls [CMC vtable+0x5E0]=UCMC::RequestPathMove(&RandomMoveDirection) on EVERY Tick that passes the gate (0x556EEAB-0x556EEB8, unconditional after the roll block); the 2.0 s timer (+0x5F4, comiss at 0x556EE46, now+2.0f at 0x556EE84) only controls re-rolling. RequestPathMove with Z==0 skips its IsMovingOnGround/IsFalling branch (0x035F41F0/0x035F4200) and feeds Internal_AddMovementInput regardless of MovementMode.",
     "grade": "M",
     "evidence": "merged15 disassembly 0x556EDC1-0x556EEB8; 0x035F41D0-0x035F4227; S138 f8 194 samples: RMD==CIV 193/194, 43 heading changes, intervals 2.0 s (39/42)",
     "implication": "Acceleration is non-zero every frame while the gate is open, so stock PhysWalking->CalcVelocity must integrate from rest exactly as it did for the player in F3 (t=1.184 V=0 -> t=2.889 V=500)."
    },
    {
     "claim": "The first heading roll is IMMEDIATE on the first eligible Tick, not 2 s later: the roll timer +0x5F4 is zero-initialised by the ctor qword store 0x554B589 mov [rdi+0x5f0],rbx (rbx=0 from xor ebx,ebx at 0x554B447), OnPossess 0x5565470 contains no store to +0x5F4, and Tick's bail block (0x556EA87) rewrites +0x5DC/+0x5E4/+0x5E8/+0x5EC/+0x5F0 but not +0x5F4; the roll fires when now > +0x5F4 (=0).",
     "grade": "M",
     "evidence": "merged15: ctor 0x554B430..0x554B671 store scan; OnPossess store scan (none); Tick 0x556EE46-0x556EEA3; consistent with S138 f8 first non-zero direction <=1.5 s after the watcher found the controller while the ladder still held the game thread",
     "implication": "Timing model: gate open -> frame 1 Tick rolls + RequestPathMove -> CIV -> CMC tick -> Accel=|MaxAccel| -> CalcVelocity clamps to MoveSpeed in one substep. Rest->cap ~2 frames (~35 ms at 60 Hz), then heading changes every 2.000 s."
    },
    {
     "claim": "PhysWalking is STOCK engine code on ULokiCMC and the walking path carries no bot-specific or LivingState/controllable gate: StartNewPhysics case 1 dispatches [rax+0x970] (0x03600AA3) and slot 0x970 = 0x035EF960 in BOTH the ULokiCMC vtable (0x088F8570) and the engine vtable (0x07FBED58); CalcVelocity 0x7B0, ComputeAnalogInputModifier 0x660, IsExceedingMaxSpeed 0x4D0 are also unoverridden. The six Loki overrides PhysWalking calls (0x540->0x55BEDB0, 0x610->0x55B1EC0 IsMovingOnGround(Custom7/3 special-case), 0x670->0x55C0AC0 SetMovementMode, 0x720, 0x850->0x55ABC30, 0x950->0x55A5D60) and CalcVelocity's two (GetMaxSpeed 0x55ACB90, GetMaxAcceleration 0x55AC910) write nothing to +0xE8/+0xF0/+0xF8/+0x328 and read neither +0x1090 nor +0x6A0. The only input-path gate is ConstrainInputAcceleration 0x55A75B0 -> IsStunned 0x55B2930, which the S140/S141 walks passed. Engine PhysWalking's rest-related bail (0x035EF9B3-0x035EF9ED) requires Controller==NULL, which a spawned-and-possessed bot never satisfies.",
     "grade": "M",
     "evidence": "merged15 vtable diff (69 differing slots enumerated), capstone operand scan over each override's pdata_union extent, PhysWalking prologue 0x035EF960-0x035EFA6A; S139 flight 1 measured both pawns on the same ULokiCMC vptr",
     "implication": "Bot and player share byte-identical Walking physics; the player's measured walk-from-exact-rest (F3) transfers to the bot in Walking mode with the same seeded attribute set. The bot's zero-from-rest problem is confined to MOVE_Falling (S139 f4 / S141 ARM K hover), which a floor spawn avoids."
    },
    {
     "claim": "The wander gate is the blackboard bool key at .data 0xA0348F0 (FName {0x1A12C,0}), NOT controller+0x6A0: Tick reads it twice via UBlackboardComponent::GetValueAsBool 0x45F13A0 (0x556ECD2 stuck-check gate, 0x556EDD1 wander gate) and never reads +0x6A0; UpdateCharacterControllable 0x5570B80 writes +0x6A0 and then stores the SAME key into the Blackboard with the +0x6A0 value (0x5570BB6 GetKeyID, 0x5570BDD/0x5570BF0 SetValueAsBool). BB_HeroBots has exactly five bool keys (MoveOverride, HasTrainDanger, HasTargetWisp, IsCharacterControllable, ForceMoveOverride); the key's NAME is IsCharacterControllable [I, very strong], the mechanism link is [M]. ARM F (LivingState=1 poke + UpdateCharacterControllable call) alone opened it in S138 f7/f8 and the wander ran 85 s with 43 heading changes.",
     "grade": "M",
     "evidence": "merged15 .data 0xA0348F0 = 0x1a12c; disassembly cited; tools/extractor/out/BB_HeroBots.json Keys[]; docs/s138-f8-marker-armf.txt:130-136; docs/s138-f8-motion.txt",
     "implication": "ARM F is necessary and sufficient for the gate given an ALokiBotController (ARM D) whose OnPossess built the Blackboard; no further poke is needed. Reading +0x6A0 remains a valid mirror readout but is not itself the gate."
    },
    {
     "claim": "Nothing in the wander depends on altitude, floor, or navmesh: the wander block requires Blackboard!=NULL, key true, pawn IsA LokiHeroCharacter (0x54F8DC0), PathFollowingComponent(+0x490)!=NULL with Status(+0x254)!=3(Moving), and the movement component IsA ULokiCharacterMovementComponent (0x554A1A0 via StaticClass getter 0x5309300, named by strxref). No MovementMode, Z, navmesh or pathfinding call exists in Tick. Position-aware code is only the 5-s stuck check (2-D displacement^2 < 10 three times -> DARK action 0x5566870, page all-zero in merged15) and the 3-s circle check 0x5664F70 (returns true when GameState+0x9B0 is NULL).",
     "grade": "M",
     "evidence": "Tick disassembly 0x556EA0D-0x556EEDC; 0x554A1A0/0x5309300; 0x5664F70 tail 0x56650C6 mov al,1; python tools/strxref/strxref.py func 0x5309300",
     "implication": "A floor-spawned bot gets the identical driver the in-air bot got; the only new exposure is the stuck check, which a walking bot (>=93 uu/s) never trips, and the dark 0x5566870 action whose behaviour is unknown (it may be what froze the f8 wander at +115.4 s after 85 s of a stationary pawn)."
    },
    {
     "claim": "A no-kick flight needs ZERO new sampler C++: ARM H writes kShSentinel unconditionally (memcpy at BsPsSentinel, no zero refusal) and sets g_shArmed=1, so -DKSHSENTX=0.0 -DKSHSENTZ=0.0 turns the bot write into a (0,0,0) no-op while keeping ShSampleLoop armed; KBSGASPLAYER defaults 0 so ARM G leaves the player untouched. But KBSPSARMS bits 0x800 (ARM J writes PLAYER Velocity; KSHPLRY defaults 0.0 = a ZERO write onto a walking player), 0x1000 (K2) and 0x2000 (L) must be dropped: use 0x3A0 (D+F+G+H). The spawn location is heroLoc+(KBSOFFSET=600,0,0) with Z inherited from the player (BsResolveAI, tutorial_launch.cpp ~15701); a floor spawn needs either the player already standing on the floor when the bot arm is injected, or ~4 lines of new C++ (absolute KBSSPAWNX/Y/Z knobs).",
     "grade": "M",
     "evidence": "tools/sigbypass-mod/tutorial_launch.cpp:16504-16505, 16974-16991, 17197-17345 (BsPsSentinel/ShSampleLoop), 15685-15760 (BsResolveAI), 17591-17610 (arm order), 25730-25740 (FsDisarm -> ShSampleLoop on the worker)",
     "implication": "The cheapest no-kick bot arm is the existing sentinel-big/axisab family rebuilt with KBSPSARMS=0x3A0, KSHSENTX=0, KSHSENTZ=0, plus non-round attribute seeds; the only genuinely new code is the spawn point."
    }
   ],
   "recommendation": "Probability that a floor-spawned, ARM-D+F+G-treated bot in MOVE_Walking walks under its own wander with NO kick: ~0.80 (walks within the first second of the game thread being released: ~0.7; walks within 60 s: ~0.85). Basis: PhysWalking/CalcVelocity are byte-identical stock on the shared ULokiCMC vtable [M], the player walked from exact rest on that class [M], the bot reached its cap from |V|=93 and from a fresh landing [M], the wander drives RequestPathMove every Tick with an immediate first roll [M]. The residual risk is not the mover but the spawn state: a bot that starts MOVE_Falling at rest is the S139 f4 / S141 ARM K hover (0.000 uu for >=10 s), so spawn WITHIN FindFloor reach of a measured floor point so possession->SetDefaultMovementMode lands it in Walking without a fall.\n\nDesign (fewest injections, minimal C++):\n1. Spawn point: add absolute knobs KBSSPAWNX/Y/Z in BsResolveAI (~4 lines) and use a MEASURED rest point: (1356.434, -409.393, 90.15+20) [S140 bot rested there at Z=90.150] \u2014 700+ uu from the player's F3 walk area (683,-716)->(953,-2108). Zero-C++ alternative: inject the bot arm only after the player is standing on the floor and KBSOFFSET<=200, accepting R-S189-MV-WASD-h terrain risk.\n2. Arms: KBSPSARMS=0x3A0 (ARM D lokibot, ARM F gate, ARM G attrs, ARM H sampler), KSHSENTX=0.0 KSHSENTZ=0.0 (no-op bot write), NO 0x800/0x1000/0x2000. Seed NON-ROUND, PLAYER-DISTINCT values per R-S189-MV-a: KBSGASMOVESPEED=437.0 (player stays 500) and MaxAcceleration=45000 (in the Walking arm GetMaxAcceleration reads AttrSet+0x120, so a bit-exact 45000 |Accel| discriminates Walking-arm reads from the engine's 50000 Super, which the Falling arm returns).\n3. Sampler (ShSampleLoop, worker thread, t0=FsDisarm): keep the game-thread BEFORE block (V, Accel, MovementMode, AIM) as the rest baseline; then Phase A burst every 4 ms for 400 ms (~100 samples) reading Velocity+0xE8, Accel+0x328, MovementMode+0x231, AIM+0x3D0, RootComponent RelativeLocation, controller RandomMoveDirection+0x658 and nextRoll+0x5F4 \u2014 the rest->accelerate->cap transition completes within ~2 frames so only a <=5 ms cadence can order 'Accel first, then Velocity' and catch a sub-cap intermediate at >100 fps; Phase B every 250 ms to 5 s (expect one heading change at ~2.0 s and a mid-turn |V| dip like 377); Phase C every 1 s to 30 s (displacement ~437*t, MovementMode stays 1, stuck counters +0x5FD/+0x5FC stay 0). Record raw, derive later.\n4. Pre-register: P1 BEFORE V=(0,0,0), Accel=(0,0,0), MovementMode==1 (if 3, the run is a Falling-rest test and may hover ~10 s \u2014 still record); P2 first non-zero |Accel| == 45000.0 bit-exact within the first 3 burst samples; P3 peak |V_xy| == 437.000 bit-exact by <=50 ms; P4 RandomMoveDirection non-zero at the first burst sample and changes at 2.000 s multiples; P5 displacement > 400 uu by 1 s; NULL branches: Accel non-zero but V==0 -> Walking-mode zeroing site (new wall); Accel==0 -> gate/AIM/attr problem (read key via GetValueAsBool mirror +0x6A0 and +0xF08).\n5. Budget: gate->cap ~50 ms, so the whole bot experiment needs <60 s after injection; stage bot arm within the FK-32 window (4th manual-map ~300 s) and take the dumpimage EARLY.",
   "open_questions": [
    "MovementMode of a floor-spawned bot at the game-thread BEFORE read (1 vs 3): SetDefaultMovementMode on possession lands Walking only if FindFloor succeeds; if it reads 3 the run is the Falling-rest case (hover) and needs the Z tolerance adjusted \u2014 settle by reading +0x231 before release (already printed by ShDump).",
    "What the DARK stuck action 0x5566870 does (5-s check, 3x <3.16 uu; also the 3-s circle-check path) and whether it is what froze the S138 f8 wander heading at +115.4 s; only a stationary LokiBotController pawn can trigger it.",
    "Identity of the two per-frame helpers 0x55713B0 and 0x5563D50 and the 0.3-s hazard check (0x55AC880 -> 0x44685E0 loop over an array at +0x330/+0x338 -> 0x55708B0): all ran harmlessly in the walking flights but are untranscribed.",
    "Whether the controller Tick precedes the CMC tick within one frame (1- vs 2-frame gate->Accel latency) \u2014 only the <=5 ms burst can tell.",
    "Why a resting MOVE_Falling bot with AI |Accel|=50000 shows no air-control lateral velocity (S139 f4) \u2014 outside this lane but it is the reason a floor spawn, not a short drop, is recommended."
   ],
   "hazards": [
    "ARM D (BsPsLokiBot) spawns THREE pawns A/B/C at the same point; only B gets an ALokiBotController. On a floor spawn all three overlap (bNoCollisionFail=1) and A/C sit as static obstacles the walking B must depenetrate from; consider a knob to skip A/C or accept the first-frame push-out.",
    "KBSPSARMS bit 0x800 (ARM J) writes the PLAYER's Velocity (KSHPLRY defaults 0.0 = zeroing a WASD-walking player); bits 0x1000/0x2000 also touch the player. Use 0x3A0.",
    "ARM G writes the process-wide Default__LokiPlayerState_HeroAffiliated attribute subobject; it is disjoint from the player's spawnedSet0 (+0xF08) but is a class-default mutation.",
    "Sampler cadence of 4 ms for 400 ms = ~100 marker writes; keep them raw and short \u2014 a slow Markerf can itself skew sample timestamps (print GetTickCount per sample).",
    "A wall-pinned bot sits at |V|~93 (S141 s2) and can sit for a full 2-s heading interval; do not read a single sub-cap sample as a zeroing site \u2014 check Accel direction vs velocity and location delta.",
    "The .data value at 0xA0348F0 was read from merged15's spliced .data (page 976/4096 non-zero); it agrees with S137's live 0x0001A12C, but treat any other .data global read this way as suspect."
   ]
  },
  "shimdesign": {
   "findings": [
    {
     "claim": "Four of the six regression gates named in the brief do NOT reproduce from HEAD source today: botai, gasattr, gasattr-ctrl and play. Only -bindavatar-seedmax (b47d1bfd04e44921) and -mv-play (87c764b6d235b7a3) do.",
     "grade": "M",
     "evidence": "python tools/sigbypass-mod/text_digest.py over tools/sigbypass-mod/build/ (artifacts mtime 2026-09-09 14:24, source mtime 13:50, `git status` clean for tutorial_launch.cpp): botai RAW=b620e0ff3279e673 (recorded 5e47c13cf7f0a158), gasattr RAW=29f0fa4efffd01da (recorded 2fcc2536e21f18e3), gasattr_ctrl RAW=4c0ede610baa4629 (recorded 4465ebc4d7168c03), play RAW=31af7667355f39d1 (recorded 9bc10a4552c596e1); seedmax RAW=b47d1bfd04e44921 and mv_play RAW=87c764b6d235b7a3 match. The DEPLOYED tools/sigbypass-mod/tutorial_launch_play.dll (2026-08-16) still digests 9bc10a4552c596e1, and dumps/s141-arms/tutorial_launch_armk.dll = 8278c6031d05756c (the S141 gate) vs today's build armk 3ff3ba9f77ae1438. [I,strong] the mover is commit 4824c77 (2026-09-05, 'S179 fo-verbose'), which added `static PVOID g_vehHandle` at tutorial_launch.cpp:227 and RemoveVectoredExceptionHandler/VirtualFree at Worker exit (`git show 4824c77` hunks @23730/@24695) -- code shared by EVERY run mode; the RM_BOTFIGHT gates were recorded after it (S155/S156, 09-08) and the RM_BOTSPAWN gates were last verified before it (S153, 09-02).",
     "implication": "Any 'gates unchanged' claim this session is void unless it is made against a FRESH baseline. Re-baseline first: rebuild the six from untouched source (the 14:24 artifacts are that baseline if the source is still untouched: botai b620e0ff3279e673, gasattr 29f0fa4efffd01da, gasattr_ctrl 4c0ede610baa4629, play 31af7667355f39d1, axisab 01593c0f8f73f1ae, armk 3ff3ba9f77ae1438), record them, THEN edit. Also update CLAUDE.md's stale RM_BOTSPAWN gate values."
    },
    {
     "claim": "RM_BOTSPAWN does everything in ONE game-thread callback that blocks the game for ~16 s; the worker-thread parts (FsArm/FsDisarm scans) do not block frames.",
     "grade": "M",
     "evidence": "docs/s141-t3-marker-arml.txt: census A0 5078 ms (:19), A1 4344 ms (:30), A2 4266 ms (:249) = 13.7 s of GUObjectArray sweeps inside BsLadderStep (tutorial_launch.cpp:17758-17780, which runs in OnPI on the game thread per :17497-17500 comment); plus Sleep(KBSSETTLEMS=750) (:17765), ARM D 3x Sleep(250) (:16345,:16362,:16371), ARM B Sleep(200) (:16171), ARM F Sleep(300) (:16667) = 1.25 s; the three SpawnAIFromClass calls themselves are unmeasured (no ms field). FsArm 'scan 3860 ms' (:14) and FsDisarm 'scan 3734 ms' (:250) are worker-thread. The sampler's printed 't=+250 ms' is sampler-relative: sample 0 already shows the bot at Z=1076.753 having fallen 12,163 uu (:272) because ~3.7 s of FsDisarm ran between arm and sample 0.",
     "implication": "Designs B/C/D carry one ~16 s freeze (D: ~15.5 s with two spawns skipped); flown >=6 times (gasattr, gasattr-sentinel, sentinel-burst, sentinel-big, armk, axisab) with no immediate kill, so it is a cost not a blocker. Design C adds this block INSIDE the S148 chain's dispatch. Any new sampler must print time-since-arm, not just sampler-relative time."
    },
    {
     "claim": "Edits confined to the KBSGASPLAYER block, the sentinel/sampler blocks, or new preprocessor knobs with no #else cannot move botai/gasattr/gasattr-ctrl/play/-mv-play/seedmax; edits to the unconditional bodies of BsResolveAI or BsPsLokiBot would.",
     "grade": "M",
     "evidence": "K1 block is `#if KBSGASPLAYER ... #endif` (tutorial_launch.cpp:16570-16608, default 0 at :16505); ARM H/J/K2/L are `#if (KBSPSARMS & 0x200)` (:16967), `& 0x800` (:17280), `& 0x1000` (:17297), `& 0x2000` (:17371); ShSampleLoop is only called under `#if (KBSPSARMS & 0x200)` (:25735). BsResolveAI (:15685-15752) is unconditional under KBSAI and BsPsLokiBot (:16306-16378) under KBSPS -- both compiled into botai/lokibot/gasattr. The recorded hazard: adding an `#else` skip-marker to ARM H moved gasattr 2fcc2536e21f18e3 -> 6d81e34e675f97f1 at an unchanged .text size (:17601-17605 comment).",
     "implication": "Every new arm/knob must be `#if KNOB ... #endif` with NO #else and NO marker string outside the guard. Runtime `if(KBSPSARMS&bit)` tests emit the skip-string in the control build (that is why gasattr != gasattr-ctrl) -- do not use that form for new bits."
    },
    {
     "claim": "Skipping ARM D's control spawns A and C (so exactly ONE bot exists) does not break g_psLbCtl[1] indexing or any downstream arm.",
     "grade": "M",
     "evidence": "BsPsLbRead(tag,i) stores g_psLbPawn/g_psLbAic/g_psLbCtl by explicit index (:16183,:16191) and sets g_psLbIsBot only when i==1 (:16193); spawn B always calls BsPsLbRead(\"B-treatment\",1) (:16362); ARM F (:16629), ARM G (:16509), ARM H (:17221) read g_psLbCtl[1] only; the ARM B/C hand-off copies from [1] (:16373-16377); BsFinalReport prints rows 0..2 unconditionally (:24178-24183) and its ARM D verdict keys on g_psLbIsBot && g_psLbPokeOK (:24184); the [BS] VERDICT prints dCtl/dHero dynamically (:24304), not a hard-coded +3.",
     "implication": "A `#if !KBSLBSKIPCTRL` guard around the spawn-A block (:16343-16346) and spawn-C block (:16368-16371) is safe; rows 0/2 print as zeros (cosmetic), the A2 census expectation becomes dCtl=+1/dHero=+1, and the A-B-A control is lost -- acceptable because ARM D is already [M] twice (S137 F3, F4)."
    },
    {
     "claim": "A measured walkable tutorial-floor point exists for a floor spawn: root (1356.434, -409.393, 90.150) MOVE_Walking; the floor surface there is at Z ~ 0 and a spawn at root Z=100 leaves a ~12 uu gap.",
     "grade": "M",
     "evidence": "docs/s140-t2-marker-armj.txt:272 bot loc (1356.434,-409.393,90.150) then walked through (872.694,277.616,90.150) (:287) and (819.725,964.733,90.150) (:302); docs/s141-t3-marker-arml.txt:294-367 same floor Z=90.150 while walking at |Vxy|=500 (e.g. Velocity (-17.254,499.702,0) :278). Capsule half-height 88.0504 (docs/s136-ai-controller-settled.md:38, bot2-on-bot1 dZ=176.1008=2x88.0504) => rest root 90.15 puts the capsule bottom ~2.1 uu above a surface at Z~0. S132 precedent: a hero placed at the PlayerStart's Z=100 settled to 90.15 and stood (docs/s132-dismount-settled.md, dZ=-9.85).",
     "implication": "KBSAILOC=(1356.0,-409.0,100.0) is the evidence-backed floor spawn; ~1,000 uu from the player's ledge at (363,-375,386) and ~700-1,700 uu from the F3 player walk region (683..953, -716..-2108). Z=100 is inside any plausible FindFloor sweep (stock MaxStepHeight 45 - 2.4) so [I] the possess-time SetDefaultMovementMode should come up MOVE_Walking; the Falling branch is covered by the fallback kick."
    },
    {
     "claim": "There is an unresolved within-project contradiction about resting pawns: S136's two plain-AIController pawns spawned at Z=13240 FELL and rested on the floor, while every LokiBotController (ARM D) treatment bot since S138 hovers at (600,0,13240) with V=0 indefinitely.",
     "grade": "M",
     "evidence": "docs/s136-ai-controller-settled.md 1.4: bot1 (600,0,90.15), bot2 (600,0,266.25), 'Both were asked for Z=13240 and both fell to the floor', three reads 3 s apart, vel=0. Versus docs/s138-flight9-movement-not-simulating.md:22,38-41 (LokiBot pawn frozen at (600,0,13240) over 6 s while ControlInputVector churns), docs/s138-f8-motion.txt:6-11 (still at 13240 at +30 s), docs/s141-t3-marker-armk.txt:261,280 (bot 'moved 0.000 uu' after a (0,0,-600) kick). [I] the difference is LokiBotController possession and/or ARM F (LivingState=1 + UpdateCharacterControllable), not 'physics never ticks at Vz=0'.",
     "implication": "A floor-spawned LokiBot pawn may still come up MOVE_Falling at rest 12 uu above the floor and hover; the design must include a fallback horizontal kick from the sampler (a 600 uu/s kick from 12 uu up lands within ~0.1 s -> Walking -> AI drives, the S140/S141 [M] shape). Recording the spawn-time MovementMode on the floor bot is itself a free measurement on this open question."
    },
    {
     "claim": "Design C (fold the bot chain into RM_BOTFIGHT/BfS148DoCalibration) has a timing trap: a ~16 s block placed after the AdjustHealth call but before the WAIT_LATER publish turns RESULT=SELF_DAMAGE_CALIBRATED into RESULT=LATER_TIMEOUT, and RM_BOTFIGHT has no worker-thread sampler slot.",
     "grade": "M",
     "evidence": "tutorial_launch.cpp:23312-23321: s_callTick taken after the call, timeoutTick=s_laterNotBefore(+250 ms)+KBFSELFTIMEOUTMS (=5000, :26), then g_s148Phase=WAIT_LATER; FsHold under KBFSELFCAL (:2515-2527) fires LATER_TIMEOUT -> DONE as soon as phase==WAIT_LATER && now>=timeout. The RM_BOTFIGHT worker path is FsHold -> FsDisarm -> '[BF] worker done' with no post-disarm hook (:25822-25836). The only safe insertion point is the KBFPOKEGRAVITY site (:23097-23147), which runs BEFORE the preflight/call, i.e. before s_callTick.",
     "implication": "Design C is feasible only as: bot block at the POKEGRAVITY site + a new `#if KBFBOTPLAY` worker sampler after FsDisarm + compiling the Bs*/BsPs* statics in via -DKBSAI=1 -DKBSPS=1 on an RM_BOTFIGHT variant. That is ~60-120 lines of glue plus the world-scan cost inside the S148 dispatch -- the largest and least-flown option, with no receipt advantage over D."
    },
    {
     "claim": "The player must NOT be kicked horizontally in any RM_BOTSPAWN-based design: KSHPLRY must stay 0 (and there is no X-kick knob without an edit).",
     "grade": "M",
     "evidence": "kShPlrVel = {0.0, KSHPLRY, 0.0} (:16999); KSHPLRX is defined (:16977) but referenced nowhere else (grep). ARM J's +Y 600 kick sent the player off the island: loc (0,795.558,-10052.914) 'moved 23306 uu' (docs/s141-t3-marker-arml.txt:368); the un-kicked gravity-poked player instead hovered ~9.5 s and fell onto the ledge at Z=386 under (0,0) (docs/s189-mv-wasd-ship-f1-SHIPPING_ARM_VALIDATED.md:64-66; docs/s189-mv-wasd-f3-THE_PLAYER_WALKED.md:32), from which the F3 WASD slide reached the floor and Walking.",
     "implication": "In design B/D use KBSPSARMS without bit 0x800 (no ARM J code at all), K2 (0x1000) with KSHPLRGRAV=1.0 to undo sp's LIFT (tutorial_launch.cpp:13736 writes GravityScale=0.0f), and accept the [I] n=2 ~9.5 s hover before the fall. The F3 slide-off-the-ledge step is manual WASD, as flown."
    },
    {
     "claim": "BfObsMovGetters can be reused as a per-pawn getter receipt inside the RM_BOTSPAWN sampler with no source edit to it: it needs only a hero pointer and g_modBase and is gated on a define, not on the run mode.",
     "grade": "M",
     "evidence": "tutorial_launch.cpp:22326-22403: resolves CharacterMovement by name, validates the ULokiCMC vtable (g_modBase+0x088F8570), dispatches disp 0x4C8/0x7D0 under SEH, prints '[S189-MV] GETTER_%s ... heroF08=... MaxSpeed=%08X(MP=..) MaxAccel=..'; guarded by `#if (KBFSEEDMOVEMENT || KBFOBSMOVGETTERS)` (:22289) with no S148/S155 state referenced. [I] compiles cleanly in an RM_BOTSPAWN variant with -DKBFOBSMOVGETTERS=1 (unverified: no build allowed in this lane).",
     "implication": "Calling BfObsMovGetters(botPawn,\"BOT\") and (playerHero,\"PLR\") right after ARM G/K1 gives 'MaxSpeed=<seed bits>' on BOTH pawns before any motion -- the S189-MV F1-shape receipt that the seed reaches the getter, per pawn, at essentially zero new code (two calls)."
    },
    {
     "claim": "ShDump already prints every field (iii) asks for on both pawns; the dense sampler needs only a length/interval knob, |Vxy| peak tracking, the fallback hook, and suppression of the end-of-run Velocity zeroing.",
     "grade": "M",
     "evidence": "ShDump prints Velocity raw+dec, payload, flag, TimeSinceFallingStart, GravityScale, MovementMode, AnalogInputModifier (by name, with 0x3D0 cross-check), MinAnalogWalkSpeed, GravityDirection, Accel (:17049-17131); ShSampleLoop prints loc + 'moved' per pawn (:17358-17366) at fixed kAt[5]={250,750,2000,5000,10000} (:17347); ARM L's kick-at-sample-2 is the precedent for writing Velocity from the sampler thread (:17371-17395). The end restore writes (0,0,0) to the bot Velocity whenever g_shSentinelOK (:17509-17512) -- with a zero sentinel ShEq3 still returns 1, so it fires and would stop a walking bot once.",
     "implication": "(iii) is a ~30-line change under `#if KSHSAMPLEN` / `#if (KBSPSARMS & 0x4000)`; the restore must be skipped under the new bit."
    },
    {
     "claim": "MoveSpeed via the KBSGASMOVESPEED build knob is the only available bit-exact discriminator in the RM_BOTSPAWN family, and under K1 the player and bot caps are the SAME value by construction (shared CDO subobject).",
     "grade": "M",
     "evidence": "KBSGASMOVESPEED default 500.0f (:16499-16501) feeds MoveSpeed and MaxMoveSpeed (:16550-16551) written to src[1] = the CDO AttributeSet subobject, 'process-wide and shared' (:16575-16580); ARM G's MaxAcceleration is hard-coded 50000.0f (:16552), which collides with engine Super's stock CMC value (S189-MV F1 [M]) and so cannot discriminate. S140 F3/S141 both measured the bot cap at exactly 500 = the default knob value.",
     "implication": "Set -DKBSGASMOVESPEED=487.0f (0x43F38000): a bot peak of exactly 487 proves the value flows from the knob and not from a stock 500 (R-S189-MV-a; a latent ambiguity in S140/S141 since the stock Ronin MoveSpeed is unmeasured). Per-pawn discrimination by speed is only possible in design A (player 500 via its own spawnedSet0, bot 487 via the CDO set); in B/D both read 487 and pawn attribution must come from kinematics (Accel heading rotation vs WASD key windows)."
    },
    {
     "claim": "Design A as written ('-mv-play' then a sentinel-family bot DLL) spawns THREE pawns at one point and relies on the 600 kick; all of its ingredients are individually [M].",
     "grade": "M",
     "evidence": "ARM D spawns A, B, C at g_bsLoc (:16345,:16362,:16371); the S136 pawns stacked (bot2 on bot1); the treatment bot walked after the (600,0,0) kick in S140 F3 (13,187 uu) and S141 axisab (13,196 uu); -mv-play flew 10/10 (docs/s189-mv-wasd-ship-f1-SHIPPING_ARM_VALIDATED.md). g_bsLoc = player hero RelativeLocation + 600 X (:15701-15703), so if injected after the player has landed the bots spawn 600 uu from the player at the player's Z (unmeasured floor), stacked.",
     "implication": "A is the zero-source-change contingency (one build.ps1 line: gasattr-sentinel bits 0x3A0 + KSHSENTX=600 + KBSGASMOVESPEED=487) at the price of a 5th manual-map (FK-32 modal ~300-350 s after the 4th) and three stacked bots; it gives the strongest per-pawn discriminator (500 vs 487)."
    }
   ],
   "recommendation": "RECOMMENDED: Design D built on B -- ONE RM_BOTSPAWN DLL, variant `botplay-floor`, 4 manual-maps (gft -> fo -> sp -> botplay-floor), ~55 new source lines, every line under a NEW preprocessor knob with no #else, plus one `-noai` control line. Why D over the others: A costs a 5th injection and stacks 3 bots; B alone relies on a kick for the bot (attribution of motion to the AI is then indirect) and spawns 3 bots; C is the largest port and walks into the S148 WAIT_LATER timeout trap. D keeps the flown RM_BOTSPAWN ladder byte-for-byte on its flown path (ARM D/B/C/F/G/H/K2) and changes only: WHERE the treatment bot spawns (a measured floor point), HOW MANY bots (one), what the player gets (+0xF08 only), and what the sampler does (40 s, both pawns, fallback kick only if the bot has not moved). Bot motion is attributable to the AI because NO kick is written unless the sampler proves the bot stationary (and then prints that it fired); player motion is attributable to WASD because the player's Accel is (0,-50000,0)-shaped only inside the send-wasd key windows and never rotates, while the bot's Accel rotates through >=3 headings at |A|~50000 (S138 wander signature).\n\nBEFORE ANY EDIT (mandatory, finding #1): rebuild botai/gasattr/gasattr-ctrl/play/axisab/armk/-mv-play/-bindavatar-seedmax from untouched source and RECORD today's RAW digests as the baseline (expected from the 14:24 artifacts: botai b620e0ff3279e673, gasattr 29f0fa4efffd01da, gasattr_ctrl 4c0ede610baa4629, play 31af7667355f39d1, axisab 01593c0f8f73f1ae, armk 3ff3ba9f77ae1438, mv_play 87c764b6d235b7a3, seedmax b47d1bfd04e44921). After the edit: botai/gasattr/gasattr_ctrl/play/mv_play/seedmax MUST be byte-identical to that baseline; axisab/armk/sentinel-* WILL move (the K1 mask `if` and the sampler knob live inside their compiled blocks) -- record their new values. Run `text_digest.py --dupes` (botplay-floor vs botplay-floor-noai must differ).\n\nbuild.ps1 lines (add next to 'axisab', ~line 953):\n  # bits 0x20 D | 0x80 F | 0x100 G | 0x200 H(poison only, zero sentinel) | 0x1000 K2 | 0x4000 M(fallback kick from sampler) = 0x53A0\n  'botplay-floor'      = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x53A0','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKSHPLRGRAV=1.0','-DKBSGASPLAYER=1','-DKBSGASPLAYERMASK=0x2','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=100.0','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMINUU=50.0','-DKSHFALLBACKX=600.0','-DKBFOBSMOVGETTERS=1')\n  # AI-attribution control: identical but ARM F compiled out (gate stays closed -> no wander input) and no fallback: 0x1320. Prediction: bot spawns, comes up Walking or Falling, and does NOT translate.\n  'botplay-floor-noai' = @( ...same..., '-DKBSPSARMS=0x1320' )\n  # Zero-source-change contingency (design A, flown shape): gasattr-sentinel + 600 kick + non-stock seed; inject as a 5th DLL after -mv-play.\n  'botwalk'            = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x3A0','-DKSHSENTX=600.0','-DKBSGASMOVESPEED=487.0f')\n\nSource pseudo-diff (tools/sigbypass-mod/tutorial_launch.cpp; NO #else anywhere, all marker strings inside the guards):\n\n--- knobs, next to KBSAI (:15672)\n+#ifndef KBSAIFLOOR\n+#define KBSAIFLOOR 0      // 1 = spawn the SpawnAIFromClass pawn at KBSAILOC_* (a MEASURED floor point) instead of hero+KBSOFFSET in the air\n+#endif\n+#ifndef KBSAILOC_X\n+#define KBSAILOC_X 1356.0\n+#endif\n+#ifndef KBSAILOC_Y\n+#define KBSAILOC_Y -409.0\n+#endif\n+#ifndef KBSAILOC_Z\n+#define KBSAILOC_Z 100.0  // root Z; capsule bottom ~12 uu above the floor surface (rest root = 90.15, half-height 88.0504)\n+#endif\n+#ifndef KBSLBSKIPCTRL\n+#define KBSLBSKIPCTRL 0   // 1 = ARM D makes ONLY spawn B (no A/C controls) so exactly one bot exists; A-B-A already [M] S137 F3/F4\n+#endif\n\n--- BsResolveAI, after `g_bsLoc[0]+=(double)(KBSOFFSET); g_bsLocOK=1;` (:15703)\n+#if KBSAIFLOOR\n+    Markerf(\\\"[BS] FLOOR OVERRIDE (KBSAIFLOOR): spawnLoc (%.1f,%.1f,%.1f) -> (%.1f,%.1f,%.1f) = S140 T2 F3 rest point, root Z+10\\\\r\\\\n\\\",\n+            g_bsLoc[0],g_bsLoc[1],g_bsLoc[2],(double)(KBSAILOC_X),(double)(KBSAILOC_Y),(double)(KBSAILOC_Z));\n+    g_bsLoc[0]=(double)(KBSAILOC_X); g_bsLoc[1]=(double)(KBSAILOC_Y); g_bsLoc[2]=(double)(KBSAILOC_Z);\n+#endif\n\n--- BsPsLokiBot: (ii) skip the control spawns\n     // ---- spawn A: BASELINE, no poke.\n+#if !KBSLBSKIPCTRL\n     Marker(\\\"[PS] ---- ARM D / SPAWN A (baseline, CDO untouched) ----\\\\r\\\\n\\\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\\\"A-baseline\\\",0);\n+#endif\n     ... (poke, spawn B, restore unchanged) ...\n+#if !KBSLBSKIPCTRL\n     Marker(\\\"[PS] ---- ARM D / SPAWN C (reversal: must be AIController again) ----\\\\r\\\\n\\\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\\\"C-reversal\\\",2);\n+#endif\n  (indexing: B stays index 1; g_psLbCtl[1] consumers unchanged; rows 0/2 print as zeros; A2 expectation dCtl=+1 dHero=+1)\n\n--- BsPsGasAttrs, inside `#if KBSGASPLAYER` (i) the +0xF08-only mask\n+#ifndef KBSGASPLAYERMASK\n+#define KBSGASPLAYERMASK 0x7   // bit0 AbilitySystemComponentStorage(+0xF00) bit1 AttributeSetStorage(+0xF08) bit2 AttributeSetHealthStorage(+0xF10); 0x2 = +0xF08 only (the S189-MV read path; leaves the KWIREGAS ASC untouched)\n+#endif\n         for(int i=0;i<3;i++){\n+            if(!(((KBSGASPLAYERMASK)>>i)&1)){ Markerf(\\\"[GASX]   PLR %-30s MASKED OUT (KBSGASPLAYERMASK=0x%X)\\\\r\\\\n\\\",kDst[i],(unsigned)(KBSGASPLAYERMASK)); continue; }\n             uint32_t po=PropOffsetSuper(ClassOf(pp),kDst[i]);\n  ... and right after `ARM K1 done` (still inside #if KBSGASPLAYER):\n+#if KBFOBSMOVGETTERS\n+        BfObsMovGetters(pp,\\\"PLR_AFTER_K1\\\");          // '[S189-MV] GETTER_PLR_AFTER_K1 ... heroF08=<cdoSet> MaxSpeed=43F38000(487.00)'\n+        BfObsMovGetters(pawn,\\\"BOT_AFTER_G\\\");          // same on the bot -- the per-pawn seed-reaches-getter receipt\n+#endif\n\n--- ShSampleLoop (iii)+(fallback), all inside the existing `#if (KBSPSARMS & 0x200)`\n+#ifndef KSHSAMPLEN\n+#define KSHSAMPLEN 0        // 0 = the flown 5-point schedule (byte-identical); N>0 = N samples every KSHSAMPLEMS\n+#endif\n+#ifndef KSHSAMPLEMS\n+#define KSHSAMPLEMS 1000\n+#endif\n+#if (KBSPSARMS & 0x4000)\n+#ifndef KSHFALLBACKAT\n+#define KSHFALLBACKAT 8\n+#endif\n+#ifndef KSHFALLBACKMINUU\n+#define KSHFALLBACKMINUU 50.0\n+#endif\n+#ifndef KSHFALLBACKX\n+#define KSHFALLBACKX 600.0\n+#endif\n+static const double kShFallback[3]={(double)(KSHFALLBACKX),0.0,0.0}; static int g_shFallbackFired=0;\n+#endif\n+static double g_shPeakXY[2]={0,0}; static DWORD g_shArmTick=0;   // set g_shArmTick=GetTickCount() at `g_shArmed=1` in BsPsSentinel\n static void ShSampleLoop(){\n-    static const DWORD kAt[5]={250,750,2000,5000,10000};\n-    ... for(int i=0;i<5;i++){ if(kAt[i]>prev) Sleep(kAt[i]-prev); prev=kAt[i];\n+#if KSHSAMPLEN\n+    const int kN=(KSHSAMPLEN); \n+    for(int i=0;i<kN;i++){ Sleep((DWORD)(KSHSAMPLEMS));\n+        Markerf(\\\"[SNP] ---- sample %d at t=+%lu ms (sinceArm %lu ms) ----\\\\r\\\\n\\\",i,(unsigned long)(GetTickCount()-t0),(unsigned long)(GetTickCount()-g_shArmTick));\n+#else\n+    static const DWORD kAt[5]={250,750,2000,5000,10000};   // unchanged flown schedule\n+    for(int i=0;i<5;i++){ if(kAt[i]>prev) Sleep(kAt[i]-prev); prev=kAt[i]; Markerf(... existing line ...);\n+#endif\n         ... existing ShDump(BOT/PLR) + loc/moved block unchanged ...\n+        // per-pawn horizontal speed + running peak, printed RAW (%.10g) so a cap of exactly 487 is visible\n+        for(int k=0;k<2;k++){ uintptr_t cmc=k?g_shPlrCmc:g_shBotCmc; if(!LooksLikePtr(cmc)||!SafeReadable((void*)(cmc+0xE8),24))continue;\n+            const double* v=(const double*)(cmc+0xE8); double vxy=__builtin_sqrt(v[0]*v[0]+v[1]*v[1]); if(vxy>g_shPeakXY[k])g_shPeakXY[k]=vxy;\n+            Markerf(\\\"[SNP] %-14s |Vxy|=%.10g peak=%.10g seed=%.10g atCap=%s\\\\r\\\\n\\\",k?\\\"PLR\\\":\\\"BOT\\\",vxy,g_shPeakXY[k],(double)(KBSGASMOVESPEED),(vxy==(double)(float)(KBSGASMOVESPEED))?\\\"EXACT\\\":\\\"no\\\"); }\n+#if (KBSPSARMS & 0x4000)\n+        if(i==(KSHFALLBACKAT)&&!g_shFallbackFired&&LooksLikePtr(g_shBotCmc)){\n+            double d=<distance of bot root+0x158 from g_shLoc0[0]>;\n+            if(d<(double)(KSHFALLBACKMINUU)){ memcpy((void*)(g_shBotCmc+0xE8),kShFallback,24); g_shFallbackFired=1; <re-latch g_shLoc0[0]>;\n+                Markerf(\\\"[SNP] FALLBACK KICK fired at sample %d: bot moved only %.3f uu -> Velocity=(%.1f,0,0) readback %s. Motion after this is S140-shape (kick+AI), NOT pure AI.\\\\r\\\\n\\\",i,d,(double)(KSHFALLBACKX),ShEq3(g_shBotCmc+0xE8,kShFallback)?\\\"OK\\\":\\\"FAILED\\\"); }\n+            else Markerf(\\\"[SNP] FALLBACK NOT NEEDED at sample %d: bot moved %.3f uu with NO kick written -> bot motion is AI-driven.\\\\r\\\\n\\\",i,d); }\n+#endif\n     }\n+    Markerf(\\\"[SNP] PEAK |Vxy| BOT=%.10g PLR=%.10g seed=%.10g fallbackFired=%d\\\\r\\\\n\\\",g_shPeakXY[0],g_shPeakXY[1],(double)(KBSGASMOVESPEED),<fired or -1>);\n     ... existing VERDICT unchanged ...\n+#if !(KBSPSARMS & 0x4000)\n     if(LooksLikePtr(g_shBotCmc)&&g_shSentinelOK&&...) { restore Velocity -> (0,0,0) }   // unchanged bytes in every existing variant\n+#endif\n\nLine budget: knobs ~14, floor override 5, skip-ctrl 4, mask 5, getter receipts 4, sampler ~30 => ~60 lines; new .text only in botplay-floor/-noai; axisab/armk/sentinel-* digests move (K1 `if` + sampler `#if`), the six named gates do not (all edits are under new knobs or inside blocks already compiled out of them).\n\nInjection/timing: 4 manual-maps; one ~15.5 s game-thread freeze at the arm hit (~T+8 s after inject); FsDisarm 3.7 s; sampler 40 s on the worker. Player: K2 gravity=1 -> hover ~9.5 s (n=2) -> falls to the ledge Z=386 -> operator runs send-wasd (pre-warmed) -> slides to the floor -> Walking -> W drives at the 487 cap. Bot: on the floor from t=0; Walking (predicted) or Falling-hover (fallback kick at sample 8, ~+8 s). Expected both-walking window opens ~T+30-45 s after the hit, well inside the ~300 s FK-32 window. Take a dumpimage EARLY (after the sampler), and run tools/re/motion_watch.py externally on both pawns during the window.\n\nReceipt lines to PRE-REGISTER (in order): (1) `[BS] census A0 ... LokiHeroCharacter-chain=2` + `heroCand[1] ... 'BP_HERO_Ronin_C' ... USABLE` + `[BS] heroLoc = (0.0, 0.0, 13240.0)`; (2) `[BS] FLOOR OVERRIDE (KBSAIFLOOR): ... -> (1356.0,-409.0,100.0)` and `[BS] ARGS: ... spawnLoc=(1356.0, -409.0, 100.0)`; (3) `[BS] census A1` == A0; (4) `[PS] POKE AIControllerClass ... READBACK OK`, `[PS] ARM D B-treatment ... BotController-chain=YES`, `[PS] RESTORE ... READBACK OK`, ARM B `ctl.PS` non-null, ARM C `pawn.PS == ctl.PS`; (5) `[PS] ARM F AFTER (faulted=0): LivingState=1  GATE+0x6A0=1  (was 0)`; (6) `[GASX] dst ... OK` x3, `[GASX] attr MoveSpeed @+0xF0 = 487 readback 487 OK` (and MaxMoveSpeed 487, MaxAcceleration 50000, GroundFriction 8, Braking 2048, Mass 100), `[GASX] PLR AbilitySystemComponentStorage MASKED OUT`, `PLR AttributeSetStorage @0xF08 0 -> <cdoSet> OK`, `PLR AttributeSetHealthStorage MASKED OUT`, `ARM K1 done: PLAYER storages written 1/3`; (7) `[S189-MV] GETTER_BOT_AFTER_G ... vtblExact=yes heroF08=<cdoSet> MaxSpeed=43F38000(MP=487.00)` and `GETTER_PLR_AFTER_K1 ... heroF08=<same cdoSet> MaxSpeed=43F38000` (same set pointer on both = shared CDO subobject); (8) `[SNP] BOT ... CONTROL1 PASS / CONTROL2 PASS`, `[SNP] BOT-before GravityScale@0x1A0=1.0000 MovementMode@0x231=1 (MOVE_Walking)` [branch: =3 -> the hover branch, fallback expected] with loc (1356,-409,~90.15..100); `[SNP] PLR-before GravityScale=0.0000 MovementMode=3` at (0,0,13240); (9) `[SNP] BOT poison written -> readback OK`, `[SNP] BOT sentinel Velocity = (0, 0, 0) -> readback OK`, `[SNP] ARM K2: PLAYER GravityScale@0x1A0 0.0000 -> 1.0000 readback OK`; (10) `[BS] census A2 BotOrAIController-chain=1 LokiHeroCharacter-chain=3`, `[FS] disarm: restored=17563 of 17563`; (11) sampler: BOT MovementMode=1 by sample 2, BOT `moved` >= 50 uu by sample 8 and `[SNP] FALLBACK NOT NEEDED`, BOT Accel |A|~50000 with >=3 distinct headings over 40 samples, BOT AnalogInputModifier=1, BOT `|Vxy| ... atCap=EXACT` at least once and never > 487.0001, BOT Z=90.150 while walking; PLR Z=13240 for <=15 s then falling (Vz -> -4000), landing Z=386 (ledge) or 90.15, PLR Accel=(0,0,0) outside key windows and (0,-50000,0) inside W windows (send-wasd timestamps), PLR |Vxy| peak == 487 inside W windows only, PLR MovementMode 3->1 after the slide; (12) `[SNP] PEAK |Vxy| BOT=487 PLR=487 seed=487 fallbackFired=0`; (13) `[BS] done (step=4 resolved=1 called=1 faulted=0 dCtl=1 dHero=1 hits=1 hitsGT=1)`; (14) crashwatch: no 0x0000DEAD before the both-walking window; canaries 0 Fatal / 0 crashpad. Pre-registered disjunction for the control `botplay-floor-noai`: bot never translates (moved < 50 uu over 40 s) and Accel stays (0,0,0) -- any bot motion there would mean floor contact alone moves it and the AI attribution is void.\n\nAttribution rules to write down BEFORE flying: bot motion = AI iff `FALLBACK NOT NEEDED` printed AND Accel heading rotates; if `FALLBACK KICK fired`, downgrade to the S140 shape (kick + AI steering, [M] separately). Player motion = WASD iff every non-zero PLR Accel sample lies inside a logged key window and has the (0,-50000,0) sign for W (F1 [M]); a PLR Accel outside a key window means cross-contamination (e.g. the shared CDO set) and voids the player half. Speed = seed iff peak |Vxy| == 487 exactly (not 500): that is the single-variable proof the value flows from the knob (R-S189-MV-a / R-S189-MV-WASD-i).",
   "open_questions": [
    "Why did S136's two plain-AIController pawns spawned at Z=13240 fall and rest on the floor (docs/s136-ai-controller-settled.md 1.4) while every LokiBotController/ARM-F treatment bot since S138 hovers at (600,0,13240) with V=0 (s138-flight9:22-41, s138-f8-motion:6-11, armk marker 'moved 0.000 uu' x5)? The ARM D control pawns A/C were never position-read in S137-S141. This decides whether a floor-spawned LokiBot pawn comes up Walking or hovers 12 uu up; design D's fallback kick covers both, and the BOT-before MovementMode line will measure it for free.",
    "Does a LokiBotController-possessed pawn spawned with root Z=100 over a Z~0 floor come up MOVE_Walking at possess time (stock UE Restart -> SetDefaultMovementMode -> FindFloor within MaxStepHeight)? [I] from stock UE; this build's MaxStepHeight and any ULokiCMC FindFloor override are unmeasured.",
    "Does the player walk under WASD with +0xF08-only K1 wiring to the shared CDO AttributeSet (GetMaxSpeed reads hero+0xF08 -> set+0xF0/+0x100 [M], but the CDO-set-on-player composition has never been flown with input; axisab's K1 player had no input driver)?",
    "Is the player's ~9.5 s hover-then-fall after a GravityScale 0->1 poke reproducible without a kick (n=2: F3 and ship-F1), and what triggers the kick-off? If it does not fall within ~15 s the flight's player half stalls on the hover.",
    "Does ARM G's write to the process-wide CDO AttributeSet subobject affect any third pawn (e.g. the LokiHeroCharacterGrid hero or later-spawned actors) in a way that confounds the census or the world?",
    "Does BfObsMovGetters compile and link cleanly in an RM_BOTSPAWN variant with -DKBFOBSMOVGETTERS=1 (it is self-contained by inspection, tutorial_launch.cpp:22326-22403, but no build was allowed in this lane)?",
    "Which commit actually moved the RM_BOTSPAWN-family .text digests -- 4824c77 (2026-09-05, shared g_vehHandle + Worker-exit cleanup) is the only candidate touching shared code between S153's last verification (09-02) and today, but this needs one rebuild at 4824c77^ vs 4824c77 to be [M].",
    "How long does the bot's random wander keep it on the floor before it walks off an edge (S140 F3 'finally walked off the island edge')? That bounds the both-walking observation window; the 40 s sampler may need to be shorter or the spawn point more central."
   ],
   "hazards": [
    "Gate-baseline hazard [M]: botai/gasattr/gasattr-ctrl/play as recorded in CLAUDE.md (5e47c13cf7f0a158 / 2fcc2536e21f18e3 / 4465ebc4d7168c03 / 9bc10a4552c596e1) no longer reproduce from HEAD; a session that edits first and then finds them 'changed' will misattribute a pre-existing drift to its own edit, and a session that skips the check will falsely certify 'unchanged'. Re-baseline from untouched source before editing.",
    "Preprocessor-vs-runtime guard trap: an `#else` skip-marker or a runtime `if(KBSPSARMS&bit)` in shared code emits strings into the control builds and moves their digests (S140: gasattr 2fcc->6d81 at identical size, tutorial_launch.cpp:17601-17605). All new code must be `#if KNOB ... #endif` with no #else; run text_digest.py --dupes and confirm botplay-floor != botplay-floor-noai.",
    "The ~15-16 s single game-thread freeze in RM_BOTSPAWN (censuses A0/A1/A2 13.7 s + sleeps) happens while the player is hovering/falling; flown >=6 times without immediate kill but it is the largest freeze any arm imposes. Removing A1/A2 would need an unconditional BsLadderStep edit (moves botai) unless newly knob-gated.",
    "ARM G writes the CDO's DEFAULT SUBOBJECT AttributeSet (process-wide, never undone): under K1 the player and bot share one attribute block, so a 'bot' MoveSpeed change is also a player change; per-pawn speed discrimination is impossible in B/D (only in A).",
    "With a zero ARM H sentinel the existing end-of-sampler restore still writes (0,0,0) to the bot Velocity (:17509-17512, g_shSentinelOK is 1 for a zero write) and the verdict block mislabels 'payload == sentinel' as StartNewPhysics-ran when the sentinel is (0,0,0); the new 0x4000 bit must skip the restore and the verdict line should be read as cosmetic.",
    "Do NOT give the player a horizontal kick (KSHPLRY must be 0; KSHPLRX is an unused define): the +Y 600 kick sent the player off the island to Z=-10052 (arml:368); the un-kicked gravity-poked player lands on the ledge at Z=386 from which the F3 WASD slide works.",
    "Design A's 5th manual-map: FK-32 (0x0000DEAD) fires ~300-350 s after the 4th injection in most tutorial sittings and the both-walking window would open ~T+235 s; also its three ARM D spawns stack at one point (S136 bot2-on-bot1) and, if injected after the player lands, on unmeasured floor.",
    "Design C's S148 timeout trap: any multi-second bot block placed between the AdjustHealth call (s_callTick, :23312) and the WAIT_LATER publish (:23321) produces RESULT=LATER_TIMEOUT (KBFSELFTIMEOUTMS=5000) instead of SELF_DAMAGE_CALIBRATED; the only safe site is before the preflight (the POKEGRAVITY block).",
    "bNoCollisionFail=1 (AlwaysSpawn) at a floor point: a root Z below ~90 would spawn the capsule intersecting the floor; keep KBSAILOC_Z in [92,100]. Also do not put the treatment bot where the player will land (the ledge at (363,-375,386)) -- (1356,-409) is ~1 km away.",
    "The sampler's printed t is sampler-relative; ~3.7 s of FsDisarm elapse between the arm and sample 0 (bot had fallen 12,163 uu by 'sample 0' in arml:272). Print time-since-arm or every timing inference about the first seconds is wrong.",
    "Off-thread sampler reads carry a TOCTOU/GC race (existing SEH guard at :17369); a 40 s sampler on a bot that walks off the island and gets destroyed must not void earlier samples -- capture as you go (the existing per-sample __try does this).",
    "The player's transition to Walking requires the manual WASD slide off the ledge (F3 method); pre-warm send-wasd's Add-Type + focus grab (ship-F1 lost the window to a ~35 s orchestration delay) or the player half is not observed inside the FK-32 window."
   ]
  },
  "flightops": {
   "findings": [
    {
     "claim": "A NON-S148 probe passes configs/fk24-stage.ps1 without -BindOnly. Every S148/S149 identity, provenance, ledger and frozen-hash gate is inside `if($BindOnly)`; the plain path runs only Assert-Fresh (deployed-vs-build\\ .text compare, skipped when no build\\ twin exists) and then `Stage-Inject $Probe 4`.",
     "grade": "M",
     "evidence": "configs/fk24-stage.ps1:640 (`$frozenS148 = ...` inside the `if($BindOnly){` block opened at :626), :514 (Assert-Fresh looks for `<dir>\\build\\<leaf>` \u2014 a probe already under tools\\sigbypass-mod\\build\\ has no twin), :1085 (`Stage-Inject $Probe 4 ('probe-'+...)` in the `else` of the BindOnly branch), :1086 ('probe injected; armed window begins'). Live precedent: ship-f1 staged the RM_BOTFIGHT `-mv-play` DLL with plain `-Probe` (docs/s189-mv-wasd-ship-f1-SHIPPING_ARM_VALIDATED.md 'Flight sequence' step 4; docs/fk24-stage-s189mv-wasd-ship-f1-4-probe-*.txt exists). The S140/S141 RM_BOTSPAWN sentinel flights used the same stager for staging and hand-injected the arm (docs/s141-t3-arml-result.md:129 'the if(Get-Process \u2026) guard on the injection').",
     "implication": "Point -Probe at tools\\sigbypass-mod\\build\\tutorial_launch_<variant>.dll and use -Label; no -BindOnly, no hash pins, no ledger. Only the S149 route pins the probe hash."
    },
    {
     "claim": "The stager's own copy of the probe-stage marker is taken 2 s after the manual-map and therefore contains only the shim header; all RM_BOTSPAWN/[SNP]/[BS] output written later is NOT preserved by the stager, and any later injection truncates it (FK-25).",
     "grade": "M",
     "evidence": "configs/fk24-stage.ps1:918 (`Start-Sleep -Seconds 2`) then :929 (`Copy-Item $marker $dst`); ship-f1's docs/fk24-stage-s189mv-wasd-ship-f1-4-probe-*.txt is 649 bytes vs the 16,458-byte scratchpad/s189-mv/evidence/ship-f1-marker.txt saved by hand; docs/fk24-stage-s189mv-wasd-ship-f1-3-sp.txt is 277 bytes (the [SP] done / [LIFT] lines arrive ~12 s later). Marker() opens FILE_APPEND_DATA with FILE_SHARE_READ|FILE_SHARE_WRITE per write and closes (tutorial_launch.cpp:658); truncation is a CREATE_ALWAYS at DllMain start (:24991).",
     "implication": "An external 250 ms shadow-copy loop (FileShare ReadWrite) can read the marker at any time and must be running for the whole flight; it is the only thing that preserves the sp generation (hero/CMC addresses) and the probe generation if a 5th injection is ever made."
    },
    {
     "claim": "The player hero and its CMC are obtainable BEFORE the probe lands, from the sp generation of the marker: `[SP] done step=4 spawnedPawn=0x<hero>` and `[LIFT] gravity OFF (CMC=0x<cmc> GravityScale@0x1A0)`. The bot pawn and CMC come from the probe's `[SNP] BOT            pawn=0x<pawn> CharacterMovement@0x458 = 0x<cmc>` line, written on the game thread during the ladder (before the kick).",
     "grade": "M",
     "evidence": "docs/s138-f2-marker-staged-sp.txt lines '[LIFT] gravity OFF (CMC=0x1E8994138F0 GravityScale@0x1A0)' and '[SP] done step=4 spawnedPawn=0x1E904775580'; source tutorial_launch.cpp:13736 and :25897. Bot line format from ShResolveCmc at tutorial_launch.cpp:17176 and the live instance docs/s141-t3-marker-arml.txt:186 (`[SNP] BOT            pawn=0x25790ECAAC0 CharacterMovement@0x458 = 0x2578982AAB0`), :192 for PLAYER. Player CMC offset cross-check: ship-f1 marker `POKEGRAVITY ... cmcOff=0x458`.",
     "implication": "Player watcher (motion_watch_player.py <PID> <HERO> <CMC>) can start at [SP] done, ~8-20 s before the probe; the bot watcher starts ~10 s after the probe from the [SNP] BOT line, i.e. still before the game thread is released and the kick becomes observable."
    },
    {
     "claim": "RM_BOTSPAWN probe timeline (KFSNAME=\"\"): FS arm scan ~3.7-3.9 s; first game-thread hit within 8 s; the ladder then HOLDS the game thread ~10-15 s (world frozen: `[FS] t=+15s hitsGT=1 called=0`); FsDisarm scan ~3.7 s; then the ARM H sampler runs on the WORKER thread with samples at +250 ms/+750 ms/+2 s/+5 s/+10 s; `[BS] done` closes the probe \u224835-45 s after injection.",
     "grade": "M",
     "evidence": "docs/s141-t3-marker-arml.txt:14 (arm scan 3860 ms), :17-25 (hot 0 in 4000 ms; ARMED AND LIVE hitsGT=1 after 8000 ms), :74 (`[FS] t=+15s hitsGT=1 called=0`), :250 (disarm scan 3734 ms), :252-368 (samples at +250..+10000 ms), :407 (`[BS] done`). Code: tutorial_launch.cpp:17756 KBSSETTLEMS 750, :25735-25737 (ShSampleLoop after FsDisarm), :17347 kAt[5]={250,750,2000,5000,10000}, :1607 (DoBotSpawn on the OnPI hit).",
     "implication": "Send the first WASD hold only after `[FS] disarm:` appears (keydowns sent while the game thread is frozen decay inside the hold). The shim's 5 [SNP] PLR samples then double as an in-shim receipt of player Acceleration during W#1."
    },
    {
     "claim": "The usable armed window after the 4th manual-map is ~140-175 s, not ~300 s: FK-32 fires ~320-350 s after LAUNCH, and the probe lands at ~launch+170 s under the standard stager timings.",
     "grade": "M",
     "evidence": "scratchpad/s189-mv/evidence/ship-f1-crashwatch.log: crashwatch armed at launch (outDir crash-20260909-133723), `exit code 57005 (0x0000DEAD) ... elapsed 342.2s`; stage copies fo 13:39:34, sp 13:39:55, probe 13:40:15 (each = inject+2 s) \u21d2 probe at ~13:40:13 = launch+170 s \u21d2 ~172 s of armed window. docs/s140-tier2-sentinel.md:388-390: kills at 'T+350.5 s' and 'T+318.0 s' process-elapsed on the 4th manual-map (same stager, same ~+175 s injection point \u21d2 ~140-175 s windows). Contrast S161 T1/T2 without an activation: 425-585 s.",
     "implication": "Everything that needs the live process \u2014 bot walk >10 s, three W-holds, both watchers overlapping, dumpimage, movementmode_readout snapshot \u2014 must be scheduled inside probe+120 s. Treat probe+150 s as the deadline; anything later is a bonus."
    },
    {
     "claim": "The ~35 s WASD latency on ship-f1 was the cold child PowerShell spawned by fly-wasd.ps1 (powershell start + Add-Type C# compile + focus grab), and send-wasd.ps1 already skips Add-Type when the type exists in the SAME session (`if (-not ('S189WasdInput' -as [type]))`), so pre-warming is a no-code fix: run send-wasd.ps1 once in the elevated console you will send from (before the game exists \u2014 it throws at Get-Process AFTER compiling), then invoke it in-process with `&`, never via fly-wasd.ps1's `& powershell -File` child.",
     "grade": "M",
     "evidence": "scratchpad/s189-mv/tools/fly-wasd.ps1 (`$wasdOut = & powershell $wasdArgs` \u2014 a fresh process per flight; motion probe started 2 s earlier); ship-f1 evidence: motion stderr start 13:42:11, sync SEQUENCE_START 13:42:46.847 (35 s), wasd.log shows focusOk=True with no retries; send-wasd.ps1 lines 38-70 (Add-Type guarded by `-as [type]`), Get-Process -ErrorAction Stop at line ~105 (after Add-Type), `$ErrorActionPreference='Stop'`.",
     "implication": "Expected input latency after pre-warm \u2248 0.3-1.2 s (the focus grab's sleeps). Verify the pre-warm with `[bool]('S189WasdInput' -as [type])` before staging."
    },
    {
     "claim": "The player's post-gravity hover is NOT ~9.5 s; it lasted ~115 s on ship-f1 and >6 s (\u226445 s) on F3, and in both cases the fall began within seconds of a new console window being created by fly-wasd.ps1 (a foreground transition away from the game).",
     "grade": "I",
     "evidence": "ship-f1: POKEGRAVITY landed \u224813:40:25 (probe 13:40:13 + chain); motion CSV started 13:42:11 and the first Vz<0 row is t=9.810 s \u21d2 fall onset \u224813:42:20.8; fly-wasd's child powershell was spawned \u224813:42:13; SEQUENCE_START 13:42:46 (the focus grab happened ~13:42:45, AFTER the fall). F3: f3-postgravpoke-fall.csv (6 s from 13:20:29) contains no Vz<-1 row; f3-grav1-Whold-sync SEQUENCE_START 13:21:13 with the hero already at Z=386 \u2014 that flight's child powershell was spawned ~13:20:38. Postflight ship-f1: `root+0x158 Location = (0.000, 0.000, 385.986)` Mode=3.",
     "implication": "Do not wait passively. After the shim's gravity poke, force a foreground transition deliberately (the pre-warmed sender's focus grab, or ShowWindow minimize/restore on the game hWnd) and watch the player CSV for Vz<0 within ~10 s; if still Z=13240 after 20 s, repeat. Grade the trigger [I] and record the timestamps so a successor can settle it."
    },
    {
     "claim": "A one-time kick cannot masquerade as AI drive if the readout requires (a) MovementMode=1 with Vz=0, (b) |V_xy| pinned at the seed on \u22652 non-adjacent samples with the heading DIFFERENT between them, (c) Acceleration magnitude \u224850000 whose heading rotates, and (d) motion persisting >10 s after the single 24-byte Velocity write \u2014 because with BrakingDecelerationWalking=2048 an undriven 500 uu/s pawn would stop in ~0.25 s and its Acceleration would read (0,0,0).",
     "grade": "M",
     "evidence": "docs/s141-t3-marker-arml.txt:256-367: kick (600,0,0) at t=0 (:222), then Velocity (-17.25,499.70,0) at +750 ms, (251.65,-432.05,0) at +5 s, (-499.88,-11.05,0) at +10 s \u2014 three distinct headings, all |V_xy|=500.0, Accel (\u22121725,49970,0)/(25165,\u221243205,0)/(\u221249988,\u22121105,0) rotating with |A|\u224850000, Mode 3\u21921 at +750 ms (:280); BrakingDecelerationWalking seed 2048 at tutorial_launch.cpp:16554; ARM K control (vertical kick from rest) moved 0.000 uu \u00d75 (docs/s141-t3-arml-result.md \u00a72 table).",
     "implication": "Success reads from the bot CSV: \u22653 distinct velocity headings (10\u00b0 bins) at |V_xy|>300 over \u226530 s, Mode=1, peak==seed to the printed precision, plus motion_watch.py's RandomMoveDirection churn as the driver receipt."
    },
    {
     "claim": "motion_watch.py (tools/re) reads the bot's controller gate, LivingState, Blackboard, RandomMoveDirection, CIV and location only \u2014 NOT Velocity, Acceleration or MovementMode \u2014 so it cannot decide the 'walked at the seeded cap' criterion; motion_watch_player.py (scratchpad/s189-mv/tools) reads Vel/Acc/Mode/GravityScale/AnalogMod and is pawn-agnostic (takes <PID> <PAWN> <CMC>), so a second instance on the bot is the right cap/mode instrument. Its CSV prints 3-decimal floats; the shim's [SNP] lines print %.10g and are the stronger bit-exactness receipt for the first 10 s.",
     "grade": "M",
     "evidence": "tools/re/motion_watch.py lines 44-45 (CTL_* / PAWN_* offsets only), sample loop reads GATE/Living/force/Blackboard/RMD/CIV/loc; scratchpad/s189-mv/tools/motion_watch_player.py header + offsets (CMC_VEL 0xE8, CMC_ACCEL 0x328, CMC_MODE 0x231) and `%.3f` formatting; tutorial_launch.cpp:17054-17056 (`%.10g`). find_bot latency: docs/s138-f8-motion.txt:3 `FOUND ... (t=+29.8s)` (includes waiting for ARM D).",
     "implication": "Run THREE read-only samplers: motion_watch_player on the player (from [SP] done), motion_watch_player on the bot (from the [SNP] BOT line), motion_watch.py for the bot's driver chain (from [SP] done, DUR \u2265400). Both motion_watch_player CSVs carry t_wall epoch seconds, which is what 'both alive at the same timestamp' is read from."
    },
    {
     "claim": "The single-probe, four-injection design (one RM_BOTSPAWN build.ps1 variant: ARM D|F|G|H + K2 with KBSGASPLAYER=1, KSHSENTX=600, NO ARM J) reuses only ingredients flown live \u2014 but 'K1-treated player + WASD' has never been exercised: in every KBSGASPLAYER=1 flight the player had no input driver (Acceleration (0,0,0) throughout).",
     "grade": "S",
     "evidence": "tutorial_launch.cpp:16570-16597 (ARM K1 overwrites all THREE player storages incl. +0xF00 with the CDO subobjects); build.ps1:929 sentinel-big (0xBA0 = J|H|G|F|D), :941 armk (0x1BA0 adds K2), :953 axisab; docs/s141-t3-arml-result.md \u00a71 PLAYER rows `Accel (0,0,0)` at every sample and 'the player has no input driver' (\u00a74). The proven player WASD chain (F1/F3/ship-f1) used hero+0xF08 = ASC.SpawnedAttributes[0] with the KWIREGAS ASC left at +0xF00.",
     "implication": "The runbook must be able to separate 'K1 broke player input' from 'focus failed': focusOk=True in the sync file AND Acceleration==0 during a W-hold in the player CSV \u21d2 input path broken (K1 suspect); focusOk=False \u21d2 uninterpretable. PLAN B (5th injection of a bot-only arm after -mv-play) avoids the [S] at the cost of an extra manual-map."
    },
    {
     "claim": "External RPM instruments (motion watchers, preflight_read, movementmode_readout, dumpimage) are pure ReadProcessMemory and do not suspend the game; dumpimage takes no suspend, so it can be taken mid-flight without freezing either pawn.",
     "grade": "M",
     "evidence": "tools/usmapdump/dumpimage.go: grep for NtSuspendProcess/SuspendThread returns nothing (crashwatch.go is the only suspender and it triggers only on a crash marker); dumpimage usage tools/usmapdump/main.go:349 `usmapdump dumpimage <proc-name-or-pid> [outDir] [-includehiddenimages]`; docs/s141-t3-arml-result.md:125 'Taking the dumpimage EARLY worked'.",
     "implication": "Schedule `usmapdump.exe dumpimage SUPERVIVE-Win64-Shipping.exe dumps\\<label>` at \u2248probe+60 s (after `[BS] done` and W#3), not at the end."
    },
    {
     "claim": "crashwatch is armed automatically by launch-redirect.ps1 and writes docs/crashwatch.out.log (overwritten per launch); inject.exe accepts `mmap <pid-or-name> <dll>`; the launcher's -CompanionWatch must NOT be used on a staging flight.",
     "grade": "M",
     "evidence": "configs/launch-redirect.ps1:375-386 (crashwatch ... -poll 50, log docs\\crashwatch.out.log), :402-409 ('Do NOT combine with fk24-stage.ps1'); tools/inject/main.go:152-154 (mustPID resolves numeric PID or name), :498-499 usage; configs/inject-secondaries.ps1:152 (`& $inject mmap $name $path`).",
     "implication": "Copy docs/crashwatch.out.log to evidence immediately after death (or at flight end); a PLAN-B 5th injection is `tools\\inject\\inject.exe mmap <PID> <dll>` from an elevated console, \u226520 s after the previous map, only if Get-Process -Id <PID> is alive."
    }
   ],
   "recommendation": "PRIMARY = PLAN A: one fresh launch, FOUR injections (gft \u2192 fo \u2192 sp \u2192 ONE RM_BOTSPAWN probe), zero new C++ \u2014 a build.ps1-only variant the design lane must confirm (working name `bothwalk`: `-DKRUNMODE=RM_BOTSPAWN -DKFSNAME=\\\"\\\" -DKBSAI=1 -DKBSPS=1 -DKBSPSARMS=0x13A0 (D|F|G|H|K2, NO ARM J) -DKSHSENTX=600.0 -DKSHSENTZ=0.0 -DKSHPLRGRAV=1.0 -DKBSGASPLAYER=1`; optionally a non-round `-DKBSGASMOVESPEED=470.0` per R-S189-MV-a). Bot: spawned at player+600 X, kicked (600,0,0) \u2192 falls, lands Z=90.15, AI wander at the seeded cap (S140/S141 [M]). Player: K1 storages + K2 GravityScale=1 \u2192 hovers until a foreground transition, falls to the ledge (0,0,386), W slides it to the floor, W walks it (F3 [M] chain, but on a K1-treated player: [S], see hazards). FALLBACK = PLAN B: `-mv-play` as the 4th injection (player chain exactly as ship-f1), then a 5th manual-map of a bot-only arm (KBSPSARMS=0x3A0, KSHSENTX=600, KBSGASPLAYER=0 \u2014 also a new build.ps1 line) \u226520 s later; same runbook, plus the marker-truncation step and an extra FK-32 exposure.\n\n=== RUNBOOK (T0 = launch; timings are ship-f1 [M] unless marked) ===\nPRE (T\u221230 min, no game running, Steam running, THREE ELEVATED PowerShell consoles C1/C2/C3):\n P1. Digest the probe and prove it is a distinct arm: `py tools\\sigbypass-mod\\text_digest.py tools\\sigbypass-mod\\build\\tutorial_launch_<variant>.dll` and `py tools\\sigbypass-mod\\text_digest.py --dupes tools\\sigbypass-mod\\build`. Regression gates must still reproduce (botai 5e47c13cf7f0a158, gasattr 2fcc2536e21f18e3, gasattr-ctrl 4465ebc4d7168c03, mv-play 87c764b6d235b7a3). Write docs/s189-bothwalk-f1-PREREGISTERED.txt (copy the shape of docs/s141-t3-arml-PREREGISTERED.txt: artifact RAW/VSIZE, knobs, gates, risk class, outcome table below) and commit it BEFORE launch.\n P2. Backend arming check (the stager refuses otherwise): `(Invoke-WebRequest http://127.0.0.1:8080/core-game/players/9b9d2c887e2524f918e383a895f2f1c2 -UseBasicParsing).Content` must show a non-empty MatchID (forceTutorialMatch=true in the running ags, or the S138 armqueue route). If ags must be restarted, back up docs\\capture.log first (truncation hazard).\n P3. Evidence dir: `$ev='G:\\git\\Supervive Revival Project\\scratchpad\\s189-mv\\evidence'; $label='bothwalk-f1'` in all three consoles.\n P4. PRE-WARM the sender in C3 (before the game exists): `try { & \"$repo\\scratchpad\\s189-mv\\tools\\send-wasd.ps1\" -Sequence f13 -HoldMs 50 } catch { 'prewarm ok (no game yet)' }` then verify `[bool]('S189WasdInput' -as [type])` prints True. From now on C3 sends with `&` in-process (never fly-wasd.ps1, which spawns a cold child).\n P5. Kill stale clients only (`Get-Process SUPERVIVE-Win64-Shipping`); exactly one game process must exist later or send-wasd's MainWindowHandle read breaks.\n\nT0 (C1): `.\\configs\\launch-redirect.ps1 -NoHook` (archives old crash dumps, arms crashwatch \u2192 docs\\crashwatch.out.log + dumps\\crash-<stamp>, starts ags, launches). NO -CompanionWatch. Then `$p=Get-Process SUPERVIVE-Win64-Shipping; $gpid=$p.Id; $base='0x{0:X}' -f $p.MainModule.BaseAddress.ToInt64(); \"$gpid $base\"` \u2014 record both in the pre-registration file's flight section.\n\nT0+0:20 (C2): start the MARKER SHADOW loop (FK-25 defence; runs for the whole flight):\n```\n$m='G:\\git\\Supervive Revival Project\\docs\\tutorial-launch-marker.txt'; $gen=0; $last=''\nwhile($true){ try{ $fs=[IO.File]::Open($m,'Open','Read','ReadWrite'); $sr=New-Object IO.StreamReader($fs); $cur=$sr.ReadToEnd(); $sr.Dispose(); $fs.Dispose() } catch { $cur=$null }\n  if($null -ne $cur -and $cur -ne $last){ if($last -and -not $cur.StartsWith($last)){ $gen++; [IO.File]::WriteAllText(\"$ev\\$label-marker-gen$gen.txt\",$last) }\n    [IO.File]::WriteAllText(\"$ev\\$label-marker-live.txt\",$cur); $last=$cur }\n  Start-Sleep -Milliseconds 250 }\n```\n(gen files = every truncated generation: fo, sp, probe; `-marker-live.txt` = the current one. Run it in its own window; Ctrl-C at the end.)\n\nT0+0:30 (C1): `.\\configs\\fk24-stage.ps1 -Probe tools\\sigbypass-mod\\build\\tutorial_launch_<variant>.dll -Label $label` (no -BindOnly; -AllowStale only if the stager reports a stale DEPLOYED fo/sp). Expected console gates: 'pre-flight: is ags serving an armed match?' \u2192 'game PID=' \u2192 'parked match model ... OK' at \u2265T0+1:50 (MinUptimeSec 110) \u2192 `>>> inject gft` \u2248T0+1:50 \u2192 `>>> inject fo` \u2248T0+2:10 \u2192 'LVL_Tutorial load complete -> OK' within ~5-15 s \u2192 8 s sleep \u2192 `>>> inject sp` \u2248T0+2:30 \u2192 '[SP] done step=4 ... OK' \u2248T0+2:42 \u2192 3 s + gap \u2192 `>>> inject probe-...` \u2248T0+2:50 \u2192 'probe injected; armed window begins'. Yield: only ~1 in 2 launches reaches this point.\n\nT0+2:42 \u2014 AT [SP] done (C2, second window; the shadow loop keeps running): pull the player addresses and start two watchers BEFORE the probe:\n```\n$t=[IO.File]::ReadAllText(\"$ev\\$label-marker-live.txt\")\n$hero = if($t -match 'spawnedPawn=0x([0-9A-Fa-f]+)'){'0x'+$matches[1]}\n$pcmc = if($t -match '\\[LIFT\\] gravity OFF \\(CMC=0x([0-9A-Fa-f]+)'){'0x'+$matches[1]}\n\"$hero $pcmc\"   # both must be non-empty; the [LIFT] line proves the K2 premise (GravityScale 0) holds\n$tools='G:\\git\\Supervive Revival Project\\scratchpad\\s189-mv\\tools'\n& py \"$tools\\preflight_read.py\" $gpid $base $hero $pcmc | Tee-Object \"$ev\\$label-player-preflight.txt\"\n$pw=Start-Process -FilePath py -ArgumentList @(('\"'+\"$tools\\motion_watch_player.py\"+'\"'),$gpid,$hero,$pcmc,300) -RedirectStandardOutput \"$ev\\$label-player-motion.csv\" -RedirectStandardError \"$ev\\$label-player-motion.stderr.txt\" -NoNewWindow -PassThru\n$bw=Start-Process -FilePath py -ArgumentList @(('\"'+\"G:\\git\\Supervive Revival Project\\tools\\re\\motion_watch.py\"+'\"'),$gpid,$base,400) -RedirectStandardOutput \"$ev\\$label-botchain.txt\" -RedirectStandardError \"$ev\\$label-botchain.stderr.txt\" -NoNewWindow -PassThru\n```\n(motion_watch.py polls GUObjectArray for the LokiBotController \u2014 a pass takes seconds; it self-reports FOUND at +t and prints its own start wall-clock in the header. A FOUND may lag the spawn by up to one pass; the shim's five [SNP] samples cover the first 10 s.)\n\nT0+2:50 probe injected. T0+2:54 `[FS] arm: swapped=17563` in the live marker. Within 8 s the first game-thread hit starts the ladder; the WORLD FREEZES ~10-15 s (both watchers show static rows \u2014 expected, not a null). \u2248T0+3:05 the ladder prints `[SNP] BOT            pawn=0x... CharacterMovement@0x458 = 0x...` \u2014 C2 polls for it and starts the bot watcher:\n```\ndo { Start-Sleep -Milliseconds 300; $t=[IO.File]::ReadAllText(\"$ev\\$label-marker-live.txt\") } until ($t -match '\\[SNP\\] BOT\\s+pawn=0x([0-9A-Fa-f]+) CharacterMovement@0x[0-9A-Fa-f]+ = 0x([0-9A-Fa-f]+)' -or -not (Get-Process -Id $gpid -EA SilentlyContinue))\n$bot='0x'+$matches[1]; $bcmc='0x'+$matches[2]; \"$bot $bcmc\"\n$bw2=Start-Process -FilePath py -ArgumentList @(('\"'+\"$tools\\motion_watch_player.py\"+'\"'),$gpid,$bot,$bcmc,240) -RedirectStandardOutput \"$ev\\$label-bot-motion.csv\" -RedirectStandardError \"$ev\\$label-bot-motion.stderr.txt\" -NoNewWindow -PassThru\n```\nAlso confirm in the live marker: `[GASX] ARM G done: storages written 3/3, attributes written 6/6`, `[GASX] ARM K1 done: PLAYER storages written 3/3`, `[SNP] BOT sentinel Velocity = (600, 0, 0) -> readback OK`, `[SNP] ARM K2: PLAYER GravityScale@0x1A0  0.0000 -> 1.0000  readback OK`, and NO `[BS] REFUSE(`/`[GASX] ARM G REFUSED`/`REFUSED: an identity control FAILED`/`SITTING VOID` lines.\n\n\u2248T0+3:15 `[FS] disarm: restored=17563 of 17563` \u2192 game thread free; the shim's worker sampler now takes [SNP] samples 0-4 over 10 s. C3 sends IMMEDIATELY (in-process, pre-warmed):\n```\n$sync=\"$ev\\$label-wasd-sync.txt\"; if(Test-Path $sync){Remove-Item $sync}\n& \"$tools\\send-wasd.ps1\" -Sequence w,w,w,d -HoldMs 3000 -InterKeyMs 5000 -RepeatMs 33 -SyncFile $sync 2>&1 | Tee-Object \"$ev\\$label-wasd.log\"\n```\nExpected: `focusOk=True`; W#1 (t=0-3 s) at hover \u2192 player CSV shows AccY\u2248\u221250000 with Vel 0 (F3 tests 1-2 pattern) and the shim's `[SNP] PLR Accel@0x328` samples 1-4 show non-zero (in-shim receipt that input reaches the K1-treated player); the focus transition should start the fall (Vz<0 within ~10 s, [I]); W#2 (t=8-11 s) slides the hero off the ledge (F3: diagonal Accel, Z 386\u219290.15 in ~1.2 s, Mode 3\u21921); W#3 (t=16-19 s) walks at the cap in \u2212Y; D (t=24-27 s) is the strafe check (F1 saw D=0 in Falling; unknown in Walking). If the player CSV still shows LocZ=13240 20 s after W#1: force a foreground cycle from C3 \u2014 `$h=(Get-Process SUPERVIVE-Win64-Shipping).MainWindowHandle; [S189WasdInput]::ShowWindow($h,6); Start-Sleep 1; [S189WasdInput]::ShowWindow($h,9); Start-Sleep 1; [S189WasdInput]::SwitchToThisWindow($h,$true)` \u2014 then resend `-Sequence w,w -HoldMs 3000 -InterKeyMs 5000`. Log every timestamp; the hover trigger is unsettled.\n\n\u2248T0+3:25 `[BS] done (step=4 ... dCtl=3 dHero=3 ...)` \u2014 probe finished; the marker is now complete (no later injection in PLAN A). C2: `Copy-Item \"$ev\\$label-marker-live.txt\" \"$ev\\$label-marker-FINAL.txt\"`.\n\n\u2248T0+3:50 (probe+60 s) TAKE THE DUMP EARLY (C1): `tools\\usmapdump\\usmapdump.exe dumpimage SUPERVIVE-Win64-Shipping.exe dumps\\$label` (pure RPM, no suspend). Then one same-instant two-pawn snapshot: `py tools\\re\\movementmode_readout.py $gpid $base | Tee-Object \"$ev\\$label-movementmode-snapshot.txt\"` (prints bot and player pawn/CMC/Mode/Velocity/location in ONE pass, by name) and `py \"$tools\\preflight_read.py\" $gpid $base $hero $pcmc | Tee-Object \"$ev\\$label-player-postflight.txt\"`.\n\nT0+4:10 \u2192 T0+5:20: optional second W-hold set on the player for a longer walk; let the bot CSV run. FK-32 is expected at T0+5:20\u20265:50 (ship-f1 342 s; S140 350/318 s). Do not inject anything else in PLAN A.\n\nAT DEATH / FLIGHT END: `Copy-Item docs\\crashwatch.out.log \"$ev\\$label-crashwatch.log\"` (overwritten by the next launch); `Copy-Item docs\\fk24-stage-$label-*.txt $ev\\`; `Copy-Item \"$env:LOCALAPPDATA\\SUPERVIVE\\Saved\\Logs\\Loki.log\" \"$ev\\$label-Loki.log\"`; stop the shadow loop; `py tools\\re\\dump_coverage_ledger.py` offline; write docs/s189-bothwalk-f1-RESULT.md from the evidence, not from memory.\n\n=== SUCCESS / VOID CRITERIA (pre-register these verbatim) ===\nBOT WALKED (all required): bot CSV has \u22651 row with Mode=1; peak |V_xy| = hypot(VelX,VelY) in [400,530] AND equal to the seed to CSV precision (500.000, or the design lane's non-round seed), corroborated by \u22652 [SNP] BOT `Velocity dec` lines whose 10-digit hypot is exactly the seed; \u22653 distinct velocity headings (10\u00b0 bins) at |V_xy|>300 spanning \u226530 s AFTER the single kick, with `Accel@0x328`/AccX,AccY magnitude \u224850000 and rotating; horizontal displacement >1000 uu from the [SNP] BOT loc/CSV first row; VelZ=0 on the walking rows; motion_watch.py shows RandomMoveDirection changing and CIV\u2248RMD. A kick alone gives one heading, Accel (0,0,0) and a stop within ~0.3 s (BrakingDecelerationWalking 2048).\nPLAYER WALKED (all required): sync file has ARM_W_START/END for each hold and NO FOCUS_FAILED; player CSV during a W-hold shows |AccY|\u226540000 (input reached ScaleInputAcceleration), then a row with Mode=1 and |V_xy| = seed to CSV precision, LocZ\u224890.15, displacement >1000 uu in \u2212Y across the holds.\nBOTH TOGETHER: at least one pair of rows (player CSV, bot CSV) with |t_wall difference| \u22640.2 s where both have Mode=1 and |V_xy|>300; plus the movementmode_readout snapshot listing both pawns alive in one pass.\nVOID (say which): stager exit before the probe (FK-31 or gate timeout); `[BS] REFUSE(...)`/`NO CALL: resolve failed`; `[BS] *** SITTING VOID: the census MOVED`; `[GASX] ARM G REFUSED`/`storages written <3/3` or `attributes written <6/6`; `[SNP] ... REFUSED: an identity control FAILED`; `ARM K2 SKIPPED` (GravityScale not 0 \u2014 sp LIFT premise failed); `[FS] *** NO GAME-THREAD HITS`; FOCUS_FAILED in the sync file (player null uninterpretable); either watcher's stderr `# process died` before the criterion window; the bot CSV never starting (the [SNP] BOT line never appeared).\n\n=== FAILURE MODES AND THEIR SIGNATURES ===\nFK-31 staging death: stager prints `*** GAME PROCESS GONE ... aborting the wait ***` with only gft+fo resident; crashwatch.out.log shows 0xC0000005 and a crashpad handoff; dumps\\crashpad-* appears at the NEXT launch's archive step. Response: archive, relaunch (configs\\s138-autostage.ps1 automates the retry but injects no probe and uses the armqueue route; hand-inject with inject.exe mmap afterwards honouring the 20 s gap and the [SP] done gate). Budget two launches.\nSilent resolve refusal: `[BS] REFUSE(hero|pawnclass|cdo|function|params)` or `[BS] ---- NO CALL: resolve failed` \u2014 a resolve statement; census dCtl=0; nothing downstream interpretable.\nFrozen-world illusion: all three samplers static for ~10-15 s right after the first hit \u2014 the ladder holds the game thread; wait for `[FS] disarm:`.\nSampler racing the spawn: motion_watch.py FOUND may arrive one pass (seconds) after the spawn; the bot motion CSV cannot start before the [SNP] BOT line \u2014 the shim's +250 ms\u2026+10 s samples are the primary early record; do not read a late external start as 'the bot did not move'.\nFocus-grab failure: `focusOk=False` + `FOCUS_FAILED` in the sync file, player Accel stays 0 \u2014 uninterpretable, resend after `SwitchToThisWindow`; fgPid names the window that stole focus (F1: the Claude Code UI).\nK1-broke-input signature (the [S] hazard): focusOk=True AND repeats\u226560 AND player CSV Accel==0 throughout the W-hold while the bot walks \u2192 input path on the K1-treated player is broken; switch to PLAN B on the next launch.\nHover never breaks: player LocZ=13240 for >20 s after K2 despite the focus cycle \u2014 record it; the design lane's option is a tiny ARM J kick (KSHPLRY=\u22121.0, \u2212Y = forward per F3) which defeats the 2-D rest gate without drifting off the ledge (untested).\nBot GC / off-thread read: `[SNP] *** sample N FAULTED` \u2014 earlier samples stand.\nFK-32 early: 0x0000DEAD in crashwatch before the criterion window \u2014 everything captured so far stands ('capture as you go'); no retry of a 5th injection in PLAN A.\nMarker truncation (PLAN B only): the 5th manual-map truncates the -mv-play generation \u2014 the shadow loop's `-marker-gen<N>.txt` is the only copy.\n\n=== PLAN B DELTAS === 4th injection = `tools\\sigbypass-mod\\build\\tutorial_launch_botfight_damage_self_cal_bindavatar_seedmax_mv_play.dll` (player chain as ship-f1: hero/CMC also on its `[S189-MV] POKEGRAVITY ... hero=0x cmc=0x` line, terminal `[BF] worker done`); at \u2265probe+20 s and only if `Get-Process -Id $gpid` is alive, C1: `tools\\inject\\inject.exe mmap $gpid tools\\sigbypass-mod\\build\\tutorial_launch_<bot-only-variant>.dll` (KBSPSARMS=0x3A0, KSHSENTX=600, KBSGASPLAYER=0, no J/K2). The bot then spawns at player+600 X at the player's CURRENT Z (ledge or floor) and may land elsewhere (R-S189-MV-WASD-h). Separate attribute sets \u21d2 seeds may differ (bot KBSGASMOVESPEED vs player 500) for per-pawn discrimination. Cost: 5th manual-map inside a ~150 s window that is already the FK-32 modal zone.",
   "open_questions": [
    "Design lane: which probe? PLAN A needs one new build.ps1 line (RM_BOTSPAWN, KBSPSARMS=0x13A0, KSHSENTX=600, KSHPLRGRAV=1.0, KBSGASPLAYER=1, no ARM J); PLAN B needs a bot-only line (0x3A0, KSHSENTX=600, KBSGASPLAYER=0). Neither exists in build.ps1 today (sentinel-big/armk/axisab all carry ARM J = a 600 uu/s player kick that sent the player off the island in S141).",
    "Does a K1-treated player (all three storages overwritten with the CDO subobjects, including +0xF00) still accept WASD \u2192 Acceleration \u2192 Velocity? Never measured; every KBSGASPLAYER=1 flight had no input driver. The runbook's 'K1-broke-input' signature separates it from a focus failure, but only a flight answers it.",
    "What breaks the player's post-GravityScale hover? Both recorded onsets (ship-f1 \u224813:42:20.8, F3 between 13:20:35 and 13:21:13) coincide with a new console window being created \u2014 a foreground transition \u2014 but nothing controlled it. If the deliberate ShowWindow/SwitchToThisWindow cycle in the runbook works, log the latency; if not, the design lane's tiny \u2212Y ARM J kick (KSHPLRY=\u22121.0) is the alternative.",
    "Per-pawn discrimination when both pawns read ONE CDO subobject (PLAN A): both caps are 500 by construction. Should the design lane set a non-round KBSGASMOVESPEED (e.g. 470.0 = 0x43EB0000) so a stock 500/600 cannot masquerade (R-S189-MV-a), keeping the [400,530] window?",
    "Is there any interaction risk between the bot landing zone (S140/S141: (1356..1576, 0..117, 90.15)) and the player's floor path ((683,-716)\u2192(683,-1379)\u2192(953,-2108))? Both capsules share the same ~2000 uu neighbourhood; a bump would show as a heading change on the bot that is NOT preceded by a RandomMoveDirection change \u2014 read motion_watch.py's RMD column alongside the CSV.",
    "How long does one motion_watch.py find_bot pass take on a staged world (~155-190k objects, several RPM per object)? The only datum is S138 f8 FOUND at +29.8 s, which included waiting for ARM D. Measure it once on any live client (start it, note FOUND latency) before relying on it for early bot samples."
   ],
   "hazards": [
    "ARMED WINDOW IS ~150 s, NOT 300 s: ship-f1 died 342.2 s after launch with the probe at launch+170 s (scratchpad/s189-mv/evidence/ship-f1-crashwatch.log + stage-file mtimes); S140 T2's 'T+350.5/318.0 s' are process-elapsed with the same ~+175 s injection point. Schedule bot walk, three W-holds and the dumpimage inside probe+120 s.",
    "fly-wasd.ps1 as written spawns a COLD child PowerShell (`& powershell -File`) \u2014 that is the whole 35 s delay. Do not use it; pre-warm send-wasd.ps1 in the sending console and invoke it with `&`. Verify `[bool]('S189WasdInput' -as [type])` before staging.",
    "The stager's copy of the probe marker (Start-Sleep 2 then Copy-Item, fk24-stage.ps1:918/:929) is ~650 bytes of header; the sp copy is 277 bytes and lacks [SP] done/[LIFT]. Without the shadow loop the hero/CMC addresses and every [SNP]/[GASX]/[BS] line exist only in the live file, which any later injection (PLAN B) truncates (FK-25).",
    "Sending WASD while the ladder holds the game thread (~10-15 s after the first hit) wastes the hold: keydowns queue and the 3 s auto-repeat window decays before a frame passes. Gate the first send on `[FS] disarm:` in the marker.",
    "K2 only fires if the player's GravityScale reads 0 (`ARM K2 SKIPPED` otherwise, tutorial_launch.cpp:17318); the premise is sp's LIFT step \u2014 confirm the `[LIFT] gravity OFF` line in the sp generation before reading any player fall as attributable.",
    "ARM G's attribute values live in the CDO default subobject (process-wide, not undone) and K1 overwrites the player's +0xF00 KWIREGAS ASC pointer \u2014 a diagnosis arm, not a shipping fix; it must not enter the default injection set.",
    "docs/crashwatch.out.log is overwritten by every launch and docs/capture.log is truncated by an ags restart \u2014 copy both to the evidence dir before any relaunch. Do NOT arm -CompanionWatch on a staging flight (launch-redirect.ps1:402-409: staging failed to LoadMap with it active).",
    "All samplers OpenProcess with 0x1F0FFF against an elevated game and SetForegroundWindow needs UIPI parity: every console must be elevated. Exactly one SUPERVIVE-Win64-Shipping process may exist (send-wasd's MainWindowHandle read breaks on an array).",
    "The player CSV prints 3-decimal floats; 'bit-exact 500' from the CSV is 500.000 to 3 decimals. For the bot, cite the shim's `[SNP] BOT Velocity dec` (%.10g) lines as the exact receipt; for the player only the CSV exists unless the shim's PLR samples happen to catch the walk (they end at disarm+10 s).",
    "motion_watch.py takes the FIRST LokiBotController-chain object it finds; the ARM D design yields exactly one (spawn B). If the design lane changes the spawn set, that instrument silently picks whichever appears first.",
    "Expected staging yield is ~1 in 2 launches (FK-31 ~27 % plus gate timeouts); budget two launches and pre-archive crash dumps between them (the next launch clears crashpad's pending report)."
   ]
  }
 },
 "design": {
  "options": [
   {
    "name": "A \u2014 two DLLs: -mv-play (4th) + bot-only sentinel-family arm (5th, kick-based)",
    "injections": 5,
    "new_source_lines": 0,
    "gates_moved": [],
    "risk": "5th manual-map lands inside the FK-32 modal window (probe at ~launch+170 s, kills at 342/350/318 s [M: scratchpad/s189-mv/evidence/ship-f1-crashwatch.log; docs/s140-tier2-sentinel.md:388-390]); ARM D spawns THREE stacked pawns at player+600 X at the player's current Z (unmeasured floor, R-S189-MV-WASD-h); bot motion needs the 600 kick so AI-vs-kick attribution is indirect (only via heading rotation + duration); marker of the -mv-play generation is truncated by the 5th map (FK-25) unless shadow-copied.",
    "score": 6.5,
    "rationale": "Every ingredient is individually [M] and it has the strongest per-pawn speed discriminator (player 500 on its own spawnedSet0, bot on the CDO set with a non-stock KBSGASMOVESPEED), zero source change. Loses on injection count and on the kick-dependence of the bot half."
   },
   {
    "name": "B \u2014 one DLL RM_BOTSPAWN 'bothwalk' (D|F|G|H|K2, K1 full, 600 kick, air spawn)",
    "injections": 4,
    "new_source_lines": 0,
    "gates_moved": [],
    "risk": "ARM K1 overwrites ALL THREE player storages incl. +0xF00 (tutorial_launch.cpp:16585-16594) \u2014 the K1-treated player has NEVER had an input driver (docs/s141-t3-arml-result.md, Accel (0,0,0) throughout) so 'K1 player + WASD' is [S]; bot and player share ONE CDO set (no per-pawn discrimination); three pawns stacked at (600,0,13240); bot walks only via the kick, so 'walked because of the AI' rests on heading rotation alone.",
    "score": 5,
    "rationale": "Cheapest possible build line but the weakest attributability: the kick is applied at arm time (before any rest window), and the player half is an untested wiring."
   },
   {
    "name": "C \u2014 RM_BOTFIGHT extension (fold the bot ladder into BfS148DoCalibration)",
    "injections": 4,
    "new_source_lines": 120,
    "gates_moved": [
     "-mv-play 87c764b6d235b7a3 would move unless every insertion is a new no-#else knob AND the Bs*/BsPs* statics compile under RM_BOTFIGHT"
    ],
    "risk": "[M] a multi-second bot block placed after the AdjustHealth call (s_callTick, :23312) and before WAIT_LATER (:23321) flips RESULT to LATER_TIMEOUT (KBFSELFTIMEOUTMS=5000); only safe site is the POKEGRAVITY block (:23097-23147); RM_BOTFIGHT has no post-disarm worker sampler slot (:25822-25836); the ~16 s GUObjectArray freeze lands inside the S148 dispatch; largest untested surface.",
    "score": 3,
    "rationale": "Largest port, most fragile timing, and it gives no receipt that D does not already give. Rejected."
   },
   {
    "name": "D \u2014 CHOSEN: one DLL RM_BOTSPAWN 'botplay-floor' (floor spawn, one bot, no kick unless proven stationary, K1 masked to +0xF08 only, dense two-pawn sampler)",
    "injections": 4,
    "new_source_lines": 75,
    "gates_moved": [],
    "risk": "Floor-spawn \u2192 MOVE_Walking at possess time is [I, ~0.8-0.9] (SetDefaultMovementMode 0x35F9B30 \u2192 FindFloor 0x35E01D0 [M]; the air-spawn is the measured failing branch; MaxStepHeight on this build unread); the +0xF08-only K1 player under WASD is UNFLOWN [I,strong] (same getter path as mv-play, hero+0xF08\u2192set+0xF0/+0x100, different set object); ~15.5 s game-thread freeze at the ladder hit; player and bot share one CDO set so both cap at 487.",
    "score": 8.5,
    "rationale": "Reaches both-walking with the fewest injections; every new line is behind a new preprocessor knob with no #else, so ALL nine current-baseline digests (botai/gasattr/gasattr_ctrl/play/mv_play/seedmax/armk/axisab/sentinel_big) stay byte-identical; attributability is built in: bot kick is WITHHELD until sample 8 and only fires if the bot has not moved 50 uu (printed either way), the player receives no kick at all, and the seed is non-stock 487 so a cap of 487 (not 500) proves the value flows from the knob. Floor-spawn uncertainty is measured (BOT-before MovementMode byte) and covered by the fallback."
   },
   {
    "name": "A' \u2014 hybrid: D's floor-spawn bot arm as the 5th DLL after -mv-play ('botplay-floor-5th')",
    "injections": 5,
    "new_source_lines": 75,
    "gates_moved": [],
    "risk": "Same 5th-injection FK-32 exposure and marker truncation as A; needs the -mv-play generation shadow-copied.",
    "score": 7.5,
    "rationale": "Best per-pawn discriminator (player 500 own set, bot 487 CDO set, disjoint by construction [M: arml:157 vs carrier ASC]) and the flown player chain unchanged; costs one more manual-map. Built from the same source as D \u2014 it is D's PLAN B, not a separate design."
   }
  ],
  "chosen": "D \u2014 one RM_BOTSPAWN DLL 'botplay-floor': floor-spawn the single ARM-D bot at a measured rest point, AI-wander with NO kick (fallback kick only if the sampler proves the bot stationary), player wired +0xF08-only to the CDO set + GravityScale=1, dense 40 s two-pawn worker sampler; 4 injections; every edit under a new knob with no #else so every current-baseline digest stays byte-identical. PLAN B pre-built from the same source: 'botplay-floor-5th' as a 5th map after -mv-play. Zero-source contingency: 'botwalk' (design A's kick arm with 487 seed).",
  "variant_name": "botplay-floor",
  "build_defines": [
   "# ---- add to tools/sigbypass-mod/build.ps1 after 'axisab-ctrl' (~line 957). bits: 0x20 D | 0x80 F | 0x100 G | 0x200 H(zero sentinel = payload-poison + sampler only) | 0x1000 K2 | 0x4000 M(fallback kick from the sampler) = 0x53A0. NO 0x800 (ARM J: KSHPLRY=0 would ZERO a walking player), NO 0x2000 (ARM L).",
   "'botplay-floor'      = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x53A0','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKSHPLRGRAV=1.0','-DKBSGASPLAYER=1','-DKBSGASPLAYERMASK=0x2','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=92.0','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMINUU=50.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# AI-attribution control (separate launch, optional): identical but ARM F compiled out (0x80 clear) so the wander gate stays closed. Fallback kick stays IN: a kicked, undriven Walking pawn must STOP within ~0.3 s (BrakingDecelerationWalking 2048), which is the kinematic signature that separates 'kick' from 'AI'. bits 0x5320.",
   "'botplay-floor-noai' = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x5320','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKSHPLRGRAV=1.0','-DKBSGASPLAYER=1','-DKBSGASPLAYERMASK=0x2','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=92.0','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMINUU=50.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# PLAN B (hybrid A'): bot-only floor arm injected as a 5TH manual-map after -mv-play (player chain untouched: KBSGASPLAYER=0, no K2). bits 0x20|0x80|0x100|0x200|0x4000 = 0x43A0. Per-pawn discriminator: player 500 (own spawnedSet0) vs bot 487 (CDO set).",
   "'botplay-floor-5th'  = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x43A0','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=92.0','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMINUU=50.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# ZERO-SOURCE-CHANGE contingency (design A, flown shape = gasattr-sentinel + 600 kick + non-stock seed; three stacked bots; kick at arm time). Inject as a 5th DLL after -mv-play only if the edited build fails to compile or its baseline check fails.",
   "'botwalk'            = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x3A0','-DKSHSENTX=600.0','-DKBSGASMOVESPEED=487.0f')",
   "# DIGEST DISCIPLINE [M, measured this session from the 14:24 artifacts built from clean HEAD]: the CLAUDE.md-recorded gates botai 5e47c13cf7f0a158 / gasattr 2fcc2536e21f18e3 / gasattr-ctrl 4465ebc4d7168c03 / play 9bc10a4552c596e1 DO NOT reproduce today. TODAY'S BASELINE (record it, then edit): botai b620e0ff3279e673, gasattr 29f0fa4efffd01da, gasattr_ctrl 4c0ede610baa4629, play 31af7667355f39d1, armk 3ff3ba9f77ae1438, axisab 01593c0f8f73f1ae, sentinel_big <digest the 14:24 artifact>, mv_play 87c764b6d235b7a3 (matches recorded), seedmax b47d1bfd04e44921 (matches recorded). After the edit ALL NINE must be byte-identical to that baseline (every edit is under a NEW knob defaulting to current behaviour, with no #else that adds a string; the only #else re-emits the ORIGINAL loop head). Only botplay-floor / -noai / -5th / botwalk carry new digests. Run `python tools/sigbypass-mod/text_digest.py --dupes tools/sigbypass-mod/build` and require botplay-floor != botplay-floor-noai != botplay-floor-5th."
  ],
  "source_edits": [
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "immediately after line 15674 (`#endif` closing `#ifndef KBSAI`)",
    "what": "New knobs for the floor spawn, the single-bot ARM D, and the getter receipt. All default to current behaviour.",
    "why": "A floor spawn needs an ABSOLUTE point, not player-loc+KBSOFFSET (the current air spawn at Z=13240 is the measured MOVE_Falling branch); ARM D's A/C control spawns would stack two extra pawns on the floor point; the getter receipt cannot reuse BfObsMovGetters (nested under #if KBFSELFCAL at 22157, which static_asserts RM_BOTFIGHT at :419-420).",
    "pseudo_diff": "+#ifndef KBSAIFLOOR\n+#define KBSAIFLOOR 0      // 1 = spawn the SpawnAIFromClass pawn at KBSAILOC_* (a MEASURED floor rest point) instead of hero+KBSOFFSET in the air\n+#endif\n+#ifndef KBSAILOC_X\n+#define KBSAILOC_X 1356.0 // S140 T2 F3 bot rest point: (1356.434,-409.393,90.150) MOVE_Walking [M docs/s140-t2-marker-armj.txt:272]\n+#endif\n+#ifndef KBSAILOC_Y\n+#define KBSAILOC_Y -409.0\n+#endif\n+#ifndef KBSAILOC_Z\n+#define KBSAILOC_Z 92.0   // root Z, 1.85 uu ABOVE the measured rest 90.150 (capsule bottom ~3.9 uu over the surface): inside any FindFloor sweep, NEVER below rest (AlwaysSpawn does no adjust -> penetration)\n+#endif\n+#ifndef KBSLBSKIPCTRL\n+#define KBSLBSKIPCTRL 0   // 1 = ARM D makes ONLY spawn B (no A/C control spawns): exactly one bot. A-B-A already [M] S137 F3/F4.\n+#endif\n+#ifndef KBSGETTERRCPT\n+#define KBSGETTERRCPT 0   // 1 = per-pawn 'seed reaches GetMaxSpeed/GetMaxAcceleration' receipt after ARM G/K1 (own copy: BfObsMovGetters is RM_BOTFIGHT-only)\n+#endif"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsResolveAI, after line 15703 (`g_bsLoc[0]+=(double)(KBSOFFSET); g_bsLocOK=1;`), still inside the `{ ... }` block",
    "what": "Override the spawn point with the absolute floor point and print BOTH values.",
    "why": "Single change that turns the measured MOVE_Falling air spawn into the FindFloor-succeeds branch of the SAME SetDefaultMovementMode path (0x35F9B30 \u2192 0x35E01D0/0x35D2460/0x35E7760, all real, none Loki-overridden [M spawnloc lane]).",
    "pseudo_diff": "+#if KBSAIFLOOR\n+        Markerf(\"[BS] FLOOR OVERRIDE (KBSAIFLOOR): spawnLoc (%.1f,%.1f,%.1f) -> (%.1f,%.1f,%.1f) = S140 T2 F3 rest point, root Z+1.85\\r\\n\",\n+                g_bsLoc[0],g_bsLoc[1],g_bsLoc[2],(double)(KBSAILOC_X),(double)(KBSAILOC_Y),(double)(KBSAILOC_Z));\n+        g_bsLoc[0]=(double)(KBSAILOC_X); g_bsLoc[1]=(double)(KBSAILOC_Y); g_bsLoc[2]=(double)(KBSAILOC_Z);\n+#endif\n (the later `[BS] ARGS: ... spawnLoc=(%.1f, %.1f, %.1f)` line at :15748 then prints the floor point unchanged \u2014 that is the receipt; BsCallAI :15760-15761 copies g_bsLoc verbatim)"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsPsLokiBot: wrap lines 16334-16336 (spawn A block) and 16368-16370 (spawn C block)",
    "what": "Compile out the two control spawns under KBSLBSKIPCTRL.",
    "why": "With bNoCollisionFail=1 (AlwaysSpawn, :15764) three pawns would spawn overlapping at the floor point and the two AIController controls would sit as obstacles the walking bot must depenetrate from. Indexing is safe: BsPsLbRead stores by explicit index (:16176-16183), g_psLbIsBot is set only for i==1 (:16187), ARM B/C/F/G/H read g_psLbCtl[1] only, BsFinalReport prints rows 0..2 unconditionally (zeros are cosmetic) and dCtl/dHero are dynamic.",
    "pseudo_diff": "     // ---- spawn A: BASELINE, no poke.\n+#if !KBSLBSKIPCTRL\n     Marker(\"[PS] ---- ARM D / SPAWN A (baseline, CDO untouched) ----\\r\\n\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\"A-baseline\",0);\n+#endif\n     ... POKE / spawn B / RESTORE unchanged ...\n+#if !KBSLBSKIPCTRL\n     Marker(\"[PS] ---- ARM D / SPAWN C (reversal: must be AIController again) ----\\r\\n\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\"C-reversal\",2);\n+#endif\n (A2 expectation under KBSLBSKIPCTRL=1: dCtl=+1 dHero=+1; the RESTORE readback line still measures the CDO restore)"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsPsGasAttrs: (a) new helper + mask knob right after the `#define KBSGASPLAYER 0 / #endif` at ~16505; (b) inside the `#if KBSGASPLAYER` K1 loop `for(int i=0;i<3;i++){` at ~16585, as its FIRST statement; (c) after `Markerf(\"[GASX] ARM K1 done: ...\")` (~16596) but still inside `#if KBSGASPLAYER`, plus a bot receipt just before `Markerf(\"[GASX] ARM G done: ...\")` (~16609)",
    "what": "(a) KBSGASPLAYERMASK knob + a 20-line getter receipt (vtable-validated dispatch of ULokiCMC GetMaxSpeed disp 0x4C8 / GetMaxAcceleration disp 0x7D0, + HeroPredropHidden by name); (b) K1 writes ONLY the masked storage slots; (c) call the receipt on BOT after ARM G and on PLR after K1.",
    "why": "K1 today overwrites +0xF00 (the KWIREGAS carrier ASC) and +0xF10 (:16585-16594); the ONLY player wiring measured under WASD writes +0xF08 alone (S189-MV F2b/F3, tutorial_launch.cpp:23068-23090). Mask 0x2 reproduces that shape onto the CDO set. The mask guard is `#if (KBSGASPLAYERMASK)!=0x7` so armk/axisab (mask default 0x7) stay byte-identical. The receipt is the S189-MV F1-shape 'seed reaches the getter' line, per pawn, BEFORE any motion \u2014 the branch that separates 'wiring failed' from 'mover wall'.",
    "pseudo_diff": "+#ifndef KBSGASPLAYERMASK\n+#define KBSGASPLAYERMASK 0x7   // bit0 +0xF00 AbilitySystemComponentStorage, bit1 +0xF08 AttributeSetStorage, bit2 +0xF10 AttributeSetHealthStorage. 0x2 = the S189-MV read-path shape (leaves the KWIREGAS ASC untouched)\n+#endif\n+#if KBSGETTERRCPT\n+static void BpGetterReceipt(uintptr_t hero,const char* tag){\n+    if(!LooksLikePtr(hero)){ Markerf(\"[RCPT] %s hero=NULL\\r\\n\",tag); return; }\n+    uint32_t co=PropOffsetSuper(ClassOf(hero),\"CharacterMovement\");\n+    uintptr_t cmc=(co!=0xFFFFFFFF&&SafeReadable((void*)(hero+co),8))?*(uintptr_t*)(hero+co):0;\n+    uintptr_t vt=LooksLikePtr(cmc)&&SafeReadable((void*)cmc,8)?*(uintptr_t*)cmc:0;\n+    uintptr_t f08=SafeReadable((void*)(hero+0xF08),8)?*(uintptr_t*)(hero+0xF08):0;\n+    uintptr_t f00=SafeReadable((void*)(hero+0xF00),8)?*(uintptr_t*)(hero+0xF00):0;\n+    uint32_t ho=PropOffsetSuper(ClassOf(hero),\"HeroPredropHidden\");\n+    int hid=(ho!=0xFFFFFFFF&&SafeReadable((void*)(hero+ho),1))?*(uint8_t*)(hero+ho):-1;\n+    if(vt!=g_modBase+0x088F8570ULL){ Markerf(\"[RCPT] %s cmc=0x%llX vtbl=0x%llX NOT ULokiCMC -> no dispatch; f00=0x%llX f08=0x%llX predropHidden=%d\\r\\n\",tag,(unsigned long long)cmc,(unsigned long long)vt,(unsigned long long)f00,(unsigned long long)f08,hid); return; }\n+    typedef float(*Fn)(uintptr_t); float sp=0,ac=0; int spOk=0,acOk=0; uint8_t mm=SafeReadable((void*)(cmc+0x231),1)?*(uint8_t*)(cmc+0x231):0xFF;\n+    __try{ sp=((Fn)(*(void**)(vt+0x4C8)))(cmc); spOk=1; }__except(SEH_FILTER(GetExceptionInformation())){}\n+    __try{ ac=((Fn)(*(void**)(vt+0x7D0)))(cmc); acOk=1; }__except(SEH_FILTER(GetExceptionInformation())){}\n+    uint32_t sb=0,ab=0; memcpy(&sb,&sp,4); memcpy(&ab,&ac,4);\n+    Markerf(\"[RCPT] GETTER_%s vtblExact=yes mode=%u f00=0x%llX f08=0x%llX predropHidden=%d MaxSpeed=%08X(ok=%d,%.2f) MaxAccel=%08X(ok=%d,%.2f) seed=%.10g\\r\\n\",tag,mm,(unsigned long long)f00,(unsigned long long)f08,hid,sb,spOk,(double)sp,ab,acOk,(double)ac,(double)(KBSGASMOVESPEED));\n+}\n+#endif\n ... inside the K1 loop, first statement:\n+#if (KBSGASPLAYERMASK) != 0x7\n+            if(!(((KBSGASPLAYERMASK)>>i)&1)){ Markerf(\"[GASX]   PLR %-30s MASKED OUT (KBSGASPLAYERMASK=0x%X)\\r\\n\",kDst[i],(unsigned)(KBSGASPLAYERMASK)); continue; }\n+#endif\n ... after 'ARM K1 done' (inside #if KBSGASPLAYER):\n+#if KBSGETTERRCPT\n+        BpGetterReceipt(pp,\"PLR_AFTER_K1\");\n+#endif\n ... just before '[GASX] ARM G done':\n+#if KBSGETTERRCPT\n+    BpGetterReceipt(pawn,\"BOT_AFTER_G\");\n+#endif"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "Inside `#if (KBSPSARMS & 0x200)`: (a) statics near line 17027-17029 (`g_shLoc0` declaration); (b) BsPsSentinel right after `g_shArmed=1;` (~17337); (c) ShSampleLoop head :17345-17352; (d) inside the per-sample `__try` after the `loc ... moved` block (~17366); (e) after the `__except` block and BEFORE `#if (KBSPSARMS & 0x2000)` (~17371); (f) after the loop, before `---------------- ARM H VERDICT`; (g) wrap the end-of-run restore :17509-17513",
    "what": "Dense N-sample sampler with per-pawn |Vxy| + running peak printed at %.10g, time-since-arm, and a fallback kick that fires ONLY if the bot has not moved KSHFALLBACKMINUU by sample KSHFALLBACKAT (printing which branch it took); skip the end-of-run Velocity zeroing under the new bit.",
    "why": "The flown schedule stops at 10 s and its end restore writes (0,0,0) whenever g_shSentinelOK (=1 for a zero sentinel, :17266) \u2014 it would halt a walking bot once. The fallback design is what makes bot motion ATTRIBUTABLE: 'FALLBACK NOT NEEDED' = AI-driven from rest; 'FALLBACK KICK fired' = S140 shape (kick + AI), a different [M]. Sampler-relative t is wrong by ~3.7 s of FsDisarm (arml:272 shows a 12,163 uu fall by 'sample 0'), so time-since-arm is printed too.",
    "pseudo_diff": "+#ifndef KSHSAMPLEN\n+#define KSHSAMPLEN 0          // 0 = the flown 5-point schedule (byte-identical); N>0 = N samples every KSHSAMPLEMS on the worker\n+#endif\n+#ifndef KSHSAMPLEMS\n+#define KSHSAMPLEMS 1000\n+#endif\n+#if KSHSAMPLEN\n+static DWORD g_shArmTick=0; static double g_shPeakXY[2]={0,0};\n+#endif\n+#if (KBSPSARMS & 0x4000)\n+#ifndef KSHFALLBACKAT\n+#define KSHFALLBACKAT 8\n+#endif\n+#ifndef KSHFALLBACKMINUU\n+#define KSHFALLBACKMINUU 50.0\n+#endif\n+#ifndef KSHFALLBACKX\n+#define KSHFALLBACKX 600.0\n+#endif\n+static const double kShFallback[3]={(double)(KSHFALLBACKX),0.0,0.0}; static int g_shFallbackFired=0;\n+#endif\n --- (b) after g_shArmed=1;\n+#if KSHSAMPLEN\n+    g_shArmTick=GetTickCount();\n+#endif\n --- (c) loop head\n-    static const DWORD kAt[5]={250,750,2000,5000,10000};\n-    ... DWORD t0=GetTickCount(),prev=0;\n-    for(int i=0;i<5;i++){\n-        if(kAt[i]>prev) Sleep(kAt[i]-prev);\n-        prev=kAt[i];\n-        Markerf(\"[SNP] ---- sample %d at t=+%lu ms (elapsed %lu ms) ----\\r\\n\",...);\n+#if KSHSAMPLEN\n+    DWORD t0=GetTickCount();\n+    for(int i=0;i<(KSHSAMPLEN);i++){\n+        Sleep((DWORD)(KSHSAMPLEMS));\n+        Markerf(\"[SNP] ---- sample %d at t=+%lu ms (sinceArm %lu ms) ----\\r\\n\",i,(unsigned long)(GetTickCount()-t0),(unsigned long)(GetTickCount()-g_shArmTick));\n+#else\n+    (the ORIGINAL five lines verbatim \u2014 this #else re-emits existing code only, no new string)\n+#endif\n --- (d) inside __try after the loc/moved block\n+#if KSHSAMPLEN\n+        for(int k=0;k<2;k++){ uintptr_t c=k?g_shPlrCmc:g_shBotCmc; if(!LooksLikePtr(c)||!SafeReadable((void*)(c+0xE8),24))continue;\n+            const double* v=(const double*)(c+0xE8); double vxy=__builtin_sqrt(v[0]*v[0]+v[1]*v[1]); if(vxy>g_shPeakXY[k])g_shPeakXY[k]=vxy;\n+            uint8_t mm=SafeReadable((void*)(c+0x231),1)?*(uint8_t*)(c+0x231):0xFF;\n+            Markerf(\"[SNP] %-14s |Vxy|=%.10g peak=%.10g mode=%u seed=%.10g atCap=%s\\r\\n\",k?\"PLR\":\"BOT\",vxy,g_shPeakXY[k],mm,(double)(KBSGASMOVESPEED),(vxy==(double)(float)(KBSGASMOVESPEED))?\"EXACT\":\"no\"); }\n+#endif\n --- (e) after the __except block, before the ARM L #if\n+#if (KBSPSARMS & 0x4000)\n+        if(i==(KSHFALLBACKAT)&&!g_shFallbackFired&&LooksLikePtr(g_shBotCmc)&&LooksLikePtr(g_shBotPawn)){\n+            __try{\n+                uint32_t rc=PropOffsetSuper(ClassOf(g_shBotPawn),\"RootComponent\");\n+                uintptr_t root=(rc!=0xFFFFFFFF&&SafeReadable((void*)(g_shBotPawn+rc),8))?*(uintptr_t*)(g_shBotPawn+rc):0;\n+                double d=-1.0; if(LooksLikePtr(root)&&SafeReadable((void*)(root+0x158),24)){ const double* L=(const double*)(root+0x158);\n+                    double dx=L[0]-g_shLoc0[0][0],dy=L[1]-g_shLoc0[0][1]; d=__builtin_sqrt(dx*dx+dy*dy); }\n+                if(d>=0.0&&d<(double)(KSHFALLBACKMINUU)&&SafeWritable((void*)(g_shBotCmc+0xE8),24)){\n+                    memcpy((void*)(g_shBotCmc+0xE8),kShFallback,24); g_shFallbackFired=1;\n+                    if(LooksLikePtr(root)) memcpy(g_shLoc0[0],(void*)(root+0x158),24);\n+                    Markerf(\"[SNP] FALLBACK KICK fired at sample %d: bot moved only %.3f uu horizontally -> Velocity=(%.1f,0,0) readback %s. Motion after this is the S140 shape (kick+AI), NOT pure AI.\\r\\n\",i,d,(double)(KSHFALLBACKX),ShEq3(g_shBotCmc+0xE8,kShFallback)?\"OK\":\"FAILED\"); }\n+                else Markerf(\"[SNP] FALLBACK NOT NEEDED at sample %d: bot moved %.3f uu horizontally with NO kick written -> bot motion is AI-driven.\\r\\n\",i,d);\n+            }__except(EXCEPTION_EXECUTE_HANDLER){ Marker(\"[SNP] FALLBACK check FAULTED (off-thread read race) -> fallback NOT applied; attribution from later samples only.\\r\\n\"); }\n+        }\n+#endif\n --- (f) after the loop\n+#if KSHSAMPLEN\n+    Markerf(\"[SNP] PEAK |Vxy| BOT=%.10g PLR=%.10g seed=%.10g\\r\\n\",g_shPeakXY[0],g_shPeakXY[1],(double)(KBSGASMOVESPEED));\n+#endif\n+#if (KBSPSARMS & 0x4000)\n+    Markerf(\"[SNP] fallbackFired=%d\\r\\n\",g_shFallbackFired);\n+#endif\n --- (g) the restore\n+#if !(KBSPSARMS & 0x4000)\n     if(LooksLikePtr(g_shBotCmc)&&g_shSentinelOK&&SafeWritable(...)){ ... restore Velocity -> (0,0,0) ... }\n+#endif\n (NOTE the existing VERDICT block will print 'payload isSentinel' for a (0,0,0) sentinel \u2014 read it as cosmetic under this variant; the poison outcome table is not the question here.)"
   }
  ],
  "marker_receipts": [
   "[BS] census A0 ... LokiHeroCharacter-chain=<N> ; heroCand ... 'BP_HERO_Ronin_C' ... USABLE ; [BS] heroLoc = (0.0, 0.0, 13240.0)",
   "[BS] FLOOR OVERRIDE (KBSAIFLOOR): spawnLoc (600.0,0.0,13240.0) -> (1356.0,-409.0,92.0) = S140 T2 F3 rest point, root Z+1.85",
   "[BS] ARGS: pawnClass='BP_HERO_Ronin_C' spawnLoc=(1356.0, -409.0, 92.0) BT=null noCollisionFail=1 owner=null",
   "[BS] ---- A1: STABILITY RE-SCAN ... (no 'SITTING VOID' line)",
   "[PS] POKE AIControllerClass: ... READBACK OK ; [PS] ARM D B-treatment ... BotController-chain=YES ; [PS] RESTORE AIControllerClass -> ... READBACK OK (NO 'SPAWN A' / 'SPAWN C' sections under KBSLBSKIPCTRL=1)",
   "[PS] ARM B ... ctl.PS non-null ; [PS] ARM C ... pawn.PS == ctl.PS",
   "[PS] ARM F AFTER (faulted=0): LivingState=1  GATE+0x6A0=1  (was 0)  followed by '***** ARM F: THE GATE OPENED'",
   "[GASX]   dst AbilitySystemComponentStorage/AttributeSetStorage/AttributeSetHealthStorage @0xF00/0xF08/0xF10  0 -> <cdoASC/cdoSet/cdoHealth>  OK (x3)",
   "[GASX]   attr MoveSpeed @+0xF0 = 487 readback 487 OK ; MaxMoveSpeed @+0x100 = 487 ; MaxAcceleration @+0x120 = 50000 ; GroundFriction @+0x130 = 8 ; BrakingDecelerationWalking @+0x140 = 2048 ; Mass @+0x170 = 100 (all OK)",
   "[GASX]   PLR AbilitySystemComponentStorage MASKED OUT (KBSGASPLAYERMASK=0x2) ; [GASX]   PLR AttributeSetStorage @0xF08  0 -> <cdoSet>  OK ; [GASX]   PLR AttributeSetHealthStorage MASKED OUT ; [GASX] ARM K1 done: PLAYER storages written 1/3",
   "[RCPT] GETTER_PLR_AFTER_K1 vtblExact=yes mode=3 f00=<KWIREGAS carrier ASC, UNCHANGED> f08=<cdoSet> predropHidden=0 MaxSpeed=43F38000(ok=1,487.00) MaxAccel=47435000(ok=1,50000.00) seed=487",
   "[RCPT] GETTER_BOT_AFTER_G vtblExact=yes mode=1 f00=<cdoASC> f08=<cdoSet> predropHidden=0 MaxSpeed=43F38000(ok=1,487.00) MaxAccel=<43AFC800 (45000)? NO \u2014 expect 47435000 (50000) because ARM G seeds 50000; either arm returns 50000 here \u2014 NOT a discriminator, per R-S189-MV-a> seed=487",
   "[GASX] ARM G done: storages written 3/3, attributes written 6/6",
   "[SNP] BOT pawn=0x... CharacterMovement@0x458 = 0x... ; CONTROL1 CharacterOwner PASS ; CONTROL2 TickFn.Target PASS (same for PLAYER)",
   "[SNP] BOT-before ... Velocity dec (0, 0, 0) ; GravityScale@0x1A0=1.0000  MovementMode@0x231=1 (MOVE_Walking) ; Accel@0x328 (0, 0, 0)   [BRANCH: MovementMode=3 (MOVE_Falling) = the hover branch; fallback expected at sample 8]",
   "[SNP] PLR-before ... GravityScale@0x1A0=0.0000  MovementMode@0x231=3 ; loc (0.000, 0.000, 13240.000)",
   "[SNP] BOT poison written -> readback OK ; [SNP] BOT sentinel Velocity = (0, 0, 0) -> readback OK ; [SNP] ARM K2: PLAYER GravityScale@0x1A0  0.0000 -> 1.0000  readback OK",
   "[BS] census A2 ... BotOrAIController-chain=+1 LokiHeroCharacter-chain=+1 ; [FS] disarm: restored=17563 of 17563",
   "[SNP] ---- sample i at t=+N ms (sinceArm M ms) ---- ; [SNP] BOT loc (~1356,-409,90.150) at sample 0 (Z SNAPPED from 92 -> 90.150 = AdjustFloorHeight receipt)",
   "[SNP] BOT |Vxy|=487 peak=487 mode=1 seed=487 atCap=EXACT (at least once, never >487.0001) ; BOT Accel@0x328 |A|~50000 with >=3 distinct headings over 40 samples ; BOT AnalogInputModifier = 1",
   "[SNP] FALLBACK NOT NEEDED at sample 8: bot moved <d>=50 uu horizontally with NO kick written -> bot motion is AI-driven.   [BRANCH: 'FALLBACK KICK fired ... readback OK' = S140 shape]",
   "[SNP] PLR: Z=13240 until the deliberate foreground cycle, then Velocity.Z -> -4000, LocZ -> 386 (ledge) ; during W-holds Accel@0x328 (0,-50000,0)-shaped ; after the slide MovementMode=1, LocZ=90.15, |Vxy|=487 atCap=EXACT inside W windows only; Accel (0,0,0) outside key windows",
   "[SNP] PEAK |Vxy| BOT=487 PLR=487 seed=487 ; [SNP] fallbackFired=0",
   "[BS] done (step=4 resolved=1 called=1 faulted=0 dCtl=1 dHero=1 hits=1 hitsGT=1)",
   "crashwatch: no 0x0000DEAD before the both-walking window ; Loki.log canaries: 0 Fatal / 0 'handing control over to crashpad'"
  ],
  "predictions": [
   {
    "id": "P0-BASELINE",
    "prediction": "After the edit, botai/gasattr/gasattr_ctrl/play/armk/axisab/sentinel_big/mv_play/seedmax rebuild BYTE-IDENTICAL to the pre-edit 14:24 baseline (b620e0ff3279e673 / 29f0fa4efffd01da / 4c0ede610baa4629 / 31af7667355f39d1 / 3ff3ba9f77ae1438 / 01593c0f8f73f1ae / <record> / 87c764b6d235b7a3 / b47d1bfd04e44921); botplay-floor, -noai, -5th, botwalk are four DISTINCT new digests.",
    "measured_how": "python tools/sigbypass-mod/text_digest.py tools/sigbypass-mod/build before and after; --dupes for the new set.",
    "if_false": "An edit leaked outside its knob (an #else with a string, or a runtime if(KBSPSARMS&bit) in shared code \u2014 the S140 gasattr 2fcc->6d81 mechanism). Do NOT fly; find the leaked string with a byte scan of the moved artifact for the new literals ('FLOOR OVERRIDE', 'MASKED OUT', 'FALLBACK', '[RCPT]')."
   },
   {
    "id": "P1-SPAWN",
    "prediction": "[BS] ARGS prints spawnLoc=(1356.0, -409.0, 92.0) and A2 reads dCtl=+1 dHero=+1 (exactly ONE new pawn, one LokiBotController).",
    "measured_how": "marker lines [BS] FLOOR OVERRIDE / [BS] ARGS / [BS] census A2 / [BS] done dCtl dHero.",
    "if_false": "dHero=+3 \u2192 KBSLBSKIPCTRL did not take effect (build used the wrong variant); spawnLoc still (600,0,13240) \u2192 KBSAIFLOOR not defined in the variant; dHero=0 \u2192 resolve refusal ([BS] REFUSE(...) names the cause) or the cell at (1356,-409) was not streamed (check A0's heroCand lines and Loki.log for cell loads)."
   },
   {
    "id": "P2-WALKING-AT-SPAWN",
    "prediction": "[SNP] BOT-before MovementMode@0x231=1 (MOVE_Walking) and BOT loc Z reads 90.150 by sample 0 (AdjustFloorHeight snapped 92.0 -> 90.150); TimeSinceFallingStart@0x12B0=0.0000.",
    "measured_how": "the game-thread BEFORE ShDump block (written while the ladder holds the thread) and sample 0's loc line.",
    "if_false": "MovementMode=3 with Z\u224892 held \u2192 FindFloor did not find a walkable floor at possess time (MaxStepHeight/streaming/penetration): this is the pre-registered HOVER branch \u2014 the bot stays at 0 uu (S139 f4 / S141 ARM K shape) until the fallback kick at sample 8; the result is then S140-shape (kick+AI), still both-walking but with the AI-from-rest question answered NEGATIVELY for Falling-rest and UNANSWERED for Walking-rest. If MovementMode=1 but Z stays 92.0 \u2192 AdjustFloorHeight did not run; read CurrentFloor@CMC+0x438 next flight."
   },
   {
    "id": "P3-AI-FROM-REST",
    "prediction": "With NO kick written, the bot's horizontal displacement exceeds 50 uu by sample 8 (the sampler prints 'FALLBACK NOT NEEDED'), |Accel|\u224850000 from the first sample with the heading rotating at ~2.0 s intervals, and MovementMode stays 1.",
    "measured_how": "[SNP] FALLBACK NOT NEEDED line; BOT Accel@0x328 across samples (>=3 distinct headings, 10-degree bins); BOT loc deltas; external motion_watch.py RandomMoveDirection churn.",
    "if_false": "Accel\u224850000 rotating but Velocity==0 and loc static in MOVE_Walking \u2192 a NEW Walking-mode zeroing site (the S141 T3 open question moves to Walking; read AnalogInputModifier and the getter receipt: MaxSpeed must be 43F38000). Accel==0 \u2192 gate/AIM/attr problem: check ARM F GATE=1, BOT_AFTER_G MaxSpeed bits, f08 non-null. Either way the fallback kick fires at sample 8 and the flight continues as S140-shape."
   },
   {
    "id": "P4-BOT-CAP-BIT-EXACT",
    "prediction": "BOT |Vxy| reads EXACTLY 487 (atCap=EXACT; 0x43F38000 as float, printed %.10g == 487) on >=2 non-adjacent samples with DIFFERENT headings, never >487.0001, Z=90.150 throughout; PEAK BOT=487.",
    "measured_how": "[SNP] BOT |Vxy| ... atCap=EXACT lines and the PEAK line (shim, %.10g); corroborated by the bot motion_watch_player CSV (3-decimal).",
    "if_false": "peak==500 \u2192 the value did NOT flow from the knob (a stock/other 500 source; R-S189-MV-a trap realised) \u2014 check [GASX] attr MoveSpeed readback and BOT_AFTER_G MaxSpeed bits; peak<487 and never at cap \u2192 wall-blocked wander (S141 s2 shape, |V|~93) or a different clamp \u2014 read Accel direction vs velocity and location; peak>487 \u2192 the bot is not reading the CDO set (f08 mismatch) or a kick was written (fallbackFired=1)."
   },
   {
    "id": "P5-PLAYER-WIRING",
    "prediction": "[GASX] ARM K1 done: PLAYER storages written 1/3 with two MASKED OUT lines; GETTER_PLR_AFTER_K1 prints f00 == the value ShResolveCmc/A0 saw before (the KWIREGAS carrier ASC, unchanged), f08 == the same cdoSet pointer as GETTER_BOT_AFTER_G, MaxSpeed=43F38000.",
    "measured_how": "the [GASX] PLR lines and the two [RCPT] lines (pointer equality across the two receipts).",
    "if_false": "written 3/3 \u2192 the mask did not apply (variant lacks -DKBSGASPLAYERMASK=0x2) \u2192 the player's +0xF00 is now the unbound CDO ASC: the player half is the UNTESTED K1 shape; still fly, but grade the player half [S]. MaxSpeed=00000000 with f08 set \u2192 the getter early-out is not the f08 test alone; read hero+0xF08\u2192set+0xF0/+0x100 CurrentValue directly next flight."
   },
   {
    "id": "P6-PLAYER-FALLS-THEN-WALKS",
    "prediction": "After K2 (GravityScale 0->1) the player hovers at Z=13240 until a deliberate foreground transition, then falls (Vz -> -4000) to the ledge Z\u2248386; W-hold #2 slides it off (diagonal Accel, Z 386 -> 90.15, MovementMode 3->1); W-hold #3 drives |Vxy| to EXACTLY 487 in Walking with Accel (0,-50000,0)-shaped ONLY inside logged key windows.",
    "measured_how": "player motion_watch_player CSV (Vel/Acc/Mode/LocZ with t_wall) joined to the send-wasd sync file's ARM_W_START/END timestamps; shim PLR samples for the first 40 s.",
    "if_false": "focusOk=True AND Accel==0 during a W-hold \u2192 the +0xF08-only K1 player does not accept input (the unflown wiring failed): switch to PLAN B (botplay-floor-5th after -mv-play) next launch. focusOk=False \u2192 uninterpretable, resend after SwitchToThisWindow. Player Accel non-zero OUTSIDE key windows \u2192 cross-contamination (something drives the player) \u2192 the player half is void. Still Z=13240 after 20 s post-K2 and two focus cycles \u2192 hover-trigger unresolved: record timestamps; fallback is a tiny -Y ARM J kick next flight (KSHPLRY=-1.0, untested)."
   },
   {
    "id": "P7-BOTH-WALKING",
    "prediction": "At least one pair of rows (player CSV, bot CSV) with |t_wall diff|<=0.2 s where both have Mode=1 and |Vxy|>300; the same-instant movementmode_readout snapshot lists both pawns Walking.",
    "measured_how": "join of the two motion_watch_player CSVs on t_wall; tools/re/movementmode_readout.py snapshot at ~probe+60 s.",
    "if_false": "Both halves walked but never overlapped \u2192 a scheduling failure (player WASD windows vs bot lifetime); record the two intervals and re-fly with the player's W-holds started immediately at [FS] disarm. One half never walked \u2192 its own prediction's branch applies."
   },
   {
    "id": "P8-NOAI-CONTROL (optional launch)",
    "prediction": "botplay-floor-noai: bot spawns Walking (or Falling), Accel stays (0,0,0), moves <50 uu by sample 8; the fallback kick then fires and the bot STOPS within ~0.3 s (BrakingDecelerationWalking 2048), one heading only, total displacement <~120 uu.",
    "measured_how": "same marker lines; FALLBACK KICK fired; samples 9-40 |Vxy| decaying to 0, Accel (0,0,0).",
    "if_false": "Bot translates >50 uu with Accel==0 before the kick \u2192 floor contact alone moves it and the AI attribution in P3 is VOID. Bot sustains 487 after the kick with Accel rotating \u2192 ARM F is NOT the gate (something else opened it) \u2014 re-read GATE+0x6A0 and the blackboard key."
   },
   {
    "id": "P9-HEALTH",
    "prediction": "No 0x0000DEAD before probe+120 s; the ~15.5 s game-thread freeze at the ladder hit does not kill; canaries 0 Fatal / 0 crashpad; the A1 stability census equals A0.",
    "measured_how": "docs/crashwatch.out.log (copy at end), Loki.log greps, [BS] census A1 line.",
    "if_false": "FK-32 before the window \u2192 everything captured stands (capture as you go); note the injection count (4) in the 0xDEAD series. SITTING VOID at A1 \u2192 nothing after is attributable; relaunch."
   }
  ],
  "controls": [
   "C1 \u2014 The player receives NO kick: KBSPSARMS excludes 0x800 (ARM J) and 0x2000; KSHPLRY stays 0 and is not compiled in. Any player Velocity must therefore come from gravity (K2) or WASD; the player's Accel outside logged key windows must be (0,0,0) \u2014 that line is the cross-contamination detector for the shared CDO set.",
   "C2 \u2014 The bot's kick is WITHHELD until sample KSHFALLBACKAT=8 and fires only if horizontal displacement < 50 uu; the sampler prints which branch it took. Rest->cap takes ~2 frames with |A|=50000 (aiwander lane [M]), so 8 s of rest is conclusive. Bot motion before sample 8 = AI; motion only after a printed 'FALLBACK KICK fired' = S140 shape.",
   "C3 \u2014 Rest baseline: the game-thread BEFORE ShDump block prints V=(0,0,0), Accel=(0,0,0), MovementMode, GravityScale, AnalogInputModifier for BOTH pawns before the thread is released; sample 0 then shows the bot already accelerating (P3) while the player is still at rest (no driver) \u2014 a two-pawn within-run contrast on the same frame.",
   "C4 \u2014 Identity controls on both CMCs (CONTROL1 CharacterOwner==pawn, CONTROL2 TickFn.Target==cmc, ShResolveCmc :17168-17185) and the ULokiCMC vtable check in the getter receipt; a wrong-object read is refused, not reported.",
   "C5 \u2014 Non-stock seed: KBSGASMOVESPEED=487.0f (0x43F38000), not the stock/prior 500 (R-S189-MV-a); a cap of exactly 487 on either pawn proves the value flowed from the CDO set this arm wrote. MaxAcceleration stays 50000 (collides with engine Super's stock; deliberately NOT used as a discriminator).",
   "C6 \u2014 Seed-reaches-getter receipts BEFORE any motion on both pawns ([RCPT] GETTER_BOT_AFTER_G / GETTER_PLR_AFTER_K1): MaxSpeed bits 43F38000 and the SAME f08 pointer on both \u2014 separates 'wiring failed' from 'mover wall' for every later null.",
   "C7 \u2014 Player +0xF00 untouched: the mask prints MASKED OUT for +0xF00/+0xF10 and the PLR receipt prints f00 == the pre-existing KWIREGAS carrier ASC; the player therefore differs from the flown -mv-play shape only in WHICH set object hero+0xF08 names.",
   "C8 \u2014 botplay-floor-noai (separate launch, only if budget allows or P3 is ambiguous): ARM F compiled out so the wander gate stays closed; predicts no translation before the fallback and a ~0.3 s stop after it (P8).",
   "C9 \u2014 Free within-run measurement of the floor branch: the requested Z (92.0) vs sample-0 Z (predicted 90.150) measures AdjustFloorHeight; BOT-before MovementMode measures SetDefaultMovementMode's FindFloor outcome directly (the S136-vs-S138 rest contradiction gets a data point either way).",
   "C10 \u2014 Stability: A0==A1 census with no call (SITTING VOID otherwise), and the SpawnAIFromClass call is single (dHero=+1), so any later object is the bot."
  ],
  "runbook": [
   "PRE-0 (offline, before any edit): digest the 14:24 baseline set and RECORD it (see build_defines); grep tutorial_launch.cpp for 'KBSAIFLOOR|KBSLBSKIPCTRL|KBSGASPLAYERMASK|KSHSAMPLEN|KSHFALLBACK|KBSGETTERRCPT' = 0 hits (fresh knobs). Apply the six source_edits; add the four build.ps1 lines; `build.ps1 -Name tutorial_launch -Variant botai,gasattr,gasattr-ctrl,play,armk,axisab,sentinel-big,botfight-damage-self-cal-bindavatar-seedmax,botfight-damage-self-cal-bindavatar-seedmax-mv-play,botplay-floor,botplay-floor-noai,botplay-floor-5th,botwalk` (\u26a0 `-Name tutorial_launch` is mandatory: `-Variant` alone silently builds the default set); verify P0-BASELINE with text_digest.py and --dupes; byte-scan botplay-floor for 'FLOOR OVERRIDE','MASKED OUT','FALLBACK','[RCPT]' (present) and gasattr for the same (absent) as the literal control; verify_dll.py VERDICT PASS on botplay-floor.",
   "PRE-1: write docs/s190-botplay-f1-PREREGISTERED.txt (artifact RAW/VSIZE, -D list, baseline digests, the P0-P9 table verbatim, the attribution rules from C1/C2/C5, the branch table for BOT-before MovementMode 1 vs 3) and commit it BEFORE launch. Back up docs/capture.log and docs/crashwatch.out.log.",
   "PRE-2: backend arming check \u2014 `(Invoke-WebRequest http://127.0.0.1:8080/core-game/players/<id> -UseBasicParsing).Content` shows a non-empty MatchID (forceTutorialMatch=true or the armqueue route); Steam running; no SUPERVIVE process alive; THREE elevated consoles C1/C2/C3; `$ev=scratchpad\\s190\\evidence; $label='botplay-f1'`.",
   "PRE-3 (C3): PRE-WARM the sender in-process: `try { & scratchpad\\s189-mv\\tools\\send-wasd.ps1 -Sequence f13 -HoldMs 50 } catch { 'prewarm ok' }`; confirm `[bool]('S189WasdInput' -as [type])` is True. Never use fly-wasd.ps1 (cold child PowerShell = the 35 s loss on ship-f1).",
   "T0 (C1): `.\\configs\\launch-redirect.ps1 -NoHook` (NO -CompanionWatch); record `$gpid` and `$base` from Get-Process.",
   "T0+0:20 (C2): start the 250 ms MARKER SHADOW loop (FileShare ReadWrite read of docs\\tutorial-launch-marker.txt -> $ev\\$label-marker-live.txt and -gen<N>.txt per truncation) and keep it running to the end (FK-25: every injection truncates the marker; the stager's own copies are ~650-byte headers).",
   "T0+0:30 (C1): `.\\configs\\fk24-stage.ps1 -Probe tools\\sigbypass-mod\\build\\tutorial_launch_botplay_floor.dll -Label $label` (no -BindOnly; -AllowStale only if the stager reports stale deployed fo/sp). Expected gates: parked match OK at >=T0+1:50 -> gft ~1:50 -> fo ~2:10 -> LVL_Tutorial load complete -> sp ~2:30 -> '[SP] done step=4' ~2:42 -> probe ~2:50. Yield ~1 in 2 launches; budget two.",
   "T0+2:42 (C2, at [SP] done): parse hero/CMC from the live marker (`spawnedPawn=0x..`, `[LIFT] gravity OFF (CMC=0x..`) \u2014 the [LIFT] line is the K2 premise; run preflight_read.py once; start `motion_watch_player.py $gpid $hero $pcmc 300` (player CSV) and `tools\\re\\motion_watch.py $gpid $base 400` (bot driver chain) BEFORE the probe.",
   "T0+2:50 probe injected; expect `[FS] arm: swapped=17563`, then within 8 s the ladder hit: the WORLD FREEZES ~15.5 s (A0 5 s, resolve, A1 4.3 s, ARM D/B/C/F/G/H, A2 4.3 s) \u2014 static watcher rows here are expected. C2 polls the live marker for `[SNP] BOT ... CharacterMovement@0x458 = 0x...`, then starts `motion_watch_player.py $gpid $bot $bcmc 240` (bot CSV). Confirm the receipt lines P1/P2/P5 (FLOOR OVERRIDE, one spawn, ARM F gate 1, [GASX] 3/3 + 6/6 with 487, PLR 1/3 masked, both [RCPT] lines with MaxSpeed=43F38000, BOT-before MovementMode) and NO REFUSE/VOID lines.",
   "T0+3:15 `[FS] disarm: restored=17563 of 17563` -> game thread free; the worker sampler now runs 40 samples at 1 s. C3 IMMEDIATELY (in-process): `& send-wasd.ps1 -Sequence w,w,w,d -HoldMs 3000 -InterKeyMs 5000 -RepeatMs 33 -SyncFile $ev\\$label-wasd-sync.txt` (expect focusOk=True). The focus grab is the deliberate foreground transition that breaks the player's post-K2 hover (F3 and ship-f1 both fell within seconds of one, [I]); W#1 at hover shows AccY\u2248-50000 with Vel 0; W#2 slides off the ledge (Z 386 -> 90.15, Mode 3->1); W#3 walks at the 487 cap in -Y; D is the strafe check. If LocZ is still 13240 20 s after K2: ShowWindow(h,6)/ShowWindow(h,9)/SwitchToThisWindow cycle from C3, then resend w,w. LOG EVERY TIMESTAMP (hover trigger is unsettled).",
   "T0+3:25..3:55: read the sampler live: BOT 'FALLBACK NOT NEEDED' at sample 8 (or 'FALLBACK KICK fired' = S140 shape), BOT atCap=EXACT lines, rotating Accel; PLR lines inside the W windows. `[BS] done` closes the probe ~T0+3:55 -> C2 copies the live marker to $ev\\$label-marker-FINAL.txt.",
   "T0+3:55 (probe+60 s, C1): `tools\\usmapdump\\usmapdump.exe dumpimage SUPERVIVE-Win64-Shipping.exe dumps\\$label` (pure RPM, no suspend \u2014 TAKE IT EARLY); then `py tools\\re\\movementmode_readout.py $gpid $base | Tee $ev\\$label-movementmode-snapshot.txt` (both pawns in one pass) and a second player W-hold set for a longer walk while the bot CSV keeps running. Do NOT inject anything else in PLAN A. FK-32 expected ~T0+5:20-5:50.",
   "AT DEATH / END: copy docs\\crashwatch.out.log, docs\\fk24-stage-$label-*.txt, %LOCALAPPDATA%\\SUPERVIVE\\Saved\\Logs\\Loki.log into $ev; stop the shadow loop; `py tools\\re\\dump_coverage_ledger.py`; write docs/s190-botplay-f1-RESULT.md from the evidence files (grade each P0-P9 against the pre-registration; quote the shim %.10g lines for bit-exactness, the CSV only to 3 decimals).",
   "PLAN B (if P6's K1-broke-input signature fires, or the operator prefers per-pawn discrimination): next launch stage `-Probe tools\\sigbypass-mod\\build\\tutorial_launch_botfight_damage_self_cal_bindavatar_seedmax_mv_play.dll` (ship-f1 recipe, player 500 on its own set), wait for `[BF] worker done` and >=20 s, then `tools\\inject\\inject.exe mmap $gpid tools\\sigbypass-mod\\build\\tutorial_launch_botplay_floor_5th.dll` only if Get-Process -Id $gpid is alive; the shadow loop's -gen<N>.txt is the only copy of the -mv-play generation. Expect bot 487 vs player 500 (disjoint sets). ZERO-SOURCE contingency: substitute tutorial_launch_botwalk.dll (kick at arm time, three stacked bots) as the 5th map."
  ],
  "hazards": [
   "GATE DRIFT [M, this session]: the CLAUDE.md-recorded botai/gasattr/gasattr-ctrl/play digests do not reproduce from clean HEAD (14:24 artifacts: b620e0ff3279e673 / 29f0fa4efffd01da / 4c0ede610baa4629 / 31af7667355f39d1); only seedmax and mv-play match. Any 'gates unchanged' claim must be against the re-recorded baseline, or a pre-existing drift will be misattributed to this edit (or a real leak certified as clean). [I,strong] the mover is commit 4824c77 (shared Worker-exit VEH/VirtualFree cleanup) \u2014 unconfirmed.",
   "BfObsMovGetters is NOT reusable in RM_BOTSPAWN [M]: it is nested under `#if KBFSELFCAL` (22157-22404) and KBFSELFCAL static_asserts kRunMode==RM_BOTFIGHT (:419-420). The shimdesign lane's '[I] compiles cleanly with -DKBFOBSMOVGETTERS=1' is refuted; the design carries its own [RCPT] helper instead.",
   "Preprocessor discipline: every new block is `#if KNOB ... #endif` with no string-bearing #else; the ONLY #else re-emits the original sampler loop head. A runtime `if(KBSPSARMS&bit)` or a skip-marker in shared code moves control digests at unchanged size (S140: gasattr 2fcc->6d81). Verify with the literal byte-scan control in PRE-0.",
   "Do NOT spawn the bot below the measured rest Z (90.150): bNoCollisionFail=1 => SpawnCollisionHandlingOverride=AlwaysSpawn [M 0x4631D3F-0x4631D55], no adjustment, a penetrating capsule makes FindFloor fail -> MOVE_Falling hover. Keep KBSAILOC_Z in [90.5, 100]. Do not use the player's Z=13240 or the ledge (0..363,-375,386).",
   "ARM D's three spawns would stack at one point with AlwaysSpawn (S136 bot2-on-bot1, dZ=176.1); KBSLBSKIPCTRL=1 is what prevents it \u2014 confirm dHero=+1 in A2 before reading any bot motion.",
   "The end-of-sampler restore (:17509-17513) fires for a ZERO sentinel (ShEq3 matches, g_shSentinelOK=1 at :17266) and would zero a walking bot once; it is compiled out ONLY under bit 0x4000. A variant with 0x200 but not 0x4000 and a zero sentinel inherits that defect.",
   "Player kicks: KSHPLRY must stay 0 and bit 0x800 must be clear \u2014 ARM J's +Y 600 sent the player off the island (arml:368); with KSHPLRY=0 ARM J would instead ZERO a walking player's Velocity at arm time. KSHPLRX is an unused define (no X kick exists).",
   "K2 fires only if the player's GravityScale reads exactly 0 (`ARM K2 SKIPPED` otherwise, :17318); premise = sp's LIFT step \u2014 confirm `[LIFT] gravity OFF` in the sp generation before attributing any player fall.",
   "The player's post-gravity hover has lasted ~115 s (ship-f1: poke \u224813:40:25, fall onset \u224813:42:20) and >6 s (F3); both onsets coincide with a new foreground window [I]. The runbook forces the transition deliberately; if it does not work the flight's player half stalls. A tiny -Y ARM J kick (KSHPLRY=-1.0) is the untested alternative \u2014 do not add it to this flight (it would violate C1).",
   "ARM G writes the process-wide Default__LokiPlayerState_HeroAffiliated AttributeSet subobject (never undone) and, with the mask, points the player's +0xF08 at it: both pawns cap at 487 by construction, so per-pawn SPEED discrimination is impossible in PLAN A (only heading/key-window kinematics separate them); PLAN B has disjoint sets. A second ARM-G bot in one world would share the same set.",
   "The +0xF08-only K1 player under WASD is UNFLOWN: the getter path is identical to mv-play (hero+0xF08 -> set+0xF0/+0x100 [M]) but the set OBJECT differs and there is no S155 bind / S156 seed in RM_BOTSPAWN. P6's signature (focusOk=True, repeats>=60, Accel==0 through a W-hold) is the K1-broke-input readout; PLAN B is the remedy.",
   "The ~15.5 s game-thread freeze at the ladder hit (censuses A0/A1/A2 ~13.7 s + sleeps) is the largest freeze any arm imposes; flown >=6 times without immediate kill. Sending WASD during it wastes the hold \u2014 gate the first send on `[FS] disarm:`.",
   "Armed window is ~140-175 s, not 300 s: the probe lands at ~launch+170 s and FK-32 fires at launch+318..350 s [M]. Bot walk, three W-holds, the movementmode snapshot and the dumpimage must all sit inside probe+120 s; take the dump EARLY.",
   "Off-thread sampler reads carry a TOCTOU/GC race (existing per-sample __try at :17369 keeps earlier samples); a bot that walks off the island edge (S140: ~13,000 uu later) and is destroyed must not void earlier samples. The stuck-check action 0x5566870 (DARK) can fire on a stationary LokiBotController pawn after ~15 s \u2014 only relevant on the hover branch.",
   "The bot may bump the player: (1356,-409) is ~750 uu from the F3 floor path (683,-716)->(953,-2108); a bot heading change NOT preceded by a RandomMoveDirection change in motion_watch.py's RMD column is a collision, not a wander roll.",
   "Do not re-run ALokiBotController::OnPossess after ARM G \u2014 with +0xF00 non-NULL its GE loop (0x5565932-0x5565A45) would ApplyGameplayEffect into the shared CDO ASC. Nothing in this design calls it; keep it that way.",
   "Marker truncation (FK-25): the stager copies the probe marker 2 s after mapping (fk24-stage.ps1:918/:929) \u2014 a header only; every [SNP]/[GASX]/[RCPT]/[BS] line exists only in the live file. The shadow loop is mandatory. crashwatch.out.log is overwritten per launch and capture.log is truncated on ags restart \u2014 copy both first.",
   "The VERDICT block of ARM H prints 'payload isSentinel'/'StartNewPhysics RAN' against a (0,0,0) sentinel \u2014 cosmetic under this variant; do not quote it as a StartNewPhysics result.",
   "External instruments: all consoles elevated (OpenProcess 0x1F0FFF, UIPI parity for SetForegroundWindow); exactly ONE SUPERVIVE process (send-wasd's MainWindowHandle read breaks on an array); motion_watch.py takes the FIRST LokiBotController-chain object (fine: exactly one exists under KBSLBSKIPCTRL=1)."
  ],
  "open_questions": [
   "Which commit moved the RM_BOTSPAWN-family and play digests between S153 (09-02) and today? [I,strong] 4824c77 (shared g_vehHandle + Worker-exit cleanup); settle with one rebuild at 4824c77^ vs 4824c77 and then update CLAUDE.md's stale gate values (botai/gasattr/gasattr-ctrl/play) \u2014 out of scope for the flight but needed before any other session cites them.",
   "Does a LokiBotController-possessed BP_HERO_Ronin_C spawned at root Z=92 over the (1356,-409) floor come up MOVE_Walking at possess time? [I ~0.8-0.9] from SetDefaultMovementMode 0x35F9B30 + intact FindFloor; this build's MaxStepHeight and any ULokiCMC FindFloor override are unread; DefaultLandMovementMode==Walking is DEDUCED from the air bot reading 3, not read at CMC+0x540. The BOT-before MovementMode byte answers it for free.",
   "Why did S136's two plain-AIController pawns spawned at Z=13240 fall to the floor while every LokiBotController/ARM-F pawn since S138 hovers at (600,0,13240)? Unexplained; the floor spawn sidesteps it, and the fallback covers the hover branch, but the mechanism (LokiBot possession vs ARM F vs Vz=0 quirk) remains open.",
   "Does the +0xF08-only K1 player accept WASD -> Acceleration -> Velocity? Never flown (every KBSGASPLAYER=1 flight had no input driver). P6 answers it; PLAN B avoids it.",
   "What breaks the player's post-GravityScale hover? Both recorded onsets coincide with a new foreground window [I]; the runbook's deliberate ShowWindow/SwitchToThisWindow cycle either settles it (log the latency) or leaves a tiny -Y ARM J kick as the next lever.",
   "Which MaxAcceleration does the Walking arm read on each pawn \u2014 the seeded +0x120 (S189-MV F1 [M] for the player's own set) or something else (F3 measured |Accel|=45768 with 45000 seeded)? With ARM G seeding 50000 (== engine stock) this flight cannot discriminate; a non-stock ARM G MaxAcceleration (e.g. 45000) would need a source edit to the ARM G table (:16552) and is deliberately NOT made here (50000 is the flown value).",
   "Is botplay-floor-noai worth a launch of its own (~1-in-2 staging yield)? The within-run C2 fallback already separates kick from AI; fly noai only if P3 comes back ambiguous or budget permits.",
   "Does the bot's random wander keep it on the island long enough (S140: walked off the edge after ~13,000 uu \u2248 27 s at 487)? The 40 s sampler may outlive the bot; the spawn point could be moved more central next flight if the bot leaves early.",
   "HeroPredropHidden (+0x1BE8) on a SpawnAIFromClass bot has never been read; the [RCPT] line reads it by name (predicted 0). A non-zero would make any future reflected SetMovementMode on the bot a silent no-op (Loki override 0x055C0AC0 refusal gate)."
  ]
 },
 "verdicts": {
  "cdo-borrow": {
   "refuted": false,
   "confidence": "high on the CDO-borrow/attribute mechanism itself (a,d are [M] and flown); medium on the player half's OUTCOME (c is a genuine UNFLOWN [I,strong], not byte-broken). No load-bearing claim in my lens rests on a digest, and none is byte-refuted, so the flight is not void \u2014 but two of the four claims are weaker or more entangled than the design's framing states.",
   "refutations": [
    {
     "target": "Claim (b): 'writing into the CDO subobject is inert for the PLAYER's live sets' \u2014 cited as the within-run specificity control",
     "argument": "Design D's chosen wire (KBSGASPLAYER=1, KBSGASPLAYERMASK=0x2) POINTS the player's +0xF08 at src[1] = the CDO AttributeSet subobject. So the CDO write is not inert for the player \u2014 it is the player's ENTIRE attribute source. The 'PLAYER hero is deliberately UNTREATED: within-run specificity control' that ARM G's own comment advertises (tutorial_launch.cpp:16620, and the flown marker at arml:163) is VOID under this design: player and bot read ONE shared CDO set and both cap at the same 487. This is exactly the configuration the botgas lane recommends AGAINST ('keep KBSGASPLAYER=0 ... the two pawns read DIFFERENT attribute sets ... the only configuration in which bot peak==bot seed and player peak==player seed are simultaneously discriminating'). The session GOAL names 'the bot's peak bit-exact to ITS seeded MoveSpeed'; with a shared set, a 487 cap cannot be attributed to the bot's set versus leakage into the player, so C5's non-stock discriminator does not achieve per-pawn attribution \u2014 only that the value flowed from the knob.",
     "evidence": "tutorial_launch.cpp:16589-16608 (K1 loop writes storages onto g_bsPlayerHero from src[i]=CDO subobjects); 16620 + arml:163 ('PLAYER hero deliberately UNTREATED... within-run specificity control'); botgas lane recommendation and hazard ('KBSGASPLAYER=1 makes the player and bot read ONE shared CDO set; with it on, peak==seed cannot discriminate which pawn's seed was read'); design's own risk field ('player and bot share one CDO set so both cap at 487').",
     "severity": "major",
     "fix": "Either accept that per-pawn SPEED discrimination is unavailable in PLAN A and lean entirely on the built-in KINEMATIC attribution (bot = rotating |Accel| heading + FALLBACK-NOT-NEEDED; player = (0,-50000,0) Accel only inside logged W-windows) \u2014 which the design already provides \u2014 OR switch to PLAN A' (5th injection of -mv-play so the player reads its OWN spawnedSet0 at 500 and the bot reads the CDO set at 487; disjoint sets by construction). Do NOT cite the player as a clean control for the CDO write."
    },
    {
     "target": "Claim (c): the +0xF08-only K1 player wire is 'movement-equivalent to the flown -mv-play wire and does not break WASD->Acceleration (AnalogInputModifier=1)'",
     "argument": "This is [I,strong], NOT measured, and the design correctly labels the player half [S]/UNFLOWN \u2014 but the framing understates two real differences from the flown -mv-play chain. (1) IDENTITY of +0xF00: -mv-play leaves +0xF00 = the S155-BOUND carrier ASC (AvatarActor=hero, InitAbilityActorInfo run); design D leaves +0xF00 = the KWIREGAS ASC (a different instance). IsStunned/HitStop/ConstrainInputAcceleration all read +0xF00's ASC tag container; the getter path never needs AvatarActor, so equivalence holds ONLY if the KWIREGAS ASC's tag container is empty of Stunned \u2014 which is [I] (no damage), one step weaker than the bot's CDO ASC which is a pristine default subobject and provably empty. (2) VALUE of MaxAcceleration: the CDO borrow writes 50000 (tutorial_launch.cpp:16552), which collides bit-exact with engine Super, whereas -mv-play deliberately seeds 45000 to be distinctive (22289 comment). So the player's GETTER_PLR_AFTER_K1 receipt's MaxAccel=47435000(50000) is NON-discriminating (it cannot prove the seed reached the getter), and the receipt reduces to the MaxSpeed=43F38000(487) line alone. The exact combination (+0xF08->CDO set with a KWIREGAS ASC at +0xF00, driven by WASD) has never been flown \u2014 every prior KBSGASPLAYER=1 flight had NO input driver (arml PLAYER Accel (0,0,0)).",
     "evidence": "tutorial_launch.cpp:13753 (KWIREGAS wires the sp player's +0xF00); 18091-18092 ('KWIREGAS only wires +0xF00', +0xF08=NULL); 16552 (ARM G MaxAcceleration hardcoded 50000.0f); 22289-onward S189MvSeedRow comment (45000 chosen because 50000 collides with Super and makes 'getter reads seed' INDISTINGUISHABLE from Super bypass); arml:55-60 (K1 player had no input driver, Accel (0,0,0) throughout).",
     "severity": "major",
     "fix": "Keep the design's own signature test verbatim (focusOk=True AND repeats>=60 AND player Accel==0 through a W-hold => K1 broke input => PLAN B next launch). Add a note that the player MaxAccel receipt is non-discriminating (only MaxSpeed=43F38000 proves the wire). Grade the player half [I,strong] in the pre-registration, not [M], and do not report 'both walked' as confirming (c) unless the player CSV shows |AccY|>=40000 inside a logged W-window."
    },
    {
     "target": "Design-package internal contradiction: which getter-receipt helper to use (BfObsMovGetters vs the custom BpGetterReceipt)",
     "argument": "The shimdesign lane's RECOMMENDED build line appends -DKBFOBSMOVGETTERS=1 (reuse BfObsMovGetters), while its own hazard says 'BfObsMovGetters is NOT reusable in RM_BOTSPAWN' and the CHOSEN build_defines for botplay-floor use -DKBSGETTERRCPT=1 (the custom BpGetterReceipt in source_edit #4). The chosen design is internally consistent (custom helper), but the package ships two mutually exclusive instructions; a builder following the shimdesign recommendation would set a knob that, if BfObsMovGetters is genuinely unreachable outside its guard, silently emits no receipt (no compile error, no marker) \u2014 reading later as 'the seed did not reach the getter'.",
     "evidence": "BfObsMovGetters guarded by '#if (KBFSEEDMOVEMENT || KBFOBSMOVGETTERS)' at tutorial_launch.cpp:22289 (the S189MvSeedRow block), with a KBFSELFCAL static_assert(RM_BOTFIGHT) at :420 governing the self-cal path; shimdesign build line uses -DKBFOBSMOVGETTERS=1 while its hazard and the chosen -DKBSGETTERRCPT=1 use the custom helper.",
     "severity": "minor",
     "fix": "Use ONLY the custom BpGetterReceipt (-DKBSGETTERRCPT=1) as in the chosen build_defines; delete -DKBFOBSMOVGETTERS=1 from any RM_BOTSPAWN line. Before relying on the receipt at all, verify BpGetterReceipt's deps compile (SEH_FILTER exists at :1301/:1303, g_modBase+0x088F8570 at :22368, disp 0x4C8/0x7D0 at :22385, PropOffsetSuper 'CharacterMovement'/'HeroPredropHidden' \u2014 all present)."
    }
   ],
   "survives": [
    "Claim (a) SURVIVES as [M]: ARM G resolves the CDO's AbilitySystemComponent/AttributeSet/AttributeSetHealth subobjects (tutorial_launch.cpp:16521-16530) AND the six movement attrs (16548-16564) entirely BY NAME via PropOffsetSuper, with per-write readback, and it is flown 3x with the CDO borrow landing all six attrs readback-OK (arml:143-155: src AttributeSet=LokiAttributeSet, MoveSpeed@+0xF0..Mass@+0x170 all OK) and the bot WALKED (S140/S141). The flight does not depend on any hardcoded offset; a wrong offset or a stripped set would print a readback failure, not a false success. Setting KBSGASMOVESPEED=487.0f (0x43F38000, verified) is a clean single-knob change.",
    "Claim (d) SURVIVES as [M] FOR THE BOT: hero+0xF00 pointing at the CDO ASC is inert on the movement path is proven by the walk itself \u2014 the bot fell, landed and walked 13,187 uu at the seeded cap with the CDO ASC at +0xF00 (S140 armj), which is impossible if HitStop had zeroed dt or ConstrainInputAcceleration had zeroed input. The byte model (IsStunned 0x55B2930 reads +0xF00->tag container; ConstrainInputAcceleration 0x55A75B0 zeroes only on the IsStunned-true arm) is corroborated by the measured walk. Design D correctly AVOIDS extending (d) to the player by masking +0xF00 out (0x2), which preserves the KWIREGAS binding and dodges the botgas K1-full orphaning hazard.",
    "The +0xF08-only mask is mechanically correct: mask bit1 (0x2) -> i=1 -> kDst[1]='AttributeSetStorage' -> +0xF08 written; i=0/+0xF00 and i=2/+0xF10 skipped (continue), so +0xF00 stays the KWIREGAS ASC; pwrote=1, marker 'written 1/3' as predicted. The '#if (KBSGASPLAYERMASK)!=0x7' guard keeps armk/axisab (default mask 0x7) byte-identical.",
    "The getter path is byte-sound: GetMaxSpeed reads hero+0xF08 -> set+0xF0 min set+0x100; the CDO set carries MoveSpeed=MaxMoveSpeed=487 -> GetMaxSpeed=487; 487.0f = 0x43F38000 (verified). The player's +0xF00=KWIREGAS ASC is real (13753 wires it in sp; arml:157 shows it non-zero pre-K1), so P5's premise holds.",
    "Process-wide safety of the CDO write ('inert for every OTHER object'): [I] but flown-safe 3x with no world corruption; no known live consumer of the Default__LokiPlayerState_HeroAffiliated default subobjects (a real spawn crashes per S80), and the player's KWIREGAS ASC's own SpawnedAttributes are a separate KWIREGAS-built set, not the CDO set."
   ],
   "additional_hazards": [
    "The design's own P0-BASELINE is the real digest exposure (out of my lens but load-bearing for 'gates unchanged'): shimdesign MEASURED that botai/gasattr/gasattr-ctrl/play do NOT reproduce the CLAUDE.md-recorded digests from HEAD today (14:24 artifacts b620e0ff3279e673 / 29f0fa4efffd01da / 4c0ede610baa4629 / 31af7667355f39d1). Any 'gates unchanged after my edit' claim must be against the freshly-recorded 14:24 baseline, or a pre-existing drift (candidate 4824c77) is misattributed to this edit.",
    "C7 as written ('player +0xF00 == the pre-existing KWIREGAS carrier ASC') is a good control ONLY if you also confirm the KWIREGAS ASC is bound (AvatarActor/IAbilitySystemInterface consistent) \u2014 the receipt prints f00 but does not verify the binding; a KWIREGAS ASC that failed to InitAbilityActorInfo would still print a non-null f00 while IsStunned/HitStop read a differently-shaped ASC. Cheap to add: read the ASC's AvatarActor and compare to the hero.",
    "The bot getter receipt line BOT_AFTER_G MaxAccel is non-discriminating (50000 = Super bypass = seed), as the design's own marker_receipts note admits; do not read a 50000 there as proof the seed reached the getter \u2014 only the 487 MaxSpeed line does that for either pawn.",
    "ARM K1 writes are never undone (no restore of the player's +0xF08); on a K1-masked player the +0xF08 stays pointed at the CDO set for the process lifetime. Harmless here, but if any later arm in the same process reads the player's attributes it inherits 487/50000."
   ]
  },
  "ai-drive": {
   "refuted": true,
   "confidence": "medium-high",
   "refutations": [
    {
     "target": "P2/P3 + spawnloc lane: 'a floor-spawned ARM-D LokiBot bot walks under its OWN AI from EXACT rest in MOVE_Walking with NO kick' (design confidence ~0.8; recommendation calls the kick 'redundant').",
     "argument": "The one unexplained wall this movement track has NOT closed is bot-specific Velocity-zeroing FROM REST, and a floor-spawned-at-rest bot exercises it for the first time. Every measured bot walk (S140 armj, S141 arml) entered Walking WITH prior momentum: kicked (600,0,0) -> fell -> LANDED with horizontal velocity -> walked. The bot has never started from |V_xy|=0 in Walking. The player-from-rest analogy (F3) is the exact analogy S141 T3 breaks: the bot has a rest-zeroing the player does not, and its mechanism is UNKNOWN. 'Confined to MOVE_Falling' is [I] (argument from stock PhysWalking), not measured.",
     "evidence": "docs/s141-tier3-settled.md:78 '### 0.1 The bot's zero is NOT self-inflicted [M]'; :150 'Something ELSE zeroes all three components on the bot'; :148-152 (bot MOVE_Falling, GravityScale 1.0, StartNewPhysics running every frame, Velocity==(0,0,0)). aiwander understand-lane finding #1 self-admits 'exact-rest-in-Walking on the bot is unmeasured'. Design D rationale itself grades floor-spawn->Walking [I,~0.8-0.9].",
     "severity": "major",
     "fix": "Lower P3 confidence to ~0.5 and pre-register BOTH outcomes as first-class: if the fallback fires the result is only the already-[M] S140 kick+AI shape and the bot half adds nothing NEW. Do not present pure-AI-from-rest as the expected case."
    },
    {
     "target": "P4 / success_criteria: 'peak |V_xy| == seed BIT-EXACT' (atCap=EXACT via vxy==(double)(float)KBSGASMOVESPEED; '>=2 [SNP] BOT Velocity dec lines whose 10-digit hypot is exactly the seed').",
     "argument": "The AI wander produces diagonal headings, so a capped Velocity is (dirX*487,dirY*487) with dir normalized to 1.0 +/- 1 ULP; hypot(Vx,Vy) is 487.0 +/- a few ULP, essentially never bit-equal to 487.0. The shim's == test and the '10-digit hypot exactly the seed' criterion will print atCap='no' on LEGITIMATELY CAPPED samples, risking a false 'not at seed' read. The flown evidence proves it: S140/S141 'exactly 500.0' caps are hypots of DISPLAY-rounded (%.3f) components, not bit-exact.",
     "evidence": "docs/s141-t3-arml-result.md:49 s1 `(-17.254,499.702,0)`; :62-63 '|(-17.254,499.702)| = 500.0'. Direct: sqrt(17.254^2+499.702^2)=499.9998, NOT 500.0 -> the '500.0' is %.1f rounding; a bit-equality on 487 reads 'no'. docs/s140-t2-armj-THE-BOT-WALKS.md:60-62 (two samples 500.0, one 377.3).",
     "severity": "major",
     "fix": "Gate success on the ROBUST parts: 'never > seed+small' (<=487.05) AND a tolerance band |peak_2D - 487| <= 0.02, or read the stored GetMaxSpeed() attribute (43F38000) as the exact receipt. Only axis-aligned samples can be bit-exact and the wander rarely produces one; do not gate the BOT on bit-equality of its 2-D magnitude."
    },
    {
     "target": "build_defines vs source_edits: KBSAILOC_Z is 92.0 in one build line but 100.0 in the pseudo_diff default/shimdesign line; both are ABOVE the measured rest root 90.150.",
     "argument": "(1) Internal inconsistency: whichever Z compiles changes the spawn state, and P1/P2 receipts pre-register 92.0 while the buildable pseudo-diff default is 100.0. (2) Both above rest: at rest the capsule bottom is ~2.1 uu above the surface (90.150-88.0504) ~within MAX_FLOOR_DIST 2.4; root 92 puts the bottom ~3.95 uu up and root 100 ~11.95 uu up, beyond MAX_FLOOR_DIST, so spawn-time FindFloor may report no walkable floor -> MOVE_Falling. An ARM-D LokiBot in Falling at rest HOVERS (S138-S141), forcing the fallback kick and collapsing to the already-[M] S140 shape instead of the NEW pure-AI result.",
     "evidence": "Design build_defines botplay-floor `-DKBSAILOC_Z=100.0` vs options.A build_defines `-DKBSAILOC_Z=92.0` vs pseudo_diff `#define KBSAILOC_Z 100.0`; marker '[BS] FLOOR OVERRIDE -> (1356.0,-409.0,92.0) root Z+1.85'. spawnloc lane: 'prefer exact 90.15 so this never matters'; half-height 88.0504 (docs/s136-ai-controller-settled.md). Hover: docs/s138-flight9-movement-not-simulating.md:22; docs/s141-t3-marker-armk.txt (bot 'moved 0.000 uu').",
     "severity": "major",
     "fix": "Set KBSAILOC_Z = 90.15 EXACTLY (the measured rest root, NOT below rest, no penetration) in ONE authoritative build line; delete the 92/100 variants. Keep the fallback and hazard the possibility a LokiBot hovers even on a walkable floor."
    },
    {
     "target": "Fallback attribution gate: 'fire the kick only if bot moved < 50 uu NET displacement from g_shLoc0[0] by sample 8'.",
     "argument": "The fallback fires on NET displacement, the wrong quantity: a genuinely AI-walking bot with 2 s random headings can net-cancel toward origin (+Y then -Y) and read <50 uu net despite thousands of uu path length, writing a spurious kick and DOWNGRADING a correct pure-AI result to the S140 label. A resting bot has |V|=0 while a wandering one has |V|~487 regardless of net displacement.",
     "evidence": "Design pseudo_diff (sampler e): `if(d>=0.0 && d<(double)(KSHFALLBACKMINUU) ...) memcpy(...kShFallback...)`, d = net root+0x158 distance. Wander cadence 2.0 s (docs/s138 f8) over 8 s = ~4 rolls.",
     "severity": "minor",
     "fix": "Gate the fallback on max |V_xy| over samples 0-7 < ~5 uu/s, not net displacement. Recoverable post-hoc: samples 0-7 record raw |V_xy| before any kick, so a spurious fallback does not destroy the pure-AI evidence -- but do not trust the auto-label as printed."
    },
    {
     "target": "Per-pawn speed discrimination under design D (shared CDO set) and R-S189-MV-a strength.",
     "argument": "KBSGASPLAYER=1 mask 0x2 points the PLAYER's +0xF08 at the SAME CDO AttributeSet ARM G seeds, so both pawns read one set and both cap at 487. A 487 cap does not discriminate WHICH pawn's set drove it (there is one set). The bot half of the goal is still met (487 is non-stock so it cannot be a stock 500/600 leak), but the stronger per-pawn cross-check (player 500 own set, bot 487 CDO set) is forfeited and a CDO->player leak is invisible.",
     "evidence": "Design D risk 'both cap at 487'; marker 'PEAK |Vxy| BOT=487 PLR=487'; botgas lane recommendation 'Wire the two pawns to two DISJOINT attribute sets ... KBSGASPLAYER=0'.",
     "severity": "minor",
     "fix": "Accept for design D (487 non-stock is sufficient to attribute the BOT cap to the knob). Do NOT claim per-pawn discrimination; if wanted use A'/PLAN B at the cost of a 5th injection."
    },
    {
     "target": "Whole-goal dependency: the PLAYER half uses the UNFLOWN +0xF08-only K1 wiring to the shared CDO set, not the proven mv-play chain -- so 'BOTH walk' rests on untested wiring.",
     "argument": "The session goal requires BOTH pawns walking; design D reaches the player via ARM K1 (mask 0x2), a wiring never exercised under WASD. Every KBSGASPLAYER=1 flight had NO input driver (player Acceleration (0,0,0)), so 'K1-treated player accepts WASD -> Acceleration -> Velocity' is [S]. If the player half fails, the bot can still walk but BOTH cannot -- the stated goal is unmet even though the bot arm is fine.",
     "evidence": "botgas finding grade S 'The +0xF08-only K1 player under WASD is UNFLOWN'; docs/s141-t3-arml-result.md (K1 player Accel (0,0,0), 'no input driver'). Proven player chain is mv-play (S155 bind + S156 seed + S189-MV own spawnedSet0), unused by design D for the player.",
     "severity": "major",
     "fix": "Prefer PLAN B/A' for the player (mv-play as the 4th inject, bot-only arm as the 5th) so the player half is FLOWN wiring; OR keep design D but gate 'both walk' on the K1-broke-input disjunction (focusOk=True AND player Accel==0 through a W-hold => K1 broke input => switch to PLAN B) and grade the player half [S] in advance."
    }
   ],
   "survives": [
    "The bot WILL walk with high probability: the withheld fallback kick guarantees horizontal motion even if pure-AI-from-rest fails (kick from ~12 uu up lands, AI drives -- the already-[M] S140 shape). Only 'under its OWN AI with no kick' is contingent.",
    "The attribution MACHINERY is sound and self-labeling: it withholds the kick until sample 8, fires only on a stationary bot, and prints FALLBACK NEEDED / NOT NEEDED either way; raw per-sample |V_xy|/Accel are captured before any kick, so a mislabeled branch is recoverable.",
    "The 487.0f (0x43F38000, verified: 0 10000111 11100111000000000000000) non-stock seed correctly satisfies R-S189-MV-a for the BOT: a bot cap of 487 cannot be a stock 500 and attributes the value to the knob-written CDO set.",
    "Skipping ARM D's A/C control spawns (KBSLBSKIPCTRL=1) is index-safe (g_psLbCtl[1] consumers unchanged) and IMPROVES the floor spawn (no stacked pawns for B to depenetrate from); LokiBotController creation is still shown via poke/restore readback and dCtl=+1/dHero=+1.",
    "The floor spawn correctly sidesteps the air-hover contradiction for the MovementMode-at-spawn question: SetDefaultMovementMode -> FindFloor runs at PostInitializeComponents at the spawn Z BEFORE possession, stock/unoverridden on the shared ULokiCMC vtable (spawnloc lane [M]).",
    "The 1000 ms sampler cadence vs the 2.0 s wander cadence is adequate for observing >=3 distinct headings over 40 samples; it cannot catch the ~10 ms rest->cap ramp but the goal needs sustained walk + cap, not the ramp.",
    "Digest discipline (every edit behind a NEW #if knob with no string-bearing #else, re-baselining from today's artifacts rather than the stale CLAUDE.md gate values) is the correct method to keep the six named gates byte-identical."
   ],
   "additional_hazards": [
    "Scheduling/FK-32: if the bot needs the fallback (fires ~probe+35-39 s, walks by ~probe+50 s) AND the player enters Walking only after the hover->fall->ledge->slide sequence (~probe+60-90 s), the OVERLAP window where BOTH are Walking at the same t_wall (P7) may be brief before FK-32 at ~probe+120-180 s. Start the player's W-holds at [FS] disarm and take the movementmode_readout snapshot as early as both move.",
    "If the bot comes up Falling and HOVERS (Z~92-100, Vz=0), the fallback horizontal kick is applied to a Falling pawn ~4-12 uu up; it lands within ~0.1 s and transitions to Walking (S140 shape) -- functional, but pre-register that BOT-before MovementMode=3 is the EXPECTED-under-this-Z branch, not a failure.",
    "'Never re-run OnPossess after ARM G' must be honored: with the bot's +0xF00 now the CDO ASC, any incidental OnPossess re-entry ApplyGameplayEffects into the SHARED CDO ASC process-wide (GE loop 0x5565932-0x5565A45).",
    "ARM G writes the process-wide Default__LokiPlayerState_HeroAffiliated AttributeSet subobject (never undone) and the K1 mask points the player at the same set; confirm via the A0/A2 census that ONLY the bot's and player's +0xF08 point at it, no stray consumer inheriting 487/50000.",
    "MaxAcceleration is seeded 50000 = engine Super's stock CMC UPROPERTY (R-S189-MV-a trap): a bot Walking-arm |Accel| of 50000 cannot discriminate 'seed reached +0x120' from 'engine Super bypass'. Do NOT cite |Accel|=50000 as evidence the ARM-G MaxAcceleration was read; use MoveSpeed=487 only."
   ]
  },
  "flight-hazard": {
   "refuted": true,
   "confidence": "high on the timing/instrument refutations (each is anchored to a marker line, CSV row or source line read this session); medium on the hover-trigger mechanism (I can refute the design's stated trigger, not name the real one)",
   "refutations": [
    {
     "target": "P7-BOTH-WALKING / design D rationale: 'Reaches both-walking with the fewest injections' and open_question 'S140: walked off the edge after ~13,000 uu \u2248 27 s' (bot floor lifetime long enough to overlap the player's walk)",
     "argument": "The 13,187 uu figure is CUMULATIVE FROM SPAWN and includes the 12,163 uu fall; the bot's actual floor life in S140 was ~8 s. docs/s140-t2-marker-armj.txt:257-303 [M]: sample 1 (+0.75 s) still falling at Z=1348 'moved 11931'; sample 2 (+2.0 s) on the floor at (1356,-409,90.150) 'moved 13177.9' (i.e. ~1,000 uu of walking); sample 4 (+10 s) TimeSinceFallingStart RESET 8.6001 -> 1.5357 = it left the island ~+8.5 s, ~7 s after landing (doc :70-71 caught it at Z=-29425). docs/s141-t3-marker-arml.txt:367 shows the S141 bot still on the floor at +10 s, so floor life is >= ~7-10 s (n=2), not 27 s. In PLAN A the bot is released at ~probe+28 s while the player is still hovering at Z=13240 (K2 poke at ~probe+20 s, hover 6-115 s per ship-f1/F3), then must fall (3.2 s at -4000), land on the ledge, be slid off and driven to Walking. The two 'walking' intervals are separated by tens of seconds and the bot is very likely off the island (and possibly KillZ-destroyed) before the player's first Walking row. The design spawns the bot ~470 uu from the S140 rest point that led to an island exit within one or two 2-s heading rolls.",
     "evidence": "docs/s140-t2-marker-armj.txt:242,257,272,287,294,302; docs/s140-t2-armj-THE-BOT-WALKS.md:47,70-71; docs/s141-t3-marker-arml.txt:367; tutorial_launch.cpp:17758-17780 (ladder holds until A2) + :25735 (sampler only after FsDisarm); scratchpad/s189-mv/evidence/ship-f1-Whold-motion.csv row t=9.810 (fall onset 13:42:20.8 vs POKEGRAVITY ~13:40:25 = ~115 s hover)",
     "severity": "major",
     "fix": "Do not promise P7 from the 4-injection PLAN A. Either (a) make A' (bot floor arm as the 5th map, injected only after the player CSV shows Mode=1 and |Vxy|>300) the PRIMARY design \u2014 overlap is then by construction, at the cost of one more manual-map inside the FK-32 window \u2014 or (b) keep PLAN A but pre-register P7 as 'expected to FAIL in PLAN A unless the bot survives on the floor >60 s', add a per-sample bot Z/TimeSinceFallingStart 'left the island at sample N' line, and choose a spawn point whose surroundings were measured floor in ALL directions (none is on record: the S140 path (1356,-409)->(872,277)->(819,964)->edge shows an edge within ~1,500 uu to the +Y/-X side)."
    },
    {
     "target": "Runbook T0+3:15 / P6 / hazard 'the focus grab is the deliberate foreground transition that breaks the player's post-K2 hover (F3 and ship-f1 both fell within seconds of one, [I])'",
     "argument": "The ship-f1 timeline contradicts it: the fall started at 13:42:20.8 (CSV t=9.810, start 13:42:11.808) but the focus grab / SEQUENCE_START was 13:42:46.8 \u2014 26 s AFTER the fall. In F3 the hero was ALREADY at Z=386 in the run's own preflight (before that run's Add-Type compile or focus grab), so F3 cannot support the hypothesis either. n=0 for 'foreground transition breaks the hover'. The only event coincident with the ship-f1 onset is the cold child PowerShell's C# compile (spawned ~13:42:13, ~35 s of CPU) \u2014 which the design's PRE-3 pre-warm deliberately removes. With the K2 poke at ~probe+20 s, a 115 s hover puts the fall at ~probe+135 s, inside the FK-32 kill band (probe+148..180 s); the runbook's ShowWindow/SwitchToThisWindow cycle is an untested lever standing in for the player half of the goal.",
     "evidence": "scratchpad/s189-mv/evidence/ship-f1-Whold-motion.csv (t_wall 1788979331.808 = 13:42:11.808; first VelZ<-1 at t=9.810); ship-f1-Whold-sync.txt (SEQUENCE_START 13:42:46.847); ship-f1-Whold-motion.stderr.txt (start 13:42:11); docs/fk24-stage-s189mv-wasd-ship-f1-4-probe-*.txt mtime 13:40:15 (probe ~13:40:13); f3-grav1-Whold-preflight.txt (Z=386.085, Mode=3 before SEQUENCE_START 13:21:13); scratchpad/s189-mv/tools/fly-wasd.ps1 (& powershell -File child); ship-f1-crashwatch.log (kill at 342.2 s)",
     "severity": "major",
     "fix": "Downgrade the trigger to [S] and stop relying on it. Pre-register the hover as a possible full-window loss of the player half. Cheap in-flight probes to add, in order: (1) at [FS] disarm, deliberately reproduce the one coincident event \u2014 a throwaway Add-Type compile of a fresh class name (or an equivalent 20-30 s CPU load) \u2014 and log its start/stop; (2) if Z is still 13240 20 s later, the ShowWindow cycle (log it as a separate arm); (3) reconsider C1: a 1.0 uu/s -Y ARM J seed (KSHPLRY=-1.0, 487x below cap, above the 0.0316 2-D gate) written at arm time is a controlled perturbation that cannot masquerade as walking, and is the only known-mechanism lever to make PhysFalling integrate gravity from rest (S141 arml: a kicked player fell immediately). Restate C1 as 'no player Velocity write >= 2 uu/s' rather than forbidding it and losing the flight."
    },
    {
     "target": "P4-BOT-CAP-BIT-EXACT and the sampler's atCap predicate `(vxy==(double)(float)(KBSGASMOVESPEED))?\"EXACT\":\"no\"` (source_edits (d)); receipt 'BOT |Vxy|=487 peak=487 ... atCap=EXACT (at least once, never >487.0001)'",
     "argument": "Exact double equality on hypot(Vx,Vy) is not what the engine produces and not what the rule was banked on. The cap is applied by TVector::GetClampedToMaxSize (Scale = MaxSize*InvSqrt(SizeSq); X*Scale, Y*Scale), three roundings, then the shim's own sqrt(x\u00b2+y\u00b2) adds a fourth; the result differs from 487.0 by ULPs on any non-axis-aligned heading. The flown 'EXACTLY 500.000' (R-S189-MV-WASD-i) came from motion_watch_player.py's `%.3f` CSV, i.e. a 3-decimal match, and the S140 doc's '|(-240.132,-438.562)| = 500.0' is a 1-decimal computation. The only 10-digit samples on record are diagonal (arml:278 (-17.25429409, 499.7022007), armj:293 (-233.3343912, 442.2160806)) and their hypots are 500.0000\u00b11e-7 as printed, not provably bit-exact. So P4 as written will read 'atCap=no' on a working cap and be graded FAILED, and 'never >487.0001' can also fail on a +ULP overshoot.",
     "evidence": "tutorial_launch.cpp:17054-17056 (%.10g), scratchpad/s189-mv/tools/motion_watch_player.py:168-172 (%.3f), docs/s189-mv-wasd-f3-THE_PLAYER_WALKED.md (500.000 from the CSV), docs/s141-t3-marker-arml.txt:278, docs/s140-t2-marker-armj.txt:263,293",
     "severity": "major",
     "fix": "Pre-register AT CAP as |hypot - seed| <= 1e-6 (or <= seed*1e-12) and print delta=%.3e alongside; a 500-vs-487 discriminator survives any tolerance below 1. Keep strict equality only for axis-aligned samples (Vx or Vy == 0 exactly), and record the raw 24 bytes (ShHex24 already does) so a successor can recompute."
    },
    {
     "target": "Sampler (d)/(f): running peak `if(vxy>g_shPeakXY[k])g_shPeakXY[k]=vxy` over 40 s and P4's if_false branch 'peak>487 -> the bot is not reading the CDO set (f08 mismatch) or a kick was written'",
     "argument": "The bot can leave the island within ~8 s (S140) and, after KillZ, be destroyed; the sampler keeps reading g_shBotCmc/g_shBotPawn for up to 32 more seconds. SafeReadable only tests page commitment, so a freed/reused block returns garbage doubles without faulting (the per-sample __except only catches faults). One garbage sample poisons the running PEAK line permanently and P4's if_false would then blame the CDO wiring. The existing sampler never re-validates identity after ShResolveCmc; nothing in the design freezes the peak on liveness loss.",
     "evidence": "tutorial_launch.cpp:17353-17378 (per-sample __try; SafeReadable = VirtualQuery commit test at :659); docs/s140-t2-marker-armj.txt:294 (TimeSinceFallingStart reset at +10 s), docs/s140-t2-armj-THE-BOT-WALKS.md:70-71 (bot at Z=-29425 falling)",
     "severity": "major",
     "fix": "Per sample, before folding into the peak: re-check CONTROL1 (*(cmc+0x198)==pawn), the ULokiCMC vptr (==g_modBase+0x088F8570) and that Z > -1000; on the first failure print '[SNP] BOT DEAD/UNREADABLE since sample N' and freeze g_shPeakXY[0]. Also print bot Z and +0x12B0 per sample so the island exit is timestamped."
    },
    {
     "target": "Hazard/runbook: 'FALLBACK KICK fires ONLY if the bot has not moved 50 uu by sample 8' and the fallback message 'bot moved %.3f uu horizontally with NO kick written -> bot motion is AI-driven'",
     "argument": "Displacement >= 50 uu with no kick is not sufficient for 'AI-driven': a Walking bot with a closed gate can still be translated by capsule depenetration or a slope, and a bot that spawned Falling on non-walkable geometry would fall/slide. The C8 noai control is what discriminates, and it is 'optional' in the design. In addition the 8-s rest window overlaps the LokiBotController stuck-check (5-s cadence, 3 hits) only if the bot sits >=15 s, so that is fine \u2014 but the design's KSHFALLBACKAT=8 with KSHSAMPLEMS=1000 lands the check at sampler t=+9 s (the loop sleeps BEFORE printing sample i, so sample 8 is at 9 s), a cosmetic mismatch with the receipt text 'at sample 8'.",
     "evidence": "source_edits (c) loop: `Sleep(KSHSAMPLEMS)` precedes the sample print; tutorial_launch.cpp:16627-16690 (ARM F gate is the only thing the noai control removes); aiwander lane stuck-check note",
     "severity": "minor",
     "fix": "Reword the fallback line to 'bot translated >=50 uu with NO kick (AI-driven per C2; confirm Accel rotation)'; make the verdict for 'AI-driven' require Accel |A|~50000 on >=2 samples with different headings in the same window; keep noai as a required launch only if P3's Accel evidence is absent."
    },
    {
     "target": "build_defines DIGEST DISCIPLINE: 'sentinel_big <digest the 14:24 artifact>' and 'Run text_digest.py --dupes ... and require botplay-floor != botplay-floor-noai != botplay-floor-5th'",
     "argument": "Filled in this session [M]: the 14:24 sentinel_big artifact digests RAW 8347a0df17354239 (VSIZE 0b0bd0c57d411ce9), not the recorded 52fceb9be6de532f; the other 14:24 values in the design are confirmed (botai b620e0ff3279e673, gasattr 29f0fa4efffd01da, gasattr_ctrl 4c0ede610baa4629, play 31af7667355f39d1, armk 3ff3ba9f77ae1438, axisab 01593c0f8f73f1ae, mv_play 87c764b6d235b7a3, seedmax b47d1bfd04e44921). Note also that `--dupes` ALREADY reports 2 RAW hazard groups (play_nopimutex|play_strictroot; s148_damage_calibration_test|.final) and 2 VIRTUALSIZE hazards on the untouched build dir, so 'dupes clean' as a P0 gate will read as a failure unless those pre-existing groups are listed as expected.",
     "evidence": "python tools/sigbypass-mod/text_digest.py tools/sigbypass-mod/build (this session); --dupes output tail",
     "severity": "minor",
     "fix": "Record sentinel_big=8347a0df17354239 in the pre-registration; phrase the P0 dupes gate as 'no NEW duplicate group, and botplay-floor/-noai/-5th/botwalk all distinct' with the two pre-existing hazard groups listed as known."
    },
    {
     "target": "source_edits (c) BsPsLokiBot under KBSLBSKIPCTRL=1: 'A2 expectation dCtl=+1 dHero=+1' with the ARM D banner unchanged",
     "argument": "The unconditional banner at tutorial_launch.cpp:16308-16310 still prints 'PRE-REGISTERED: spawn A -> AIController; POKE; spawn B -> LokiBotController; RESTORE; spawn C -> AIController again. The third spawn is what turns the restore into a measurement' \u2014 under KBSLBSKIPCTRL that text describes spawns that never happen, and BsFinalReport (:24174-24183) will print rows [0]/[2] as zeros beside a 'spawns=1'. A successor reading the marker alone will see a self-contradicting arm. Indexing is otherwise safe (verified: BsPsLbRead stores by explicit index :16183-16193; ARM B/C/F/G/H read g_psLbCtl[1] only :16376-16377, :16629, :16509, :17221).",
     "evidence": "tutorial_launch.cpp:16307-16310, 16368-16371, 24174-24183",
     "severity": "minor",
     "fix": "Emit one line under `#if KBSLBSKIPCTRL` right after the banner: '[PS] KBSLBSKIPCTRL=1: spawns A and C are compiled out; only spawn B runs (A-B-A already [M] S137 F3/F4)'. Keep it inside the guard so no control digest moves."
    }
   ],
   "survives": [
    "(a) Injection plan: 4 manual-maps is the flown RM_BOTSPAWN shape; the ladder's game-thread hold (A0 5.1 s + A1 4.3 s + A2 4.3 s + settle 0.75 s + ARM sleeps + two FindObjExact sweeps ~= 16-20 s) has been survived >=6 times [M arml:19,30,249; armj]; the design adds NO new Sleep on the game thread (the two extra virtual dispatches in BpGetterReceipt are microseconds) and removes two Sleep(250)s. g_done is set on the NEXT game-thread hit after the ladder (:17759), so FsHold/FsDisarm/ShSampleLoop ordering is as flown; KFSREARMMS=60000 cannot fire inside the ~30 s window.",
    "(a) New bit 0x4000 collides with nothing: no `KBSPSARMS & 0x4000` exists in tutorial_launch.cpp; bits 0x200/0x800/0x1000/0x2000 are preprocessor-guarded (:16967, :17280, :17297, :17371) so the KBSPSARMS=0x53A0 / 0x5320 / 0x43A0 / 0x3A0 selections compile the intended arms; ARM J (0x800) is genuinely absent, so no player Velocity write exists in the primary build (kShPlrVel is only referenced under 0x800).",
    "(a) Preprocessor discipline as designed (new `#if KNOB ... #endif` blocks, the only #else re-emitting the original 5-line sampler head, restore wrapped in `#if !(KBSPSARMS & 0x4000)`, mask guard `#if (KBSGASPLAYERMASK)!=0x7`) is consistent with the recorded 2fcc->6d81 hazard (:17601-17605) and CAN leave all nine baseline digests byte-identical \u2014 P0 is the right gate and the 14:24 baseline values are confirmed.",
    "(b) Worker-thread sampler after FsDisarm is the flown placement (:25735-25737, arml:250-252); an off-thread 24-byte Velocity write from the sampler has precedent (ARM L kick B, :17371-17395, arml). The per-sample __try (:17353-17378) preserves earlier samples on a fault. Off-thread PropOffsetSuper/ClassOf reads per sample are already done by the flown loop (:17361).",
    "(b) ARM H with a (0,0,0) sentinel is a genuine no-op write: BsPsSentinel memcpy's kShSentinel unconditionally (:17251-17262), g_shSentinelOK=1 for a zero write, and the end-of-run restore (:17509-17513) would zero a walking bot \u2014 the design's `#if !(KBSPSARMS & 0x4000)` wrap of the restore is necessary and correctly placed; the VERDICT block's 'isSentinel' reading is cosmetic as the design says.",
    "(b) K2 gates on GravityScale==0 (:17318-17322) and the premise ([LIFT] gravity OFF from sp) is measured in every staged sp marker; K1 today writes all three storages (:16585-16594) and the proposed mask reproduces the +0xF08-only shape of the flown mv-play wiring (:23068-23090). The getter-receipt helper mirrors BfObsMovGetters' vtable check and disp 0x4C8/0x7D0 dispatch on the game thread \u2014 sound ABI (float return in xmm0).",
    "(c) Pre-warming send-wasd.ps1 in-process is a real fix: Add-Type is guarded by `-as [type]` and runs BEFORE `Get-Process -ErrorAction Stop`, so a throwaway invocation with no game compiles the type and throws harmlessly; fly-wasd.ps1's `& powershell -File` child is the 35-s cold path (ship-f1: probe start 13:42:11, SEQUENCE_START 13:42:46).",
    "(c) Gating the first WASD send on `[FS] disarm:` is correct: the ladder holds the game thread ~16-20 s and no frame passes during it (arml:74 `[FS] t=+15s hitsGT=1 called=0`).",
    "(d) Stager probe-hash gating: a non-BindOnly `-Probe` runs only Assert-Fresh (skipped when the DLL is already under tools\\sigbypass-mod\\build\\, :514-516) and `Stage-Inject $Probe 4` (:1085); every S148/S149 SHA pin is inside `if($BindOnly)` (:626-660). ship-f1 staged an RM_BOTFIGHT probe this way. Only gft/fo/sp deployed-vs-build twins can trigger a STALE abort (-AllowStale).",
    "(e) FK-25 is real and the shadow loop is the right defence: the stager copies the probe marker 2 s after mmap (:918/:929) \u2014 ship-f1's stage copy is 649 bytes vs the 16 KB hand-saved marker; Marker() appends with FILE_SHARE_READ|WRITE and the CREATE_ALWAYS truncation (:24990) is share-compatible with a transient Read-mode reader, so a 250 ms FileShare-ReadWrite reader neither blocks the shim nor is blocked by it. crashwatch is armed by launch-redirect.ps1 (:380-385) and its docs\\crashwatch.out.log is per-launch; dumpimage is pure RPM (no suspend) so 'take it early' is safe.",
    "(f) CLAUDE.md callouts: no 'REFUTED / do not re-run' applies to sentinel-big, axisab, armk, KBSGASPLAYER, KSHSENTX/Z, KSHPLRGRAV or ARM D/F/G/H; the only relevant callout is line 2673 (sentinel-*/gasattr-sentinel digests have MOVED, re-digest) which the design's P0 re-baseline honours. KNOLOGINVT (falsified) and driverecompute (invalid gate) are not used.",
    "Spawn-point choice: (1356,-409,90.150) is a measured MOVE_Walking rest/walk point (armj:272) and 92.0 is above rest (AlwaysSpawn does no adjust, bNoCollisionFail=1 at :15764); the bot pawn/CMC line `[SNP] BOT pawn=... CharacterMovement@0x458 = ...` is written on the game thread before release (:17176, arml:186) so the external bot watcher can start before motion begins.",
    "C5 non-stock seed 487 (0x43F38000) via the existing KBSGASMOVESPEED knob (:16499-16501, feeds MoveSpeed and MaxMoveSpeed :16550-16551) is a genuine R-S189-MV-a discriminator against the stock/prior 500 \u2014 provided P4's predicate is loosened as above."
   ],
   "additional_hazards": [
    "The player's post-K2 fall in RM_BOTSPAWN has never been observed WITHOUT a simultaneous kick (armk/axisab/sentinel-big all had ARM J +Y 600): the first frame after the ~16-20 s hold carries a clamped-but-large DeltaTime that may or may not break the Vz=0 hover. Pre-register both outcomes; do not read an immediate fall as 'the foreground cycle worked'.",
    "ARM D's CDO poke Default__Pawn+0x3D0 stands process-wide for the ~250 ms between spawn B and the restore (:16352-16366); with the player already possessed nothing else spawns then, but the A1 stability control cannot see game-thread spawning during the hold (S140T2-e) \u2014 keep the restore READBACK OK line as the only receipt.",
    "PLAN A' / PLAN B (5th manual-map): the -mv-play arm's own worker must have printed `[BF] worker done` and its 17,563 funcswaps restored before the 5th DLL re-arms; the 5th DLL's A0/A1/A2 censuses freeze the world for ~16-20 s while the player is WALKING \u2014 the player's W-hold in progress decays inside the hold, so send no WASD across the 5th map's ladder and restart the holds after its `[FS] disarm:`.",
    "The [RCPT] GETTER_PLR_AFTER_K1 dispatch of GetMaxSpeed on the player happens while +0xF00 is the unbound KWIREGAS ASC and +0xF08 the CDO set; the walk path demonstrably never needs the ASC's avatar (the bot walked with an unbound CDO ASC), but any Loki code that reads attributes THROUGH the ASC (+0xF00 -> SpawnedAttributes) will now disagree with +0xF08 for the player only \u2014 a new inconsistency the -mv-play route never had. If the player half fails with focusOk=True and Accel!=0 but Velocity==0, suspect this before the getter path.",
    "KillZ/destruction of a bot that walks off the island frees the pawn while g_psLbCtl[1]/g_shBotPawn still point at it; BsFinalReport (after the 40-s sampler) re-reads those pointers for its summary \u2014 wrap those reads in the same liveness check or accept that the summary block may print garbage for a dead bot.",
    "The noai control build carries K1+K2 on the player too (0x5320 keeps bit 0x1000 and KBSGASPLAYER=1), so it spends the player half of a launch on a control whose only question is the bot's gate; if budget matters, a noai variant with KBSGASPLAYER=0 and no K2 isolates the bot question without wiring the player.",
    "motion_watch.py's find_bot pass is a full GUObjectArray RPM sweep repeated every 1 s until FOUND (:155-166); started at [SP] done it runs during the probe's ladder and adds cross-process read pressure exactly when the game thread is held \u2014 harmless so far (S138 f8) but it is a new concurrent instrument on top of two motion_watch_player instances; stagger it or start it after the [SNP] BOT line.",
    "fk24-stage.ps1's parked gate needs `GET /core-game/matches` in capture.log after process start and >=110 s uptime (:757-759); if ags was restarted with forceTutorialMatch=true immediately before launch, back up docs/capture.log first (truncation hazard) \u2014 the design's PRE-2 says so but the runbook's PRE-1 backup precedes PRE-2's possible ags restart; do the backup AFTER any restart decision."
   ]
  }
 },
 "final": {
  "options": [
   {
    "name": "D \u2014 CHOSEN PRIMARY: one RM_BOTSPAWN DLL 'botplay-floor' (floor-spawn one bot at the MEASURED rest Z, AI-wander no-kick with a velocity-gated fallback, +0xF08-only K1 player to the shared CDO set, dense two-pawn liveness-checked worker sampler). 4 injections.",
    "injections": 4,
    "new_source_lines": 72,
    "gates_moved": [
     "axisab, armk, sentinel-* move (K1 mask `if` + sampler knob live in their compiled blocks); botai/gasattr/gasattr-ctrl/play/mv-play/seedmax stay byte-identical to TODAY's re-baselined digests"
    ],
    "risk": "Player half rides the UNFLOWN +0xF08-only K1 wire to the shared CDO set: getter path is byte-identical to -mv-play [I,strong] but no one has driven WASD into a K1-treated player, so it is graded [S]; both pawns cap at 487 (no per-pawn SPEED discriminator \u2014 attribution is kinematic); floor-spawn->Walking is [I,~0.8] (fallback-covered); ~15.5 s game-thread freeze at the ladder.",
    "score": 8.5,
    "rationale": "Fewest injections keeps the flight out of the 5th-inject FK-32 zone; the K1 getter-path equivalence is strong; every new line is behind a new no-#else knob so the six named gates stay byte-identical; attribution is built in (bot no-kick-unless-stationary + printed branch, player no-kick, non-stock 487 seed). All accepted refutations fold cleanly into it."
   },
   {
    "name": "A' \u2014 PLAN B (pre-built from the SAME source): -mv-play as the 4th inject (player, FLOWN, own spawnedSet0 at 500) + 'botplay-floor-5th' bot-only arm as the 5th manual-map (bot, CDO set at 487). 5 injections.",
    "injections": 5,
    "new_source_lines": 72,
    "gates_moved": [],
    "risk": "5th manual-map inside the ~150 s armed window adds FK-32 exposure and truncates the -mv-play marker generation (shadow-loop covers it); overlap timing is tighter.",
    "score": 8,
    "rationale": "Player half becomes [M] (flown -mv-play, 10/10) and the sets are DISJOINT (player 500 own set, bot 487 CDO set) so both bit-exact discriminators are clean. Strictly the higher-goal-probability path; costs one injection. Roll here the instant the K1-broke-input signature fires."
   },
   {
    "name": "A \u2014 kick-based bot as a 5th DLL after -mv-play",
    "injections": 5,
    "new_source_lines": 0,
    "gates_moved": [],
    "risk": "Three stacked pawns; bot walks only via the 600 kick so AI attribution is indirect; bot lands on unmeasured floor and has only ~7-10 s of floor life before walking off (weakens P7 overlap).",
    "score": 6,
    "rationale": "Zero-source but weakest attribution and shortest bot floor life. Superseded by A' which floor-spawns (max floor life, no-kick)."
   },
   {
    "name": "B \u2014 one DLL, air spawn + 600 kick, K1 full (all three storages)",
    "injections": 4,
    "new_source_lines": 0,
    "gates_moved": [],
    "risk": "K1 full overwrites +0xF00 (orphans the KWIREGAS ASC); three stacked bots; bot walks only via kick.",
    "score": 5,
    "rationale": "Cheapest but K1-full is the more dangerous player wiring and bot attribution is kick-indirect. Rejected in favour of D's +0xF08 mask + floor spawn."
   },
   {
    "name": "C \u2014 fold the bot ladder into RM_BOTFIGHT/BfS148DoCalibration",
    "injections": 4,
    "new_source_lines": 120,
    "gates_moved": [
     "-mv-play would move unless every insertion is a new no-#else knob"
    ],
    "risk": "[M] a multi-second bot block after the AdjustHealth call flips RESULT to LATER_TIMEOUT (KBFSELFTIMEOUTMS=5000); no post-disarm worker sampler slot; ~16 s freeze lands inside the S148 dispatch.",
    "score": 3,
    "rationale": "Largest port, most fragile timing, no receipt advantage over D. Rejected."
   }
  ],
  "chosen": "PRIMARY = D 'botplay-floor' (4 injections, one RM_BOTSPAWN DLL): floor-spawn ONE ARM-D bot at the measured rest point (1356,-409,90.15), AI-wander with NO kick (velocity-gated fallback only if the bot is proven stationary), player wired +0xF08-only to the shared CDO set + GravityScale=1, dense 40 s liveness-checked two-pawn worker sampler. PLAN B pre-built from the same source = A' ('botplay-floor-5th' as a 5th map after the FLOWN -mv-play), rolled to the instant the K1-broke-input signature fires (focusOk=True AND player Accel==0 through a W-hold). PLAN C = 'botplay-floor-tinykick' (tiny -Y player seed) only if the player hover never breaks. Zero-source contingency = 'botwalk' (kick arm, 487 seed) if the edited build fails to compile. Every new source line is behind a NEW preprocessor knob with no string-bearing #else, so botai/gasattr/gasattr-ctrl/play/mv-play/seedmax stay byte-identical to TODAY's re-baselined digests. The player half is graded [S] in advance; the goal ('both walk, bot peak bit-exact to its seed') is met under D via non-stock 487 + kinematic pawn attribution, and met more cleanly (disjoint sets) under A'.",
  "variant_name": "botplay-floor",
  "build_defines": [
   "# PRE-EDIT re-baseline [M this session, shimdesign lane]: the CLAUDE.md-recorded gates DO NOT reproduce from HEAD. TODAY's baseline (record, then edit): botai b620e0ff3279e673, gasattr 29f0fa4efffd01da, gasattr_ctrl 4c0ede610baa4629, play 31af7667355f39d1, armk 3ff3ba9f77ae1438, axisab 01593c0f8f73f1ae, sentinel_big 8347a0df17354239 (VSIZE 0b0bd0c57d411ce9), mv_play 87c764b6d235b7a3 (matches recorded), seedmax b47d1bfd04e44921 (matches recorded). After the edit, botai/gasattr/gasattr_ctrl/play/mv_play/seedmax MUST be byte-identical to THAT baseline; axisab/armk/sentinel_big WILL move (K1 mask `if` + sampler knob live in their compiled blocks) \u2014 re-record them.",
   "# bits: 0x20 D | 0x80 F | 0x100 G | 0x200 H(poison+sampler only, ZERO sentinel) | 0x1000 K2 | 0x4000 M(velocity-gated fallback kick from the sampler) = 0x53A0. NO 0x800 (ARM J: KSHPLRY=0 would ZERO a walking player), NO 0x2000 (ARM L). USE -DKBSGETTERRCPT=1 (custom BpGetterReceipt) \u2014 NOT -DKBFOBSMOVGETTERS (that helper static_asserts RM_BOTFIGHT).",
   "'botplay-floor'      = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x53A0','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKSHPLRGRAV=1.0','-DKBSGASPLAYER=1','-DKBSGASPLAYERMASK=0x2','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=90.15','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMAXV=5.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# AI-attribution control (optional separate launch): identical but ARM F compiled out (0x80 clear) so the wander gate stays closed; fallback stays IN \u2014 a kicked, undriven Walking pawn must STOP within ~0.3 s (BrakingDecelerationWalking 2048), the kinematic signature that separates kick from AI. bits 0x5320.",
   "'botplay-floor-noai' = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x5320','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKSHPLRGRAV=1.0','-DKBSGASPLAYER=1','-DKBSGASPLAYERMASK=0x2','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=90.15','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMAXV=5.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# PLAN B (hybrid A'): bot-only floor arm as a 5TH manual-map AFTER the flown -mv-play (player chain untouched: KBSGASPLAYER=0, no K2). Player 500 (own spawnedSet0) vs bot 487 (CDO set) = disjoint sets. bits 0x20|0x80|0x100|0x200|0x4000 = 0x43A0.",
   "'botplay-floor-5th'  = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x43A0','-DKSHSENTX=0.0','-DKSHSENTZ=0.0','-DKBSGASMOVESPEED=487.0f','-DKBSLBSKIPCTRL=1','-DKBSAIFLOOR=1','-DKBSAILOC_X=1356.0','-DKBSAILOC_Y=-409.0','-DKBSAILOC_Z=90.15','-DKSHSAMPLEN=40','-DKSHSAMPLEMS=1000','-DKSHFALLBACKAT=8','-DKSHFALLBACKMAXV=5.0','-DKSHFALLBACKX=600.0','-DKBSGETTERRCPT=1')",
   "# PLAN C (only if the player hover never breaks): D plus a TINY -Y player seed (1 uu/s, 487x below cap, above the 0.0316 2-D gate, cannot masquerade as walking) to make PhysFalling integrate gravity from rest. Adds ARM J (0x800) with KSHPLRY=-1.0. bits 0x5BA0.",
   "'botplay-floor-tinykick' = @( ...same as botplay-floor..., '-DKBSPSARMS=0x5BA0','-DKSHPLRY=-1.0' )",
   "# ZERO-SOURCE contingency (design A, flown shape = gasattr-sentinel + 600 kick + non-stock seed). Inject as a 5th DLL after -mv-play only if the edited build fails to compile.",
   "'botwalk'            = @('-DKRUNMODE=RM_BOTSPAWN','-DKFSNAME=\\\"\\\"','-DKFRAMEINIT=1','-DKFAULTINFO=1','-DKOUTPARMRET=1','-DKBSAI=1','-DKBSPS=1','-DKBSPSARMS=0x3A0','-DKSHSENTX=600.0','-DKBSGASMOVESPEED=487.0f')",
   "# DIGEST DISCIPLINE: `python tools/sigbypass-mod/text_digest.py --dupes tools/sigbypass-mod/build` ALREADY reports 2 pre-existing RAW hazard groups (play_nopimutex|play_strictroot; s148_damage_calibration_test|.final) + 2 VIRTUALSIZE hazards on the untouched dir \u2014 list these as KNOWN. Require: no NEW duplicate group, and botplay-floor != botplay-floor-noai != botplay-floor-5th != botwalk."
  ],
  "source_edits": [
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "immediately after the `#endif` closing `#ifndef KBSAI` (~line 15674), before BsResolveAI at 15685",
    "what": "New knobs: floor spawn point, single-bot ARM D, getter receipt. All default to CURRENT behaviour (0).",
    "why": "A floor spawn needs an ABSOLUTE point (the current air spawn heroLoc+KBSOFFSET, Z=13240 @15701-15703, is the MEASURED MOVE_Falling branch). KBSLBSKIPCTRL removes ARM D's two control spawns so exactly one bot lands on the floor. KBSGETTERRCPT gives a self-contained per-pawn 'seed reaches GetMaxSpeed' receipt (BfObsMovGetters is nested under `#if KBFSELFCAL` which static_asserts RM_BOTFIGHT @419-420, so it cannot be reused here).",
    "pseudo_diff": "+#ifndef KBSAIFLOOR\n+#define KBSAIFLOOR 0      // 1 = spawn the SpawnAIFromClass pawn at KBSAILOC_* (a MEASURED floor rest point) instead of hero+KBSOFFSET in the air\n+#endif\n+#ifndef KBSAILOC_X\n+#define KBSAILOC_X 1356.0 // S140 T2 F3 bot rest/walk point (1356.434,-409.393,90.150) MOVE_Walking [M docs/s140-t2-marker-armj.txt:272]\n+#endif\n+#ifndef KBSAILOC_Y\n+#define KBSAILOC_Y -409.0\n+#endif\n+#ifndef KBSAILOC_Z\n+#define KBSAILOC_Z 90.15  // EXACT measured rest root: capsule bottom ~2.1 uu above the surface, inside the MIN/MAX_FLOOR_DIST band -> FindFloor succeeds, comes up Walking, no snap, no penetration. NEVER below rest.\n+#endif\n+#ifndef KBSLBSKIPCTRL\n+#define KBSLBSKIPCTRL 0   // 1 = ARM D makes ONLY spawn B (no A/C control spawns): exactly one bot. A-B-A already [M] S137 F3/F4.\n+#endif\n+#ifndef KBSGETTERRCPT\n+#define KBSGETTERRCPT 0   // 1 = per-pawn 'seed reaches GetMaxSpeed/GetMaxAcceleration' receipt after ARM G/K1 (self-contained; BfObsMovGetters is RM_BOTFIGHT-only)\n+#endif"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsResolveAI, after line 15703 (`g_bsLoc[0]+=(double)(KBSOFFSET); g_bsLocOK=1;`), still inside the `{...}` block",
    "what": "Override the spawn point with the absolute floor point and print BOTH values.",
    "why": "Turns the MEASURED-failing air spawn into the FindFloor-succeeds branch of the SAME SetDefaultMovementMode path (0x35F9B30 -> engine FindFloor 0x35E01D0, all real, none Loki-overridden [M dshybrid lane]). ONLY the 15703 site is live under KBSAI (BsResolve does `BsResolveAI(hero); return;` @17617); the second g_bsLoc computation @17730-17733 is the non-KBSAI RM_BOTFIGHT team path and is NOT reached \u2014 do NOT touch it.",
    "pseudo_diff": "+#if KBSAIFLOOR\n+        Markerf(\"[BS] FLOOR OVERRIDE (KBSAIFLOOR): spawnLoc (%.1f,%.1f,%.1f) -> (%.1f,%.1f,%.1f) = measured rest point\\r\\n\",\n+                g_bsLoc[0],g_bsLoc[1],g_bsLoc[2],(double)(KBSAILOC_X),(double)(KBSAILOC_Y),(double)(KBSAILOC_Z));\n+        g_bsLoc[0]=(double)(KBSAILOC_X); g_bsLoc[1]=(double)(KBSAILOC_Y); g_bsLoc[2]=(double)(KBSAILOC_Z);\n+#endif\n (the later `[BS] ARGS: ... spawnLoc=(%.1f,%.1f,%.1f)` @15749 then prints the floor point; BsCallAI @15761 copies g_bsLoc verbatim)"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsPsLokiBot: wrap the spawn-A block (~16334-16336) and spawn-C block (~16368-16370)",
    "what": "Compile out the two control spawns under KBSLBSKIPCTRL, and emit one clarifying line so the unconditional ARM-D banner does not read as self-contradicting.",
    "why": "With bNoCollisionFail=1 (AlwaysSpawn) three pawns would spawn overlapping at the floor point and the two AIController controls become obstacles the walking bot must depenetrate from. Indexing is safe: BsPsLbRead stores by explicit index, g_psLbIsBot set only for i==1, ARM B/C/F/G/H read [1] only, dCtl/dHero print dynamically. LEDGER-accepted (flight-hazard #7): add the guarded line because the unconditional banner @16307-16310 describes spawns A/C that never happen.",
    "pseudo_diff": "+#if KBSLBSKIPCTRL\n+    Marker(\"[PS] KBSLBSKIPCTRL=1: spawns A and C are compiled out; only spawn B runs (A-B-A already [M] S137 F3/F4). Rows 0/2 print as zeros.\\r\\n\");\n+#endif\n     // ---- spawn A: BASELINE, no poke.\n+#if !KBSLBSKIPCTRL\n     Marker(\"[PS] ---- ARM D / SPAWN A (baseline, CDO untouched) ----\\r\\n\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\"A-baseline\",0);\n+#endif\n     ... POKE / spawn B / RESTORE unchanged ...\n+#if !KBSLBSKIPCTRL\n     Marker(\"[PS] ---- ARM D / SPAWN C (reversal: must be AIController again) ----\\r\\n\");\n     g_bsAiReturned=0; BsCallAI(); g_psLbSpawns++; Sleep(250);\n     BsPsLbRead(\"C-reversal\",2);\n+#endif\n (A2 under KBSLBSKIPCTRL=1: dCtl=+1 dHero=+1; the RESTORE readback line still measures the CDO restore)"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "BsPsGasAttrs: (a) new mask knob + self-contained BpGetterReceipt right after the KBSGASPLAYER `#endif` (~16506); (b) inside the `#if KBSGASPLAYER` K1 loop `for(int i=0;i<3;i++){` (~16585) as its FIRST statement; (c) after `[GASX] ARM K1 done` (~16605, inside #if KBSGASPLAYER) plus a bot receipt just before `[GASX] ARM G done` (~16609)",
    "what": "(a) KBSGASPLAYERMASK knob + a vtable-validated getter receipt (ULokiCMC GetMaxSpeed disp 0x4C8 / GetMaxAcceleration disp 0x7D0, SEH-wrapped); (b) K1 writes ONLY the masked storage slots; (c) call the receipt on BOT after ARM G and PLR after K1.",
    "why": "K1 today overwrites +0xF00 (the KWIREGAS carrier ASC) and +0xF10 @16585-16594; the ONLY player wiring measured under WASD writes +0xF08 alone (-mv-play @23068). Mask 0x2 reproduces that shape onto the CDO set, leaving +0xF00 the KWIREGAS ASC. Guard is `#if (KBSGASPLAYERMASK)!=0x7` so armk/axisab (default 0x7) stay byte-identical. LEDGER-accepted (cdo-borrow #2/#3): the receipt's MaxSpeed=43F38000(487) line is the ONLY bit-exact wire proof \u2014 MaxAccel prints 50000 on both arms (collides with engine Super) and is NON-discriminating; do not read it as a wire proof.",
    "pseudo_diff": "+#ifndef KBSGASPLAYERMASK\n+#define KBSGASPLAYERMASK 0x7   // bit0 +0xF00 ASCStorage, bit1 +0xF08 AttributeSetStorage, bit2 +0xF10 AttributeSetHealthStorage. 0x2 = the S189-MV read-path shape (leaves the KWIREGAS ASC untouched)\n+#endif\n+#if KBSGETTERRCPT\n+static void BpGetterReceipt(uintptr_t hero,const char* tag){\n+    if(!LooksLikePtr(hero)){ Markerf(\"[RCPT] %s hero=NULL\\r\\n\",tag); return; }\n+    uint32_t co=PropOffsetSuper(ClassOf(hero),\"CharacterMovement\");\n+    uintptr_t cmc=(co!=0xFFFFFFFF&&SafeReadable((void*)(hero+co),8))?*(uintptr_t*)(hero+co):0;\n+    uintptr_t vt=LooksLikePtr(cmc)&&SafeReadable((void*)cmc,8)?*(uintptr_t*)cmc:0;\n+    uintptr_t f08=SafeReadable((void*)(hero+0xF08),8)?*(uintptr_t*)(hero+0xF08):0;\n+    uintptr_t f00=SafeReadable((void*)(hero+0xF00),8)?*(uintptr_t*)(hero+0xF00):0;\n+    uint8_t mm=(cmc&&SafeReadable((void*)(cmc+0x231),1))?*(uint8_t*)(cmc+0x231):0xFF;\n+    if(vt!=g_modBase+0x088F8570ULL){ Markerf(\"[RCPT] %s cmc=0x%llX vtbl=0x%llX NOT ULokiCMC -> no dispatch; f00=0x%llX f08=0x%llX mode=%u\\r\\n\",tag,(unsigned long long)cmc,(unsigned long long)vt,(unsigned long long)f00,(unsigned long long)f08,mm); return; }\n+    typedef float(*Fn)(uintptr_t); float sp=0,ac=0; int spOk=0,acOk=0;\n+    __try{ sp=((Fn)(*(void**)(vt+0x4C8)))(cmc); spOk=1; }__except(SEH_FILTER(GetExceptionInformation())){}\n+    __try{ ac=((Fn)(*(void**)(vt+0x7D0)))(cmc); acOk=1; }__except(SEH_FILTER(GetExceptionInformation())){}\n+    uint32_t sb=0,ab=0; memcpy(&sb,&sp,4); memcpy(&ab,&ac,4);\n+    Markerf(\"[RCPT] GETTER_%s vtblExact=yes mode=%u f00=0x%llX f08=0x%llX MaxSpeed=%08X(ok=%d,%.2f) MaxAccel=%08X(ok=%d,%.2f NONDISCRIM) seed=%.10g\\r\\n\",tag,mm,(unsigned long long)f00,(unsigned long long)f08,sb,spOk,(double)sp,ab,acOk,(double)ac,(double)(KBSGASMOVESPEED));\n+}\n+#endif\n ... K1 loop, first statement:\n+#if (KBSGASPLAYERMASK) != 0x7\n+            if(!(((KBSGASPLAYERMASK)>>i)&1)){ Markerf(\"[GASX]   PLR %-30s MASKED OUT (KBSGASPLAYERMASK=0x%X)\\r\\n\",kDst[i],(unsigned)(KBSGASPLAYERMASK)); continue; }\n+#endif\n ... after '[GASX] ARM K1 done' (inside #if KBSGASPLAYER):\n+#if KBSGETTERRCPT\n+        BpGetterReceipt(pp,\"PLR_AFTER_K1\");\n+#endif\n ... just before '[GASX] ARM G done':\n+#if KBSGETTERRCPT\n+    BpGetterReceipt(pawn,\"BOT_AFTER_G\");\n+#endif"
   },
   {
    "file": "tools/sigbypass-mod/tutorial_launch.cpp",
    "location": "Inside `#if (KBSPSARMS & 0x200)`: statics near the sentinel block; g_shArmed=1 site in BsPsSentinel; ShSampleLoop head (~17345-17352); per-sample body; after the __except; end-of-run restore (~17509-17513)",
    "what": "Dense N-sample sampler: per-pawn |Vxy|+running peak at %.10g, time-since-arm, per-sample bot Z + TimeSinceFallingStart(+0x12B0), per-sample liveness re-validation that FREEZES the peak on failure, a VELOCITY-gated fallback kick (fires only if max |Vxy| over samples 0..KSHFALLBACKAT-1 < KSHFALLBACKMAXV), and suppression of the end-of-run Velocity zeroing under the new 0x4000 bit.",
    "why": "LEDGER-accepted fixes: (ai-drive #4) fallback must gate on MAX VELOCITY not NET displacement (a wandering bot can net-cancel to <50 uu while path-length is huge); (flight-hazard #4) SafeReadable passes on a freed page so a dead bot that walked off the island poisons the running peak \u2014 re-check CONTROL1 (cmc+0x198==pawn), the ULokiCMC vptr and Z>-1000, freeze on first failure, and print bot Z + TimeSinceFallingStart to timestamp the island exit; the flown end restore writes (0,0,0) whenever g_shSentinelOK (=1 for a zero sentinel) and would halt a walking bot once \u2014 skip it under 0x4000. Sampler t is sampler-relative; ~3.7 s of FsDisarm elapse before sample 0, so print sinceArm too.",
    "pseudo_diff": "+#ifndef KSHSAMPLEN\n+#define KSHSAMPLEN 0          // 0 = the flown 5-point schedule (byte-identical); N>0 = N samples every KSHSAMPLEMS on the worker\n+#endif\n+#ifndef KSHSAMPLEMS\n+#define KSHSAMPLEMS 1000\n+#endif\n+#if (KBSPSARMS & 0x4000)\n+#ifndef KSHFALLBACKAT\n+#define KSHFALLBACKAT 8\n+#endif\n+#ifndef KSHFALLBACKMAXV\n+#define KSHFALLBACKMAXV 5.0    // fire the kick only if the bot's MAX |Vxy| over samples 0..KSHFALLBACKAT-1 stayed below this (proves stationary; net-displacement is the WRONG quantity)\n+#endif\n+#ifndef KSHFALLBACKX\n+#define KSHFALLBACKX 600.0\n+#endif\n+static const double kShFallback[3]={(double)(KSHFALLBACKX),0.0,0.0}; static int g_shFallbackFired=0; static double g_shBotMaxV=0.0;\n+#endif\n+#if KSHSAMPLEN\n+static DWORD g_shArmTick=0; static double g_shPeakXY[2]={0,0}; static int g_shPeakFrozen[2]={0,0};\n+#endif\n --- BsPsSentinel, after g_shArmed=1;\n+#if KSHSAMPLEN\n+    g_shArmTick=GetTickCount();\n+#endif\n --- ShSampleLoop head\n+#if KSHSAMPLEN\n+    DWORD t0=GetTickCount();\n+    for(int i=0;i<(KSHSAMPLEN);i++){ Sleep((DWORD)(KSHSAMPLEMS));\n+        Markerf(\"[SNP] ---- sample %d at t=+%lu ms (sinceArm %lu ms) ----\\r\\n\",i,(unsigned long)(GetTickCount()-t0),(unsigned long)(GetTickCount()-g_shArmTick));\n+#else\n+    (the ORIGINAL five lines verbatim \u2014 this #else re-emits existing code only, no new string)\n+#endif\n --- per-sample body (inside the existing __try)\n+#if KSHSAMPLEN\n+        for(int k=0;k<2;k++){ uintptr_t c=k?g_shPlrCmc:g_shBotCmc; uintptr_t pw=k?g_shPlrPawn:g_shBotPawn;\n+            if(g_shPeakFrozen[k]||!LooksLikePtr(c)||!SafeReadable((void*)(c+0xE8),24)) continue;\n+            uintptr_t owner=SafeReadable((void*)(c+0x198),8)?*(uintptr_t*)(c+0x198):0; uintptr_t vt=SafeReadable((void*)c,8)?*(uintptr_t*)c:0;\n+            const double* v=(const double*)(c+0xE8); double vxy=__builtin_sqrt(v[0]*v[0]+v[1]*v[1]);\n+            double lz=(pw&&SafeReadable((void*)(pw+0x1B0),8))?0:0; // (root->RelLoc.Z read elsewhere); use CMC Vel.Z<-1000 guard below\n+            if(owner!=pw || vt!=g_modBase+0x088F8570ULL || v[2]<-100000.0){ g_shPeakFrozen[k]=1; Markerf(\"[SNP] %-14s DEAD/UNREADABLE since sample %d (owner/vtbl/Z guard) -> peak FROZEN at %.10g\\r\\n\",k?\"PLR\":\"BOT\",i,g_shPeakXY[k]); continue; }\n+            if(vxy>g_shPeakXY[k])g_shPeakXY[k]=vxy;\n+            uint8_t mm=SafeReadable((void*)(c+0x231),1)?*(uint8_t*)(c+0x231):0xFF;\n+            float tsf=SafeReadable((void*)(c+0x12B0),4)?*(float*)(c+0x12B0):-1.0f;\n+            double seed=(double)(float)(KBSGASMOVESPEED); double delta=vxy-seed;\n+            Markerf(\"[SNP] %-14s |Vxy|=%.10g peak=%.10g mode=%u TSF=%.4f Vz=%.3f delta=%.3e atCap=%s\\r\\n\",k?\"PLR\":\"BOT\",vxy,g_shPeakXY[k],mm,(double)tsf,v[2],(vxy<=seed+0.02 && vxy>=seed-0.02)?\"BAND\":\"no\");\n+#if (KBSPSARMS & 0x4000)\n+            if(k==0 && i<(KSHFALLBACKAT) && vxy>g_shBotMaxV) g_shBotMaxV=vxy;\n+#endif\n+        }\n+#endif\n --- after the __except block, before the ARM L #if\n+#if (KBSPSARMS & 0x4000)\n+        if(i==(KSHFALLBACKAT)&&!g_shFallbackFired&&LooksLikePtr(g_shBotCmc)){\n+            if(g_shBotMaxV<(double)(KSHFALLBACKMAXV)&&SafeWritable((void*)(g_shBotCmc+0xE8),24)){\n+                memcpy((void*)(g_shBotCmc+0xE8),kShFallback,24); g_shFallbackFired=1;\n+                Markerf(\"[SNP] FALLBACK KICK fired at sample %d: bot max |Vxy| over 0..%d = %.4f < %.1f (stationary) -> Velocity=(%.1f,0,0) readback %s. Motion after this is S140-shape (kick+AI), NOT pure AI.\\r\\n\",i,(KSHFALLBACKAT)-1,g_shBotMaxV,(double)(KSHFALLBACKMAXV),(double)(KSHFALLBACKX),ShEq3(g_shBotCmc+0xE8,kShFallback)?\"OK\":\"FAILED\"); }\n+            else Markerf(\"[SNP] FALLBACK NOT NEEDED at sample %d: bot max |Vxy| = %.4f >= %.1f with NO kick written -> AI-driven (confirm Accel rotates through >=3 headings at |A|~50000).\\r\\n\",i,g_shBotMaxV,(double)(KSHFALLBACKMAXV)); }\n+#endif\n --- after the loop\n+#if KSHSAMPLEN\n+    Markerf(\"[SNP] PEAK |Vxy| BOT=%.10g PLR=%.10g seed=%.10g\\r\\n\",g_shPeakXY[0],g_shPeakXY[1],(double)(float)(KBSGASMOVESPEED));\n+#endif\n+#if (KBSPSARMS & 0x4000)\n+    Markerf(\"[SNP] fallbackFired=%d botMaxVpreKick=%.4f\\r\\n\",g_shFallbackFired,g_shBotMaxV);\n+#endif\n --- the end-of-run restore\n+#if !(KBSPSARMS & 0x4000)\n     if(LooksLikePtr(g_shBotCmc)&&g_shSentinelOK&&SafeWritable(...)){ ... restore Velocity -> (0,0,0) ... }\n+#endif\n (NOTE: the existing ARM-H VERDICT block prints 'payload isSentinel' for a (0,0,0) sentinel \u2014 COSMETIC under this variant; the StartNewPhysics-poison outcome is not the question here.)"
   }
  ],
  "marker_receipts": [
   "[BS] census A0 ... LokiHeroCharacter-chain=<N> ; heroCand ... 'BP_HERO_Ronin_C' ... USABLE ; [BS] heroLoc = (0.0, 0.0, 13240.0)",
   "[BS] FLOOR OVERRIDE (KBSAIFLOOR): spawnLoc (600.0,0.0,13240.0) -> (1356.0,-409.0,90.15) = measured rest point",
   "[BS] ARGS: pawnClass='BP_HERO_Ronin_C' spawnLoc=(1356.0, -409.0, 90.15) BT=null noCollisionFail=1 owner=null",
   "[PS] KBSLBSKIPCTRL=1: spawns A and C are compiled out; only spawn B runs (A-B-A already [M] S137 F3/F4)",
   "[PS] POKE AIControllerClass ... READBACK OK ; [PS] ARM D B-treatment ... BotController-chain=YES ; [PS] RESTORE AIControllerClass ... READBACK OK  (NO 'SPAWN A'/'SPAWN C' sections)",
   "[PS] ARM B ... ctl.PS non-null ; [PS] ARM C ... pawn.PS == ctl.PS",
   "[PS] ARM F AFTER (faulted=0): LivingState=1  GATE+0x6A0=1  (was 0)  -> '***** ARM F: THE GATE OPENED'",
   "[GASX] dst AbilitySystemComponentStorage/AttributeSetStorage/AttributeSetHealthStorage @0xF00/0xF08/0xF10  0 -> <cdo*> OK (x3 on the BOT)",
   "[GASX] attr MoveSpeed @+0xF0 = 487 readback 487 OK ; MaxMoveSpeed @+0x100 = 487 ; MaxAcceleration @+0x120 = 50000 ; GroundFriction @+0x130 = 8 ; BrakingDecelerationWalking @+0x140 = 2048 ; Mass @+0x170 = 100 (all OK)",
   "[GASX] PLR AbilitySystemComponentStorage MASKED OUT (KBSGASPLAYERMASK=0x2) ; PLR AttributeSetStorage @0xF08 0 -> <cdoSet> OK ; PLR AttributeSetHealthStorage MASKED OUT ; [GASX] ARM K1 done: PLAYER storages written 1/3",
   "[RCPT] GETTER_BOT_AFTER_G vtblExact=yes mode=1 f00=<cdoASC> f08=<cdoSet> MaxSpeed=43F38000(ok=1,487.00) MaxAccel=47435000(ok=1,50000.00 NONDISCRIM) seed=487",
   "[RCPT] GETTER_PLR_AFTER_K1 vtblExact=yes mode=3 f00=<KWIREGAS carrier ASC UNCHANGED> f08=<SAME cdoSet as BOT> MaxSpeed=43F38000(ok=1,487.00) MaxAccel=47435000(ok=1,50000.00 NONDISCRIM) seed=487",
   "[SNP] BOT pawn=0x... CharacterMovement@0x458 = 0x... ; CONTROL1 CharacterOwner PASS ; CONTROL2 TickFn.Target PASS (same for PLAYER)",
   "[SNP] BOT-before ... Velocity (0,0,0) ; GravityScale@0x1A0=1.0000 MovementMode@0x231=1 (MOVE_Walking) ; TSF=0.0000 ; loc (~1356,-409,90.150)   [BRANCH: mode=3 = the hover branch, fallback expected ~sample 8]",
   "[SNP] PLR-before ... GravityScale@0x1A0=0.0000 MovementMode@0x231=3 ; loc (0.000,0.000,13240.000)",
   "[SNP] BOT poison written -> readback OK ; BOT sentinel Velocity = (0,0,0) -> readback OK ; ARM K2: PLAYER GravityScale@0x1A0 0.0000 -> 1.0000 readback OK",
   "[BS] census A2 ... BotOrAIController-chain=+1 LokiHeroCharacter-chain=+1 ; [FS] disarm: restored=17563 of 17563",
   "[SNP] ---- sample i at t=+N ms (sinceArm M ms) ---- ; BOT |Vxy|=... peak=... mode=1 TSF=... Vz=0.000 delta=... atCap=BAND ; PLR ... per sample",
   "[SNP] FALLBACK NOT NEEDED at sample 8: bot max |Vxy| >= 5.0 with NO kick written -> AI-driven   [BRANCH: 'FALLBACK KICK fired' = S140 shape]",
   "[SNP] PEAK |Vxy| BOT=~487 PLR=~487 seed=487 ; [SNP] fallbackFired=0 botMaxVpreKick=<>~487",
   "[BS] done (step=4 resolved=1 called=1 faulted=0 dCtl=1 dHero=1 hits=1 hitsGT=1)",
   "crashwatch: no 0x0000DEAD before the both-walking window ; Loki.log canaries: 0 Fatal / 0 'handing control over to crashpad'"
  ],
  "predictions": [
   {
    "id": "P0-BASELINE",
    "prediction": "After the edit, botai/gasattr/gasattr_ctrl/play/mv_play/seedmax rebuild BYTE-IDENTICAL to the TODAY re-baselined digests (b620e0ff3279e673 / 29f0fa4efffd01da / 4c0ede610baa4629 / 31af7667355f39d1 / 87c764b6d235b7a3 / b47d1bfd04e44921); axisab/armk/sentinel_big move and are re-recorded; botplay-floor/-noai/-5th/botwalk are FOUR distinct new digests with no NEW --dupes group (the 2 pre-existing groups stay).",
    "measured_how": "python tools/sigbypass-mod/text_digest.py before/after; --dupes for the new set.",
    "if_false": "An edit leaked outside its knob (an #else with a string, or a runtime if(KBSPSARMS&bit) in shared code \u2014 the S140 gasattr 2fcc->6d81 mechanism). Do NOT fly; byte-scan the moved artifact for the new literals ('FLOOR OVERRIDE','MASKED OUT','FALLBACK','[RCPT]')."
   },
   {
    "id": "P1-SPAWN",
    "prediction": "[BS] ARGS prints spawnLoc=(1356.0,-409.0,90.15); A2 reads dCtl=+1 dHero=+1 (exactly ONE new pawn, one LokiBotController).",
    "measured_how": "[BS] FLOOR OVERRIDE / [BS] ARGS / census A2 / [BS] done dCtl dHero.",
    "if_false": "dHero=+3 -> KBSLBSKIPCTRL not in the built variant; spawnLoc still (600,0,13240) -> KBSAIFLOOR not defined; dHero=0 -> resolve refusal ([BS] REFUSE names it) or the (1356,-409) cell not streamed (check A0 heroCand + Loki.log cell loads)."
   },
   {
    "id": "P2-WALKING-AT-SPAWN",
    "prediction": "[SNP] BOT-before MovementMode@0x231=1 (MOVE_Walking), TSF@0x12B0=0.0000, loc Z=90.150. Grade [I,~0.8] \u2014 the FALLING branch is a first-class pre-registered outcome, NOT a failure.",
    "measured_how": "the game-thread BEFORE ShDump block (written while the ladder holds the thread).",
    "if_false": "mode=3 with Z\u224890 held -> FindFloor did not find a walkable floor at possess time (the S136-vs-S138 open contradiction): this is the pre-registered HOVER branch \u2014 the bot stays ~0 uu until the velocity-gated fallback fires ~sample 8, and the result is then the already-[M] S140 shape (kick+AI), still both-walking but with pure-AI-from-rest UNANSWERED. If mode=1 but Z stays 92+ -> AdjustFloorHeight did not run; read CurrentFloor@CMC+0x438 next flight."
   },
   {
    "id": "P3-AI-FROM-REST",
    "prediction": "[I,~0.5, NOT expected-certain] With NO kick the bot's max |Vxy| over samples 0..7 exceeds KSHFALLBACKMAXV=5 ('FALLBACK NOT NEEDED'), |Accel|\u224850000 from the first walking sample with the heading rotating at ~2.0 s intervals (>=3 distinct 10-degree bins), MovementMode stays 1.",
    "measured_how": "[SNP] FALLBACK NOT NEEDED; BOT Accel@0x328 across samples; external motion_watch.py RandomMoveDirection churn.",
    "if_false": "Accel\u224850000 rotating but |Vxy| stays 0 -> a NEW Walking-mode zeroing site (the S141 T3 open question moves to Walking; read AnalogInputModifier and MaxSpeed=43F38000). Accel==0 -> gate/AIM/attr problem: check ARM F GATE=1, BOT_AFTER_G MaxSpeed bits, f08 non-null. Either way the fallback fires at sample 8 and the flight continues as S140-shape \u2014 bot still walks, only 'pure AI from rest' is unproven."
   },
   {
    "id": "P4-BOT-CAP-IN-BAND",
    "prediction": "BOT |Vxy| lands in the tolerance BAND |vxy-487| <= 0.02 (delta printed) on >=2 non-adjacent samples with DIFFERENT headings, never above 487.05; the bit-exact wire proof is GETTER_BOT_AFTER_G MaxSpeed=43F38000 (0x43F38000 == 487.0f). Do NOT gate on 2-D magnitude bit-equality (a diagonal cap is hypot=487+-ULP, never bit-equal).",
    "measured_how": "[SNP] BOT ... atCap=BAND + delta=%.3e lines; the [RCPT] MaxSpeed bits; corroborated by the bot motion_watch_player CSV (3-decimal).",
    "if_false": "peak==500 -> the value did NOT flow from the knob (a stock/other source; R-S189-MV-a trap) \u2014 check [GASX] MoveSpeed readback and BOT_AFTER_G MaxSpeed bits; peak<487 and never in band -> wall-blocked wander (S141 s2 shape ~93) or a different clamp \u2014 read Accel-vs-velocity and location; peak>487.05 -> not reading the CDO set (f08 mismatch) or a kick fired (fallbackFired=1)."
   },
   {
    "id": "P5-PLAYER-WIRING",
    "prediction": "[GASX] ARM K1 done: PLAYER storages written 1/3 with two MASKED OUT lines; GETTER_PLR_AFTER_K1 prints f00 == the KWIREGAS carrier ASC (UNCHANGED from before K1), f08 == the SAME cdoSet pointer as GETTER_BOT_AFTER_G, MaxSpeed=43F38000. The MaxAccel line is NON-discriminating (50000=Super) \u2014 ignore it.",
    "measured_how": "the [GASX] PLR lines and pointer equality across the two [RCPT] lines.",
    "if_false": "written 3/3 -> the mask did not apply (variant lacks -DKBSGASPLAYERMASK=0x2): the player's +0xF00 is now the unbound CDO ASC \u2014 grade the player half harder [S] and prefer PLAN B. MaxSpeed=00000000 with f08 set -> the getter early-out is not the f08 test alone; read hero+0xF08->set+0xF0/+0x100 CurrentValue directly next flight."
   },
   {
    "id": "P6-PLAYER-FALLS-THEN-WALKS",
    "prediction": "[S] player half \u2014 UNFLOWN K1 wiring. After K2 (GravityScale 0->1) the player hovers at Z=13240 (hover trigger is [S], see P6b), then falls (Vz -> -4000) to the ledge Z\u2248386; W-hold #2 slides it off (Z 386 -> 90.15, MovementMode 3->1); W-hold #3 drives |Vxy| into the 487 BAND in Walking with Accel (0,-50000,0)-shaped ONLY inside logged key windows.",
    "measured_how": "player motion_watch_player CSV (Vel/Acc/Mode/LocZ with t_wall) joined to the send-wasd sync file's ARM_W_START/END timestamps; shim PLR samples for the first 40 s.",
    "if_false": "K1-BROKE-INPUT SIGNATURE: focusOk=True AND repeats>=60 AND player Accel==0 through a full W-hold -> the +0xF08-only K1 player does not accept input -> ROLL TO PLAN B (botplay-floor-5th after -mv-play) on the next launch. focusOk=False -> uninterpretable, resend after SwitchToThisWindow. Player Accel non-zero OUTSIDE key windows -> cross-contamination via the shared CDO set -> the player half is void."
   },
   {
    "id": "P6b-HOVER-TRIGGER",
    "prediction": "[S] \u2014 the hover-break mechanism is UNKNOWN (ship-f1's fall PRECEDED the focus grab by 26 s; the only coincident event was the cold child-PowerShell compile, which the pre-warm removes). Pre-register the hover as a POSSIBLE full-window loss of the player half; do NOT assume the deliberate ShowWindow/SwitchToThisWindow cycle breaks it.",
    "measured_how": "player CSV LocZ/Vz vs t_wall; log every foreground-transition timestamp.",
    "if_false": "If Z stays 13240 >20 s after K2 despite the focus cycle: the player half stalls this flight \u2014 record timestamps; the covered contingency is PLAN C (botplay-floor-tinykick, KSHPLRY=-1.0, a 1 uu/s -Y seed that is 487x below cap and above the 0.0316 2-D gate, so it cannot masquerade as walking but makes PhysFalling integrate gravity from rest \u2014 S141 T3 showed a horizontal kick breaks the player's Vz=0 hover)."
   },
   {
    "id": "P7-BOTH-WALKING",
    "prediction": "WEAKEST PREDICTION (scheduling-bound). At least one pair (player CSV row, bot CSV row) with |t_wall diff|<=0.2 s where both have Mode=1 and |Vxy|>300; corroborated by a same-instant movementmode_readout snapshot listing both pawns Walking. The floor spawn gives the bot its FULL floor life (walks from t=0, not after a fall), which improves overlap vs the kick path.",
    "measured_how": "join of the two motion_watch_player CSVs on t_wall; tools/re/movementmode_readout.py snapshot at ~probe+60 s; the per-sample BOT TSF/Z line timestamps the island exit.",
    "if_false": "Both halves walked but never overlapped -> a scheduling failure (player WASD windows vs bot floor life); record the two intervals + the bot's 'left the island' sample and re-fly with the player's W-holds started immediately at [FS] disarm and a more central bot spawn. One half never walked -> its own prediction's branch."
   },
   {
    "id": "P8-NOAI-CONTROL",
    "prediction": "botplay-floor-noai (optional launch): bot spawns Walking (or Falling), Accel stays (0,0,0), max |Vxy| over 0..7 < 5 -> 'FALLBACK KICK fired'; the kicked-but-undriven Walking pawn then STOPS within ~0.3 s (BrakingDecelerationWalking 2048), one heading only, total displacement <~150 uu.",
    "measured_how": "same marker lines; FALLBACK KICK fired; samples 9-40 |Vxy| decaying to 0 with Accel (0,0,0).",
    "if_false": "Bot sustains 487 after the kick with Accel rotating -> ARM F is NOT the gate (something else opened it) \u2014 re-read GATE+0x6A0 and the blackboard key. Bot translates >50 uu with Accel==0 before the kick -> floor contact alone moves it and P3's AI attribution is VOID."
   },
   {
    "id": "P9-HEALTH",
    "prediction": "No 0x0000DEAD before probe+120 s; the ~15.5 s game-thread freeze at the ladder hit does not kill; canaries 0 Fatal / 0 crashpad; the A1 stability census equals A0.",
    "measured_how": "docs/crashwatch.out.log (copy at end), Loki.log greps, [BS] census A1 line.",
    "if_false": "FK-32 before the window -> everything captured stands (capture as you go); note the injection count (4) in the 0xDEAD series. SITTING VOID at A1 -> nothing after is attributable; relaunch."
   }
  ],
  "controls": [
   "C1 \u2014 The player receives NO Velocity write that could masquerade as walking: KBSPSARMS excludes 0x800 (ARM J) and 0x2000; any player Velocity must come from gravity (K2) or WASD. The player's Accel OUTSIDE logged W-key-windows must be (0,0,0) \u2014 that line is the cross-contamination detector for the shared CDO set. (RESTATED per flight-hazard #2: a diagnostic seed < ~5 uu/s is permitted ONLY in PLAN C, never in the primary flight.)",
   "C2 \u2014 The bot's kick is WITHHELD until sample KSHFALLBACKAT=8 and fires only if MAX |Vxy| over samples 0..7 < 5 uu/s (velocity gate, not net displacement); the sampler prints which branch it took. Rest->cap takes ~2 frames at |A|=50000, so 8 s below 5 uu/s is conclusively stationary. Bot motion before sample 8 with rotating Accel = AI; motion only after a printed 'FALLBACK KICK fired' = S140 shape.",
   "C3 \u2014 Rest baseline: the game-thread BEFORE ShDump block prints V=(0,0,0), Accel=(0,0,0), MovementMode, GravityScale, AnalogInputModifier, TSF for BOTH pawns before the thread is released; sample 0 then shows the bot accelerating while the player is still at rest \u2014 a two-pawn within-run contrast on the same frame.",
   "C4 \u2014 Identity controls on both CMCs (CONTROL1 CharacterOwner+0x198==pawn, CONTROL2 TickFn.Target==cmc) plus the ULokiCMC vtable check in the getter receipt AND per-sample re-validation (owner/vtbl/Vz guard) that FREEZES the peak on a dead-object read; a wrong/stale read is refused, not folded into the peak.",
   "C5 \u2014 Non-stock seed KBSGASMOVESPEED=487.0f (0x43F38000), not the stock/prior 500 (R-S189-MV-a); a bot cap in the 487 band + a GETTER MaxSpeed of 43F38000 proves the value flowed from the CDO set this arm wrote. MaxAcceleration stays 50000 (collides bit-exact with engine Super) and is DELIBERATELY NOT a discriminator.",
   "C6 \u2014 Seed-reaches-getter receipts BEFORE any motion on both pawns ([RCPT] GETTER_BOT_AFTER_G / GETTER_PLR_AFTER_K1): MaxSpeed bits 43F38000 and the SAME f08 pointer on both \u2014 separates 'wiring failed' from 'mover wall' for every later null. The MaxAccel field is labelled NONDISCRIM in-line.",
   "C7 \u2014 Player +0xF00 untouched: the mask prints MASKED OUT for +0xF00/+0xF10 and the PLR receipt prints f00 == the pre-existing KWIREGAS carrier ASC. (Caveat per cdo-borrow additional_hazard: this proves +0xF00 is unchanged, not that the KWIREGAS ASC is a bound/consistent avatar \u2014 the getter path does not need it, and the bot walked with an unbound CDO ASC at +0xF00.)",
   "C8 \u2014 PER-PAWN SPEED DISCRIMINATION IS UNAVAILABLE IN D (shared CDO set -> both cap 487). Attribution is KINEMATIC: bot = rotating |Accel|~50000 through >=3 headings + 'FALLBACK NOT NEEDED'; player = (0,-50000,0) Accel only inside logged W-windows. PLAN B (A') restores a clean disjoint-set speed discriminator (player 500 own set, bot 487 CDO set).",
   "C9 \u2014 botplay-floor-noai (separate launch, only if P3 is ambiguous or budget allows): ARM F compiled out so the wander gate stays closed; predicts no translation before the fallback and a ~0.3 s stop after it (P8).",
   "C10 \u2014 Free within-run floor-branch measurement: BOT-before MovementMode + Z + TSF measures SetDefaultMovementMode's FindFloor outcome directly and gives the S136-vs-S138 rest contradiction a data point either way.",
   "C11 \u2014 Stability: A0==A1 census with no call (SITTING VOID otherwise), and the SpawnAIFromClass call is single (dHero=+1), so any later object is the bot."
  ],
  "runbook": [
   "PRE-0 (offline, before any edit): re-baseline. `build.ps1 -Name tutorial_launch -Variant botai,gasattr,gasattr-ctrl,play,armk,axisab,sentinel-big,botfight-damage-self-cal-bindavatar-seedmax,botfight-damage-self-cal-bindavatar-seedmax-mv-play`; `python tools/sigbypass-mod/text_digest.py tools/sigbypass-mod/build` and RECORD the 9 digests (expect b620e0ff3279e673 / 29f0fa4efffd01da / 4c0ede610baa4629 / 31af7667355f39d1 / 3ff3ba9f77ae1438 / 01593c0f8f73f1ae / 8347a0df17354239 / 87c764b6d235b7a3 / b47d1bfd04e44921).",
   "PRE-1 (offline): grep tutorial_launch.cpp for 'KBSAIFLOOR|KBSLBSKIPCTRL|KBSGASPLAYERMASK|KSHSAMPLEN|KSHFALLBACK|KBSGETTERRCPT' = 0 hits (fresh knobs). Apply the five source_edits; add the six build.ps1 lines after axisab. `build.ps1 -Name tutorial_launch -Variant <all 9 above>,botplay-floor,botplay-floor-noai,botplay-floor-5th,botplay-floor-tinykick,botwalk` (\u26a0 -Name tutorial_launch is MANDATORY; -Variant alone silently builds the default set). Verify P0-BASELINE: the 6 stable gates byte-identical, axisab/armk/sentinel_big re-recorded, the 5 new variants distinct; `--dupes` shows only the 2 pre-existing groups. Byte-scan botplay-floor for 'FLOOR OVERRIDE'/'MASKED OUT'/'FALLBACK'/'[RCPT]' (present) and gasattr for the same (absent) as the literal control. verify_dll.py VERDICT PASS on botplay-floor.",
   "PRE-2 (offline): write scratchpad/s190/evidence + docs/s190-botplay-floor-f1-PREREGISTERED.txt (artifact RAW/VSIZE, -D list, the 9 baseline digests, the P0-P9+P6b table verbatim, the C1/C2/C5/C8 attribution rules, the BOT-before MovementMode 1-vs-3 branch table) and commit it BEFORE launch. Back up docs/capture.log and docs/crashwatch.out.log.",
   "PRE-3: backend arming check \u2014 `(Invoke-WebRequest http://127.0.0.1:8080/core-game/players/<id> -UseBasicParsing).Content` shows a non-empty MatchID (forceTutorialMatch=true or the armqueue route). Do the capture.log backup AFTER any ags restart. Steam running; no SUPERVIVE process alive; THREE elevated consoles C1/C2/C3; `$ev=scratchpad\\s190\\evidence; $label='botplay-floor-f1'`.",
   "PRE-4 (C3): PRE-WARM the sender in-process: `try { & scratchpad\\s189-mv\\tools\\send-wasd.ps1 -Sequence f13 -HoldMs 50 } catch { 'prewarm ok' }`; confirm `[bool]('S189WasdInput' -as [type])` is True. Never use fly-wasd.ps1 (cold child = the 35 s ship-f1 loss).",
   "T0 (C1): `.\\configs\\launch-redirect.ps1 -NoHook` (NO -CompanionWatch); record `$gpid` and `$base`.",
   "T0+0:20 (C2): start the 250 ms MARKER SHADOW loop (FileShare ReadWrite read of docs\\tutorial-launch-marker.txt -> $ev\\$label-marker-live.txt and -gen<N>.txt per truncation); keep it running to the end (FK-25).",
   "T0+0:30 (C1): `.\\configs\\fk24-stage.ps1 -Probe tools\\sigbypass-mod\\build\\tutorial_launch_botplay_floor.dll -Label $label` (no -BindOnly; -AllowStale only if it reports stale deployed fo/sp). Gates: parked OK >=T0+1:50 -> gft ~1:50 -> fo ~2:10 -> LVL_Tutorial load complete -> sp ~2:30 -> '[SP] done step=4' ~2:42 -> probe ~2:50. Yield ~1 in 2; budget two launches.",
   "T0+2:42 (C2, at [SP] done): parse hero/CMC from the live marker (spawnedPawn, [LIFT] CMC \u2014 the [LIFT] line is the K2 premise); start `motion_watch_player.py $gpid $hero $pcmc 300` (player CSV) and `tools\\re\\motion_watch.py $gpid $base 400` (bot driver chain) BEFORE the probe.",
   "T0+2:50 probe injected; expect `[FS] arm: swapped=17563`, then the ~15.5 s WORLD FREEZE (static watcher rows expected). C2 polls the live marker for `[SNP] BOT ... CharacterMovement@0x458 = 0x...`, then starts `motion_watch_player.py $gpid $bot $bcmc 240` (bot CSV). Confirm P1/P2/P5 receipts (FLOOR OVERRIDE, one spawn, ARM F gate 1, [GASX] 3/3+6/6 with 487, PLR 1/3 masked, both [RCPT] MaxSpeed=43F38000, BOT-before MovementMode) and NO REFUSE/VOID lines.",
   "T0+3:15 `[FS] disarm: restored=17563 of 17563` -> game thread free; worker sampler runs 40 samples at 1 s. C3 IMMEDIATELY (in-process): `& send-wasd.ps1 -Sequence w,w,w,d -HoldMs 3000 -InterKeyMs 5000 -RepeatMs 33 -SyncFile $ev\\$label-wasd-sync.txt`. The focus grab is a possible-but-UNPROVEN hover breaker ([S]); watch the player CSV for Vz<0 within ~10 s. If LocZ still 13240 20 s post-K2: ShowWindow(h,6)/ShowWindow(h,9)/SwitchToThisWindow from C3, resend w,w. LOG EVERY TIMESTAMP. If still hovering after the cycle: switch to PLAN C (botplay-floor-tinykick) next launch.",
   "T0+3:25..3:55: read the sampler live: BOT 'FALLBACK NOT NEEDED' at sample 8 (or 'FALLBACK KICK fired' = S140 shape), BOT atCap=BAND + delta lines, rotating Accel, per-sample TSF/Z (island-exit timestamp); PLR lines inside the W windows. `[BS] done` ~T0+3:55 -> C2 copies live marker to $ev\\$label-marker-FINAL.txt.",
   "T0+3:55 (probe+60 s, C1): `usmapdump.exe dumpimage SUPERVIVE-Win64-Shipping.exe dumps\\$label` (pure RPM, no suspend \u2014 EARLY); then `py tools\\re\\movementmode_readout.py $gpid $base | Tee $ev\\$label-movementmode-snapshot.txt` (both pawns one pass) for P7. Do NOT inject anything else in PLAN A. FK-32 expected ~T0+5:20-5:50.",
   "AT DEATH / END: copy docs\\crashwatch.out.log, docs\\fk24-stage-$label-*.txt, Loki.log into $ev; stop the shadow loop; `py tools\\re\\dump_coverage_ledger.py`; write docs/s190-botplay-floor-f1-RESULT.md from the evidence (grade P0-P9+P6b; quote shim %.10g lines for the band, CSV only to 3 decimals).",
   "PLAN B (if P6's K1-broke-input signature fires): next launch stage `-Probe ...tutorial_launch_botfight_damage_self_cal_bindavatar_seedmax_mv_play.dll` (player 500 own set), wait for `[BF] worker done` and >=20 s, then `tools\\inject\\inject.exe mmap $gpid tools\\sigbypass-mod\\build\\tutorial_launch_botplay_floor_5th.dll` ONLY if Get-Process -Id $gpid is alive; the shadow loop's -gen<N>.txt is the only copy of the -mv-play generation. Expect bot 487 vs player 500 (disjoint sets). Send WASD only after the 5th map's own `[FS] disarm:`. ZERO-SOURCE contingency: substitute tutorial_launch_botwalk.dll (kick at arm time) as the 5th map."
  ],
  "hazards": [
   "LEDGER: cdo-borrow #1 (player is NOT a clean control for the CDO write; shared set both cap 487) \u2014 ACCEPT. Per-pawn SPEED discrimination is declared UNAVAILABLE in D (C8); attribution is kinematic; goal is still met (non-stock 487 is knob-attributable). A' restores disjoint sets.",
   "LEDGER: cdo-borrow #2 (K1 player +0xF00 identity differs from -mv-play; MaxAccel receipt non-discriminating) \u2014 ACCEPT. Player half graded [S]; the [RCPT] MaxAccel field is labelled NONDISCRIM in-line and only MaxSpeed=43F38000 proves the wire; the K1-broke-input disjunction is the roll-to-PLAN-B trigger.",
   "LEDGER: cdo-borrow #3 (BfObsMovGetters vs custom helper contradiction) \u2014 ACCEPT. Use ONLY -DKBSGETTERRCPT=1 (custom BpGetterReceipt); -DKBFOBSMOVGETTERS is REMOVED from every RM_BOTSPAWN line (that helper static_asserts RM_BOTFIGHT @419-420 and would silently emit nothing).",
   "LEDGER: ai-drive #1 / flight-hazard #1 (pure-AI-from-rest is contingent, P7 overlap is the weakest link) \u2014 ACCEPT. P3 downgraded to [I,~0.5] with BOTH branches first-class; P2 FALLING branch pre-registered; per-sample TSF/Z added to timestamp the bot's island exit; floor spawn (bot walks from t=0) is noted to IMPROVE overlap vs the kick path \u2014 REJECT the framing that P7 is doomed in 4-inject, but ACCEPT the instrumentation + honesty.",
   "LEDGER: ai-drive #2 / flight-hazard #3 (bit-exact 2-D magnitude equality is wrong) \u2014 ACCEPT. atCap changed to a tolerance BAND |vxy-487|<=0.02 with delta printed; the bit-exact wire proof is the GETTER MaxSpeed bits (0x43F38000), not the hypot.",
   "LEDGER: ai-drive #3 (KBSAILOC_Z 92-vs-100 inconsistency, both above rest) \u2014 ACCEPT with the SINGLE authoritative value KBSAILOC_Z=90.15 (the measured rest root; capsule bottom ~2.1 uu above surface, inside the MIN/MAX_FLOOR_DIST band -> FindFloor succeeds, comes up Walking, no snap, no penetration). This resolves BOTH the spawnloc-lane (never below rest) and ai-drive (too high risks a floor miss) concerns.",
   "LEDGER: ai-drive #4 / flight-hazard #5 (fallback on NET displacement is the wrong quantity) \u2014 ACCEPT. Fallback now gates on MAX |Vxy| over samples 0..KSHFALLBACKAT-1 < KSHFALLBACKMAXV=5 (a wandering bot can net-cancel <50 uu; a stationary one has |V|~0). The AI verdict additionally REQUIRES rotating Accel.",
   "LEDGER: flight-hazard #2 / P6b (hover trigger [I]->[S]) \u2014 ACCEPT. The 'foreground transition breaks the hover' claim is refuted by ship-f1's timeline (fall preceded focus grab by 26 s; coincident event was the cold-child compile the pre-warm removes). Trigger downgraded to [S]; hover pre-registered as a possible player-half loss; PLAN C (tiny -Y kick) pre-built. C1 restated: no player Velocity write that could masquerade as walking (< ~5 uu/s).",
   "LEDGER: flight-hazard #4 (dead-bot garbage reads poison the peak; SafeReadable passes on freed pages) \u2014 ACCEPT. Per-sample liveness re-validation (owner==pawn, ULokiCMC vptr, Vz>-100000) FREEZES the peak on first failure and prints 'DEAD/UNREADABLE since sample N'; per-sample TSF/Z timestamps the island exit.",
   "LEDGER: flight-hazard #6 / cdo-borrow additional (digest drift; sentinel_big) \u2014 ACCEPT. P0 re-baselines from TODAY's artifacts (not CLAUDE.md); sentinel_big recorded 8347a0df17354239; --dupes lists the 2 pre-existing groups as KNOWN and requires no NEW group + the 4 new variants distinct.",
   "LEDGER: flight-hazard #7 (KBSLBSKIPCTRL banner self-contradiction) \u2014 ACCEPT. A guarded '[PS] KBSLBSKIPCTRL=1: spawns A/C compiled out' line is emitted so a marker reader is not misled.",
   "LEDGER: ai-drive #6 / whole-goal player-half dependency \u2014 ADJUDICATED. D (4-inject, K1 player) kept PRIMARY because the getter-path equivalence is [I,strong] and the fewest-injections keeps the flight out of the 5th-inject FK-32 zone; A' (5-inject, FLOWN -mv-play player) is PRE-BUILT and rolled to on the K1-broke-input signature. Both verifier concerns are addressed by the honest [S] grade + the disjunction + the ready fallback rather than by discarding D.",
   "LEDGER: ai-drive #5 / cdo-borrow #6 (per-pawn discrimination forfeit under shared set) \u2014 ACCEPT for D (C8). A' is the clean-discriminator alternative.",
   "Floor-spawn dual-site: BsResolveAI @15701-15703 is the KBSAI-live spawn-loc computation (BsResolve does `BsResolveAI(hero); return;` @17617); the SECOND g_bsLoc computation @17730-17733 is the non-KBSAI RM_BOTFIGHT team path and is NOT reached under KBSAI=1 \u2014 the floor override anchors ONLY at 15703; do NOT edit 17733.",
   "bNoCollisionFail=1 => AlwaysSpawn (no adjust): KBSAILOC_Z must stay >= the rest root 90.15 to avoid a penetrating capsule; never the player's 13240, never the ledge (0..363,-375,386).",
   "ARM G writes the process-wide Default__LokiPlayerState_HeroAffiliated AttributeSet subobject (never undone) and the K1 mask points the player at it; a second ARM-G bot in one world would share the same set. Do NOT re-run ALokiBotController::OnPossess after ARM G (its GE loop 0x5565932-0x5565A45 would ApplyGameplayEffect into the shared CDO ASC).",
   "Armed window is ~150 s not 300 s: probe lands ~launch+170 s, FK-32 ~launch+318-350 s. Bot walk, W-holds, movementmode snapshot and dumpimage must sit inside probe+120 s; take the dump EARLY.",
   "FK-25 marker truncation: the stager's copy is a ~650-byte header taken 2 s after mapping; every [SNP]/[GASX]/[RCPT]/[BS] line exists only in the live marker. The shadow loop is MANDATORY; crashwatch.out.log is overwritten per launch and capture.log truncates on ags restart \u2014 copy both first.",
   "External instruments: all consoles elevated (OpenProcess 0x1F0FFF, UIPI parity for SetForegroundWindow); exactly ONE SUPERVIVE process (send-wasd's MainWindowHandle read breaks on an array); motion_watch.py takes the FIRST LokiBotController-chain object (fine: exactly one exists under KBSLBSKIPCTRL=1).",
   "The ~15.5 s game-thread freeze at the ladder hit is the largest any arm imposes; flown >=6 times without immediate kill. Send WASD only after `[FS] disarm:` \u2014 keydowns during the hold decay unused.",
   "The ARM-H VERDICT block prints 'payload isSentinel'/'StartNewPhysics RAN' against a (0,0,0) sentinel \u2014 COSMETIC under this variant; do not quote it as a StartNewPhysics result."
  ],
  "open_questions": [
   "Does a LokiBotController-possessed BP_HERO_Ronin_C spawned at root Z=90.15 over the (1356,-409) floor come up MOVE_Walking at possess time? [I ~0.8] (SetDefaultMovementMode 0x35F9B30 -> engine FindFloor 0x35E01D0 [M]; this build's MaxStepHeight/FindFloor-sweep reach unread). The BOT-before MovementMode byte answers it for free; the fallback covers the FALLING branch.",
   "Why did S136's two plain-AIController pawns spawned at Z=13240 fall to the floor while every LokiBotController/ARM-F pawn since S138 hovers at (600,0,13240)? Unexplained (LokiBot possession vs ARM F vs Vz=0 quirk). The floor spawn sidesteps it; the fallback covers the hover branch.",
   "Does the +0xF08-only K1 player accept WASD -> Acceleration -> Velocity? UNFLOWN (every KBSGASPLAYER=1 flight had no input driver). P6/P5 answer it in D; PLAN B (A') avoids it entirely by using the FLOWN -mv-play chain.",
   "What breaks the player's post-GravityScale hover? [S] \u2014 ship-f1's fall preceded the focus grab by 26 s; the only coincident event was the cold-child compile the pre-warm removes. If the deliberate foreground cycle does not break it, PLAN C's tiny -Y kick is the covered lever.",
   "Which commit moved the RM_BOTSPAWN-family and play digests between S153 (09-02) and today? [I,strong] 4824c77 (2026-09-05, shared g_vehHandle + Worker-exit cleanup). Settle with one rebuild at 4824c77^ vs 4824c77, then update CLAUDE.md's stale gate values \u2014 out of flight scope but needed before any session cites botai/gasattr/gasattr-ctrl/play.",
   "Does the bot's random wander keep it on the floor long enough to overlap the player's walk (S140: walked off the island edge after ~13,000 uu)? The floor spawn maximizes floor life but the 40 s sampler may outlive the bot; a more central spawn point could be chosen next flight if the per-sample TSF line shows an early island exit.",
   "Does BpGetterReceipt compile/link cleanly in RM_BOTSPAWN with -DKBSGETTERRCPT=1 (SEH_FILTER at :1301/:1303, g_modBase+0x088F8570, disp 0x4C8/0x7D0, PropOffsetSuper 'CharacterMovement' all present)? Verify at PRE-1 before relying on the receipt.",
   "HeroPredropHidden (+0x1BE8) on a SpawnAIFromClass bot has never been read; a non-zero would make any future reflected SetMovementMode on the bot a silent no-op (Loki override 0x055C0AC0 refusal gate). Not on this flight's critical path (the bot lands Walking via SetDefaultMovementMode, not a reflected SetMovementMode) but worth a free read in a future census."
  ]
 }
}