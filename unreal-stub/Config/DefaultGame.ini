; DefaultGame.ini — ProjectVersion lives here in UE5 (under GeneralProjectSettings)
; so the engine's NetworkChecksum derivation can pick it up. Mirrors the value
; from DefaultEngine.ini for safety in case engine init picks one or the other.

[/Script/EngineSettings.GeneralProjectSettings]
ProjectName=Loki
ProjectVersion=1.0.0.0
